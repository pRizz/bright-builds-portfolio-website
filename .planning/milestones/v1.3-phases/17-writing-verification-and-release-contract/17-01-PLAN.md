---
phase: 17-writing-verification-and-release-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/browser-release.playwright.ts
  - docs/release-readiness.md
  - scripts/release-readiness.ts
  - scripts/release-readiness.test.ts
  - scripts/verify-release.test.ts
  - scripts/verify-static.ts
autonomous: true
requirements:
  - VERIFY-01
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04
user_setup: []
must_haves:
  truths:
    - "Static verification names and proves writing route coverage through helper-derived generated-output checks."
    - "Browser release checks include writing routes in exhaustive axe/layout loops and explicit keyboard plus reduced-motion scenarios."
    - "Release-readiness docs and guards state writing route coverage as part of `bun run install:browser && bun run verify`."
    - "Release evidence labels include only automated writing coverage and keep hosted/manual/live-link claims out of automated labels."
  artifacts:
    - path: "tests/browser-release.playwright.ts"
      provides: "Helper-derived writing keyboard reachability and reduced-motion browser release coverage"
      contains: "representativeWritingDetailRoute"
    - path: "docs/release-readiness.md"
      provides: "Human release contract naming writing route coverage in the aggregate gate"
      contains: "writing route coverage"
    - path: "scripts/release-readiness.ts"
      provides: "Required writing document facts and centralized truthful evidence labels"
      contains: "writing route coverage"
    - path: "scripts/release-readiness.test.ts"
      provides: "Unit guards for writing document facts and release-readiness labels"
      contains: "reports missing writing route coverage guidance"
    - path: "scripts/verify-release.test.ts"
      provides: "Release evidence label test coverage that rejects overclaimed automated evidence"
      contains: "writing route coverage"
    - path: "scripts/verify-static.ts"
      provides: "Static verifier terminal evidence label for already-helper-derived writing output assertions"
      contains: "writing route coverage"
  key_links:
    - from: "tests/browser-release.playwright.ts"
      to: "src/domain/writing.ts"
      via: "writingDetailRoutes(), publicWritingEntries(), relatedProjectDetailPageProjects()"
      pattern: "writingDetailRoutes|publicWritingEntries|relatedProjectDetailPageProjects"
    - from: "tests/browser-release.playwright.ts"
      to: "src/domain/projects.ts"
      via: "projectDetailPath() for related project navigation from writing"
      pattern: "projectDetailPath"
    - from: "scripts/release-readiness.ts"
      to: "docs/release-readiness.md"
      via: "requiredReleaseReadinessDocumentFacts exact text guards"
      pattern: "writing route coverage"
    - from: "scripts/verify-release.ts"
      to: "scripts/release-readiness.ts"
      via: "releaseEvidenceLabels() spreads releaseReadinessEvidenceLabels()"
      pattern: "releaseReadinessEvidenceLabels"
    - from: "scripts/verify-static.ts"
      to: "src/domain/seo.ts"
      via: "existing metadataForWritingEntry(), writingBlogPostingJsonLd(), writingItemListJsonLd(), sitemapXml() assertions"
      pattern: "metadataForWritingEntry|writingBlogPostingJsonLd|writingItemListJsonLd|sitemapXml"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 17-2026-06-14T18-47-38
generated_at: 2026-06-14T19:02:22Z
---

<objective>
Close Phase 17 by wiring writing route verification into the existing static, browser, release-readiness, and release evidence gates without adding new release surfaces.

Purpose: The aggregate release gate should prove writing routes, metadata, cross-links, accessibility coverage, and evidence labels while staying local, deterministic, and honest about what is automated.
Output: One narrow release-contract pass over browser tests, release-readiness docs/guards/tests, evidence label tests, and static verifier terminal evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/17-writing-verification-and-release-contract/17-CONTEXT.md
@.planning/phases/17-writing-verification-and-release-contract/17-RESEARCH.md
@.planning/phases/16-writing-metadata-and-structured-data/16-01-SUMMARY.md
@.planning/phases/16-writing-metadata-and-structured-data/16-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@package.json
@tests/browser-release.playwright.ts
@docs/release-readiness.md
@scripts/release-readiness.ts
@scripts/release-readiness.test.ts
@scripts/verify-release.ts
@scripts/verify-release.test.ts
@scripts/verify-static.ts
@src/domain/writing.ts
@src/domain/seo.ts
@src/domain/routes.ts

<interfaces>
Writing route helpers that must remain the source of truth:

```ts
export function publicWritingEntries(entries?: readonly WritingEntry[]): readonly PublicWritingEntry[];
export function writingDetailPath(entry: Pick<WritingEntry, "slug">): string;
export function writingDetailRoutes(entries?: readonly WritingEntry[]): readonly string[];
export function relatedProjectDetailPageProjects(
  entry: Pick<WritingEntry, "relatedProjectSlugs">,
  projects?: readonly ProjectStory[],
): readonly ProjectDetailPageProject[];
```

Browser route coverage source:

```ts
export const prerenderRoutes = [
  ...siteRoutes.map((route) => route.path),
  ...projectDetailRoutes(),
  ...writingDetailRoutes(),
];
```

Release evidence label flow:

```ts
export function releaseReadinessEvidenceLabels(): readonly string[];
export function releaseEvidenceLabels(): readonly string[] {
  return [
    "contrast/readability",
    "focus-visible",
    "focus state",
    "image alt",
    "interactive motion surfaces",
    "reduced-motion",
    ...releaseReadinessEvidenceLabels(),
  ];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit writing browser release scenarios</name>
  <files>tests/browser-release.playwright.ts</files>
  <read_first>
    - `.planning/phases/17-writing-verification-and-release-contract/17-CONTEXT.md` decisions D-04, D-05, D-06, D-07, D-12, and D-13.
    - `.planning/phases/17-writing-verification-and-release-contract/17-RESEARCH.md` sections "Pattern 2: Exhaustive Route Loops Plus Representative Flows" and "Common Pitfalls".
    - `tests/browser-release.playwright.ts` for current `prerenderRoutes` axe/layout loops, keyboard focus helpers, and reduced-motion helper patterns.
    - `src/domain/writing.ts` for `writingDetailRoutes()`, `publicWritingEntries()`, and `relatedProjectDetailPageProjects()`.
    - `src/domain/projects.ts` for `projectDetailPath()`.
    - `src/domain/routes.ts` for `prerenderRoutes`.
  </read_first>
  <action>Update `tests/browser-release.playwright.ts` only. Keep the existing axe and desktop/mobile dark layout loops over `prerenderRoutes` unchanged per D-04, because those loops already include `/writing` and every public writing detail route. Import `writingDetailRoutes`, `publicWritingEntries`, and `relatedProjectDetailPageProjects` from `../src/domain/writing`, and import `projectDetailPath` from `../src/domain/projects` next to the existing `projectDetailRoutes` import. Add `representativeWritingDetailRoute()` that returns `writingDetailRoutes()[0]` and throws `Expected at least one public writing detail route for release coverage.` if no route exists. Add `representativeWritingRelatedProjectRoute()` that finds the first public writing entry with a related detail-page project, returns `projectDetailPath(project)`, and throws `Expected at least one public writing entry with a related project detail route for release coverage.` if none exists. Extend the existing keyboard test so focus from `/` reaches `/writing`; focus from `/writing` reaches the representative writing detail route; focus from the representative writing detail route reaches `/writing`; and focus from that writing detail route reaches the representative related project route per D-05. Extend the reduced-motion test route list to include `representativeWritingDetailRoute()` in addition to `/` and the representative project detail route per D-06. Do not add live external-link crawling, hosted audits, screenshot baselines, network-dependent checks, new dependencies, or a writing-page OpenLinks CTA per D-07, D-12, and D-13.</action>
  <verify>
    <automated>bun run typecheck && bun run build && bun run verify:browser</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'writingDetailRoutes|publicWritingEntries|relatedProjectDetailPageProjects|projectDetailPath' tests/browser-release.playwright.ts` finds helper-derived imports/usages.
    - `rg -n 'representativeWritingDetailRoute|representativeWritingRelatedProjectRoute' tests/browser-release.playwright.ts` finds clear-failing representative helpers.
    - `rg -n 'focus reaches Writing nav|focus reaches public writing detail route|focus reaches Writing index from writing detail route|focus reaches related project route from writing detail route' tests/browser-release.playwright.ts` finds explicit keyboard assertions.
    - `rg -n 'representativeWritingDetailRoute\\(\\)' tests/browser-release.playwright.ts` finds writing reduced-motion coverage in the reduced-motion route list.
    - `rg -n 'live external|hosted audit|screenshot|baseline|fetch\\(|network|OpenLinks CTA' tests/browser-release.playwright.ts` exits 1.
    - `bun run typecheck` exits 0.
    - `bun run build` exits 0.
    - `bun run verify:browser` exits 0.
  </acceptance_criteria>
  <done>Browser release coverage proves writing axe/layout through existing route loops and explicitly proves writing keyboard reachability plus reduced-motion behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add writing release-readiness facts and truthful evidence labels</name>
  <files>docs/release-readiness.md, scripts/release-readiness.ts, scripts/release-readiness.test.ts, scripts/verify-release.test.ts</files>
  <read_first>
    - `.planning/phases/17-writing-verification-and-release-contract/17-CONTEXT.md` decisions D-08, D-09, D-10, D-11, D-12, D-13, D-14, and D-15.
    - `.planning/phases/17-writing-verification-and-release-contract/17-RESEARCH.md` sections "Pattern 3: Required-Facts Release Document Guard" and "Pattern 4: Centralized Truthful Evidence Labels".
    - `docs/release-readiness.md` for the existing primary gate, static output, automated gates, Cloudflare, preview, and production smoke wording.
    - `scripts/release-readiness.ts` for `requiredReleaseReadinessDocumentFacts`, `releaseReadinessDocumentFindings()`, and `releaseReadinessEvidenceLabels()`.
    - `scripts/release-readiness.test.ts` for document fixture-removal tests and Arrange/Act/Assert style.
    - `scripts/verify-release.ts` and `scripts/verify-release.test.ts` for inherited evidence label behavior and anti-overclaim assertions.
    - `src/domain/writing.ts` for `writingDetailRoutes()`.
  </read_first>
  <behavior>
    - Test 1: Removing `writing route coverage` from a release-readiness fixture reports the missing writing route coverage fact.
    - Test 2: Removing `writing metadata, JSON-LD, sitemap, related-project link, and forbidden runtime residue coverage` reports the missing writing static coverage fact.
    - Test 3: Removing `writing axe, layout, keyboard, and reduced-motion coverage` reports the missing writing browser coverage fact.
    - Test 4: Removing the representative public writing detail route from the docs reports the missing writing smoke route fact.
    - Test 5: `releaseReadinessEvidenceLabels()` and `releaseEvidenceLabels()` include `writing route coverage` while the joined label string excludes `hosted audit`, `network`, and `live link`.
  </behavior>
  <action>Update the docs and release helper tests together. In `scripts/release-readiness.ts`, import `writingDetailRoutes` from `../src/domain/writing`; add `representativeWritingDetailRoute()` mirroring the project detail helper and throwing `Expected at least one public writing detail route for release coverage.`. Add required document facts for `writing route coverage`, `writing metadata, JSON-LD, sitemap, related-project link, and forbidden runtime residue coverage`, `writing axe, layout, keyboard, and reduced-motion coverage`, and the representative writing route. Add `writing route coverage` to `releaseReadinessEvidenceLabels()` after `project detail route coverage`, and do not add a new package script because `bun run verify` already includes `verify:browser`, `verify:static`, and `verify:release` per D-11. In `docs/release-readiness.md`, state that `bun run install:browser && bun run verify` includes writing route coverage; add writing static coverage to the SEO/static section; add writing axe/layout/keyboard/reduced-motion coverage to the browser section; mention `/writing` and the representative writing detail route in static artifact and preview/production smoke paths; keep manual external-link smoke checks explicitly manual; and keep OpenLinks as low-intrusion footer/about/contact plus `Person.sameAs` identity infrastructure, not a writing-page CTA per D-12 and D-13. In `scripts/release-readiness.test.ts`, add focused fixture-removal tests for the writing facts above, preserving explicit `// Arrange`, `// Act`, and `// Assert` sections per D-14. In `scripts/verify-release.test.ts`, update expected release evidence labels to include `writing route coverage` and keep anti-overclaim assertions rejecting hosted, network, and live-link wording per D-10.</action>
  <verify>
    <automated>bun run test scripts/release-readiness.test.ts scripts/verify-release.test.ts && bun run verify:release</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'writing route coverage|writing metadata, JSON-LD, sitemap, related-project link, and forbidden runtime residue coverage|writing axe, layout, keyboard, and reduced-motion coverage' docs/release-readiness.md scripts/release-readiness.ts scripts/release-readiness.test.ts scripts/verify-release.test.ts` finds docs, guards, and tests.
    - `rg -n 'representativeWritingDetailRoute|writingDetailRoutes' scripts/release-readiness.ts` finds helper-derived representative route selection.
    - `rg -n 'writing route coverage' scripts/release-readiness.test.ts scripts/verify-release.test.ts` finds label test coverage.
    - `rg -n 'hosted audit|network|live link' scripts/verify-release.test.ts` finds anti-overclaim assertions.
    - `rg -n 'new package script|verify:writing|live external-link crawling|screenshot baseline|dynamic OG|CMS|MDX|RSS|site search|search UI|search page|tag archive|writing-page OpenLinks CTA' docs/release-readiness.md scripts/release-readiness.ts scripts/release-readiness.test.ts scripts/verify-release.test.ts` exits 1.
    - `git diff --exit-code -- package.json bun.lock` exits 0.
    - `bun run test scripts/release-readiness.test.ts scripts/verify-release.test.ts` exits 0.
    - `bun run verify:release` exits 0 after `bun run build` has produced `.output/public`.
  </acceptance_criteria>
  <done>Release-readiness docs, required document facts, and evidence labels truthfully name automated writing route coverage without claiming hosted/manual/live-link automation.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten static verifier evidence and prove aggregate release gate</name>
  <files>scripts/verify-static.ts</files>
  <read_first>
    - `.planning/phases/17-writing-verification-and-release-contract/17-CONTEXT.md` decisions D-01, D-02, D-03, D-10, D-11, and D-15.
    - `.planning/phases/17-writing-verification-and-release-contract/17-RESEARCH.md` sections "Current Coverage Audit", "Pitfall 5", and "Minimal Plan Structure".
    - `scripts/verify-static.ts` for existing helper-derived writing body, metadata, BlogPosting, ItemList, sitemap inclusion/exclusion, related-project link, OpenLinks `sameAs`, remote asset, and forbidden runtime residue assertions.
    - `src/domain/writing.ts` and `src/domain/seo.ts` for the helper surfaces already consumed by `scripts/verify-static.ts`.
    - `package.json` for the aggregate `verify` script.
  </read_first>
  <action>Update `scripts/verify-static.ts` only if the existing writing static assertions remain helper-derived. Keep the existing imports and assertion architecture that read `publicWritingEntries()`, `writingDetailRoutes()`, `metadataForWritingEntry()`, `writingBlogPostingJsonLd()`, `writingItemListJsonLd()`, `sitemapXml()`, and related-project helpers per D-01 and D-02. Change only the final success `console.log()` wording so passing `bun run verify:static` explicitly names `writing route coverage` alongside metadata, JSON-LD, assets, sitemap, and robots per D-03. Do not rewrite the static verifier, duplicate writing slugs, add package scripts, touch release docs/labels again, add live external-link crawling, add screenshot baselines, add hosted audits, add runtime fetching, or add dependencies. After Task 1 and Task 2 are complete, run the full repo-native verification sequence ending with `bun run install:browser && bun run verify` per D-15. If Chromium is already installed, still run the exact clean-builder command sequence unless the local environment cannot reinstall; document any equivalent command in the plan summary.</action>
  <verify>
    <automated>bun run build && bun run verify:static && bun run verify:release && bun run install:browser && bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'writing route coverage' scripts/verify-static.ts` finds the static verifier success evidence wording.
    - `rg -n 'publicWritingEntries|writingDetailRoutes|metadataForWritingEntry|writingBlogPostingJsonLd|writingItemListJsonLd|sitemapXml|relatedProjectDetailPageProjects' scripts/verify-static.ts` confirms helper-derived assertions remain present.
    - `rg -n 'agentic-engineering-workflows|portable-identity-and-owned-surfaces' scripts/verify-static.ts` exits 1 unless the match is in generated docs copied from approved smoke-check prose; verifier code must not duplicate writing slugs.
    - `rg -n 'verify:writing|live external-link crawling|hosted audit|screenshot baseline|fetch\\(|runtime content|CMS|MDX|RSS|search|dynamic OG' scripts/verify-static.ts package.json` exits 1.
    - `git diff --exit-code -- package.json bun.lock` exits 0.
    - `bun run build` exits 0.
    - `bun run verify:static` exits 0 and its output includes `writing route coverage`.
    - `bun run verify:release` exits 0 and its output includes `writing route coverage`.
    - `bun run install:browser && bun run verify` exits 0, or the plan summary documents the exact equivalent if Chromium provisioning cannot run in the local environment.
  </acceptance_criteria>
  <done>`bun run verify:static`, `bun run verify:release`, and the aggregate release gate prove writing coverage with truthful labels and no new release surfaces.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| checked-in writing registry -> browser release tests | Public writing route helpers choose which generated routes the release browser scenarios must exercise. |
| generated static output -> release verifiers | Static HTML, CSS, sitemap, and route assets become the evidence scanned by `verify:static`, `verify:browser`, and `verify:release`. |
| release docs -> release-readiness guard | Human release claims in Markdown are enforced by required text facts. |
| evidence labels -> release decision | Human-readable labels summarize automated checks and can overclaim if not test-guarded. |
| generated anchors -> external destinations | External links remain policy-checked locally and manually smoke-checked after deploy, without live crawling in the automated gate. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-17-01-01 | I | `scripts/verify-static.ts` | mitigate | Keep helper-derived assertions for writing body content, metadata, BlogPosting JSON-LD, ItemList JSON-LD, sitemap inclusion/exclusion, related project links, OpenLinks `sameAs`, and forbidden runtime residue; only tighten terminal evidence wording. |
| T-17-01-02 | R | `scripts/release-readiness.ts`, `scripts/verify-release.test.ts` | mitigate | Add tested `writing route coverage` labels only after browser writing scenarios and required docs facts exist; keep tests rejecting `hosted audit`, `network`, and `live link` wording. |
| T-17-01-03 | D | `tests/browser-release.playwright.ts` | mitigate | Keep local deterministic Playwright and axe checks; avoid live external calls, screenshots, hosted audits, and network-dependent checks. |
| T-17-01-04 | I | `scripts/verify-release.ts`, `scripts/release-readiness.ts` | mitigate | Preserve existing forbidden token/runtime API scanners and external-link policy checks; do not add public token env prefixes, runtime content fetching, or live crawling. |
| T-17-01-05 | S/R | OpenLinks identity metadata and visible surfaces | mitigate | Preserve existing footer/about/contact visible OpenLinks placement and `Person.sameAs`; release docs keep OpenLinks as identity infrastructure rather than adding a writing-page CTA. |
| T-17-01-06 | T/I | `docs/release-readiness.md` | mitigate | Add required writing facts and fixture-removal tests so docs cannot omit coverage that labels claim. |
</threat_model>

<verification>
Plan-level verification:

```bash
bun run test scripts/release-readiness.test.ts scripts/verify-release.test.ts
bun run typecheck
bun run build
bun run verify:browser
bun run verify:static
bun run verify:release
bun run install:browser && bun run verify
```

Also review the final diff for unintended scope:

```bash
git diff -- tests/browser-release.playwright.ts docs/release-readiness.md scripts/release-readiness.ts scripts/release-readiness.test.ts scripts/verify-release.test.ts scripts/verify-static.ts
git diff --exit-code -- package.json bun.lock
```
</verification>

<success_criteria>
- VERIFY-01: `bun run verify:static` proves generated writing HTML body content, metadata, BlogPosting JSON-LD, writing index ItemList JSON-LD, sitemap inclusion/exclusion, related-project links, OpenLinks `sameAs`, and forbidden runtime residue through helper-derived checks, and the passing output names writing route coverage.
- VERIFY-02: `bun run verify:browser` keeps exhaustive writing axe/layout coverage through `prerenderRoutes` and explicitly covers writing keyboard reachability plus reduced-motion behavior.
- VERIFY-03: `docs/release-readiness.md` and `releaseReadinessDocumentFindings()` require writing route coverage as part of `bun run install:browser && bun run verify`.
- VERIFY-04: `bun run verify` passes with writing included in browser/static/release gates, and evidence labels include `writing route coverage` without claiming hosted audits, network checks, or live-link crawling.
- No new dependencies, package scripts, live external-link crawling, screenshot baselines, hosted audits, runtime content fetching, CMS/MDX/RSS/search/tag archive behavior, dynamic OG endpoints, or writing-page OpenLinks CTA are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/17-writing-verification-and-release-contract/17-01-SUMMARY.md`.
</output>
