---
phase: 15-writing-routes-and-dark-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/writing.ts
  - src/domain/writing.test.ts
  - src/domain/writing-validation.ts
  - src/domain/writing-validation.test.ts
  - src/domain/routes.ts
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - LINK-02
must_haves:
  truths:
    - "The route registry exposes /writing as a nav route after Projects and before Contact."
    - "Static prerender route data includes /writing and every public /writing/{slug} route from writingDetailRoutes()."
    - "Project-to-writing links derive from public writing data, not reciprocal project record fields."
    - "Authored writing link blocks reject unsafe href protocols before route rendering consumes them."
  artifacts:
    - path: "src/domain/writing.ts"
      provides: "Public writing selectors plus publicWritingEntriesForProject(project)"
      exports:
        - "publicWritingEntriesForProject"
    - path: "src/domain/writing-validation.ts"
      provides: "Unsafe link href validation for WritingBodyBlock link blocks"
      contains: "unsafe_link_href"
    - path: "src/domain/routes.ts"
      provides: "Top-level /writing route and writing detail prerender inclusion"
      contains: "id: \"writing\""
    - path: "src/domain/writing.test.ts"
      provides: "Unit coverage for writing route/nav/prerender and project-to-writing helpers"
    - path: "src/domain/writing-validation.test.ts"
      provides: "Unit coverage for safe and unsafe writing link href validation"
  key_links:
    - from: "src/domain/routes.ts"
      to: "src/domain/writing.ts"
      via: "writingDetailRoutes import"
      pattern: "writingDetailRoutes"
    - from: "src/domain/writing.ts"
      to: "src/routes/projects/[slug].tsx"
      via: "publicWritingEntriesForProject(selectedProject())"
      pattern: "publicWritingEntriesForProject"
    - from: "src/domain/writing-validation.ts"
      to: "src/routes/writing/[slug].tsx"
      via: "validated WritingBodyBlock link hrefs"
      pattern: "unsafe_link_href"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-06-13T16-56-50
generated_at: 2026-06-13T17:17:54.013Z
---

<objective>
Create the Phase 15 route and domain contracts that downstream UI plans can consume.

Purpose: Keep writing route exposure, project cross-link derivation, and authored link safety in pure TypeScript helpers before route components render them.
Output: Updated writing helpers, route registry, and focused Vitest coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-writing-domain-foundation/14-01-SUMMARY.md
@.planning/phases/14-writing-domain-foundation/14-02-SUMMARY.md
@.planning/phases/15-writing-routes-and-dark-ui/15-CONTEXT.md
@.planning/phases/15-writing-routes-and-dark-ui/15-RESEARCH.md
@.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/writing.ts
@src/domain/writing.test.ts
@src/domain/writing-validation.ts
@src/domain/writing-validation.test.ts
@src/domain/routes.ts
@src/domain/projects.ts
@src/domain/project-detail-routes.test.ts

<interfaces>
From `src/domain/writing.ts`, keep these existing contracts and add only the helper named in Task 1:

```ts
export type PublicWritingEntry = WritingEntry & {
  status: "published";
};

export function publicWritingEntries(
  entries?: readonly WritingEntry[],
): readonly PublicWritingEntry[];

export function maybePublicWritingEntryBySlug(
  slug: string,
  entries?: readonly WritingEntry[],
): PublicWritingEntry | null;

export function writingDetailPath(entry: Pick<WritingEntry, "slug">): string;

export function writingDetailRoutes(entries?: readonly WritingEntry[]): readonly string[];

export function relatedProjectDetailPageProjects(
  entry: Pick<WritingEntry, "relatedProjectSlugs">,
  projects?: readonly ProjectStory[],
): readonly ProjectDetailPageProject[];
```

From `src/domain/projects.ts`, project-related writing must accept the selected detail project shape:

```ts
export type ProjectDetailPageProject = ProjectStory & {
  tier: "flagship";
  includeInProjectIndex: true;
  detail: ProjectDetailStory;
};
```

Do not add Markdown, MDX, CMS, RSS, search, pagination, runtime fetches, project reciprocal fields, writing metadata helpers, JSON-LD helpers, sitemap assertions, release-readiness labels, or new dependencies in this plan.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project-to-writing lookup and link href validation</name>
  <files>src/domain/writing.ts, src/domain/writing.test.ts, src/domain/writing-validation.ts, src/domain/writing-validation.test.ts</files>
  <read_first>
    - `src/domain/writing.ts` for current public selector, detail path, and selected-project relationship helper signatures.
    - `src/domain/writing.test.ts` for existing Arrange/Act/Assert unit test style.
    - `src/domain/writing-validation.ts` for `WritingCurationErrorCode`, `bodyIssues()`, and `hasBlockContent()`.
    - `src/domain/writing-validation.test.ts` for exact issue-code assertions.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-CONTEXT.md` sections "Project Cross-Links" and "Writing Detail".
    - `.planning/phases/15-writing-routes-and-dark-ui/15-RESEARCH.md` Pitfall 5 "Unsafe or Unvalidated Link Body Blocks".
  </read_first>
  <behavior>
    - Test 1: `publicWritingEntriesForProject({ slug: "openlinks" })` returns the public entry slug `portable-identity-and-owned-surfaces`.
    - Test 2: `publicWritingEntriesForProject({ slug: "opencode-cloud" })` returns the public entry slug `agentic-engineering-workflows`.
    - Test 3: `publicWritingEntriesForProject({ slug: "missing-project" } as ProjectDetailPageProject)` returns `[]`.
    - Test 4: `validateWritingEntry()` returns an `unsafe_link_href` error for link blocks with `href: "javascript:alert(1)"`, `href: "data:text/html,<p>x</p>"`, `href: "http://example.com"`, or `href: "//evil.example/path"`.
    - Test 5: `validateWritingEntry()` returns no link-href issue for `href: "/projects/openlinks"`, `href: "/writing/agentic-engineering-workflows"`, `href: "#content"`, and `href: "https://example.com/path"`.
  </behavior>
  <action>Add `export function publicWritingEntriesForProject(project: Pick<ProjectDetailPageProject, "slug">, entries: readonly WritingEntry[] = curatedWriting): readonly PublicWritingEntry[]` to `src/domain/writing.ts`. The implementation must call `publicWritingEntries(entries)` and filter entries whose `relatedProjectSlugs.includes(project.slug)`; do not read or modify project records. Extend `WritingCurationErrorCode` in `src/domain/writing-validation.ts` with `"unsafe_link_href"`. Add link href safety checks for `WritingBodyBlock` values where `block.kind === "link"`: allow only hrefs starting with `#`, hrefs starting with `/` but not `//`, or hrefs starting with `https://`; reject all other protocols with message `Writing link blocks must use an internal path, same-page anchor, or HTTPS URL.`. Keep existing required-label/href content validation intact. Add Vitest coverage before implementation, then make it pass. This implements the locked Project Cross-Links decision and the research security mitigation without adding a parser or runtime dependency.</action>
  <verify>
    <automated>bun run test src/domain/writing.test.ts src/domain/writing-validation.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'export function publicWritingEntriesForProject' src/domain/writing.ts` finds the exported helper.
    - `rg -n 'relatedProjectSlugs\\.includes\\(project\\.slug\\)' src/domain/writing.ts` finds the relationship filter.
    - `rg -n '"unsafe_link_href"' src/domain/writing-validation.ts src/domain/writing-validation.test.ts` finds the issue code and tests.
    - `rg -n 'javascript:alert\\(1\\)|data:text/html|http://example\\.com|//evil\\.example/path' src/domain/writing-validation.test.ts` finds unsafe href regression cases.
    - `rg -n 'https://example\\.com/path|/projects/openlinks|/writing/agentic-engineering-workflows|#content' src/domain/writing-validation.test.ts` finds allowed href cases.
    - `bun run test src/domain/writing.test.ts src/domain/writing-validation.test.ts` exits 0.
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>`publicWritingEntriesForProject()` derives public writing links from writing data, unsafe link block hrefs fail curation validation, and tests prove both behaviors.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add /writing to route, nav, and prerender data</name>
  <files>src/domain/routes.ts, src/domain/writing.test.ts</files>
  <read_first>
    - `src/domain/routes.ts` for `RouteId`, `siteRoutes`, `navigationRoutes`, `prerenderRoutes`, and `routeByPath()`.
    - `src/domain/writing.ts` for `writingDetailRoutes()`.
    - `src/domain/project-detail-routes.test.ts` for the existing route/prerender assertion style.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md` section "`/writing` Index".
  </read_first>
  <behavior>
    - Test 1: `routeByPath("/writing")` returns route id `writing`, label `Writing`, heading `Writing`, and static check text `Curated notes on agentic engineering, open systems, identity, and practical web software.`.
    - Test 2: `navigationRoutes.map((route) => route.label)` equals `["Home", "About", "Projects", "Writing", "Contact"]`.
    - Test 3: `prerenderRoutes` includes `"/writing"` and every route returned by `writingDetailRoutes()`.
  </behavior>
  <action>Update `src/domain/routes.ts` to import `writingDetailRoutes` from `./writing`. Extend `RouteId` to `"home" | "about" | "projects" | "writing" | "contact"`. Insert the writing route object immediately after the projects route and before contact with exact values: `id: "writing"`, `path: "/writing"`, `label: "Writing"`, `title: "Writing | Peter Ryszkiewicz"`, `description: "Curated notes and essays from Peter Ryszkiewicz on agentic engineering, open systems, identity, and practical web software."`, `heading: "Writing"`, `staticCheckText: "Curated notes on agentic engineering, open systems, identity, and practical web software."`, `nav: true`. Change `prerenderRoutes` to include `...writingDetailRoutes()` after `...projectDetailRoutes()`. Add the route/nav/prerender tests to `src/domain/writing.test.ts`; keep full writing-specific metadata, JSON-LD, sitemap-specific assertions, and release labels out of scope for Phase 16 and Phase 17.</action>
  <verify>
    <automated>bun run test src/domain/writing.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'type RouteId = "home" \\| "about" \\| "projects" \\| "writing" \\| "contact"' src/domain/routes.ts` finds the updated route union.
    - `rg -n 'id: "writing"' src/domain/routes.ts` finds the writing route object.
    - `rg -n 'label: "Writing"' src/domain/routes.ts` finds the navigation label.
    - `rg -n 'Curated notes on agentic engineering, open systems, identity, and practical web software\\.' src/domain/routes.ts src/domain/writing.test.ts` finds the static check text and test expectation.
    - `rg -n '\\.\\.\\.writingDetailRoutes\\(\\)' src/domain/routes.ts` finds the prerender route inclusion.
    - `rg -n 'Home", "About", "Projects", "Writing", "Contact' src/domain/writing.test.ts` finds the nav order test.
    - `bun run test src/domain/writing.test.ts` exits 0.
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>`/writing` is a first-class route/nav entry and static prerender route data is derived from `writingDetailRoutes()` instead of duplicated slugs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| checked-in writing registry -> public route list | Maintainer-authored records become generated visitor routes. |
| checked-in writing link block -> browser navigation | Authored href strings become clickable links in the static UI. |
| writing registry -> project detail UI | Writing relationships become project-page links without reciprocal project data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-01-01 | I | `src/domain/routes.ts` / `writingDetailRoutes()` | mitigate | Generate writing detail routes only from `writingDetailRoutes()`, which filters through `publicWritingEntries()`, and test draft/hidden/archived exclusion through existing and expanded writing tests. |
| T-15-01-02 | T | `src/domain/writing-validation.ts` link blocks | mitigate | Add `unsafe_link_href` validation allowing only `#`, `/` but not `//`, and `https://` hrefs before route rendering consumes link blocks. |
| T-15-01-03 | I | `publicWritingEntriesForProject()` | mitigate | Filter only `publicWritingEntries(entries)` by `relatedProjectSlugs.includes(project.slug)` so project pages never derive links from draft, hidden, or archived writing. |
| T-15-01-04 | E | runtime API/secrets | accept | This plan adds no runtime API calls, environment variables, network fetches, or server endpoints; existing no-runtime-GitHub checks remain unchanged. |
</threat_model>

<verification>
Run after both tasks:

```bash
bun run test src/domain/writing.test.ts src/domain/writing-validation.test.ts
bun run typecheck
```
</verification>

<success_criteria>
- `/writing` is present in `navigationRoutes` after Projects and before Contact.
- `prerenderRoutes` includes `"/writing"` and every `writingDetailRoutes()` result.
- `publicWritingEntriesForProject()` exists and derives project-related writing from public writing entries only.
- Unsafe writing link href protocols fail curation validation with `unsafe_link_href`.
- No Phase 16 metadata/JSON-LD/sitemap-specific assertions or Phase 17 release-readiness label changes are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/15-writing-routes-and-dark-ui/15-01-SUMMARY.md`.
</output>
