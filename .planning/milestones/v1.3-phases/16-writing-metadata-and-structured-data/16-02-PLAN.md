---
phase: 16-writing-metadata-and-structured-data
plan: 02
type: execute
wave: 2
depends_on:
  - 16-01
files_modified:
  - src/routes/writing/index.tsx
  - src/routes/writing/[slug].tsx
  - scripts/verify-static.ts
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
user_setup: []
must_haves:
  truths:
    - "Generated /writing HTML exposes route-specific metadata and ItemList JSON-LD before hydration."
    - "Every generated public /writing/{slug} HTML page exposes entry-specific metadata, article Open Graph fields, Twitter fields, and BlogPosting JSON-LD before hydration."
    - "Generated sitemap output includes /writing and public writing details while excluding non-public and unknown writing paths."
    - "Writing route social metadata uses the checked-in static fallback image and no runtime image generation path."
    - "Unknown writing slug fallback stays non-leaking and does not reveal requested or unpublished slugs."
  artifacts:
    - path: "src/routes/writing/index.tsx"
      provides: "Writing index route head metadata plus Person and ItemList JSON-LD scripts"
      contains: "writingItemListJsonLd"
    - path: "src/routes/writing/[slug].tsx"
      provides: "Writing detail route head metadata, article fields, and BlogPosting JSON-LD"
      contains: "metadataForWritingEntry"
    - path: "scripts/verify-static.ts"
      provides: "Narrow static output assertions for writing metadata, JSON-LD, sitemap, and social fallback"
      contains: "assertMetadataForWritingEntry"
  key_links:
    - from: "src/routes/writing/[slug].tsx"
      to: "src/domain/seo.ts"
      via: "metadataForWritingEntry and writingBlogPostingJsonLd"
      pattern: "metadataForWritingEntry|writingBlogPostingJsonLd"
    - from: "src/routes/writing/index.tsx"
      to: "src/domain/seo.ts"
      via: "writingItemListJsonLd and jsonLdScriptContent"
      pattern: "writingItemListJsonLd|jsonLdScriptContent"
    - from: "scripts/verify-static.ts"
      to: "src/domain/seo.ts"
      via: "metadataForWritingEntry, writingBlogPostingJsonLd, writingItemListJsonLd, sitemapXml"
      pattern: "metadataForWritingEntry|writingBlogPostingJsonLd|writingItemListJsonLd|sitemapXml"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-06-14T15-12-27
generated_at: 2026-06-14T15:35:41Z
---

<objective>
Wire Phase 16 writing metadata helpers into Solid route heads and prove the generated static output.

Purpose: Make `/writing` and public `/writing/{slug}` routes discoverable to crawlers and social previews while preserving the Phase 15 visible UI and Phase 17 release boundary.
Output: Updated writing route head rendering and narrow static verifier assertions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-writing-metadata-and-structured-data/16-CONTEXT.md
@.planning/phases/16-writing-metadata-and-structured-data/16-RESEARCH.md
@.planning/phases/16-writing-metadata-and-structured-data/16-UI-SPEC.md
@.planning/phases/16-writing-metadata-and-structured-data/16-01-SUMMARY.md
@.planning/phases/15-writing-routes-and-dark-ui/15-04-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/seo.ts
@src/domain/writing.ts
@src/domain/routes.ts
@src/routes/writing/index.tsx
@src/routes/writing/[slug].tsx
@scripts/verify-static.ts

<interfaces>
Plan 16-01 must provide these helpers:

```ts
export function metadataForWritingEntry(entry: PublicWritingEntry, profile?: Profile): PageMetadata;
export function writingBlogPostingJsonLd(entry: PublicWritingEntry, profile?: Profile): WritingBlogPostingJsonLd;
export function writingItemListJsonLd(entries?: readonly PublicWritingEntry[], profile?: Profile): WritingItemListJsonLd;
export function jsonLdScriptContent(value: unknown): string;
```

Existing route safety contract:

```ts
const entry = () => maybePublicWritingEntryBySlug(params.slug ?? "");
```

Keep this lookup as the only source of public detail data. Unknown-slug fallback must not echo `params.slug`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render writing index ItemList JSON-LD without changing visible UI</name>
  <files>src/routes/writing/index.tsx</files>
  <read_first>
    - `src/routes/writing/index.tsx` for current `/writing` metadata, `personJsonLd()`, asset links, and visible copy.
    - `src/domain/seo.ts` for `writingItemListJsonLd()`, `jsonLdScriptContent()`, `metadataForRoute()`, `personJsonLd()`, and `siteAssetLinks`.
    - `src/domain/writing.ts` for `publicWritingEntries()` ordering.
    - `.planning/phases/16-writing-metadata-and-structured-data/16-UI-SPEC.md` sections "`/writing` Index", "Route-Visible Non-Regression Contract", and "OpenLinks Identity Contract".
    - `.planning/phases/16-writing-metadata-and-structured-data/16-CONTEXT.md` decisions D-02, D-08, D-09, D-12, D-13, and D-16.
  </read_first>
  <action>Update `src/routes/writing/index.tsx` only. Add `writingItemListJsonLd` to the existing SEO imports. Define `const writingItemListJsonLdValue = writingItemListJsonLd(writingEntries);` next to the existing `personJsonLdValue`. Keep the existing `metadataForRoute(route)` index metadata source because `/writing` route copy is already route-specific per D-02. Render a second non-visual `<script type="application/ld+json">{jsonLdScriptContent(writingItemListJsonLdValue)}</script>` adjacent to the existing Person JSON-LD script. Preserve all visible Phase 15 copy exactly: `Notes and essays`, H1 `Writing`, the lead text, card labels, empty state, and footer/header behavior. Do not add OpenLinks CTA placement, badges, share widgets, visible schema copy, RSS/search/tag archive UI, CMS/MDX/content loaders, dynamic image routes, or runtime fetches.</action>
  <verify>
    <automated>bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'writingItemListJsonLd|writingItemListJsonLdValue' src/routes/writing/index.tsx` finds index JSON-LD wiring.
    - `rg -n 'personJsonLdValue|jsonLdScriptContent\\(writingItemListJsonLdValue\\)' src/routes/writing/index.tsx` finds Person and ItemList JSON-LD script rendering.
    - `rg -n 'Notes and essays|<h1 class="page-title">Writing</h1>|Curated notes on agentic engineering, open systems, identity, and practical web software\\.' src/routes/writing/index.tsx` still finds unchanged visible copy.
    - `rg -n 'OpenLinks CTA|schema|JSON-LD|RSS|search|tag archive|CMS|MDX|fetch\\(|dynamic image|og image route' src/routes/writing/index.tsx` exits 1.
    - `bun run typecheck` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <done>`/writing` renders helper-derived ItemList JSON-LD before hydration while preserving the approved dark-primary index UI.</done>
</task>

<task type="auto">
  <name>Task 2: Render writing detail metadata, article tags, and BlogPosting JSON-LD</name>
  <files>src/routes/writing/[slug].tsx</files>
  <read_first>
    - `src/routes/writing/[slug].tsx` for current public lookup, fallback copy, visible article body, and related project rendering.
    - `src/routes/projects/[slug].tsx` for existing `HeadLink`, `siteAssetLinks`, Open Graph, Twitter, and JSON-LD rendering pattern.
    - `src/domain/seo.ts` for `metadataForWritingEntry()`, `writingBlogPostingJsonLd()`, `jsonLdScriptContent()`, and `siteAssetLinks`.
    - `src/domain/writing.ts` for `maybePublicWritingEntryBySlug()` and `PublicWritingEntry`.
    - `.planning/phases/16-writing-metadata-and-structured-data/16-UI-SPEC.md` sections "`/writing/{slug}` Detail", "Unknown Writing Slug", "Sitemap And Social Fallback", and "OpenLinks Identity Contract".
    - `.planning/phases/16-writing-metadata-and-structured-data/16-CONTEXT.md` decisions D-01 through D-07, D-09, D-12, D-13, and D-16.
  </read_first>
  <action>Update `src/routes/writing/[slug].tsx` only. Change the `@solidjs/meta` import to include `Link as HeadLink`, `Meta`, and `Title`. Import `metadataForWritingEntry`, `siteAssetLinks`, `writingBlogPostingJsonLd`, and keep `jsonLdScriptContent` from `../../domain/seo`; remove the route-level `personJsonLdValue` script from public writing details once BlogPosting JSON-LD includes `author` and `creator` from `personJsonLd()`. Inside `WritingArticle`, set `const metadata = metadataForWritingEntry(entry);` and `const jsonLd = writingBlogPostingJsonLd(entry);`. Replace the local title string with helper output. Render `<Title>`, description, canonical, `siteAssetLinks`, Open Graph title/description/url/type/image/width/height/alt, Twitter card/title/description/image/image alt, optional `article:published_time` from `metadata.openGraph.article?.maybePublishedTime`, optional `article:modified_time` from `metadata.openGraph.article?.maybeModifiedTime`, and one `article:tag` meta per `metadata.openGraph.article?.tags ?? []`. Render `<script type="application/ld+json">{jsonLdScriptContent(jsonLd)}</script>` before the article body. Keep unknown fallback title and description exactly as approved and do not add canonical/Open Graph/Twitter/JSON-LD for unknown slugs unless the implementation can do so without exposing `params.slug`; the fallback must not echo requested, draft, hidden, archived, or private content. Preserve all visible Phase 15 article copy and related project rendering. Do not add publisher/comment/feed/CMS fields, dynamic OG endpoints, runtime content fetches, new OpenLinks placements, or share widgets.</action>
  <verify>
    <automated>bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'metadataForWritingEntry|writingBlogPostingJsonLd|const metadata = metadataForWritingEntry\\(entry\\)|const jsonLd = writingBlogPostingJsonLd\\(entry\\)' 'src/routes/writing/[slug].tsx'` finds detail helper usage.
    - `rg -n 'rel="canonical"|og:title|og:type|og:image|twitter:card|article:published_time|article:modified_time|article:tag' 'src/routes/writing/[slug].tsx'` finds required head tags.
    - `rg -n 'siteAssetLinks|HeadLink|jsonLdScriptContent\\(jsonLd\\)' 'src/routes/writing/[slug].tsx'` finds asset and JSON-LD rendering.
    - `rg -n 'No public writing here \\| Writing \\| Bright Builds|Return to the writing index to browse published notes\\.|Browse writing' 'src/routes/writing/[slug].tsx'` finds unchanged fallback copy.
    - `rg -n 'params\\.slug.*No public writing|publisher|commentCount|aggregateRating|RSS|CMS|MDX|fetch\\(|OpenLinks profile' 'src/routes/writing/[slug].tsx'` exits 1.
    - `bun run typecheck` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <done>Each public writing detail route renders helper-derived metadata, article Open Graph tags, Twitter tags, static social fallback fields, and BlogPosting JSON-LD before hydration.</done>
</task>

<task type="auto">
  <name>Task 3: Add narrow static output assertions for writing metadata and sitemap behavior</name>
  <files>scripts/verify-static.ts</files>
  <read_first>
    - `scripts/verify-static.ts` for current `assertMetadata`, `assertJsonLdContains`, `assertSitemapProjectDetailCoverage`, `maybeWritingForDetailRoute`, and writing output exclusion helpers.
    - `src/domain/seo.ts` for `metadataForWritingEntry()`, `writingBlogPostingJsonLd()`, `writingItemListJsonLd()`, `sitemapXml()`, and static social fallback behavior.
    - `src/domain/writing.ts` for `publicWritingEntries()`, `writingDetailRoutes()`, and `writingDetailPath()`.
    - `.planning/phases/16-writing-metadata-and-structured-data/16-CONTEXT.md` decisions D-10, D-11, D-14, D-15, and D-16.
    - `.planning/phases/16-writing-metadata-and-structured-data/16-UI-SPEC.md` section "Verification Contract".
  </read_first>
  <action>Update `scripts/verify-static.ts` only. Import `metadataForWritingEntry`, `writingBlogPostingJsonLd`, and `writingItemListJsonLd` from `../src/domain/seo`. Add `assertMetadataForWritingEntry(entry: PublicWritingEntry, html: string)` that calls existing `assertMetadata(writingDetailPath(entry), metadataForWritingEntry(entry), html)`, then asserts article meta tags: `article:published_time` only when `entry.maybePublishedOn` exists, `article:modified_time` only when `entry.maybeUpdatedOn` exists, and `article:tag` for every topic/tag. Add `assertWritingBlogPostingJsonLd(entry, html)` that uses `writingBlogPostingJsonLd(entry)` and `assertJsonLdContains()` for `BlogPosting`, entry title, entry summary, canonical writing URL, `personJsonLd().sameAs[0]`, and `https://openlinks.us/`. In the route assertion loop, replace the current writing-detail `continue` with `assertMetadataForWritingEntry(maybeWriting, html); assertWritingBlogPostingJsonLd(maybeWriting, html); continue;`. In the top-level `/writing` branch, add `assertJsonLdContains(html, ["ItemList", JSON.stringify(writingItemListJsonLd()), ...publicWritingEntries().map((entry) => `${peterProfile.canonicalOrigin}${writingDetailPath(entry)}`)]);`. Add `assertSitemapWritingCoverage(root)` next to project sitemap coverage: read `sitemap.xml`, assert `<loc>${peterProfile.canonicalOrigin}/writing</loc>` and every `writingDetailRoutes()` URL exist, and assert `/writing/unknown-writing-slug`, `/writing/draft-`, `/writing/hidden-`, and `/writing/archived-` are absent. Call it after `assertSitemapProjectDetailCoverage(outputRoot)`. Do not edit release-readiness docs, browser release labels, aggregate evidence wording, Playwright tests, package scripts, or add broad Phase 17 release-gate coverage.</action>
  <verify>
    <automated>bun run generate:static-metadata && bun run build && bun run verify:static</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'metadataForWritingEntry|writingBlogPostingJsonLd|writingItemListJsonLd' scripts/verify-static.ts` finds static verifier imports/usages.
    - `rg -n 'assertMetadataForWritingEntry|assertWritingBlogPostingJsonLd|assertSitemapWritingCoverage' scripts/verify-static.ts` finds new assertion helpers.
    - `rg -n 'article:published_time|article:modified_time|article:tag|BlogPosting|ItemList|https://openlinks\\.us/' scripts/verify-static.ts` finds writing metadata and JSON-LD assertions.
    - `rg -n '/writing/unknown-writing-slug|/writing/draft-|/writing/hidden-|/writing/archived-' scripts/verify-static.ts` finds sitemap/output exclusion assertions.
    - `rg -n 'release-readiness|release evidence|verify:release|browser label|Playwright' scripts/verify-static.ts` exits 1.
    - `bun run generate:static-metadata` exits 0.
    - `bun run build` exits 0.
    - `bun run verify:static` exits 0.
  </acceptance_criteria>
  <done>Generated static output proves writing metadata, BlogPosting JSON-LD, index ItemList JSON-LD, sitemap inclusion/exclusion, and static social fallback behavior without Phase 17 release-contract expansion.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| route params -> public writing lookup | `params.slug` is untrusted and must pass through `maybePublicWritingEntryBySlug()` before metadata or JSON-LD is rendered. |
| helper output -> Solid route head | Metadata and JSON-LD helper values become generated head/script HTML before hydration. |
| generated sitemap/static output -> crawler discovery | Static files expose public URLs to crawlers and social parsers. |
| external identity/social URLs -> generated metadata | OpenLinks and social image URLs must remain canonical, intentional, and token-free. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-02-01 | I | `src/routes/writing/[slug].tsx` fallback | mitigate | Keep `maybePublicWritingEntryBySlug(params.slug ?? "")` as the only entry source and preserve fallback copy that never echoes `params.slug` or unpublished entry data. |
| T-16-02-02 | T/I | JSON-LD script rendering | mitigate | Render BlogPosting and ItemList JSON-LD only through `jsonLdScriptContent()` and verify generated HTML contains expected safe JSON-LD fields. |
| T-16-02-03 | I | sitemap discovery | mitigate | Assert sitemap includes `/writing` and `writingDetailRoutes()` while excluding unknown and non-public writing path patterns. |
| T-16-02-04 | S/R | author identity metadata | mitigate | BlogPosting author/creator must come from `personJsonLd()` so OpenLinks `sameAs` matches the existing low-intrusion identity graph. |
| T-16-02-05 | I/E | runtime content, image, and token exposure | mitigate | Grep modified route/verifier files for runtime fetch/API/token/CMS/MDX/dynamic image additions and use the checked-in `/social/bright-builds-og.png` fallback through helper-derived metadata. |
| T-16-02-06 | D | Phase boundary drift | mitigate | Restrict static verifier changes to Phase 16 behavior; do not edit release-readiness docs, evidence labels, browser-release expansion, or aggregate release gate wording. |
</threat_model>

<verification>
Plan-level verification:

```bash
bun run format:check
bun run check
bun run typecheck
bun run test
bun run generate:static-metadata
bun run build
bun run verify:static
```

Browser verification is not required for this plan because route-visible UI must remain unchanged and Phase 17 owns broad browser release label expansion. If the implementation unexpectedly changes visible route UI, run the existing browser checks before summary.
</verification>

<success_criteria>
- `/writing` generated HTML contains route-specific metadata, static social fallback tags, Person JSON-LD, and writing ItemList JSON-LD before hydration.
- Each public `/writing/{slug}` generated HTML contains helper-derived metadata, article Open Graph fields, Twitter tags, static social fallback tags, and BlogPosting JSON-LD before hydration.
- `sitemap.xml` remains route-derived and includes `/writing` plus every `writingDetailRoutes()` URL while excluding non-public and unknown writing paths.
- Unknown writing slugs preserve the non-leaking fallback.
- No CMS, MDX, RSS, search, tag archive, dynamic OG endpoint, runtime content fetch, token exposure, new UI surface, OpenLinks CTA duplication, release-readiness docs, browser release labels, or aggregate evidence wording is added.
</success_criteria>

<output>
After completion, create `.planning/phases/16-writing-metadata-and-structured-data/16-02-SUMMARY.md`.
</output>
