---
phase: 16-writing-metadata-and-structured-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/seo.ts
  - src/domain/writing-metadata.test.ts
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
user_setup: []
must_haves:
  truths:
    - "Writing metadata decisions are pure helper outputs, not copied route-component literals."
    - "Each public writing detail entry has helper-derived title, summary description, canonical URL, article Open Graph fields, Twitter fields, and the checked-in social image fallback."
    - "Writing detail JSON-LD is a static BlogPosting object derived from PublicWritingEntry and personJsonLd()."
    - "The writing index JSON-LD is an ItemList in the same order as publicWritingEntries()."
    - "Sitemap helper tests prove writing public inclusion and draft/hidden/archived exclusion through writingDetailRoutes() and sitemapXml()."
  artifacts:
    - path: "src/domain/seo.ts"
      provides: "Writing metadata, BlogPosting JSON-LD, ItemList JSON-LD, and shared static social fallback helpers"
      exports:
        - "metadataForWritingEntry"
        - "writingBlogPostingJsonLd"
        - "writingItemListJsonLd"
    - path: "src/domain/writing-metadata.test.ts"
      provides: "Focused unit coverage for Phase 16 pure metadata, JSON-LD, social fallback, escaping, and sitemap behavior"
      contains: "metadataForWritingEntry"
  key_links:
    - from: "src/domain/seo.ts"
      to: "src/domain/writing.ts"
      via: "PublicWritingEntry, publicWritingEntries, writingDetailPath, writingDetailRoutes"
      pattern: "PublicWritingEntry|publicWritingEntries|writingDetailPath|writingDetailRoutes"
    - from: "src/domain/seo.ts"
      to: "src/domain/profile.ts"
      via: "peterProfile and personJsonLd(profile)"
      pattern: "personJsonLd\\(profile\\)"
    - from: "src/domain/writing-metadata.test.ts"
      to: "src/domain/seo.ts"
      via: "metadataForWritingEntry, writingBlogPostingJsonLd, writingItemListJsonLd, sitemapXml"
      pattern: "metadataForWritingEntry|writingBlogPostingJsonLd|writingItemListJsonLd|sitemapXml"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 16-2026-06-14T15-12-27
generated_at: 2026-06-14T15:35:41Z
---

<objective>
Create the pure SEO/domain helper contracts for Phase 16 writing metadata, structured data, sitemap behavior, and static social fallback reuse.

Purpose: Keep metadata decisions centralized and testable before route components consume them, per D-01 through D-14.
Output: Extended `src/domain/seo.ts` helper surface plus focused Vitest coverage in `src/domain/writing-metadata.test.ts`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-writing-metadata-and-structured-data/16-CONTEXT.md
@.planning/phases/16-writing-metadata-and-structured-data/16-RESEARCH.md
@.planning/phases/16-writing-metadata-and-structured-data/16-UI-SPEC.md
@.planning/phases/14-writing-domain-foundation/14-01-SUMMARY.md
@.planning/phases/15-writing-routes-and-dark-ui/15-04-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/seo.ts
@src/domain/writing.ts
@src/domain/routes.ts
@src/domain/profile.ts
@src/domain/portfolio-surfaces.test.ts
@src/domain/project-detail-routes.test.ts

<interfaces>
Current SEO metadata shape to extend:

```ts
export type PageMetadata = {
  title: string;
  description: string;
  canonical: string;
  openGraph: {
    title: string;
    description: string;
    url: string;
    type: "website";
    image: SocialImageMetadata;
  };
  twitter: {
    card: "summary_large_image";
    title: string;
    description: string;
    image: SocialImageMetadata;
  };
};
```

Current writing contracts to consume:

```ts
export type PublicWritingEntry = WritingEntry & { status: "published" };
export function publicWritingEntries(entries?: readonly WritingEntry[]): readonly PublicWritingEntry[];
export function writingDetailPath(entry: Pick<WritingEntry, "slug">): string;
export function writingDetailRoutes(entries?: readonly WritingEntry[]): readonly string[];
```

New helper names are locked for downstream plans:

```ts
export function metadataForWritingEntry(entry: PublicWritingEntry, profile?: Profile): PageMetadata;
export function writingBlogPostingJsonLd(entry: PublicWritingEntry, profile?: Profile): WritingBlogPostingJsonLd;
export function writingItemListJsonLd(entries?: readonly PublicWritingEntry[], profile?: Profile): WritingItemListJsonLd;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add writing detail metadata helper and article social fields</name>
  <files>src/domain/seo.ts, src/domain/writing-metadata.test.ts</files>
  <read_first>
    - `src/domain/seo.ts` for `PageMetadata`, `metadataForRoute`, `metadataForProject`, `socialImageForProfile`, and existing JSON-LD patterns.
    - `src/domain/writing.ts` for `PublicWritingEntry`, `publicWritingEntries()`, `writingDetailPath()`, and nullable date/topic/tag fields.
    - `src/domain/profile.ts` for `peterProfile.canonicalOrigin`.
    - `src/domain/project-detail-routes.test.ts` for share metadata test style and static fallback assertions.
    - `.planning/phases/16-writing-metadata-and-structured-data/16-CONTEXT.md` decisions D-01 through D-06 and D-14.
  </read_first>
  <behavior>
    - Test 1: `metadataForWritingEntry(publicWritingEntries()[0], peterProfile)` returns title `${entry.title} | Writing | Bright Builds`, description `entry.summary`, canonical `${peterProfile.canonicalOrigin}${writingDetailPath(entry)}`, `openGraph.type === "article"`, and Twitter card `summary_large_image`.
    - Test 2: Detail metadata reuses `${peterProfile.canonicalOrigin}/social/bright-builds-og.png` with 1200x630 dimensions for both Open Graph and Twitter images.
    - Test 3: Article metadata emits `maybePublishedTime` only when `entry.maybePublishedOn` exists, emits `maybeModifiedTime` only when `entry.maybeUpdatedOn` exists, and includes tags from `[...entry.topics, ...entry.tags]`.
    - Test 4: Existing `metadataForRoute(routeByPath("/writing"), peterProfile)` still returns index title `Writing | Peter Ryszkiewicz`, index description from `siteRoutes`, canonical `/writing`, `og:type` website, and the same static social fallback.
  </behavior>
  <action>First create `src/domain/writing-metadata.test.ts` with the tests above using explicit `// Arrange`, `// Act`, and `// Assert` sections. Then update `src/domain/seo.ts`: import type `PublicWritingEntry` and `writingDetailPath` from `./writing`; widen `PageMetadata.openGraph.type` from `"website"` to `"website" | "article"`; add optional `article?: { maybePublishedTime?: string; maybeModifiedTime?: string; tags: readonly string[] }` to `PageMetadata.openGraph`; export `metadataForWritingEntry(entry: PublicWritingEntry, profile: Profile = peterProfile): PageMetadata`. The helper must derive title, description, canonical, Open Graph, and Twitter values exactly from the writing entry and `writingDetailPath(entry)` per D-03 and D-04. It must reuse `socialImageForProfile(profile)` per D-05 and D-06. It must not import route components, use `window`, use `fetch`, read files, add a dependency, generate per-writing images, or emit runtime API/image endpoints.</action>
  <verify>
    <automated>bun run test src/domain/writing-metadata.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'metadataForWritingEntry' src/domain/seo.ts src/domain/writing-metadata.test.ts` finds the helper and tests.
    - `rg -n 'type: "website" \\| "article"|article\\?:' src/domain/seo.ts` finds the widened metadata type.
    - `rg -n 'maybePublishedTime|maybeModifiedTime|tags:' src/domain/seo.ts src/domain/writing-metadata.test.ts` finds article social metadata.
    - `rg -n 'summary_large_image|/social/bright-builds-og\\.png|1200|630' src/domain/writing-metadata.test.ts` finds static social fallback assertions.
    - `rg -n 'fetch\\(|window\\.|api\\.github|GITHUB_TOKEN|dynamic OG|per-writing image|runtime image' src/domain/seo.ts src/domain/writing-metadata.test.ts` exits 1.
    - `bun run test src/domain/writing-metadata.test.ts` exits 0.
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>`metadataForWritingEntry()` is a pure tested source of truth for writing detail metadata, article social fields, and checked-in fallback image reuse.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add BlogPosting, ItemList, escaping, and sitemap helper coverage</name>
  <files>src/domain/seo.ts, src/domain/writing-metadata.test.ts</files>
  <read_first>
    - `src/domain/seo.ts` for `personJsonLd()`, `jsonLdScriptContent()`, `projectItemListJsonLd()`, `projectJsonLd()`, and `sitemapXml()`.
    - `src/domain/writing.ts` for `PublicWritingEntry`, `WritingBodyBlock`, `publicWritingEntries()`, `writingDetailPath()`, and `writingDetailRoutes()`.
    - `src/domain/portfolio-surfaces.test.ts` for `personJsonLd`, `jsonLdScriptContent`, and `sitemapXml` assertion patterns.
    - `src/domain/writing.test.ts` for writing fixture shape and non-public status cases.
    - `.planning/phases/16-writing-metadata-and-structured-data/16-CONTEXT.md` decisions D-07 through D-14.
    - `.planning/phases/16-writing-metadata-and-structured-data/16-UI-SPEC.md` sections "OpenLinks Identity Contract" and "Verification Contract".
  </read_first>
  <behavior>
    - Test 1: `writingBlogPostingJsonLd(entry, peterProfile)` returns `@context: "https://schema.org"`, `@type: "BlogPosting"`, headline/name/description/url/mainEntityOfPage from the entry and canonical path, `image` from the checked-in fallback, `author` and `creator` from `personJsonLd(peterProfile)`, keywords from topics plus tags, `about` from topics plus tags, and `articleBody` from section headings plus paragraph/list/callout/link labels.
    - Test 2: BlogPosting JSON-LD includes `datePublished` only when `maybePublishedOn` exists and `dateModified` only when `maybeUpdatedOn` exists; it does not emit `publisher`, `comment`, `commentCount`, `isAccessibleForFree`, `hasPart`, `aggregateRating`, or feed/CMS fields.
    - Test 3: `jsonLdScriptContent(writingBlogPostingJsonLd(entryWithAngleBrackets, peterProfile))` escapes `<` as `\\u003c`.
    - Test 4: `writingItemListJsonLd(publicWritingEntries(fixtures), peterProfile)` returns ordered `ListItem` entries for published entries only, with each item as `BlogPosting` containing headline/name/description/url.
    - Test 5: `sitemapXml(["/writing", ...writingDetailRoutes(fixtures)], peterProfile)` contains `/writing` and the public detail route while excluding draft, hidden, archived, unknown, and unsupported writing paths.
  </behavior>
  <action>Extend the same test file before implementation, then update `src/domain/seo.ts`. Export local structured-data types `WritingBlogPostingJsonLd` and `WritingItemListJsonLd`. Import `publicWritingEntries`, `writingDetailPath`, `writingDetailRoutes` only as needed from `./writing`; keep `writingDetailRoutes` usage in tests if `seo.ts` does not need it. Add `writingBlogPostingJsonLd(entry: PublicWritingEntry, profile: Profile = peterProfile)` that returns only supported fields per D-07: `@context`, `@type`, `headline`, `name`, `description`, `url`, `mainEntityOfPage`, `image`, `author`, `creator`, optional `datePublished`, optional `dateModified`, `keywords`, `about`, and `articleBody`. Use a private `writingArticleBodyText(entry)` helper that flattens section headings plus block text without HTML, hrefs, Markdown parsing, or `innerHTML`; for `link` blocks include only `block.label`. Use a small `withoutUndefinedProperties` helper or inline object spread so absent dates do not serialize as explicit `undefined` properties. Add `writingItemListJsonLd(entries: readonly PublicWritingEntry[] = publicWritingEntries(), profile: Profile = peterProfile)` returning an `ItemList` with `ListItem.position = index + 1` and `item` as a compact `BlogPosting` with headline/name/description/url per D-08. Cover route-derived sitemap behavior through `writingDetailRoutes()` and `sitemapXml()` tests per D-10 and D-11. Reuse `personJsonLd(profile)` per D-12 so OpenLinks remains in `sameAs`, and do not add OpenLinks UI placement per D-13, publisher claims, comment/feed/CMS fields, runtime content fetching, or new dependencies. The focused unit coverage satisfies D-14.</action>
  <verify>
    <automated>bun run test src/domain/writing-metadata.test.ts && bun run test && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'WritingBlogPostingJsonLd|writingBlogPostingJsonLd|WritingItemListJsonLd|writingItemListJsonLd|writingArticleBodyText' src/domain/seo.ts src/domain/writing-metadata.test.ts` finds the helper contracts.
    - `rg -n 'BlogPosting|ItemList|ListItem|articleBody|mainEntityOfPage|datePublished|dateModified|keywords|about' src/domain/seo.ts src/domain/writing-metadata.test.ts` finds required structured-data fields.
    - `rg -n 'personJsonLd\\(profile\\)|https://openlinks\\.us/' src/domain/seo.ts src/domain/writing-metadata.test.ts` finds author identity reuse and OpenLinks sameAs coverage.
    - `rg -n 'publisher|commentCount|aggregateRating|isAccessibleForFree|hasPart|rss|feed|CMS|MDX|innerHTML' src/domain/seo.ts` exits 1.
    - `rg -n 'JSON\\.stringify\\(value\\)\\.replace\\(/</g, "\\\\u003c"\\)' src/domain/seo.ts` finds the existing `jsonLdScriptContent()` escaping implementation.
    - `bun run test src/domain/writing-metadata.test.ts` exits 0.
    - `bun run test` exits 0.
    - `bun run typecheck` exits 0.
  </acceptance_criteria>
  <done>Writing JSON-LD, writing index ItemList JSON-LD, safe serialization, and route-derived sitemap behavior are pure, tested, and derived from public writing helpers.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| checked-in writing registry -> metadata helpers | Authored content becomes crawler/social metadata and JSON-LD. |
| writing helpers -> sitemap XML | Public route helpers determine sitemap-visible writing URLs. |
| profile identity -> structured data | `personJsonLd()` carries Peter/pRizz identity and OpenLinks `sameAs` into author metadata. |
| JSON-LD object -> script tag content | Structured data will be serialized into generated HTML. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-16-01-01 | T/I | `writingBlogPostingJsonLd()` and `jsonLdScriptContent()` | mitigate | Serialize route scripts only through `jsonLdScriptContent()` and test that `<` is escaped as `\\u003c`. |
| T-16-01-02 | I | `writingItemListJsonLd()` and sitemap tests | mitigate | Derive list and sitemap inputs from `publicWritingEntries()` and `writingDetailRoutes()`; add fixture tests proving draft, hidden, archived, unknown, and unsupported paths are absent. |
| T-16-01-03 | S/R | `personJsonLd()` reuse in writing author metadata | mitigate | Reuse centralized `personJsonLd(profile)` so GitHub/OpenLinks identity matches existing visible footer/about/contact surfaces and avoids contradictory author metadata. |
| T-16-01-04 | I | social image metadata | mitigate | Reuse checked-in `${canonicalOrigin}/social/bright-builds-og.png` via `socialImageForProfile(profile)`; do not add remote images, tokens, server endpoints, or runtime image generation. |
| T-16-01-05 | E/I | runtime content/API dependencies | mitigate | Keep helpers pure data-in/data-out functions and grep for `fetch`, `window`, GitHub API/token strings, CMS, MDX, RSS, and runtime image wording in modified files. |
</threat_model>

<verification>
Plan-level verification:

```bash
bun run test src/domain/writing-metadata.test.ts
bun run test
bun run typecheck
```

No browser verification is required for this plan because it creates pure helper contracts only; Plan 16-02 proves generated route head output.
</verification>

<success_criteria>
- `metadataForWritingEntry()` derives entry-specific writing detail metadata with article Open Graph fields and static social fallback reuse.
- `writingBlogPostingJsonLd()` emits supported BlogPosting fields only and reuses `personJsonLd()` for OpenLinks-aware author identity.
- `writingItemListJsonLd()` emits ordered public writing ListItem entries that match `publicWritingEntries()`.
- Tests prove JSON-LD escaping and sitemap public inclusion/private exclusion through route-derived helpers.
- No runtime content fetches, CMS/MDX/RSS/search/tag archive behavior, dynamic OG endpoints, or new UI surfaces are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/16-writing-metadata-and-structured-data/16-01-SUMMARY.md`.
</output>
