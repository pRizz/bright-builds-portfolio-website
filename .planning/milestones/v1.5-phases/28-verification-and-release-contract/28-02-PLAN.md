---
phase: 28-verification-and-release-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/social-previews/config.ts
  - scripts/verify-release.ts
  - scripts/verify-release.test.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-02, VERIFY-04]
user_setup: []
must_haves:
  truths:
    - "Maintainer can run `bun run build && bun run verify:release` and generated social preview PNGs are checked against both a per-image budget and a total generated-preview budget."
    - "Release budget output reports the fallback social image, each generated social preview PNG, and the generated social preview PNG total from local `.output/public` files."
    - "Automated release evidence labels name only local checks that actually run and do not imply Cloudflare deployment, hosted validators, current live GitHub state, live external-link reachability, or preview smoke checks."
    - "Release verification continues to scan built output for visitor-runtime GitHub API/client/token residue without adding live-network checks."
  artifacts:
    - path: "scripts/social-previews/config.ts"
      provides: "Shared per-image and total generated social preview budget constants"
      exports: ["maxSocialPreviewPngBytes", "maxTotalSocialPreviewPngBytes"]
    - path: "scripts/verify-release.ts"
      provides: "Release budget report, generated social preview budget violations, truthful automated evidence labels, and built-output residue checks"
      exports: ["budgetReportForFiles", "budgetViolationsForReport", "releaseEvidenceLabels"]
    - path: "scripts/verify-release.test.ts"
      provides: "Budget, forbidden runtime residue, and evidence-label regression coverage"
      contains: "generated social preview"
  key_links:
    - from: "scripts/verify-release.ts"
      to: "scripts/social-previews/config.ts"
      via: "maxSocialPreviewPngBytes and maxTotalSocialPreviewPngBytes"
      pattern: "maxSocialPreviewPngBytes|maxTotalSocialPreviewPngBytes"
    - from: "scripts/verify-release.ts"
      to: ".output/public/social/generated/**/*.png"
      via: "releaseFiles(root) -> budgetReportForFiles(files)"
      pattern: "social/generated/"
    - from: "scripts/verify-release.ts"
      to: "scripts/release-readiness.ts"
      via: "automatedReleaseReadinessEvidenceLabels() remains local-only evidence input"
      pattern: "automatedReleaseReadinessEvidenceLabels"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-06-22T15-45-43
generated_at: 2026-06-22T16:02:52Z
---

<objective>
Enforce generated social preview budgets and keep automated release evidence labels truthful.

Purpose: The release gate currently budgets generic static assets and the fallback social image; Phase 28 must make generated route-specific social previews first-class release-budget evidence without suggesting that live or hosted checks ran locally.
Output: Shared generated preview budget constants, release verifier budget collection/violations/output, and focused tests for budgets, evidence labels, and no visitor-runtime GitHub residue.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-verification-and-release-contract/28-CONTEXT.md
@.planning/phases/28-verification-and-release-contract/28-RESEARCH.md
@.planning/phases/25-deterministic-static-image-generation/25-03-SUMMARY.md
@.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-02-SUMMARY.md
@scripts/social-previews/config.ts
@scripts/verify-release.ts
@scripts/verify-release.test.ts
@package.json

<interfaces>
From `scripts/social-previews/config.ts`:
```ts
export const maxSocialPreviewPngBytes = 250 * 1024;
```

From `scripts/verify-release.ts`:
```ts
export type ReleaseBudgetThresholds = {
  routeHtmlBytes: number;
  totalJsBytes: number;
  totalCssBytes: number;
  socialOgImageBytes: number;
};

export type ReleaseBudgetReport = {
  routeHtmlBytes: Map<string, number>;
  totalJsBytes: number;
  totalCssBytes: number;
  assetBytes: Map<string, number>;
};

export function budgetReportForFiles(files: readonly ReleaseFile[]): ReleaseBudgetReport;
export function budgetViolationsForReport(
  report: ReleaseBudgetReport,
  thresholds: ReleaseBudgetThresholds,
): readonly ReleaseFinding[];
export function releaseEvidenceLabels(): readonly string[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add generated social preview release budgets</name>
  <files>scripts/social-previews/config.ts, scripts/verify-release.ts, scripts/verify-release.test.ts</files>
  <read_first>
    - `.planning/phases/28-verification-and-release-contract/28-CONTEXT.md`
    - `.planning/phases/28-verification-and-release-contract/28-RESEARCH.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `scripts/social-previews/config.ts`
    - `scripts/social-previews/manifest.ts`
    - `scripts/verify-release.ts`
    - `scripts/verify-release.test.ts`
    - `public/social/generated/manifest.json`
  </read_first>
  <behavior>
    - Test 1: `budgetReportForFiles()` records `social/generated/projects/example-a.png` and `social/generated/writing/example-b.png` in `generatedSocialPreviewPngBytes`, sums them into `totalGeneratedSocialPreviewPngBytes`, and still records `social/bright-builds-og.png` in `assetBytes` per D-14.
    - Test 2: `budgetViolationsForReport()` returns a finding labeled `generated social preview image budget` when any generated preview PNG exceeds `maxSocialPreviewPngBytes = 250 * 1024` per D-14.
    - Test 3: `budgetViolationsForReport()` returns a finding labeled `generated social preview total budget` when the generated preview total exceeds `maxTotalSocialPreviewPngBytes = 1024 * 1024` per D-14 and the research baseline of 753,663 bytes.
    - Test 4: `printBudgetReport()` output path is covered indirectly by `bun run verify:release`; the implementation must print `generated social preview PNG total` and sorted generated image rows so maintainers can see the local budget evidence per D-15.
  </behavior>
  <action>Add a total generated preview budget without changing the deterministic image generator. In `scripts/social-previews/config.ts`, export `maxTotalSocialPreviewPngBytes = 1024 * 1024` next to `maxSocialPreviewPngBytes`; this is a clear 1 MiB budget above the researched 753,663-byte current generated PNG total. In `scripts/verify-release.ts`, import `maxSocialPreviewPngBytes` and `maxTotalSocialPreviewPngBytes`. Extend `ReleaseBudgetThresholds` with `generatedSocialPreviewImageBytes: number` and `totalGeneratedSocialPreviewBytes: number`, and set `releaseBudgetThresholds.generatedSocialPreviewImageBytes = maxSocialPreviewPngBytes` and `releaseBudgetThresholds.totalGeneratedSocialPreviewBytes = maxTotalSocialPreviewPngBytes`. Extend `ReleaseBudgetReport` with `generatedSocialPreviewPngBytes: Map<string, number>` and `totalGeneratedSocialPreviewPngBytes: number`. In `budgetReportForFiles()`, when a file path starts with `social/generated/` and ends with `.png`, add it to `generatedSocialPreviewPngBytes` and add its byte length to the total. In `budgetViolationsForReport()`, emit per-image findings with label `generated social preview image budget`, `path` set to the image path, and message `${path} is {size}; limit is {limit}.`; emit a total finding with label `generated social preview total budget`, `path: "social/generated"`, and message `Generated social preview PNG total is {size}; limit is {limit}.`. In `printBudgetReport()`, print sorted rows for each generated preview PNG and one row exactly beginning `- generated social preview PNG total:`. Do not add a manifest parser to release verification; completeness is owned by `verify:social-previews` and `verify:static` per D-02, D-10, and D-14.</action>
  <verify>
    <automated>bun run test scripts/verify-release.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/social-previews/config.ts` contains `export const maxTotalSocialPreviewPngBytes = 1024 * 1024`
    - `scripts/verify-release.ts` contains `generatedSocialPreviewImageBytes`
    - `scripts/verify-release.ts` contains `totalGeneratedSocialPreviewBytes`
    - `scripts/verify-release.ts` contains `generatedSocialPreviewPngBytes`
    - `scripts/verify-release.ts` contains `social/generated/`
    - `scripts/verify-release.ts` contains `generated social preview image budget`
    - `scripts/verify-release.ts` contains `generated social preview total budget`
    - `scripts/verify-release.ts` contains `generated social preview PNG total`
    - `scripts/verify-release.test.ts` contains `generated social preview image budget`
    - `scripts/verify-release.test.ts` contains `generated social preview total budget`
    - `bun run test scripts/verify-release.test.ts` exits 0
  </acceptance_criteria>
  <done>Release budget reporting and violations cover generated route-specific social preview PNGs individually and in aggregate using shared social preview budget constants.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep release evidence labels local-only and automated</name>
  <files>scripts/verify-release.ts, scripts/verify-release.test.ts</files>
  <read_first>
    - `.planning/phases/28-verification-and-release-contract/28-CONTEXT.md`
    - `.planning/REQUIREMENTS.md`
    - `scripts/verify-release.ts`
    - `scripts/verify-release.test.ts`
    - `scripts/release-readiness.ts`
    - `package.json`
  </read_first>
  <behavior>
    - Test 1: `releaseEvidenceLabels()` includes the exact label `generated social preview asset budgets` after budget support exists per D-14 and D-15.
    - Test 2: Joined automated labels do not contain `Cloudflare`, `preview`, `deploy`, `hosted`, `network`, `live link`, `live external`, `current live GitHub`, or `social-card validator` per D-03, D-15, D-17, and D-20.
    - Test 3: The forbidden output scanner test still catches `api.github.com`, `github.com/graphql`, `@octokit/`, `GITHUB_TOKEN`, `GITHUB_METADATA_TOKEN`, public GitHub token prefixes, and token-like values without echoing secret values per D-04.
  </behavior>
  <action>Update only automated local evidence wording. In `scripts/verify-release.ts`, add `generated social preview asset budgets` to `releaseEvidenceLabels()` because `verify:release` now enforces generated preview budgets locally. Keep manual checklist labels out of `releaseEvidenceLabels()` per D-15 and D-17. In `scripts/verify-release.test.ts`, update the expected label list and add negative assertions for the exact forbidden terms `Cloudflare`, `preview`, `deploy`, `hosted`, `network`, `live link`, `live external`, `current live GitHub`, and `social-card validator`. Preserve the existing forbidden output scanner coverage for visitor-runtime GitHub API/client/token residue per D-04. Do not edit `package.json`, do not add `report:freshness` or live command names to `verify`, and do not add dependency packages per D-01, D-03, and D-05.</action>
  <verify>
    <automated>bun run test scripts/verify-release.test.ts && bun run build && bun run verify:release</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-release.ts` contains `generated social preview asset budgets`
    - `scripts/verify-release.test.ts` contains `generated social preview asset budgets`
    - `scripts/verify-release.test.ts` contains `current live GitHub`
    - `scripts/verify-release.test.ts` contains `social-card validator`
    - `scripts/verify-release.test.ts` contains `api.github.com`
    - `scripts/verify-release.test.ts` contains `github.com/graphql`
    - `scripts/verify-release.test.ts` contains `GITHUB_METADATA_TOKEN`
    - `bun run test scripts/verify-release.test.ts && bun run build && bun run verify:release` exits 0
  </acceptance_criteria>
  <done>Release evidence labels truthfully name generated social preview budget evidence that runs locally, while manual/live/hosted checks stay outside automated labels and visitor-runtime GitHub residue remains guarded.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `.output/public` -> release verifier | Built files are untrusted local artifacts until scanned for sizes, runtime residue, external links, and semantic/accessibility checks. |
| generated PNG bytes -> release budget | Generated social preview PNG sizes must be measured from copied output files, not assumed from checked-in manifests. |
| automated labels -> maintainer release decision | Labels can cause false confidence if they imply hosted, live, or manual checks ran locally. |
| built text output -> scanner output | Secret-like patterns must be detected without echoing token values. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-02-01 | D | `budgetReportForFiles()` | mitigate | Enforce per-image `maxSocialPreviewPngBytes` and total `maxTotalSocialPreviewPngBytes` for all `.output/public/social/generated/**/*.png` files. |
| T-28-02-02 | R | `releaseEvidenceLabels()` | mitigate | Tests assert automated labels include only local evidence and exclude Cloudflare, hosted, preview/deploy, network, live external-link, current live GitHub, and social-card validator claims. |
| T-28-02-03 | I | `forbiddenBuiltOutputFindings()` | mitigate | Preserve token and GitHub runtime scanner tests while redacting matched token-like values from messages. |
| T-28-02-04 | T | generated static assets | mitigate | Budgets are computed from actual copied output files after build, not from route helper assumptions or manual lists. |
</threat_model>

<verification>
Run these after all tasks in this plan:

```bash
bun run test scripts/verify-release.test.ts
bun run build
bun run verify:release
```
</verification>

<success_criteria>
This plan is complete when `verify:release` measures generated route-specific social preview PNG budgets per file and in aggregate, reports those local budget facts, and keeps automated evidence labels separated from manual/live/hosted release obligations.
</success_criteria>

<output>
After completion, create `.planning/phases/28-verification-and-release-contract/28-02-SUMMARY.md`
</output>
