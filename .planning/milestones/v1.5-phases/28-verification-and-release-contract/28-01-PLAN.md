---
phase: 28-verification-and-release-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-static/metadata-jsonld-verifier.ts
  - scripts/verify-static/run-static-verification.ts
  - scripts/verify-static.test.ts
autonomous: true
requirements: [VERIFY-01, VERIFY-03]
user_setup: []
must_haves:
  truths:
    - "Maintainer can run `bun run build && bun run verify:static` and every helper-covered social preview route is checked against generated HTML metadata, JSON-LD image values, copied local PNG dimensions, and copied manifest entries."
    - "Static verification derives covered social preview routes from `maybeSocialPreviewTargetForRoutePath()` / `socialPreviewTargets()` and does not introduce a route-to-image fixture list."
    - "Covered routes fail static verification when `.output/public/social/generated/manifest.json` is missing or when its route path, asset path, dimensions, or source fingerprint disagree with the domain target."
    - "Generic routes such as `/`, `/about`, and `/contact` continue to accept `SOCIAL_PREVIEW_FALLBACK_IMAGE` without requiring a generated manifest entry."
  artifacts:
    - path: "scripts/verify-static/metadata-jsonld-verifier.ts"
      provides: "Route metadata, JSON-LD, local PNG, and social preview manifest consistency assertions"
      exports: ["assertRouteMetadataAndJsonLd", "assertMetadataImageMapsToLocalAsset"]
    - path: "scripts/verify-static/run-static-verification.ts"
      provides: "Static verification summary naming social preview manifest coverage"
      exports: ["staticVerificationSummary", "runStaticVerification"]
    - path: "scripts/verify-static.test.ts"
      provides: "Import-safe tests for manifest-aware static social preview verification"
      contains: "social preview manifest"
  key_links:
    - from: "scripts/verify-static/metadata-jsonld-verifier.ts"
      to: ".output/public/social/generated/manifest.json"
      via: "read copied manifest from static output root for covered routes"
      pattern: "social/generated/manifest\\.json"
    - from: "scripts/verify-static/metadata-jsonld-verifier.ts"
      to: "src/domain/social-previews.ts"
      via: "maybeSocialPreviewTargetForRoutePath(routePath)"
      pattern: "maybeSocialPreviewTargetForRoutePath"
    - from: "scripts/verify-static/metadata-jsonld-verifier.ts"
      to: "scripts/verify-static/output.ts"
      via: "assertPngDimensions(outputRoot, assetPath, width, height)"
      pattern: "assertPngDimensions"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-06-22T15-45-43
generated_at: 2026-06-22T16:02:52Z
---

<objective>
Make static output verification manifest-aware for generated social preview routes.

Purpose: Phase 26 verifies route-correct social image metadata and local PNG dimensions; Phase 28 must also prove the copied static manifest agrees with the helper-derived route contract so generated HTML cannot drift from checked-in preview evidence.
Output: Static verifier code and focused Vitest coverage that fail for missing or mismatched social preview manifest entries while preserving generic fallback image behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-verification-and-release-contract/28-CONTEXT.md
@.planning/phases/28-verification-and-release-contract/28-RESEARCH.md
@.planning/phases/24-social-image-data-contract/24-01-SUMMARY.md
@.planning/phases/25-deterministic-static-image-generation/25-03-SUMMARY.md
@.planning/phases/26-metadata-wiring-and-static-references/26-02-SUMMARY.md
@scripts/verify-static/metadata-jsonld-verifier.ts
@scripts/verify-static/run-static-verification.ts
@scripts/verify-static/output.ts
@scripts/verify-static.test.ts
@scripts/social-previews/manifest.ts
@src/domain/social-previews.ts

<interfaces>
From `scripts/verify-static/metadata-jsonld-verifier.ts`:
```ts
export function assertRouteMetadataAndJsonLd(
  outputRoot: string,
  routePath: string,
  html: string,
): void;

export function assertMetadataImageMapsToLocalAsset(
  outputRoot: string,
  routePath: string,
  imageUrl: string,
): void;
```

From `src/domain/social-previews.ts`:
```ts
export type SocialPreviewTarget = {
  routePath: string;
  assetPath: string;
  title: string;
  description: string;
  kind: SocialPreviewRouteKind;
  kicker: string;
  labels: readonly string[];
  alt: string;
  dimensions: { readonly width: 1200; readonly height: 630 };
  sourceFingerprint: string;
};

export function socialPreviewTargets(): readonly SocialPreviewTarget[];
export function maybeSocialPreviewTargetForRoutePath(routePath: string): SocialPreviewTarget | null;
export const SOCIAL_PREVIEW_FALLBACK_IMAGE: {
  assetPath: "/social/bright-builds-og.png";
  alt: string;
  dimensions: { readonly width: 1200; readonly height: 630 };
};
```

From `scripts/social-previews/manifest.ts`:
```ts
export type SocialPreviewManifestEntry = {
  routePath: string;
  assetPath: string;
  dimensions: { width: 1200; height: 630 };
  byteSize: number;
  sourceFingerprint: string;
  sha256: string;
};

export type SocialPreviewManifest = {
  version: 1;
  entries: readonly SocialPreviewManifestEntry[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add manifest-aware covered-route static verification</name>
  <files>scripts/verify-static/metadata-jsonld-verifier.ts, scripts/verify-static.test.ts</files>
  <read_first>
    - `.planning/phases/28-verification-and-release-contract/28-CONTEXT.md`
    - `.planning/phases/28-verification-and-release-contract/28-RESEARCH.md`
    - `standards/core/testing.md`
    - `standards/languages/typescript-javascript.md`
    - `scripts/verify-static/metadata-jsonld-verifier.ts`
    - `scripts/verify-static/output.ts`
    - `scripts/verify-static.test.ts`
    - `scripts/social-previews/manifest.ts`
    - `src/domain/social-previews.ts`
  </read_first>
  <behavior>
    - Test 1: A covered route such as `/projects` passes `assertMetadataImageMapsToLocalAsset(tempRoot, "/projects", canonicalGeneratedUrl)` only when the temp static root contains both the generated PNG and `social/generated/manifest.json` with an entry whose `routePath`, `assetPath`, `dimensions.width`, `dimensions.height`, and `sourceFingerprint` match `maybeSocialPreviewTargetForRoutePath("/projects")` per D-10 and D-11.
    - Test 2: The same covered route fails with an error containing `Missing social preview manifest in static output` when the generated PNG exists but `social/generated/manifest.json` is absent per D-10 and D-13.
    - Test 3: The same covered route fails with an error containing `Social preview manifest entry for /projects does not match` when the manifest entry exists but has a wrong `assetPath`, wrong `dimensions`, or wrong `sourceFingerprint` per D-10.
    - Test 4: A generic route such as `/about` still passes with `SOCIAL_PREVIEW_FALLBACK_IMAGE` and no generated manifest entry per D-12.
  </behavior>
  <action>Strengthen the static metadata verifier per D-10 through D-13. In `scripts/verify-static.test.ts`, update the existing `accepts the canonical generated image for a covered route` test so it writes both a valid PNG and a valid copied manifest fixture before asserting success. Add focused tests for missing copied manifest and mismatched manifest entry using Arrange/Act/Assert comments per D-08; this strengthens an existing verifier seam instead of adding broad duplicate route-map coverage per D-09. Use the current helper-derived target from `maybeSocialPreviewTargetForRoutePath("/projects")`; do not hard-code current generated PNG filenames or maintain a route-to-image list per D-07 and D-11. In `scripts/verify-static/metadata-jsonld-verifier.ts`, add manifest parsing for `social/generated/manifest.json` under the provided `outputRoot`, validate the parsed shape as `SocialPreviewManifest`, and for covered routes assert a matching manifest entry by `routePath`. The matching entry must have exactly the target `routePath`, `assetPath`, `dimensions.width`, `dimensions.height`, and `sourceFingerprint`. Keep the existing canonical-origin, expected-path, and `assertPngDimensions()` checks. Only require manifest entries when `maybeSocialPreviewTargetForRoutePath(routePath)` returns a target; fallback routes must keep using `SOCIAL_PREVIEW_FALLBACK_IMAGE` without manifest requirements per D-12. Error messages must name the route path and the failing manifest field. Do not add dependencies, Python automation, live network checks, dynamic OG routes, server functions, or manual fixture maps per D-03, D-05, D-07, and D-11.</action>
  <verify>
    <automated>bun run test scripts/verify-static.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-static/metadata-jsonld-verifier.ts` contains `social/generated/manifest.json`
    - `scripts/verify-static/metadata-jsonld-verifier.ts` contains `Missing social preview manifest in static output`
    - `scripts/verify-static/metadata-jsonld-verifier.ts` contains `Social preview manifest entry for`
    - `scripts/verify-static/metadata-jsonld-verifier.ts` contains `sourceFingerprint`
    - `scripts/verify-static.test.ts` contains `Missing social preview manifest in static output`
    - `scripts/verify-static.test.ts` contains `Social preview manifest entry for /projects does not match`
    - `scripts/verify-static.test.ts` still contains `accepts the fallback image for a generic route`
    - `scripts/verify-static.test.ts` does not contain a copied list of all current `public/social/generated/**/*.png` filenames
    - `bun run test scripts/verify-static.test.ts` exits 0
  </acceptance_criteria>
  <done>Covered-route static image assertions prove generated metadata, local PNG files, and copied manifest entries all agree with helper-derived social preview targets; generic fallback routes remain manifest-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Surface manifest coverage in static verifier evidence</name>
  <files>scripts/verify-static/run-static-verification.ts, scripts/verify-static.test.ts</files>
  <read_first>
    - `.planning/phases/28-verification-and-release-contract/28-CONTEXT.md`
    - `scripts/verify-static/run-static-verification.ts`
    - `scripts/verify-static.test.ts`
    - `scripts/verify-static/expected-route-text.ts`
    - `src/domain/social-previews.ts`
  </read_first>
  <behavior>
    - Test 1: `staticVerificationSummary({ routeCount: 16, outputRoot: ".output/public" })` returns a string that includes the exact phrase `social preview manifest` per D-10 and D-15.
    - Test 2: Import-safe route coverage tests derive every covered social preview route from `socialPreviewTargets()` and assert each target route appears in `expectedRoutes.map((check) => check.route)` per D-06, D-07, and D-11.
  </behavior>
  <action>Update static verifier evidence wording without overclaiming live or hosted checks. In `scripts/verify-static/run-static-verification.ts`, change `staticVerificationSummary()` so the returned sentence includes `social preview manifest` alongside metadata, JSON-LD, route coverage, assets, sitemap, and robots. In `scripts/verify-static.test.ts`, update the existing summary assertion to the exact new string. Add or strengthen an import-safe assertion that `socialPreviewTargets().map((target) => target.routePath)` is fully contained in `expectedRoutes.map((check) => check.route)`; this verifies route coverage remains helper-derived per D-06, D-07, and D-11. Do not add hosted crawler wording, live external-link wording, Cloudflare deployment wording, or current live GitHub wording to automated evidence labels per D-03 and D-15.</action>
  <verify>
    <automated>bun run test scripts/verify-static.test.ts && bun run build && bun run verify:static</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-static/run-static-verification.ts` contains `social preview manifest`
    - `scripts/verify-static.test.ts` contains `social preview manifest`
    - `scripts/verify-static.test.ts` contains `socialPreviewTargets`
    - `scripts/verify-static.test.ts` contains `expectedRoutes.map`
    - `scripts/verify-static/run-static-verification.ts` does not contain `Cloudflare`
    - `scripts/verify-static/run-static-verification.ts` does not contain `hosted`
    - `scripts/verify-static/run-static-verification.ts` does not contain `live link`
    - `bun run test scripts/verify-static.test.ts && bun run build && bun run verify:static` exits 0
  </acceptance_criteria>
  <done>`verify:static` terminal evidence names manifest coverage that is actually checked locally, and import-safe tests prove covered social preview routes remain derived from helper output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static output -> verifier | `.output/public` HTML, PNGs, and copied JSON manifests are untrusted generated artifacts until parsed and checked. |
| manifest JSON -> domain contract | Manifest entries must be parsed and compared to `SocialPreviewTarget` values instead of trusted by shape or path alone. |
| verifier output -> maintainer decision | Static verifier evidence must describe only local generated-output checks and not imply hosted or live-network checks. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-01-01 | T | `scripts/verify-static/metadata-jsonld-verifier.ts` | mitigate | Parse `.output/public/social/generated/manifest.json` and compare route path, asset path, dimensions, and source fingerprint to `maybeSocialPreviewTargetForRoutePath()` for covered routes. |
| T-28-01-02 | R | `staticVerificationSummary()` | mitigate | Summary wording must include only locally checked static output, metadata, JSON-LD, PNG, and manifest evidence; tests reject wording drift into hosted/live claims. |
| T-28-01-03 | I | generated static output | mitigate | Preserve existing forbidden runtime residue and PNG dimension checks while adding manifest consistency; do not add network access or visitor-runtime fetch paths. |
| T-28-01-04 | D | missing `.output/public` | mitigate | Keep `findStaticOutputRoot()` failure message `Run bun run build first` and ensure manifest checks run only after static output exists. |
</threat_model>

<verification>
Run these after all tasks in this plan:

```bash
bun run test scripts/verify-static.test.ts
bun run build
bun run verify:static
```
</verification>

<success_criteria>
This plan is complete when `bun run verify:static` checks every covered route's generated HTML social image metadata, Twitter parity, JSON-LD image field, local PNG existence, dimensions, copied manifest consistency, and existing forbidden runtime residue without adding manual route maps or live/networked checks.
</success_criteria>

<output>
After completion, create `.planning/phases/28-verification-and-release-contract/28-01-SUMMARY.md`
</output>
