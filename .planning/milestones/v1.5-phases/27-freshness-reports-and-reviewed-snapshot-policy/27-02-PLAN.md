---
phase: 27-freshness-reports-and-reviewed-snapshot-policy
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - docs/release-readiness.md
  - scripts/release-readiness.ts
  - scripts/release-readiness.test.ts
autonomous: true
requirements: [FRESH-01, FRESH-04, FRESH-05]
user_setup: []
must_haves:
  truths:
    - "Release-readiness documentation tells maintainers when and how to run `bun run report:freshness` as reviewed offline evidence."
    - "Documentation explicitly says offline freshness evidence does not prove hosted crawler validation, live external-link reachability, or current live GitHub state."
    - "Release-readiness document tests enforce the freshness report section and the reviewed-static versus live/manual boundary."
    - "Aggregate verify tests prove `bun run verify` does not invoke `report:freshness`, `freshness:live`, `smoke:hosted`, or `sync:github-metadata`."
  artifacts:
    - path: "docs/release-readiness.md"
      provides: "Maintainer-facing freshness report instructions and manual smoke boundary"
      contains: "bun run report:freshness"
    - path: "scripts/release-readiness.ts"
      provides: "Required release-readiness document facts for the freshness report contract"
      exports: ["releaseReadinessDocumentFindings"]
    - path: "scripts/release-readiness.test.ts"
      provides: "Doc-contract and aggregate verify exclusion tests for freshness report boundaries"
  key_links:
    - from: "scripts/release-readiness.ts"
      to: "docs/release-readiness.md"
      via: "requiredReleaseReadinessDocumentFacts patterns for freshness report language"
      pattern: "offline freshness report|reviewed static evidence"
    - from: "scripts/release-readiness.test.ts"
      to: "package.json"
      via: "aggregate release script contract assertions"
      pattern: "report:freshness|freshness:live|smoke:hosted|sync:github-metadata"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 27-2026-06-22T11-58-43
generated_at: 2026-06-22T12:18:51.064Z
---

<objective>
Document the reviewed snapshot and freshness report policy, then guard the release-readiness contract with tests.

Purpose: The report is only useful if maintainers understand which evidence is reviewed static evidence and which checks remain manual, hosted, or live-state smoke work.
Output: Updated release-readiness docs plus tests that require the freshness report command, static evidence language, live/manual boundaries, and aggregate verify exclusions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-CONTEXT.md
@.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-RESEARCH.md
@.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@standards/index.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/SKILL.md
@package.json
@docs/release-readiness.md
@scripts/release-readiness.ts
@scripts/release-readiness.test.ts

<interfaces>
From `scripts/release-readiness.ts`:
```ts
type RequiredDocumentFact = {
  label: string;
  pattern: RegExp;
  expectedDescription: string;
};

export function releaseReadinessDocumentFindings(
  documentPath?: string,
): readonly ReleaseFinding[];

export function manualReleaseChecklistLabels(): readonly string[];
```

From `package.json` after Plan 27-01:
```json
{
  "scripts": {
    "report:freshness": "bun run scripts/generate-freshness-report.ts",
    "verify": "bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run verify:project-helper-surface && bun run verify:visual-system && bun run verify:social-previews && bun run build && bun run verify:browser && bun run verify:static && bun run verify:release"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add release-readiness freshness report documentation contract</name>
  <files>docs/release-readiness.md, scripts/release-readiness.ts, scripts/release-readiness.test.ts</files>
  <read_first>
    - `.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-CONTEXT.md`
    - `.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-RESEARCH.md`
    - `.planning/REQUIREMENTS.md`
    - `docs/release-readiness.md`
    - `scripts/release-readiness.ts`
    - `scripts/release-readiness.test.ts`
    - `package.json`
    - `/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/SKILL.md`
  </read_first>
  <behavior>
    - Test 1: `releaseReadinessDocumentFindings("docs/release-readiness.md")` returns `[]` after adding the freshness report section.
    - Test 2: Removing `bun run report:freshness` from a fixture document produces a finding with label `release-readiness document` and expected description `bun run report:freshness`.
    - Test 3: Removing `reviewed static evidence` from a fixture document produces a finding with expected description `reviewed static evidence`.
    - Test 4: Removing `does not prove current live GitHub state`, `does not crawl live external links`, or `does not run hosted social crawler validation` from a fixture document produces release-readiness document findings for each boundary.
  </behavior>
  <action>Update `docs/release-readiness.md` with a new `## Freshness Reports` section after `## Static Output` and before `## Automated Gates`. The section must include this command block exactly: ```bash
bun run report:freshness
```. The prose must state these exact phrases: `reviewed static evidence`, `does not prove current live GitHub state`, `does not crawl live external links`, `does not run hosted social crawler validation`, and `manual smoke`. It must say the command reads checked-in curated data, `src/domain/github-metadata.snapshot.json`, generated social preview files and `public/social/generated/manifest.json`, and built `.output/public` HTML; it must say to run `bun run build` first when `.output/public` is missing. It must say `needs review` and `manual smoke` findings are review prompts, not hidden hard release gates, per D-09. It must say current live GitHub state belongs to manual smoke or explicit maintainer sync/review outside the report, per D-14. It must preserve the OpenLinks low-intrusion posture by describing OpenLinks only as an external-link policy/manual smoke origin, not as a new primary CTA. Update `requiredReleaseReadinessDocumentFacts` in `scripts/release-readiness.ts` with facts for `offline freshness report command`, `reviewed static evidence boundary`, `live GitHub boundary`, `live external-link boundary`, `social crawler boundary`, and `manual smoke severity`. Add focused tests in `scripts/release-readiness.test.ts` using the existing fixture helpers and Arrange/Act/Assert comments.</action>
  <verify>
    <automated>bun run test scripts/release-readiness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/release-readiness.md` contains `## Freshness Reports`
    - `docs/release-readiness.md` contains `bun run report:freshness`
    - `docs/release-readiness.md` contains `reviewed static evidence`
    - `docs/release-readiness.md` contains `does not prove current live GitHub state`
    - `docs/release-readiness.md` contains `does not crawl live external links`
    - `docs/release-readiness.md` contains `does not run hosted social crawler validation`
    - `docs/release-readiness.md` contains `manual smoke`
    - `scripts/release-readiness.ts` contains `offline freshness report command`
    - `scripts/release-readiness.ts` contains `reviewed static evidence boundary`
    - `scripts/release-readiness.ts` contains `live GitHub boundary`
    - `scripts/release-readiness.ts` contains `live external-link boundary`
    - `scripts/release-readiness.ts` contains `social crawler boundary`
    - `scripts/release-readiness.test.ts` contains `reports missing offline freshness report guidance`
    - `bun run test scripts/release-readiness.test.ts` exits 0
  </acceptance_criteria>
  <done>Release-readiness docs and document-contract tests distinguish offline reviewed freshness evidence from hosted crawler validation, live external-link reachability, and current live GitHub state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard aggregate verify against report, live, hosted, and sync commands</name>
  <files>scripts/release-readiness.test.ts</files>
  <read_first>
    - `.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-CONTEXT.md`
    - `.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-RESEARCH.md`
    - `package.json`
    - `scripts/release-readiness.test.ts`
    - `scripts/generate-freshness-report.ts`
  </read_first>
  <behavior>
    - Test 1: The existing aggregate release script contract still equals the exact `verify` command string from `package.json`.
    - Test 2: The aggregate release script contract asserts `verifyScript` does not contain `report:freshness`, `freshness:live`, `smoke:hosted`, `sync:github-metadata`, `scripts/sync-github-metadata.ts`, `https://`, `api.github.com`, or `github.com/graphql`.
    - Test 3: The package script `report:freshness` equals `bun run scripts/generate-freshness-report.ts`.
  </behavior>
  <action>Extend the `describe("aggregate release script contract")` test in `scripts/release-readiness.test.ts`. Keep the expected `verify` script exactly `bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run verify:project-helper-surface && bun run verify:visual-system && bun run verify:social-previews && bun run build && bun run verify:browser && bun run verify:static && bun run verify:release`. Add assertions that `packageJson.scripts["report:freshness"]` is exactly `bun run scripts/generate-freshness-report.ts`; `verifyScript` does not contain `report:freshness`, `freshness:live`, `smoke:hosted`, `sync:github-metadata`, `scripts/sync-github-metadata.ts`, `https://`, `api.github.com`, or `github.com/graphql`. This implements D-15 and D-18 and keeps optional live checks deferred rather than adding any `freshness:live` or `smoke:hosted` command per D-17.</action>
  <verify>
    <automated>bun run test scripts/release-readiness.test.ts && node -e 'const pkg=require("./package.json"); if (pkg.scripts["report:freshness"] !== "bun run scripts/generate-freshness-report.ts") process.exit(1); for (const s of ["report:freshness","freshness:live","smoke:hosted","sync:github-metadata","scripts/sync-github-metadata.ts","https://","api.github.com","github.com/graphql"]) if (pkg.scripts.verify.includes(s)) process.exit(1)' && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/release-readiness.test.ts` contains `expect(packageJson.scripts["report:freshness"]).toBe("bun run scripts/generate-freshness-report.ts")`
    - `scripts/release-readiness.test.ts` contains `expect(verifyScript).not.toContain("report:freshness")`
    - `scripts/release-readiness.test.ts` contains `expect(verifyScript).not.toContain("freshness:live")`
    - `scripts/release-readiness.test.ts` contains `expect(verifyScript).not.toContain("smoke:hosted")`
    - `scripts/release-readiness.test.ts` contains `expect(verifyScript).not.toContain("sync:github-metadata")`
    - `scripts/release-readiness.test.ts` contains `expect(verifyScript).not.toContain("api.github.com")`
    - `bun run test scripts/release-readiness.test.ts` exits 0
    - The inline `node -e` command in `<verify>` exits 0
    - `bun run check` exits 0
  </acceptance_criteria>
  <done>The release-readiness test suite proves the report command exists and that aggregate verification stays free of report, live, hosted, sync, and network command strings.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> maintainer release behavior | Release docs direct human release choices and must not overclaim offline evidence. |
| `package.json` -> aggregate release gate | The aggregate verifier determines what blocks local release and must not hide report-only or live network checks. |
| OpenLinks policy mention -> site identity posture | OpenLinks should remain a low-intrusion identity/external-link policy origin, not a primary release CTA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-27-07 | S | `docs/release-readiness.md` freshness prose | mitigate | Require exact boundary phrases stating the report `does not prove current live GitHub state`, `does not crawl live external links`, and `does not run hosted social crawler validation`. |
| T-27-08 | R | Release evidence documentation | mitigate | Add `requiredReleaseReadinessDocumentFacts` entries so missing freshness report and manual/live boundary language creates test failures. |
| T-27-09 | D | `package.json` `verify` script | mitigate | Add aggregate script tests proving `verify` excludes `report:freshness`, `freshness:live`, `smoke:hosted`, and `sync:github-metadata`. |
| T-27-10 | I | Documentation of external-link smoke checks | mitigate | Preserve manual smoke wording and avoid putting live reachability results in automated evidence labels. |
| T-27-11 | S | OpenLinks identity mention | accept | Docs mention OpenLinks only as an existing external-link policy/manual smoke origin; no UI, metadata, footer, or CTA changes are made. |
</threat_model>

<verification>
Run focused doc-contract tests and the repo-native checker for changed docs/scripts:

```bash
bun run test scripts/release-readiness.test.ts
node -e 'const pkg=require("./package.json"); if (pkg.scripts["report:freshness"] !== "bun run scripts/generate-freshness-report.ts") process.exit(1); for (const s of ["report:freshness","freshness:live","smoke:hosted","sync:github-metadata","scripts/sync-github-metadata.ts","https://","api.github.com","github.com/graphql"]) if (pkg.scripts.verify.includes(s)) process.exit(1)'
bun run check
rg -n 'bun run report:freshness|reviewed static evidence|does not prove current live GitHub state|does not crawl live external links|does not run hosted social crawler validation|manual smoke' docs/release-readiness.md
```
</verification>

<success_criteria>
- `docs/release-readiness.md` includes an offline freshness report section with the exact command `bun run report:freshness`.
- The docs state reviewed static evidence boundaries and do not claim live external-link, hosted crawler, or current GitHub-state validation.
- Release-readiness document facts fail when the freshness report command or boundary phrases are removed.
- Aggregate verify tests prove no report/live/hosted/sync/network command string is part of `bun run verify`.
</success_criteria>

<output>
After completion, create `.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-02-SUMMARY.md`.
</output>
