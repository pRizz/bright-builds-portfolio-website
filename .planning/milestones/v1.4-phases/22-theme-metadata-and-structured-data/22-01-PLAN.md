---
phase: 22-theme-metadata-and-structured-data
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/seo.ts
  - src/domain/routes.ts
  - src/routes/themes/index.tsx
  - src/routes/themes/[slug].tsx
  - src/domain/writing-metadata.test.ts
  - src/domain/theme-routes.test.ts
  - scripts/verify-static/metadata-jsonld-verifier.ts
  - scripts/verify-static/sitemap-assets-verifier.ts
  - scripts/verify-static.test.ts
  - public/sitemap.xml
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
generated_by: gsd-plan-phase-inline-fallback
lifecycle_mode: yolo
phase_lifecycle_id: 22-2026-06-18T01-09-47
generated_at: 2026-06-18T01:20:00Z
must_haves:
  truths:
    - "Theme index and public theme detail routes render route-specific title, description, canonical, Open Graph, and Twitter metadata before hydration."
    - "Theme index and public theme detail routes render static JSON-LD before hydration using helper-derived theme, project, writing, and profile relationships."
    - "Generated sitemap output includes /themes and every public theme detail route while excluding unknown or non-public theme routes."
    - "Theme metadata uses the checked-in social preview fallback and no runtime image generation or server endpoint."
  artifacts:
    - path: "src/domain/seo.ts"
      provides: "Theme metadata and theme JSON-LD helper contracts"
      exports:
        - "metadataForTheme"
        - "themeItemListJsonLd"
        - "themeCollectionPageJsonLd"
    - path: "src/domain/routes.ts"
      provides: "Helper-derived theme sitemap route inclusion"
      contains: "...themeDetailRoutes()"
    - path: "src/routes/themes/[slug].tsx"
      provides: "Theme detail head metadata, JSON-LD, and generic fallback metadata"
      contains: "metadataForTheme"
    - path: "scripts/verify-static/metadata-jsonld-verifier.ts"
      provides: "Generated HTML assertions for theme metadata and JSON-LD"
      contains: "assertThemeCollectionPageJsonLd"
  key_links:
    - from: "src/routes/themes/[slug].tsx"
      to: "src/domain/seo.ts"
      via: "metadataForTheme(), themeCollectionPageJsonLd(), jsonLdScriptContent()"
      pattern: "metadataForTheme"
    - from: "src/domain/seo.ts"
      to: "src/domain/themes.ts"
      via: "themeDetailPath(), publicThemeEntries(), related project/writing helpers"
      pattern: "themeCollectionPageJsonLd"
    - from: "scripts/verify-static/sitemap-assets-verifier.ts"
      to: "src/domain/routes.ts"
      via: "sitemapXml() and sitemapRoutes"
      pattern: "assertSitemapThemeCoverage"
---

<objective>
Add Phase 22's static metadata, structured data, sitemap, and social-preview fallback behavior for theme routes.

Purpose: Make `/themes` and every public `/themes/{slug}` route crawler/social discoverable while preserving static deployment and helper-derived public-only theme relationships.
Output: Theme SEO helpers, route head rendering, sitemap inclusion, generated sitemap asset, and static verification.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/22-theme-metadata-and-structured-data/22-CONTEXT.md
@.planning/phases/22-theme-metadata-and-structured-data/22-RESEARCH.md
@.planning/phases/22-theme-metadata-and-structured-data/22-UI-SPEC.md
@.planning/phases/19-theme-domain-foundation/19-CONTEXT.md
@.planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
@src/domain/seo.ts
@src/domain/routes.ts
@src/domain/themes.ts
@src/routes/themes/index.tsx
@src/routes/themes/[slug].tsx
@scripts/verify-static/metadata-jsonld-verifier.ts
@scripts/verify-static/sitemap-assets-verifier.ts

<interfaces>
From `src/domain/themes.ts`:
```ts
export function publicThemeEntries(themes?: readonly ThemeRecord[]): readonly PublicThemeEntry[];
export function maybePublicThemeEntryBySlug(slug: string): PublicThemeEntry | null;
export function themeDetailPath(theme: Pick<ThemeRecord, "slug">): string;
export function themeDetailRoutes(themes?: readonly ThemeRecord[]): readonly string[];
export function relatedProjectDetailPageProjectsForTheme(theme: Pick<ThemeRecord, "relatedProjectSlugs">): readonly ProjectDetailPageProject[];
export function relatedWritingEntriesForTheme(theme: Pick<ThemeRecord, "relatedWritingSlugs">): readonly PublicWritingEntry[];
```

From `src/domain/seo.ts`:
```ts
export type PageMetadata = { title: string; description: string; canonical: string; openGraph: ...; twitter: ... };
export function metadataForRoute(route: SiteRoute, profile?: Profile): PageMetadata;
export function jsonLdScriptContent(value: unknown): string;
export function sitemapXml(paths?: readonly string[], profile?: Profile): string;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add theme metadata and JSON-LD domain helpers</name>
  <files>src/domain/seo.ts, src/domain/writing-metadata.test.ts</files>
  <read_first>
    - .planning/phases/22-theme-metadata-and-structured-data/22-CONTEXT.md
    - src/domain/seo.ts
    - src/domain/themes.ts
    - src/domain/projects.ts
    - src/domain/writing.ts
    - src/domain/writing-metadata.test.ts
  </read_first>
  <action>Add theme SEO helpers to `src/domain/seo.ts`. Import `PublicThemeEntry`, `publicThemeEntries`, `relatedProjectDetailPageProjectsForTheme`, `relatedWritingEntriesForTheme`, and `themeDetailPath` from `./themes`. Export explicit types `ThemeItemListJsonLd` and `ThemeCollectionPageJsonLd`. Add `metadataForTheme(theme, profile = peterProfile): PageMetadata` with title `${theme.title} | Themes | Bright Builds`, description `theme.summary`, canonical `${profile.canonicalOrigin}${themeDetailPath(theme)}`, Open Graph type `website`, Twitter `summary_large_image`, and the existing social image fallback. Add `themeItemListJsonLd(themes = publicThemeEntries(), profile = peterProfile)` returning an ordered schema.org `ItemList` of public theme detail URLs. Add `themeCollectionPageJsonLd(theme, profile = peterProfile)` returning a schema.org `CollectionPage` with `name`, `description`, `url`, `mainEntityOfPage`, `image`, `creator: personJsonLd(profile)`, `keywords`, `about`, `mentions`, and `hasPart` derived from related selected projects and public writing entries. Use existing project and writing helpers; do not duplicate route-local state or add runtime fetches. Add focused tests to `src/domain/writing-metadata.test.ts` or a new nearby metadata test file for theme detail metadata, social fallback reuse, item list ordering, CollectionPage relationship content, safe JSON-LD serialization, and sitemap theme coverage.</action>
  <acceptance_criteria>
    - `rg -n "export function metadataForTheme" src/domain/seo.ts` finds one export.
    - `rg -n "export function themeItemListJsonLd" src/domain/seo.ts` finds one export.
    - `rg -n "export function themeCollectionPageJsonLd" src/domain/seo.ts` finds one export.
    - `rg -n "CollectionPage|ItemList" src/domain/seo.ts` finds theme structured-data types or literals.
    - `rg -n "derives theme detail metadata" src/domain/writing-metadata.test.ts` finds a focused metadata test.
    - `rg -n "creates ordered theme ItemList JSON-LD" src/domain/writing-metadata.test.ts` finds a focused item-list test.
    - `rg -n "creates CollectionPage JSON-LD" src/domain/writing-metadata.test.ts` finds a focused detail JSON-LD test.
    - `! rg -n "fetch\\(|process\\.env|GITHUB_TOKEN|runtime image|og route" src/domain/seo.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run test src/domain/writing-metadata.test.ts && bun run typecheck</automated>
  </verify>
  <done>Theme metadata and JSON-LD helpers are pure, helper-derived, public-only, covered by tests, and reuse the checked-in social preview fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Wire theme route head output and sitemap routes</name>
  <files>src/domain/routes.ts, src/routes/themes/index.tsx, src/routes/themes/[slug].tsx, src/domain/theme-routes.test.ts, public/sitemap.xml</files>
  <read_first>
    - .planning/phases/22-theme-metadata-and-structured-data/22-CONTEXT.md
    - .planning/phases/22-theme-metadata-and-structured-data/22-UI-SPEC.md
    - src/domain/routes.ts
    - src/domain/seo.ts
    - src/routes/themes/index.tsx
    - src/routes/themes/[slug].tsx
    - src/routes/writing/[slug].tsx
    - src/domain/theme-routes.test.ts
    - scripts/generate-static-metadata.ts
  </read_first>
  <action>Update `src/domain/routes.ts` so `sitemapRoutes` includes all `siteRoutes.map((route) => route.path)`, `projectDetailRoutes()`, `writingDetailRoutes()`, and `themeDetailRoutes()`. Update `src/routes/themes/index.tsx` to render `themeItemListJsonLd()` in its JSON-LD script instead of only `personJsonLd()`. Update `src/routes/themes/[slug].tsx` to import `Link as HeadLink`, `Meta`, and `Title` from `@solidjs/meta`; render generic fallback `<Title>No public theme here | Themes | Bright Builds</Title>`, generic description, and canonical `/themes` without echoing the slug; and render public theme detail `<Title>`, description, canonical, asset links, OG/Twitter tags, and JSON-LD from `metadataForTheme(theme)` and `themeCollectionPageJsonLd(theme)`. Follow the existing writing detail route head-tag pattern. Update `src/domain/theme-routes.test.ts` so sitemap route expectations include `/themes` and every public theme detail route. Run `bun run generate:static-metadata` to update `public/sitemap.xml`. Do not add visual layout changes, new visible OpenLinks CTAs, dynamic image routes, server endpoints, or release-readiness labels.</action>
  <acceptance_criteria>
    - `rg -n "themeDetailRoutes\\(\\)" src/domain/routes.ts` finds theme detail routes inside `sitemapRoutes`.
    - `rg -n "themeItemListJsonLd" src/routes/themes/index.tsx` finds index JSON-LD use.
    - `rg -n "metadataForTheme|themeCollectionPageJsonLd" 'src/routes/themes/[slug].tsx'` finds detail metadata and JSON-LD use.
    - `rg -n "No public theme here \\| Themes \\| Bright Builds" 'src/routes/themes/[slug].tsx'` finds fallback title.
    - `rg -n "/themes/agentic-engineering|/themes/open-identity" public/sitemap.xml` finds public theme detail routes.
    - `rg -n "includes theme index and detail routes in sitemap routes" src/domain/theme-routes.test.ts` finds the updated route test.
    - `! rg -n "dynamic OG|opengraph-image|ImageResponse|runtime image|release evidence|release-readiness" src/routes src/domain/routes.ts public/sitemap.xml` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run test src/domain/theme-routes.test.ts && bun run generate:static-metadata && bun run build</automated>
  </verify>
  <done>Theme routes render static head metadata and JSON-LD, generated sitemap includes public theme routes, and unknown/non-public fallback metadata remains generic.</done>
</task>

<task type="auto">
  <name>Task 3: Extend static verification for theme metadata and sitemap coverage</name>
  <files>scripts/verify-static/metadata-jsonld-verifier.ts, scripts/verify-static/sitemap-assets-verifier.ts, scripts/verify-static.test.ts</files>
  <read_first>
    - .planning/phases/22-theme-metadata-and-structured-data/22-CONTEXT.md
    - .planning/phases/22-theme-metadata-and-structured-data/22-RESEARCH.md
    - scripts/verify-static/metadata-jsonld-verifier.ts
    - scripts/verify-static/sitemap-assets-verifier.ts
    - scripts/verify-static.test.ts
    - src/domain/seo.ts
    - src/domain/themes.ts
    - src/domain/routes.ts
  </read_first>
  <action>Update `scripts/verify-static/metadata-jsonld-verifier.ts` to remove the Phase 20 theme skip. Add `assertMetadataForTheme(outputRoot, theme, html)` using `metadataForTheme(theme)`. Add `assertThemeCollectionPageJsonLd(theme, html)` that checks `CollectionPage`, theme title, description, canonical URL, one profile sameAs URL, `https://openlinks.us/`, related project URLs, and related writing URLs. Add `/themes` index JSON-LD assertions for `ItemList` and every public theme detail URL. Update `scripts/verify-static/sitemap-assets-verifier.ts` with `assertSitemapThemeCoverage(root)` that requires `/themes` and every `themeDetailRoutes()` value in `sitemap.xml`, rejects unknown theme routes, and rejects non-public theme routes when fixtures exist. Export that helper for tests. Update `scripts/verify-static.test.ts` to include the new helper in callable export checks and to assert expected route coverage includes `themeDetailRoutes()`. Keep metadata images mapped to `social/bright-builds-og.png`. Do not add browser-release checks, release-readiness checks, live reachability, or evidence-label changes.</action>
  <acceptance_criteria>
    - `! rg -n "Phase 20 boundary|if \\(maybeTheme\\) \\{\\s*return" scripts/verify-static/metadata-jsonld-verifier.ts` exits 0.
    - `rg -n "assertMetadataForTheme|assertThemeCollectionPageJsonLd|themeItemListJsonLd" scripts/verify-static/metadata-jsonld-verifier.ts` finds theme assertions.
    - `rg -n "assertSitemapThemeCoverage" scripts/verify-static/sitemap-assets-verifier.ts scripts/verify-static.test.ts` finds the sitemap helper and test import/use.
    - `rg -n "social/bright-builds-og.png" scripts/verify-static/metadata-jsonld-verifier.ts src/domain/seo.ts` confirms fallback image verification remains tied to the checked-in asset.
    - `! rg -n "release-readiness|evidence label|live reachability|fetch\\(" scripts/verify-static src/domain/seo.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run test scripts/verify-static.test.ts && bun run build && bun run verify:static</automated>
  </verify>
  <done>Static verification fails if theme metadata, theme JSON-LD, theme sitemap entries, sitemap exclusions, or social fallback asset mapping regress.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated theme registry -> public metadata | Non-public theme records must not become sitemap entries, canonical URLs, or JSON-LD entities. |
| route params -> fallback metadata | Unknown requested slugs must not be echoed in fallback title, description, canonical, body, or JSON-LD. |
| static social image -> crawler metadata | Metadata must point at the checked-in social preview fallback, not a runtime image service. |
| profile identity -> structured data | OpenLinks may appear through existing `Person.sameAs`, but not as a new dominant visible CTA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-22-01-01 | Information Disclosure | `sitemapRoutes`, `themeItemListJsonLd`, `themeCollectionPageJsonLd` | mitigate | Derive public URLs from `publicThemeEntries()` and `themeDetailRoutes()`; add tests and static verification for unknown/non-public exclusions. |
| T-22-01-02 | Information Disclosure | theme detail fallback route | mitigate | Render generic fallback metadata and body copy; acceptance checks require no raw slug echo and static verifier keeps unknown theme output absent. |
| T-22-01-03 | Spoofing/Tampering | social preview metadata | mitigate | Use `socialImageForProfile()` only; static verifier maps OG/Twitter image URLs to checked-in `social/bright-builds-og.png`. |
| T-22-01-04 | Repudiation | automated release evidence | mitigate | Keep Phase 23 release-readiness and evidence-label changes out of this plan; acceptance checks block release evidence churn. |
| T-22-01-05 | Information Disclosure | runtime content/image fetching | mitigate | Acceptance checks block `fetch`, token usage, dynamic OG image routes, and runtime image generation. |
</threat_model>

<verification>
Run targeted verification after each task, then run final `bun run verify`. Also run `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 22 --require-plans --require-verification --raw` after phase verification is written.
</verification>

<success_criteria>
- `/themes` and every public `/themes/{slug}` route have static title, description, canonical, Open Graph, and Twitter metadata.
- `/themes` renders theme `ItemList` JSON-LD and every public theme detail route renders `CollectionPage` JSON-LD before hydration.
- `public/sitemap.xml` includes `/themes`, `/themes/agentic-engineering`, and `/themes/open-identity`, and static verification rejects unknown/non-public theme routes.
- All theme metadata uses the checked-in `social/bright-builds-og.png` fallback and no dynamic image/server endpoint is added.
- `bun run verify` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/22-theme-metadata-and-structured-data/22-01-SUMMARY.md` and `.planning/phases/22-theme-metadata-and-structured-data/22-VERIFICATION.md`.
</output>
