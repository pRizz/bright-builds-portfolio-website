---
phase: 13-project-page-release-coverage
plan: 01
status: planned
created: 2026-06-03T01:49:59Z
type: execute
wave: 1
depends_on:
  - 12-project-metadata-sharing
parallel_safe: true
files_modified:
  - tests/browser-release.playwright.ts
  - scripts/release-readiness.ts
  - scripts/release-readiness.test.ts
  - scripts/verify-release.test.ts
  - docs/release-readiness.md
autonomous: true
requirements:
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04
requirements_addressed:
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04
user_setup: []
must_haves:
  truths:
    - "Static release verification keeps checking selected project detail HTML for story text, route metadata, project JSON-LD, sitemap inclusion/exclusion, local social preview mapping, and forbidden runtime GitHub residue."
    - "Browser release checks include selected project detail routes through the existing prerenderRoutes axe/layout matrix and representative project-detail keyboard and reduced-motion flows."
    - "Release-readiness docs and checks explicitly name project detail route coverage in the clean-builder command sequence bun run install:browser && bun run verify."
    - "Release evidence labels emitted by bun run verify:release include project detail route coverage without claiming hosted audits or live network checks."
    - "The final aggregate bun run verify gate passes with project detail route coverage included."
  artifacts:
    - path: "tests/browser-release.playwright.ts"
      provides: "Axe/layout loops over prerenderRoutes plus representative selected project detail keyboard and reduced-motion browser checks"
      contains: "projectDetailRoutes"
    - path: "scripts/release-readiness.ts"
      provides: "Release-readiness document facts and evidence labels for project detail route coverage"
      exports: ["releaseReadinessDocumentFindings", "releaseReadinessEvidenceLabels"]
    - path: "scripts/release-readiness.test.ts"
      provides: "Vitest regression checks for required project detail release-readiness facts"
      contains: "project detail route coverage"
    - path: "scripts/verify-release.test.ts"
      provides: "Release verifier evidence-label expectations including project detail route coverage"
      contains: "project detail route coverage"
    - path: "docs/release-readiness.md"
      provides: "Clean-builder, automated gate, preview, and production smoke guidance naming selected project detail routes"
      contains: "/projects/openlinks"
  key_links:
    - from: "tests/browser-release.playwright.ts"
      to: "src/domain/projects.ts"
      via: "representative route derived from projectDetailRoutes()[0]"
      pattern: "projectDetailRoutes\\(\\)\\[0\\]"
    - from: "tests/browser-release.playwright.ts"
      to: "src/domain/routes.ts"
      via: "existing exhaustive axe/layout loops over prerenderRoutes"
      pattern: "for \\(const route of prerenderRoutes\\)"
    - from: "scripts/release-readiness.ts"
      to: "docs/release-readiness.md"
      via: "releaseReadinessDocumentFindings required exact-text facts"
      pattern: "project detail metadata, JSON-LD, and sitemap coverage"
    - from: "scripts/verify-release.ts"
      to: "scripts/release-readiness.ts"
      via: "releaseEvidenceLabels spreads releaseReadinessEvidenceLabels"
      pattern: "releaseReadinessEvidenceLabels\\(\\)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 13-2026-06-03T01-38-02
generated_at: 2026-06-03T01:49:59Z
---

<objective>
Complete Phase 13 by making the clean-builder release gate prove selected project detail route coverage across static verification, browser behavior, release-readiness documentation, and release evidence labels.

Purpose: Phase 12 already enforces the static project metadata and JSON-LD contract. This plan closes the remaining release-coverage gaps without adding product UI, new metadata fields, runtime GitHub behavior, live network checks, or another selected-project route list.

Output: Updated Playwright release checks, release-readiness exact-text guards, release evidence expectations, documentation, and final aggregate verification evidence.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/13-project-page-release-coverage/13-CONTEXT.md
@.planning/phases/13-project-page-release-coverage/13-RESEARCH.md
@.planning/phases/12-project-metadata-sharing/12-CONTEXT.md
@.planning/phases/12-project-metadata-sharing/12-01-SUMMARY.md
@.planning/phases/12-project-metadata-sharing/12-VERIFICATION.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@tests/browser-release.playwright.ts
@scripts/release-readiness.ts
@scripts/release-readiness.test.ts
@scripts/verify-release.test.ts
@docs/release-readiness.md
@scripts/verify-static.ts
@scripts/verify-release.ts
@src/domain/projects.ts
@src/domain/routes.ts
@src/routes/projects/[slug].tsx
@package.json

<standards_guidance>
- Repo-local guidance: keep dark-primary checks strict; browser coverage must include desktop and mobile dark rendering, overflow, overlap, keyboard, and reduced-motion behavior.
- Bright Builds guidance loaded: functional core for release-readiness facts, TypeScript `maybe...` naming for nullable internals, Arrange/Act/Assert tests, repo-native Bun verification, and no Python scripts in this Bun-friendly TS repo.
- Pinned canonical standards loaded at commit 05f8d7a6c9c2e157ec4f922a05273e72dab97676: architecture, code shape, verification, testing, operability, and TypeScript/JavaScript.
- Phase 13 is not a frontend design phase. Do not create or require UI-SPEC.md.
</standards_guidance>

<interfaces>
Canonical route sources:
```ts
export function projectDetailRoutes(
  projects?: readonly ProjectStory[],
): readonly string[];

export const prerenderRoutes: readonly string[];
```

Existing browser-release helpers:
```ts
type FocusSnapshot = {
  href: string | null;
  label: string;
  visible: boolean;
};

async function keyboardFocusTargets(page: Page): Promise<readonly FocusSnapshot[]>;
function visibleFocusFailures(focusedTargets: readonly FocusSnapshot[]): readonly string[];
function hasFocusedInternalPath(
  focusedTargets: readonly FocusSnapshot[],
  expectedPath: string,
): boolean;
async function inlinePointerVars(locator: Locator): Promise<{
  pointerX: string;
  pointerY: string;
}>;
```

Existing release-readiness contract:
```ts
type RequiredDocumentFact = {
  label: string;
  text: string;
};

export function releaseReadinessDocumentFindings(
  documentPath?: string,
): readonly ReleaseFinding[];

export function releaseReadinessEvidenceLabels(): readonly string[];
```

Existing release evidence composition:
```ts
export function releaseEvidenceLabels(): readonly string[] {
  return [
    "contrast/readability",
    "focus-visible",
    "focus state",
    "image alt",
    "interactive motion surfaces",
    "reduced-motion",
    ...releaseReadinessEvidenceLabels(),
  ];
}
```
</interfaces>

<decision_coverage>
| Decision | Plan Task | Coverage |
| --- | --- | --- |
| D-01 | Task 1, Task 2 | Use `projectDetailRoutes()` / `prerenderRoutes`; no second selected route list. |
| D-02 | Task 3 | Preserve Phase 12 static verifier behavior and prove it with `bun run verify:static`. |
| D-03 | Task 3 | Static verifier changes are not planned; any executor edit must be a regression guard only. |
| D-04 | Task 1 | Browser checks visibly include project detail route coverage. |
| D-05 | Task 1 | Representative browser-only route derives from `projectDetailRoutes()[0]`. |
| D-06 | Task 1 | Existing layout matrix continues asserting `.dark`, no overflow, and no overlap on desktop/mobile. |
| D-07 | Task 1 | Keyboard coverage reaches a selected detail route from home, then reaches project index and action links on detail. |
| D-08 | Task 1 | Reduced-motion coverage runs on `/` and the representative detail route. |
| D-09 | Task 2 | Docs name project detail route coverage and the clean-builder sequence. |
| D-10 | Task 2 | Document facts fail on missing static and browser project-detail coverage wording. |
| D-11 | Task 2, Task 3 | Release evidence labels include project detail route coverage. |
| D-12 | Task 2 | Preview and production smoke guidance includes `/projects/openlinks` derived from selected routes. |
| D-13 | Task 2, Task 3 | Keep `bun run verify` and `bun run install:browser && bun run verify`. |
| D-14 | Task 2 | No network-dependent browser/link checks; docs keep manual external-link smoke guidance. |
| D-15 | Task 3 | Final completion requires the repo-native aggregate `bun run verify`. |
</decision_coverage>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add representative project detail browser coverage</name>
  <read_first>
    - .planning/phases/13-project-page-release-coverage/13-CONTEXT.md
    - .planning/phases/13-project-page-release-coverage/13-RESEARCH.md
    - tests/browser-release.playwright.ts
    - src/domain/projects.ts
    - src/domain/routes.ts
    - src/routes/projects/[slug].tsx
    - src/styles/app.css
  </read_first>
  <files>tests/browser-release.playwright.ts</files>
  <behavior>
    - Playwright still loops over every `prerenderRoutes` entry for axe scans and dark desktop/mobile layout checks.
    - Keyboard coverage derives one selected detail route from `projectDetailRoutes()[0]`, throws when no selected route exists, reaches that detail route from `/`, and reaches `/projects` plus at least one external project action link from the detail page.
    - Reduced-motion coverage runs on `/` and the representative selected detail route, proving hover transforms remain `none` and pointer CSS vars stay stable when a `.reactive-surface` is present.
  </behavior>
  <action>
    Import `projectDetailRoutes` from `../src/domain/projects` while preserving the existing `prerenderRoutes` import from `../src/domain/routes` per D-01. Add a local helper exactly shaped like:

    ```ts
    function representativeProjectDetailRoute(): string {
      const maybeRoute = projectDetailRoutes()[0];

      if (!maybeRoute) {
        throw new Error("Expected at least one selected project detail route for release coverage.");
      }

      return maybeRoute;
    }
    ```

    Keep the existing `for (const route of prerenderRoutes)` axe and layout loops unchanged except for any formatting needed by Biome. This preserves exhaustive selected route coverage for axe, `.dark`, no horizontal overflow, and no obvious overlap per D-04 and D-06.

    Extend the current keyboard test rather than replacing it. After the existing home-route assertions, assert `hasFocusedInternalPath(focusedTargets, representativeProjectDetailRoute())` with a message containing `focus reaches selected project detail route`. Then navigate to the representative detail route, collect `detailFocusedTargets`, assert `visibleFocusFailures(detailFocusedTargets)` is empty, assert `hasFocusedInternalPath(detailFocusedTargets, "/projects")`, and add a helper `hasFocusedProjectActionLink(focusedTargets)` that returns true only for an external focused link whose label includes `Open source`, `Live site`, `Live docs`, `Docs`, `Article`, or `Related source`. This proves the Project actions nav is keyboard reachable without counting the footer OpenLinks profile per D-07.

    Factor the reduced-motion route behavior into `assertReducedMotionStableOnRoute(page, route)`. For each route, navigate, pick the first visible selector from `.interactive-surface, .reactive-card, .surface-link, .focus-row, .story-card, .project-anchor-card, .theme-card, .contact-card`, hover it, and assert computed `transform` is `none`. Also read pointer vars from `.reactive-surface` before and after a mouse move when the locator is present; if no `.reactive-surface` exists on a route, throw `Expected .reactive-surface to exist for reduced-motion pointer coverage on ${route}.` Call the helper for `/` and `representativeProjectDetailRoute()` in the reduced-motion test per D-08.

    Do not add a manually maintained project path list, new screenshots, CSS changes, UI copy, live network checks, or new browser projects.
  </action>
  <verify>
    <automated>bun run build</automated>
    <automated>bun run verify:browser</automated>
  </verify>
  <acceptance_criteria>
    - `rg "projectDetailRoutes|representativeProjectDetailRoute|focus reaches selected project detail route|hasFocusedProjectActionLink|assertReducedMotionStableOnRoute" tests/browser-release.playwright.ts` finds the new browser coverage hooks.
    - `rg "for \\(const route of prerenderRoutes\\)" tests/browser-release.playwright.ts` still finds the exhaustive route loops.
    - `bun run verify:browser` passes after `bun run build`.
  </acceptance_criteria>
  <done>VERIFY-03 browser coverage includes selected project detail routes for axe, dark desktop/mobile layout, keyboard reachability, and reduced-motion behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make release-readiness docs and facts require project detail coverage</name>
  <read_first>
    - .planning/phases/13-project-page-release-coverage/13-CONTEXT.md
    - scripts/release-readiness.ts
    - scripts/release-readiness.test.ts
    - docs/release-readiness.md
    - src/domain/projects.ts
    - package.json
  </read_first>
  <files>scripts/release-readiness.ts, scripts/release-readiness.test.ts, docs/release-readiness.md</files>
  <behavior>
    - `releaseReadinessDocumentFindings("docs/release-readiness.md")` returns no findings when the checked-in document names project detail coverage.
    - Removing `project detail route coverage` from a fixture document reports a missing `project detail route coverage` fact.
    - Removing `project detail metadata, JSON-LD, and sitemap coverage` reports a missing `project detail static coverage` fact.
    - Removing `project detail axe, layout, keyboard, and reduced-motion coverage` reports a missing `project detail browser coverage` fact.
    - Removing the derived representative selected route, currently `/projects/openlinks`, reports a missing `selected project smoke route` fact.
    - `releaseReadinessEvidenceLabels()` includes `project detail route coverage`.
  </behavior>
  <action>
    In `scripts/release-readiness.ts`, import `projectDetailRoutes` from `../src/domain/projects` and add a pure helper `representativeProjectDetailRoute()` using the same fail-loud `maybeRoute` guard as Task 1 per D-01 and D-05. Add required document facts with these exact texts per D-09 and D-10:

    - label `project detail route coverage`, text `project detail route coverage`
    - label `project detail static coverage`, text `project detail metadata, JSON-LD, and sitemap coverage`
    - label `project detail browser coverage`, text `project detail axe, layout, keyboard, and reduced-motion coverage`
    - label `selected project smoke route`, text `representativeProjectDetailRoute()`

    Add `project detail route coverage` to `releaseReadinessEvidenceLabels()` per D-11. Keep existing external-link policies local and deterministic; do not add `fetch`, HTTP status checks, browser navigation to third-party origins, or runtime link crawling per D-14.

    Update `scripts/release-readiness.test.ts` in the existing Arrange/Act/Assert style. Keep the checked-in document pass test. Add focused negative tests using `releaseDocumentFixtureWithout(...)` for each exact text above and assert the messages contain:

    - `Release-readiness document is missing project detail route coverage: project detail route coverage.`
    - `Release-readiness document is missing project detail static coverage: project detail metadata, JSON-LD, and sitemap coverage.`
    - `Release-readiness document is missing project detail browser coverage: project detail axe, layout, keyboard, and reduced-motion coverage.`
    - `Release-readiness document is missing selected project smoke route: /projects/openlinks.`

    Update the evidence-label test expectation to include `project detail route coverage` after `SEO/static metadata` or another stable release-readiness label.

    Update `docs/release-readiness.md` so the exact required strings appear naturally. Add project detail wording to the Primary Release Gate description, `verify:static`, `verify:browser`, Cloudflare clean-builder guidance, preview checklist, production checklist, and post-deploy smoke check. Include the sentence `Preview and production smoke checks include one selected project detail route, currently /projects/openlinks.` or equivalent exact text containing `/projects/openlinks` per D-12 and D-13. Keep `bun run install:browser && bun run verify` intact and keep manual external-link smoke checks manual per D-14.
  </action>
  <verify>
    <automated>bun run test -- scripts/release-readiness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "project detail route coverage|project detail metadata, JSON-LD, and sitemap coverage|project detail axe, layout, keyboard, and reduced-motion coverage|/projects/openlinks" scripts/release-readiness.ts scripts/release-readiness.test.ts docs/release-readiness.md` finds the required facts, tests, and documentation.
    - `rg "projectDetailRoutes|representativeProjectDetailRoute" scripts/release-readiness.ts` confirms route coverage is derived from the project registry.
    - `! rg "fetch\\(|https?://.*status|page\\.goto\\(.*https://" scripts/release-readiness.ts scripts/release-readiness.test.ts docs/release-readiness.md` succeeds.
    - `bun run test -- scripts/release-readiness.test.ts` passes.
  </acceptance_criteria>
  <done>VERIFY-04 docs and release-readiness checks require project detail route coverage as part of the clean-builder aggregate gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Pin release evidence labels and run the aggregate gate</name>
  <read_first>
    - scripts/verify-release.test.ts
    - scripts/verify-release.ts
    - scripts/release-readiness.ts
    - scripts/verify-static.ts
    - docs/release-readiness.md
    - package.json
  </read_first>
  <files>scripts/verify-release.test.ts</files>
  <behavior>
    - `releaseEvidenceLabels()` includes `project detail route coverage` because it composes `releaseReadinessEvidenceLabels()`.
    - Release evidence labels still do not claim hosted audits, live network checks, or external suite coverage.
    - Focused tests, static verification, browser verification, release verification, and final aggregate verification pass.
  </behavior>
  <action>
    Update the expected label array in `scripts/verify-release.test.ts` so `releaseEvidenceLabels()` includes `project detail route coverage` in the release-readiness label section per D-11. Keep the existing negative assertion that labels do not contain `external suite`, and add assertions that the joined label string does not contain `hosted audit`, `network`, or `live link` per D-14.

    Do not edit `scripts/verify-release.ts` unless the test proves the existing `...releaseReadinessEvidenceLabels()` composition is broken. Do not edit `scripts/verify-static.ts` unless a focused regression guard is genuinely missing; Phase 12 already checks project detail story text, metadata, JSON-LD, sitemap inclusion/exclusion, local social preview mapping, and forbidden runtime GitHub residue per D-02 and D-03.

    Run the focused and aggregate verification sequence below. The final `bun run verify` is mandatory per D-15.
  </action>
  <verify>
    <automated>bun run format:check</automated>
    <automated>bun run check</automated>
    <automated>bun run typecheck</automated>
    <automated>bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts</automated>
    <automated>bun run build</automated>
    <automated>bun run verify:browser</automated>
    <automated>bun run verify:static</automated>
    <automated>bun run verify:release</automated>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg "project detail route coverage" scripts/verify-release.test.ts scripts/release-readiness.ts` finds release evidence coverage in both the producer and expectation.
    - `rg "Release evidence labels:.*project detail route coverage" -g '*.md' .planning/phases/13-project-page-release-coverage` is not required before execution, but the executor summary must report that `bun run verify:release` printed the label.
    - `bun run verify:static` passes after `bun run build`, proving VERIFY-02 remains enforced through the Phase 12 static verifier.
    - `bun run verify:browser` passes after `bun run build`, proving VERIFY-03.
    - `bun run verify:release` and final `bun run verify` pass, proving VERIFY-04 and D-15.
  </acceptance_criteria>
  <done>Release evidence labels are pinned, no overclaiming labels were introduced, and the aggregate release gate passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| curated project registry -> release checks | Selected project detail routes become browser and release-readiness coverage targets through `projectDetailRoutes()` and `prerenderRoutes`. |
| release documentation -> release verifier | Markdown text in `docs/release-readiness.md` is treated as a checked release contract by `releaseReadinessDocumentFindings()`. |
| built static output -> release scripts | `.output/public` HTML, CSS, sitemap, robots, and assets are scanned by `verify:static`, `verify:browser`, and `verify:release`. |
| external links -> local verification | Third-party links are policy-checked locally and smoke-checked manually; local automated checks must not depend on live network availability. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-13-01 | Information Disclosure | static project detail output | mitigate | Preserve `scripts/verify-static.ts` and `scripts/verify-release.ts` checks for runtime GitHub/API residue, token-like output, route metadata, JSON-LD, sitemap inclusion/exclusion, and local social preview mapping. |
| T-13-02 | Tampering | selected project route coverage | mitigate | Derive representative browser and readiness routes from `projectDetailRoutes()` and keep exhaustive axe/layout coverage on `prerenderRoutes`; do not add a hand-maintained selected route list. |
| T-13-03 | Repudiation | release evidence labels | mitigate | Add `project detail route coverage` to `releaseReadinessEvidenceLabels()` and pin it in `scripts/verify-release.test.ts` so `bun run verify:release` communicates covered surfaces. |
| T-13-04 | Denial of Service | local release verification | mitigate | Avoid live external-link crawling or hosted audit dependencies; keep local checks deterministic and keep live third-party checks in manual smoke guidance. |
| T-13-05 | Spoofing | release-readiness documentation | mitigate | Add exact-text document facts for project detail static coverage, browser coverage, clean-builder gate coverage, and selected smoke route coverage so docs cannot silently omit the project detail route contract. |
| T-13-06 | Elevation of Privilege | browser automation surface | accept | Playwright runs against locally built static output with no auth, cookies, or secrets; no new privileged browser actions are introduced. |
</threat_model>

<verification>
Focused and final commands for the executor:

```bash
bun run format:check
bun run check
bun run typecheck
bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts
bun run build
bun run verify:browser
bun run verify:static
bun run verify:release
bun run verify
```

Optional clean-builder proof when Chromium is not provisioned:

```bash
bun run install:browser && bun run verify
```

Before finishing, review `git diff -- tests/browser-release.playwright.ts scripts/release-readiness.ts scripts/release-readiness.test.ts scripts/verify-release.test.ts docs/release-readiness.md` and confirm there are no new UI components, metadata fields, dependencies, runtime GitHub behavior, live network checks, or per-project social image generation.
</verification>

<success_criteria>
- VERIFY-02: `bun run verify:static` confirms generated project detail HTML still contains expected story text, metadata, JSON-LD, sitemap coverage, local social preview mapping, and no forbidden runtime GitHub residue.
- VERIFY-03: `bun run verify:browser` confirms selected project detail routes are covered by axe/layout loops and representative keyboard and reduced-motion checks.
- VERIFY-04: `docs/release-readiness.md`, `releaseReadinessDocumentFindings()`, `releaseReadinessEvidenceLabels()`, and `releaseEvidenceLabels()` explicitly identify project detail route coverage in the clean-builder aggregate release gate.
- Final `bun run verify` passes.
- No UI-SPEC.md is created or required.
</success_criteria>

<output>
After completion, create `.planning/phases/13-project-page-release-coverage/13-01-SUMMARY.md`.
</output>
