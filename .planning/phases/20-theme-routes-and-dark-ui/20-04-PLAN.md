---
phase: 20-theme-routes-and-dark-ui
plan: 04
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - tests/browser-release.playwright.ts
autonomous: true
requirements:
  - SYNTH-04
requirements_addressed:
  - SYNTH-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-06-17T16-16-59
generated_at: 2026-06-17T16:41:43.377Z
must_haves:
  truths:
    - "Browser verification covers theme routes for axe and desktop/mobile dark layout through the existing prerenderRoutes loop."
    - "Keyboard focus reaches the Themes nav item, a theme detail route from /themes, the Back to themes link, related project links, and related writing links."
    - "Reduced-motion verification includes theme index and one public theme detail route without adding new motion code."
    - "No release-readiness docs or automated evidence labels overclaim Phase 23 release coverage."
  artifacts:
    - path: "tests/browser-release.playwright.ts"
      provides: "Focused theme route keyboard and reduced-motion browser checks"
      contains: "representativeThemeDetailRoute"
  key_links:
    - from: "tests/browser-release.playwright.ts"
      to: "src/domain/themes.ts"
      via: "themeDetailRoutes(), publicThemeEntries(), related project/writing helpers"
      pattern: "themeDetailRoutes|publicThemeEntries"
    - from: "tests/browser-release.playwright.ts"
      to: "src/domain/routes.ts"
      via: "existing for (const route of prerenderRoutes) axe/layout loop"
      pattern: "for \\(const route of prerenderRoutes\\)"
---

<objective>
Add focused browser accessibility coverage for Phase 20 theme routes.

Purpose: Prove the new theme UI participates in the existing dark desktop/mobile, axe, keyboard, and reduced-motion browser checks without adding Phase 23 release-readiness labels or hosted/live claims.
Output: Updated Playwright browser release test coverage for theme route keyboard and reduced-motion behavior.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
@.planning/phases/20-theme-routes-and-dark-ui/20-RESEARCH.md
@.planning/phases/20-theme-routes-and-dark-ui/20-UI-SPEC.md
@.planning/phases/20-theme-routes-and-dark-ui/20-01-SUMMARY.md
@.planning/phases/20-theme-routes-and-dark-ui/20-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@tests/browser-release.playwright.ts
@src/domain/themes.ts
@src/domain/routes.ts
@src/routes/themes/index.tsx
@src/routes/themes/[slug].tsx
@src/styles/app.css

<interfaces>
Existing browser release patterns:
```ts
for (const route of prerenderRoutes) {
  test(`axe has no violations on ${route}`, async ({ page }) => { ... });
  test(`dark layout has no overflow or obvious overlap on ${route}`, async ({ page }) => { ... });
}

async function keyboardFocusTargets(page: Page): Promise<readonly FocusSnapshot[]>;
function hasFocusedInternalPath(focusedTargets: readonly FocusSnapshot[], expectedPath: string): boolean;
```

Theme route helpers to import:
```ts
export function publicThemeEntries(): readonly PublicThemeEntry[];
export function themeDetailPath(theme: Pick<ThemeRecord, "slug">): string;
export function themeDetailRoutes(): readonly string[];
export function relatedProjectDetailPageProjectsForTheme(theme: Pick<ThemeRecord, "relatedProjectSlugs">): readonly ProjectDetailPageProject[];
export function relatedWritingEntriesForTheme(theme: Pick<ThemeRecord, "relatedWritingSlugs">): readonly PublicWritingEntry[];
```
</interfaces>
</context>

<dependency_graph>
This plan depends on 20-02 because browser checks need the theme route UI. It can run in Wave 3 alongside 20-03 because it only modifies `tests/browser-release.playwright.ts`.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Extend browser keyboard and reduced-motion checks to theme routes</name>
  <files>tests/browser-release.playwright.ts</files>
  <read_first>
    - tests/browser-release.playwright.ts
    - src/domain/themes.ts
    - src/domain/routes.ts
    - src/routes/themes/index.tsx
    - src/routes/themes/[slug].tsx
    - src/styles/app.css
    - playwright.config.ts
    - package.json
    - .planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-RESEARCH.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-UI-SPEC.md
  </read_first>
  <action>
    Update `tests/browser-release.playwright.ts` per SYNTH-04, D-07, D-14, D-15, and D-17. Add imports from `../src/domain/themes`: `publicThemeEntries`, `relatedProjectDetailPageProjectsForTheme`, `relatedWritingEntriesForTheme`, `themeDetailPath`, and `themeDetailRoutes`. Keep the existing `for (const route of prerenderRoutes)` axe and dark layout loops unchanged so `/themes` and public theme details are covered automatically after Plan 20-01.

    In the `keyboard focus reaches release-critical paths` test, define `const themeRoute = representativeThemeDetailRoute();`, `const relatedThemeProjectRoute = representativeThemeRelatedProjectRoute();`, and `const relatedThemeWritingRoute = representativeThemeRelatedWritingRoute();`. After the existing Writing nav assertion, add an assertion that focus reaches `/themes` from the home page: `expect(hasFocusedInternalPath(focusedTargets, "/themes"), "focus reaches Themes nav").toBe(true);`.

    After the existing writing detail assertions, add:
    - Navigate to `/themes`, collect `themeFocusedTargets`, assert `visibleFocusFailures(themeFocusedTargets)` is `[]`, and assert focus reaches `themeRoute`.
    - Navigate to `themeRoute`, collect `themeDetailFocusedTargets`, assert `visibleFocusFailures(themeDetailFocusedTargets)` is `[]`, assert focus reaches `/themes`, `relatedThemeProjectRoute`, and `relatedThemeWritingRoute`.

    In the reduced-motion test, extend the `routes` array to include `"/themes"` and `representativeThemeDetailRoute()`, beside `"/"`, `representativeProjectDetailRoute()`, and `representativeWritingDetailRoute()`.

    Add helper functions near the existing representative route helpers:
    - `representativeThemeDetailRoute(): string` returns the first value from `themeDetailRoutes()` or throws `Expected at least one public theme detail route for release coverage.`
    - `representativeThemeRelatedProjectRoute(): string` iterates `publicThemeEntries()`, resolves `relatedProjectDetailPageProjectsForTheme(theme)[0]`, returns `projectDetailPath(maybeProject)`, or throws `Expected at least one public theme with a related selected project detail route for release coverage.`
    - `representativeThemeRelatedWritingRoute(): string` iterates `publicThemeEntries()`, resolves `relatedWritingEntriesForTheme(theme)[0]`, returns `writingDetailPath(maybeEntry)`, or throws `Expected at least one public theme with a related public writing route for release coverage.`

    Do not edit release-readiness docs, release evidence labels, static verifier files, metadata helpers, JSON-LD helpers, sitemap-specific assertions, OpenLinks placement, schema/database files, or package scripts.
  </action>
  <verify>
    <automated>bun run build && bun run verify:browser</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'publicThemeEntries|themeDetailPath|themeDetailRoutes|relatedProjectDetailPageProjectsForTheme|relatedWritingEntriesForTheme' tests/browser-release.playwright.ts` finds theme helper imports/usages.
    - `rg 'focus reaches Themes nav|await page.goto\\("/themes"\\)|themeDetailFocusedTargets' tests/browser-release.playwright.ts` finds keyboard coverage for theme routes.
    - `rg 'representativeThemeDetailRoute|representativeThemeRelatedProjectRoute|representativeThemeRelatedWritingRoute' tests/browser-release.playwright.ts` finds helper functions.
    - `rg '"/themes"|representativeThemeDetailRoute\\(\\)' tests/browser-release.playwright.ts` finds reduced-motion route additions.
    - `rg 'release-readiness|release evidence|metadataForThemeEntry|themeJsonLd|assertSitemapThemeCoverage|schema.prisma|prisma' tests/browser-release.playwright.ts` exits 1.
    - `bun run build` exits 0.
    - `bun run verify:browser` exits 0.
  </acceptance_criteria>
  <done>Browser checks cover theme routes for existing axe/layout loops, focused keyboard navigation, and reduced-motion behavior without adding Phase 23 release claims.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Route UI -> browser test evidence | Visitor-facing HTML and focus behavior become automated Playwright evidence. |
| Theme helpers -> representative browser routes | Helper output selects representative theme routes and related links for keyboard/reduced-motion checks. |
| Browser evidence -> release confidence | Passing checks can influence confidence, but Phase 23 owns release-readiness wording and evidence labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-20-04-01 | Denial of Service | Theme route layout | mitigate | Existing `prerenderRoutes` browser loop runs dark layout overflow/overlap checks on `/themes` and public detail routes across desktop and mobile projects. |
| T-20-04-02 | Spoofing | Keyboard link paths | mitigate | Browser tests assert real focus reaches `/themes`, theme detail, related project, and related writing internal routes. |
| T-20-04-03 | Information Disclosure | Representative route selection | mitigate | Select browser routes from `themeDetailRoutes()` and `publicThemeEntries()` only, so hidden or unsupported themes are not used. |
| T-20-04-04 | Denial of Service | Motion-heavy UI | mitigate | Reduced-motion test includes `/themes` and one public detail route and asserts decorative hover/pointer behavior remains inert. |
| T-20-04-05 | Repudiation | Release evidence boundary | mitigate | Do not update release-readiness docs or evidence labels in this plan; Phase 23 owns those claims. |
</threat_model>

<verification>
Run after implementation:

```bash
bun run build
bun run verify:browser
```

If Chromium is missing:

```bash
bun run install:browser
bun run verify:browser
```

Do not run schema push tasks. Do not add release-readiness docs, release evidence labels, metadata helpers, JSON-LD, sitemap-specific assertions, OpenLinks promotion, or new browser projects.
</verification>

<success_criteria>
- Existing browser axe/layout loops include `/themes` and every public theme detail route through `prerenderRoutes`.
- Keyboard focus reaches Themes navigation, a theme detail link from `/themes`, Back to themes, a related selected project link, and a related public writing link.
- Reduced-motion verification includes `/themes` and one public theme detail route.
- `bun run verify:browser` passes.
- No Phase 23 release-readiness wording or evidence labels are changed.
</success_criteria>

<output>
After completion, create `.planning/phases/20-theme-routes-and-dark-ui/20-04-SUMMARY.md`.
</output>
