---
phase: 20-theme-routes-and-dark-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - src/routes/themes/index.tsx
  - src/routes/themes/[slug].tsx
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-04
  - SYNTH-01
  - SYNTH-04
requirements_addressed:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-04
  - SYNTH-01
  - SYNTH-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-06-17T16-16-59
generated_at: 2026-06-17T16:41:43.377Z
must_haves:
  truths:
    - "Visitors can open /themes and see public theme paths as responsive dark-primary cards."
    - "Visitors can open every public /themes/{slug} route and read the theme main idea, audience, proof points, and related projects/writing before hydration."
    - "Unknown or non-public theme slugs render one generic fallback that does not echo the slug, status, private fields, or non-public content."
    - "Theme route UI uses semantic headings, real anchors, existing dark-primary classes, and stable wrapping without whole-card click traps."
  artifacts:
    - path: "src/routes/themes/index.tsx"
      provides: "/themes index route rendering public theme card grid"
      contains: "publicThemeEntries"
    - path: "src/routes/themes/[slug].tsx"
      provides: "/themes/{slug} detail route and non-leaking fallback"
      contains: "maybePublicThemeEntryBySlug"
  key_links:
    - from: "src/routes/themes/index.tsx"
      to: "src/domain/themes.ts"
      via: "publicThemeEntries() and themeDetailPath()"
      pattern: "publicThemeEntries|themeDetailPath"
    - from: "src/routes/themes/[slug].tsx"
      to: "src/domain/themes.ts"
      via: "maybePublicThemeEntryBySlug(), relatedProjectDetailPageProjectsForTheme(), relatedWritingEntriesForTheme()"
      pattern: "maybePublicThemeEntryBySlug|relatedProjectDetailPageProjectsForTheme|relatedWritingEntriesForTheme"
    - from: "src/routes/themes/[slug].tsx"
      to: "src/domain/projects.ts and src/domain/writing.ts"
      via: "projectDetailPath() and writingDetailPath() links for resolved related records"
      pattern: "projectDetailPath|writingDetailPath"
---

<objective>
Create the visitor-facing dark theme routes.

Purpose: Turn the Phase 19 public theme helper surface into static `/themes` and `/themes/{slug}` pages that synthesize existing project and writing records without adding collaboration panels, new external action sources, or Phase 22 metadata systems.
Output: New SolidStart route files for the theme index and detail pages.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
@.planning/phases/20-theme-routes-and-dark-ui/20-RESEARCH.md
@.planning/phases/20-theme-routes-and-dark-ui/20-UI-SPEC.md
@.planning/phases/19-theme-domain-foundation/19-01-SUMMARY.md
@.planning/phases/20-theme-routes-and-dark-ui/20-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@src/domain/themes.ts
@src/domain/routes.ts
@src/routes/writing/index.tsx
@src/routes/writing/[slug].tsx
@src/routes/projects/[slug].tsx
@src/components/ReactiveSurface.tsx
@src/styles/app.css

<interfaces>
Use these existing contracts directly:
```ts
export function publicThemeEntries(): readonly PublicThemeEntry[];
export function maybePublicThemeEntryBySlug(slug: string): PublicThemeEntry | null;
export function themeDetailPath(theme: Pick<ThemeRecord, "slug">): string;
export function relatedProjectDetailPageProjectsForTheme(
  theme: Pick<ThemeRecord, "relatedProjectSlugs">,
): readonly ProjectDetailPageProject[];
export function relatedWritingEntriesForTheme(
  theme: Pick<ThemeRecord, "relatedWritingSlugs">,
): readonly PublicWritingEntry[];
```

Existing route/UI helper labels:
```ts
function writingActionLabel(entry: Pick<PublicWritingEntry, "kind">): "Read note" | "Read essay";
```

Existing dark-primary classes to reuse before adding CSS: `page-intro`, `page-title`, `lead`, `theme-grid`, `theme-card`, `project-detail-layout`, `project-detail-story`, `project-detail-aside`, `project-detail-panel`, `project-detail-stack`, `writing-related-grid`, `project-anchor-card`, `surface-card`, `interactive-surface`, `reactive-card`, `label-row`, `chip`, `tier-pill`, `link-list`, `text-link`, and `surface-link`.
</interfaces>
</context>

<dependency_graph>
This plan depends on 20-01 because `/themes` route metadata and prerender entries must exist before these route files are final. Plan 20-03 static verification and Plan 20-04 browser verification depend on these route files.
</dependency_graph>

<tasks>

<task type="auto">
  <name>Task 1: Build the /themes index card grid</name>
  <files>src/routes/themes/index.tsx</files>
  <read_first>
    - src/routes/themes/index.tsx (create if missing)
    - src/routes/writing/index.tsx
    - src/domain/themes.ts
    - src/domain/routes.ts
    - src/domain/seo.ts
    - src/components/ReactiveSurface.tsx
    - src/styles/app.css
    - .planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-RESEARCH.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-UI-SPEC.md
  </read_first>
  <action>
    Create `src/routes/themes/index.tsx` per D-01, D-02, D-12, D-13, D-14, D-15, and D-16. Import `For` and `Show` from `solid-js`; import `Link as HeadLink`, `Meta`, and `Title` from `@solidjs/meta`; import `ReactiveSurface` from `../../components/ReactiveSurface`; import `routeByPath` from `../../domain/routes`; import `jsonLdScriptContent`, `metadataForRoute`, `personJsonLd`, and `siteAssetLinks` from `../../domain/seo`; import `publicThemeEntries`, `relatedProjectDetailPageProjectsForTheme`, `relatedWritingEntriesForTheme`, `themeDetailPath`, and type `PublicThemeEntry` from `../../domain/themes`.

    Define `const route = routeByPath("/themes");`, `const metadata = metadataForRoute(route);`, `const themes = publicThemeEntries();`, and `const personJsonLdValue = personJsonLd();`. Render the existing top-level route metadata pattern for `/themes`: `<Title>{metadata.title}</Title>`, description meta, canonical link, `siteAssetLinks`, Open Graph meta tags, Twitter meta tags, and Person JSON-LD. This is only the generic top-level route metadata already supported by `SiteRoute`; do not add `metadataForThemeEntry`, theme detail metadata, theme ItemList JSON-LD, sitemap assertions, or social-preview generation.

    Render a `section.page-intro` with `<p class="eyebrow">Theme paths</p>`, one `<h1 class="page-title">Themes</h1>`, and lead text exactly `Curated routes through Peter's work, connecting durable ideas to selected projects, public writing, and proof points.`.

    Render `<Show when={themes.length > 0}>` with fallback `<div class="empty-state visual-surface">` containing `<h2 class="card-title">No public themes yet</h2>` and body copy exactly `Theme paths will appear here after public theme records are added to the curated theme registry.`.

    In the non-empty branch, render `<ReactiveSurface class="theme-grid">` containing one `<article class="theme-card interactive-surface reactive-card">` per public theme. Each card must include:
    - `<h2 class="theme-title">` with an inner real anchor `<a class="project-anchor-link" href={themeDetailPath(theme)}>`.
    - `<p class="card-copy">{theme.summary}</p>`.
    - A label row with one `tier-pill` for `theme.audience`.
    - A metadata label row with chips from helper-derived counts: `"{N} related project"` or `"{N} related projects"` from `relatedProjectDetailPageProjectsForTheme(theme).length`, and `"{N} related note"`/`"{N} related notes"`/`"{N} related essay"`/`"{N} related essays"` derived from `relatedWritingEntriesForTheme(theme)` by counting `kind`.
    - A `.link-list` containing `<a class="text-link surface-link" href={themeDetailPath(theme)}>Explore theme</a>`.

    Add small local helper functions in the same file only when needed: `relationshipCountLabel(count, singular, plural)`, `writingKindCountLabels(entries)`, and `themeRelationshipLabels(theme)`. Keep helpers pure and data-in/data-out. Do not copy project names, project descriptions, writing titles, writing summaries, project links, writing links, `collaborationAngle`, OpenLinks CTA copy, search/filter controls, or new external action sources into the index cards.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
    <automated>bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/routes/themes/index.tsx` exits 0.
    - `rg 'publicThemeEntries' src/routes/themes/index.tsx` finds the public selector.
    - `rg 'themeDetailPath' src/routes/themes/index.tsx` finds helper-derived links.
    - `rg 'Theme paths|<h1 class="page-title">Themes</h1>|Explore theme|No public themes yet' src/routes/themes/index.tsx` finds required index copy.
    - `rg 'related project|related note|related essay' src/routes/themes/index.tsx` finds helper-derived relationship labels.
    - `rg 'theme-grid|theme-card|interactive-surface|reactive-card|tier-pill|chip|link-list' src/routes/themes/index.tsx` finds existing dark-primary classes.
    - `rg 'collaborationAngle|OpenLinks profile|primary OpenLinks|metadataForThemeEntry|themeJsonLd|search|filter|schema.prisma|prisma' src/routes/themes/index.tsx` exits 1.
  </acceptance_criteria>
  <done>`/themes` renders a dark-primary card-grid index from `publicThemeEntries()` with helper-derived links and labels, generic top-level metadata only, and no Phase 21/22/23 scope.</done>
</task>

<task type="auto">
  <name>Task 2: Build the gated /themes/{slug} detail route and fallback</name>
  <files>src/routes/themes/[slug].tsx</files>
  <read_first>
    - src/routes/themes/[slug].tsx (create if missing)
    - src/routes/writing/[slug].tsx
    - src/routes/projects/[slug].tsx
    - src/routes/themes/index.tsx
    - src/domain/themes.ts
    - src/domain/projects.ts
    - src/domain/writing.ts
    - src/styles/app.css
    - .planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-RESEARCH.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-UI-SPEC.md
  </read_first>
  <action>
    Create `src/routes/themes/[slug].tsx` per D-04 through D-15. Import `useParams` from `@solidjs/router`; import `For` and `Show` from `solid-js`; import `projectDetailPath` from `../../domain/projects`; import `writingDetailPath` and type `PublicWritingEntry` from `../../domain/writing`; import `maybePublicThemeEntryBySlug`, `relatedProjectDetailPageProjectsForTheme`, `relatedWritingEntriesForTheme`, and type `PublicThemeEntry` from `../../domain/themes`.

    In the default `ThemeDetail` component, define `const params = useParams();` and `const theme = () => maybePublicThemeEntryBySlug(params.slug ?? "");`. Render `<Show when={theme()} fallback={<ThemeFallback />}>` and pass the selected theme to `ThemeArticle`.

    Add `ThemeFallback()` with only generic copy: `Theme path` eyebrow, one H1 `No public theme here`, lead text exactly `No public theme here. Browse theme paths to find a public route through Peter's work.`, and `<a class="primary-action interactive-surface" href="/themes">Browse theme paths</a>`. The fallback must not render `params.slug`, raw slug text, theme status, `collaborationAngle`, project/writing relationships, private registry fields, or any non-public theme record content.

    Add `ThemeArticle(props: { theme: PublicThemeEntry })`. It must compute `const relatedProjects = relatedProjectDetailPageProjectsForTheme(theme);` and `const relatedWriting = relatedWritingEntriesForTheme(theme);`. Render an `<article class="content-section">` with:
    - A `div.page-intro` containing `<a class="text-link detail-back-link" href="/themes">Back to themes</a>`, `<p class="eyebrow">Theme path</p>`, one `<h1 class="page-title">{theme.title}</h1>`, `<p class="lead">{theme.summary}</p>`, and a `ul.detail-status-row` with a `tier-pill` for `theme.audience`.
    - A `div.project-detail-layout` with a main `<section class="project-detail-story visual-surface" aria-labelledby="why-it-matters">`. This section must include `<h2 id="why-it-matters" class="card-title">Why it matters</h2>`, a `.story-stack.project-detail-stack` paragraph using `theme.summary`, a subsection heading `Audience` with body text `theme.audience`, and a subsection heading `Proof points` with a list of every `theme.proofPoints` item as `.chip` elements. Use heading IDs `theme-audience` and `theme-proof-points`.
    - An `<aside class="project-detail-aside" aria-label={`${theme.title} related work`}>` containing `RelatedProjectsPanel` and `RelatedWritingPanel`.

    `RelatedProjectsPanel` must render only when `relatedProjects.length > 0`. Use `<section class="project-detail-panel visual-surface" aria-labelledby="related-projects">`, heading `Related projects`, `.writing-related-grid`, and one `<article class="project-anchor-card interactive-surface reactive-card">` per project with project name, `project.oneLine`, and `<a class="text-link surface-link" href={projectDetailPath(project)}>Project details</a>`.

    `RelatedWritingPanel` must render only when `relatedWriting.length > 0`. Use `<section class="project-detail-panel visual-surface" aria-labelledby="related-writing">`, heading `Related writing`, `.writing-related-grid`, and one `<article class="surface-card">` per entry with title, one `chip` for `Note` or `Essay`, summary, and `<a class="text-link surface-link" href={writingDetailPath(entry)}>{writingActionLabel(entry)}</a>`. Implement `writingKindLabel(entry)` returning `Note` or `Essay`, and `writingActionLabel(entry)` returning `Read note` or `Read essay`.

    Do not add `Title`, `Meta`, Open Graph/Twitter tags, JSON-LD, sitemap logic, collaboration panels, practical next-action CTAs, OpenLinks CTAs, external action sources, whole-card click handlers, new CSS, new UI dependencies, new motion dependencies, schema/database tasks, search, filters, CMS/admin code, MDX, or runtime content fetches.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
    <automated>bun run check</automated>
  </verify>
  <acceptance_criteria>
    - `test -f 'src/routes/themes/[slug].tsx'` exits 0.
    - `rg 'maybePublicThemeEntryBySlug\\(params\\.slug \\?\\? ""\\)' 'src/routes/themes/[slug].tsx'` finds the public-only gate.
    - `rg 'No public theme here|Browse theme paths|href="/themes"' 'src/routes/themes/[slug].tsx'` finds fallback copy and back link target.
    - `rg 'Back to themes|Theme path|Why it matters|Audience|Proof points|Related projects|Related writing' 'src/routes/themes/[slug].tsx'` finds required detail labels.
    - `rg 'relatedProjectDetailPageProjectsForTheme|relatedWritingEntriesForTheme|projectDetailPath|writingDetailPath' 'src/routes/themes/[slug].tsx'` finds helper-derived relationships and links.
    - `rg 'Project details|Read note|Read essay' 'src/routes/themes/[slug].tsx'` finds required related-card link labels.
    - `rg 'page-intro|page-title|lead|project-detail-layout|project-detail-panel|project-anchor-card|surface-card|label-row|chip|tier-pill|link-list|interactive-surface|reactive-card' 'src/routes/themes/[slug].tsx'` finds existing dark-primary classes.
    - `rg 'collaborationAngle|OpenLinks profile|primary OpenLinks|metadataForThemeEntry|themeJsonLd|<Title|<Meta|sitemap|search|filter|schema.prisma|prisma' 'src/routes/themes/[slug].tsx'` exits 1.
  </acceptance_criteria>
  <done>`/themes/{slug}` renders only public theme detail pages from `maybePublicThemeEntryBySlug()`, shows synthesized theme/project/writing content from helpers, and uses one generic non-leaking fallback for every unavailable slug.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| URL slug -> theme detail route | `params.slug` is untrusted route input and must pass through `maybePublicThemeEntryBySlug()`. |
| Theme helpers -> visible cards | Public theme, project, and writing helper output becomes pre-hydration HTML. |
| Visitor -> external links | Phase 20 route UI should not introduce new external action sources or primary OpenLinks CTAs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-20-02-01 | Information Disclosure | `ThemeDetail` | mitigate | Gate route rendering only through `maybePublicThemeEntryBySlug(params.slug ?? "")`; fallback must not echo slugs, statuses, private fields, or non-public record content. |
| T-20-02-02 | Information Disclosure | Related project/writing panels | mitigate | Use only `relatedProjectDetailPageProjectsForTheme()` and `relatedWritingEntriesForTheme()` so display records come from selected project detail pages and public writing entries. |
| T-20-02-03 | Spoofing | Theme route CTAs | mitigate | Keep links descriptive and internal: `Explore theme`, `Back to themes`, `Project details`, `Read note`, and `Read essay`; do not add primary OpenLinks or new external action CTAs. |
| T-20-02-04 | Denial of Service | Reactive visual effects | mitigate | Use existing `ReactiveSurface` and `reactive-card` classes only; existing CSS disables decoration under reduced motion, coarse pointer, and small viewport guards. |
| T-20-02-05 | Tampering | Content source | mitigate | Do not denormalize project or writing copy into route components; render related cards from authoritative helper-resolved records. |
| T-20-02-06 | Elevation of Privilege | Database/schema scope | accept | This plan creates static UI files only and touches no auth, runtime API, ORM, database, or schema surface. |
</threat_model>

<verification>
Run after implementation:

```bash
bun run typecheck
bun run check
```

Do not run schema push tasks. Do not add route-specific theme detail metadata, JSON-LD, sitemap assertions, social-preview generation, release-readiness labels, browser-suite labels, collaboration panels, project/writing surface cross-links, new dependencies, runtime content fetches, CMS/admin code, MDX, search, or filters.
</verification>

<success_criteria>
- `/themes` route file exists and renders a public theme card grid from `publicThemeEntries()`.
- `/themes/{slug}` route file exists and renders public theme details through `maybePublicThemeEntryBySlug()`.
- Detail pages show main idea, audience, proof points, related selected projects, and related public writing from helpers.
- The fallback copy is generic and directs visitors to `/themes` without echoing unavailable slug data.
- The route UI reuses existing dark-primary classes and passes typecheck plus Biome checks.
</success_criteria>

<output>
After completion, create `.planning/phases/20-theme-routes-and-dark-ui/20-02-SUMMARY.md`.
</output>
