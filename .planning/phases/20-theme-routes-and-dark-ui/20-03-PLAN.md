---
phase: 20-theme-routes-and-dark-ui
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - tests/theme-detail-route.test.tsx
  - scripts/verify-static/config.ts
  - scripts/verify-static/expected-route-text.ts
  - scripts/verify-static/metadata-jsonld-verifier.ts
  - scripts/verify-static/sitemap-assets-verifier.ts
  - scripts/verify-static.test.ts
autonomous: true
requirements:
  - ROUTE-03
  - ROUTE-04
  - SYNTH-01
requirements_addressed:
  - ROUTE-03
  - ROUTE-04
  - SYNTH-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 20-2026-06-17T16-16-59
generated_at: 2026-06-17T16:41:43.377Z
must_haves:
  truths:
    - "Static verification finds generated HTML for /themes and every public /themes/{slug} route."
    - "Generated theme HTML contains expected index, detail, proof point, related project, and related writing text before hydration."
    - "Unavailable theme slugs have a generic source-level fallback guard and do not create static output."
    - "Static verification stays focused on generated route HTML and forbidden runtime residue without changing checked-in static metadata output."
  artifacts:
    - path: "tests/theme-detail-route.test.tsx"
      provides: "Focused render test for public detail and generic fallback behavior"
      contains: "No public theme here"
    - path: "scripts/verify-static/expected-route-text.ts"
      provides: "Theme route expected pre-hydration text derivation"
      contains: "maybeThemeForDetailRoute"
    - path: "scripts/verify-static/metadata-jsonld-verifier.ts"
      provides: "Phase 20 metadata boundary skip for theme detail routes"
      contains: "maybeThemeForDetailRoute"
    - path: "scripts/verify-static/sitemap-assets-verifier.ts"
      provides: "Theme detail route output existence and unknown-route exclusion checks"
      contains: "assertNoPrerenderedThemeRoute"
  key_links:
    - from: "scripts/verify-static/expected-route-text.ts"
      to: "src/domain/themes.ts"
      via: "publicThemeEntries(), themeDetailPath(), maybePublicThemeEntryBySlug(), relationship helpers"
      pattern: "publicThemeEntries|maybePublicThemeEntryBySlug|themeDetailPath"
    - from: "scripts/verify-static/metadata-jsonld-verifier.ts"
      to: "scripts/verify-static/expected-route-text.ts"
      via: "maybeThemeForDetailRoute()"
      pattern: "maybeThemeForDetailRoute"
---

<objective>
Add Phase 20 static output verification for theme routes.

Purpose: Prove that `/themes` and public detail routes generate the expected route/UI content before hydration, while unavailable slugs remain non-leaking and while Phase 22 metadata/JSON-LD/sitemap-specific assertions stay deferred.
Output: Focused route render test and theme-aware static verifier helpers for generated route HTML.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
@.planning/phases/20-theme-routes-and-dark-ui/20-RESEARCH.md
@.planning/phases/20-theme-routes-and-dark-ui/20-UI-SPEC.md
@.planning/phases/20-theme-routes-and-dark-ui/20-01-SUMMARY.md
@.planning/phases/20-theme-routes-and-dark-ui/20-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@src/domain/themes.ts
@src/domain/routes.ts
@src/routes/themes/index.tsx
@src/routes/themes/[slug].tsx
@scripts/verify-static/expected-route-text.ts
@scripts/verify-static/metadata-jsonld-verifier.ts
@scripts/verify-static/sitemap-assets-verifier.ts
@scripts/verify-static.test.ts

<interfaces>
Existing static verifier route text shape:
```ts
export const expectedRoutes: StaticRouteCheck[] = prerenderRoutes.map((route) => ({
  route,
  expectedTexts: ["OpenLinks profile", ...expectedTextsForRoute(route)],
  forbiddenTextPatterns: generatedOutputForbiddenPatterns,
}));
```

Theme helper contracts to consume:
```ts
export function publicThemeEntries(): readonly PublicThemeEntry[];
export function maybePublicThemeEntryBySlug(slug: string): PublicThemeEntry | null;
export function themeDetailPath(theme: Pick<ThemeRecord, "slug">): string;
export function themeDetailRoutes(): readonly string[];
export function relatedProjectDetailPageProjectsForTheme(theme: Pick<ThemeRecord, "relatedProjectSlugs">): readonly ProjectDetailPageProject[];
export function relatedWritingEntriesForTheme(theme: Pick<ThemeRecord, "relatedWritingSlugs">): readonly PublicWritingEntry[];
```

Boundary: this plan may update static verifier expected text, generated route existence, fallback safety, and forbidden runtime residue checks where Phase 20 touches route output. It must not change checked-in static metadata output, add `metadataForThemeEntry`, theme detail Open Graph/Twitter, theme JSON-LD, theme ItemList JSON-LD, `assertSitemapThemeCoverage`, release-readiness docs, release evidence labels, dynamic Open Graph routes, or browser-suite labels.
</interfaces>
</context>

<dependency_graph>
This plan depends on 20-02 because it verifies route files and generated HTML. It can run in Wave 3 alongside 20-04 because their file sets do not overlap.
</dependency_graph>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add focused theme detail render tests for public and fallback states</name>
  <files>tests/theme-detail-route.test.tsx</files>
  <read_first>
    - tests/theme-detail-route.test.tsx (create if missing)
    - src/routes/themes/[slug].tsx
    - src/domain/themes.ts
    - src/domain/projects.ts
    - src/domain/writing.ts
    - .planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-RESEARCH.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-UI-SPEC.md
  </read_first>
  <behavior>
    - Rendering the detail route with mocked slug `agentic-engineering` includes public theme title, summary, proof point text, `Related projects`, `Related writing`, `Project details`, and `Read note`.
    - Rendering the detail route with mocked slug `unknown-theme-slug` includes `No public theme here`, `Browse theme paths`, and `href="/themes"`.
    - The unknown fallback render does not include `unknown-theme-slug`, `agentic-engineering`, `open-identity`, `draft`, `hidden`, `unsupported`, `archived`, `collaborationAngle`, `OpenLinks profile`, `Project details`, `Read note`, or `Read essay`.
  </behavior>
  <action>
    Add `tests/theme-detail-route.test.tsx` using Vitest and `renderToString` from `solid-js/web`. Mock `@solidjs/router` with `vi.mock("@solidjs/router", () => ({ useParams: () => ({ slug: mockedSlug }) }))`, where `let mockedSlug = "agentic-engineering";` is reassigned inside tests before rendering. Dynamically import the default export from `../src/routes/themes/[slug]` after the mock is declared.

    Add two focused tests with Arrange/Act/Assert comments. The public test sets `mockedSlug = "agentic-engineering"` and asserts the rendered string contains `Agentic engineering`, the public summary from `curatedThemes`, `Why it matters`, `Audience`, `Proof points`, `Related projects`, `Related writing`, `Project details`, and `Read note`. The fallback test sets `mockedSlug = "unknown-theme-slug"` and asserts the rendered string contains `No public theme here`, `Browse theme paths`, and `href="/themes"`, and does not contain `unknown-theme-slug`, `Agentic engineering`, `Open identity`, `draft`, `hidden`, `unsupported`, `archived`, `collaborationAngle`, `OpenLinks profile`, `Project details`, `Read note`, or `Read essay`.

    Do not add a DOM environment dependency, Testing Library dependency, route metadata assertions, JSON-LD assertions, sitemap assertions, browser release labels, or schema/database work.
  </action>
  <verify>
    <automated>bun run test tests/theme-detail-route.test.tsx</automated>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f tests/theme-detail-route.test.tsx` exits 0.
    - `rg 'renderToString|vi\\.mock\\("@solidjs/router"|mockedSlug' tests/theme-detail-route.test.tsx` finds the route render test setup.
    - `rg 'No public theme here|Browse theme paths|unknown-theme-slug' tests/theme-detail-route.test.tsx` finds fallback assertions.
    - `rg 'Agentic engineering|Related projects|Related writing|Project details|Read note' tests/theme-detail-route.test.tsx` finds public render assertions.
    - `rg 'Testing Library|happy-dom|jsdom|metadataForThemeEntry|themeJsonLd|sitemap|schema.prisma|prisma' tests/theme-detail-route.test.tsx` exits 1.
  </acceptance_criteria>
  <done>Route-level render tests prove public detail content and generic fallback behavior without adding a new test environment dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add theme route expected text and fallback/static-output safety checks</name>
  <files>scripts/verify-static/config.ts, scripts/verify-static/expected-route-text.ts, scripts/verify-static/metadata-jsonld-verifier.ts, scripts/verify-static/sitemap-assets-verifier.ts, scripts/verify-static.test.ts</files>
  <read_first>
    - scripts/verify-static/config.ts
    - scripts/verify-static/expected-route-text.ts
    - scripts/verify-static/metadata-jsonld-verifier.ts
    - scripts/verify-static/sitemap-assets-verifier.ts
    - scripts/verify-static.test.ts
    - src/domain/themes.ts
    - src/routes/themes/index.tsx
    - src/routes/themes/[slug].tsx
    - .planning/phases/20-theme-routes-and-dark-ui/20-CONTEXT.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-RESEARCH.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-UI-SPEC.md
  </read_first>
  <behavior>
    - `expectedRoutes` includes `/themes` and every `themeDetailRoutes()` value because they are in `prerenderRoutes`.
    - `/themes` expected texts include required index copy and every public theme title, summary, audience, helper-derived relationship label, and `Explore theme` href.
    - Theme detail expected texts include title, summary, audience, proof points, related project names/one-line copy/detail links, related writing titles/summaries/action links, and `Back to themes`.
    - Theme detail routes are not forced through Phase 22 metadata/JSON-LD assertions.
    - Static verifier checks public theme detail HTML exists, unknown theme route output does not exist, and Phase 20 theme route HTML keeps existing forbidden runtime/API residue guards.
  </behavior>
  <action>
    Update `scripts/verify-static/config.ts` to export `themeDetailRouteSourcePath = "src/routes/themes/[slug].tsx"` beside `writingDetailRouteSourcePath`.

    Update `scripts/verify-static/expected-route-text.ts`. Import `themeDetailPath`, `themeDetailRoutes`, `maybePublicThemeEntryBySlug`, `publicThemeEntries`, `relatedProjectDetailPageProjectsForTheme`, `relatedWritingEntriesForTheme`, and type `PublicThemeEntry` from `../../src/domain/themes`. Add `maybeThemeForDetailRoute(route: string): PublicThemeEntry | null` using prefix `"/themes/"` and `maybePublicThemeEntryBySlug(route.slice(detailRoutePrefix.length))`. Add a theme detail branch before the top-level route branch. For `/themes`, return `routeStaticCheckText(route)`, `Theme paths`, `Themes`, `Curated routes through Peter's work, connecting durable ideas to selected projects, public writing, and proof points.`, and for every `publicThemeEntries()` item: theme title, summary, audience, `href="${escapeHtmlAttribute(themeDetailPath(theme))}"`, `Explore theme`, project count label, and writing kind count labels. For theme detail routes, return `Back to themes`, `Theme path`, theme title, summary, audience, `Why it matters`, `Audience`, `Proof points`, every proof point, `Related projects`, each related project name, oneLine, `Project details`, project detail href, `Related writing`, each related writing title, summary, `Read note` or `Read essay`, and writing detail href.

    Update `scripts/verify-static/metadata-jsonld-verifier.ts` to import `maybeThemeForDetailRoute` and add `const maybeTheme = maybeThemeForDetailRoute(routePath); if (maybeTheme) { return; }` after writing-detail handling and before `topLevelRouteForPath(routePath)`. This explicitly defers theme detail metadata/JSON-LD assertions to Phase 22 while still allowing route HTML text verification.

    Update `scripts/verify-static/sitemap-assets-verifier.ts` to import `themeDetailRoutes` from `../../src/domain/themes`, and `themeDetailRouteSourcePath` from `./config`. Add `assertThemeDetailRouteCoverage(root)` that calls `routeHtmlPath(root, route)` for every `themeDetailRoutes()` value; this is generated HTML route coverage, not metadata URL coverage. Add `assertNoPrerenderedThemeRoute(root, route)` mirroring `assertNoPrerenderedWritingRoute()`, with error text `Unexpected static theme output for ${route}: ${relative(root, maybeOutputPath)}`. Add `assertThemeFallbackSource()` that reads `themeDetailRouteSourcePath` and asserts source contains `maybePublicThemeEntryBySlug(params.slug ?? "")`, `No public theme here`, `Browse theme paths`, and `href="/themes"`. Call `assertThemeDetailRouteCoverage(outputRoot)`, `assertNoPrerenderedThemeRoute(outputRoot, "/themes/unknown-theme-slug")`, and `assertThemeFallbackSource()` inside `assertSitemapAssetsAndRobots()`. Do not add `assertSitemapThemeCoverage()`, public metadata URL checks for themes, or any theme metadata inclusion/exclusion assertion in this phase.

    Update `scripts/verify-static.test.ts` imports and tests. Import `publicThemeEntries` and `themeDetailRoutes` from `../src/domain/themes`, import `maybeThemeForDetailRoute` from `./verify-static/expected-route-text`, and import `assertNoPrerenderedThemeRoute` plus `assertThemeFallbackSource` from `./verify-static/sitemap-assets-verifier`. Add a test named `derives theme index and detail text from theme helpers` that asserts the `/themes` expected route includes each public theme title and summary, and every `themeDetailRoutes()` route maps to a non-null `maybeThemeForDetailRoute(route)` with expected title, summary, `Related projects`, and `Related writing`. Update the callable verifier export test to include `assertNoPrerenderedThemeRoute` and `assertThemeFallbackSource`.

    Do not add theme detail metadata helpers, theme JSON-LD, theme ItemList JSON-LD, Phase 22 metadata assertions, release-readiness docs, release evidence labels, dynamic Open Graph routes, browser tests, schema/database tasks, or new dependencies.
  </action>
  <verify>
    <automated>bun run test scripts/verify-static.test.ts tests/theme-detail-route.test.tsx</automated>
    <automated>bun run typecheck</automated>
    <automated>bun run build</automated>
    <automated>bun -e 'import { expectedRoutes } from "./scripts/verify-static/expected-route-text"; import { findStaticOutputRoot } from "./scripts/verify-static/output"; import { assertRouteHtml } from "./scripts/verify-static/route-html-verifier"; const root = findStaticOutputRoot(); for (const check of expectedRoutes.filter(({ route }) => route === "/themes" || route.startsWith("/themes/"))) assertRouteHtml(root, check);'</automated>
    <automated>bun -e 'import { findStaticOutputRoot } from "./scripts/verify-static/output"; import { assertNoPrerenderedThemeRoute } from "./scripts/verify-static/sitemap-assets-verifier"; assertNoPrerenderedThemeRoute(findStaticOutputRoot(), "/themes/unknown-theme-slug");'</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'themeDetailRouteSourcePath' scripts/verify-static/config.ts scripts/verify-static/sitemap-assets-verifier.ts` finds the theme route source path.
    - `rg 'maybeThemeForDetailRoute|publicThemeEntries|themeDetailRoutes|themeDetailPath|relatedProjectDetailPageProjectsForTheme|relatedWritingEntriesForTheme' scripts/verify-static/expected-route-text.ts` finds theme expected-text helpers.
    - `rg 'if \\(maybeTheme\\) \\{[[:space:]]*return;' scripts/verify-static/metadata-jsonld-verifier.ts` finds the Phase 22 metadata boundary skip.
    - `rg 'assertThemeDetailRouteCoverage|assertNoPrerenderedThemeRoute|assertThemeFallbackSource|/themes/unknown-theme-slug' scripts/verify-static/sitemap-assets-verifier.ts` finds route coverage, non-output, and fallback source checks.
    - `rg 'derives theme index and detail text from theme helpers|maybeThemeForDetailRoute|assertNoPrerenderedThemeRoute|assertThemeFallbackSource' scripts/verify-static.test.ts` finds test coverage.
    - `rg 'metadataForThemeEntry|themeJsonLd|ThemeItemList|assertSitemapThemeCoverage|release-readiness|release evidence|dynamic Open Graph|schema.prisma|prisma' scripts/verify-static/config.ts scripts/verify-static/expected-route-text.ts scripts/verify-static/metadata-jsonld-verifier.ts scripts/verify-static/sitemap-assets-verifier.ts scripts/verify-static.test.ts` exits 1.
    - `test -f .output/public/themes/index.html` exits 0 after `bun run build`.
    - `test -f .output/public/themes/agentic-engineering/index.html` exits 0 after `bun run build`.
    - `test -f .output/public/themes/open-identity/index.html` exits 0 after `bun run build`.
    - `test ! -e .output/public/themes/unknown-theme-slug/index.html` exits 0 after `bun run build`.
    - `test ! -e .output/public/themes/unknown-theme-slug.html` exits 0 after `bun run build`.
    - `git diff -- src/domain/seo.ts scripts/verify-release.ts scripts/release-readiness.ts docs/release-readiness.md tests/browser-release.playwright.ts` shows no Phase 22/23 metadata, release, docs, or browser changes from this plan.
  </acceptance_criteria>
  <done>Static verifier helpers derive theme route text and output safety from theme helpers while explicitly deferring theme detail metadata, JSON-LD, and Phase 22 metadata assertions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Theme route source -> static verifier | Source-level route fallback and helper usage become part of automated safety evidence. |
| Theme domain helpers -> generated HTML | Public helper output determines expected static HTML text for theme routes. |
| Static output -> visitor | Generated HTML must not contain runtime token/API residue or unavailable theme content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-20-03-01 | Information Disclosure | `src/routes/themes/[slug].tsx` fallback | mitigate | Render-test unknown slugs and source-check fallback copy so unavailable routes do not echo requested slugs, statuses, private fields, or non-public theme content. |
| T-20-03-02 | Information Disclosure | `.output/public/themes/*` | mitigate | Verify generated public theme detail HTML exists and unknown theme route HTML does not exist. |
| T-20-03-03 | Information Disclosure | Generated HTML residue | mitigate | Preserve existing forbidden output patterns for GitHub APIs/tokens and unsafe hrefs through targeted route HTML verification. |
| T-20-03-04 | Tampering | Static verifier route expectations | mitigate | Derive expected theme text from `publicThemeEntries()`, `themeDetailRoutes()`, and relationship helpers rather than copied route slug arrays. |
| T-20-03-05 | Repudiation | Phase 22 metadata boundary | mitigate | Add an explicit metadata verifier skip for theme detail routes and grep-guard against theme metadata/JSON-LD assertion additions in Phase 20. |
| T-20-03-06 | Spoofing | OpenLinks identity placement | mitigate | Expected text keeps `OpenLinks profile` only through existing footer coverage; no theme route OpenLinks CTA is added. |
</threat_model>

<verification>
Run after implementation:

```bash
bun run test tests/theme-detail-route.test.tsx scripts/verify-static.test.ts
bun run typecheck
bun run build
bun -e 'import { expectedRoutes } from "./scripts/verify-static/expected-route-text"; import { findStaticOutputRoot } from "./scripts/verify-static/output"; import { assertRouteHtml } from "./scripts/verify-static/route-html-verifier"; const root = findStaticOutputRoot(); for (const check of expectedRoutes.filter(({ route }) => route === "/themes" || route.startsWith("/themes/"))) assertRouteHtml(root, check);'
bun -e 'import { findStaticOutputRoot } from "./scripts/verify-static/output"; import { assertNoPrerenderedThemeRoute } from "./scripts/verify-static/sitemap-assets-verifier"; assertNoPrerenderedThemeRoute(findStaticOutputRoot(), "/themes/unknown-theme-slug");'
```

Do not run schema push tasks. Do not add theme detail metadata helpers, JSON-LD, Phase 22 metadata assertions, release-readiness docs, release evidence labels, dynamic Open Graph routes, browser-suite labels, or new dependencies.
</verification>

<success_criteria>
- Focused route render tests prove public theme detail rendering and generic unknown fallback behavior.
- Static verifier expected text covers `/themes` and every public theme detail route through helper-derived data.
- Theme detail routes are skipped from Phase 22 metadata/JSON-LD assertions while still verified for generated body content.
- Unknown theme static output is absent.
- Theme route static checks avoid checked-in static metadata changes and Phase 22 metadata URL/exclusion assertions.
- Targeted route HTML verification proves expected pre-hydration text and forbidden runtime/API residue checks for Phase 20 theme routes.
</success_criteria>

<output>
After completion, create `.planning/phases/20-theme-routes-and-dark-ui/20-03-SUMMARY.md`.
</output>
