---
phase: 12-project-metadata-sharing
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/seo.ts
  - src/domain/project-detail-routes.test.ts
  - src/domain/portfolio-surfaces.test.ts
  - src/routes/projects/[slug].tsx
  - public/sitemap.xml
  - public/robots.txt
  - scripts/verify-static.ts
  - scripts/verify-release.ts
  - scripts/verify-release.test.ts
autonomous: true
requirements:
  - META-01
  - META-02
  - META-03
  - META-04
user_setup: []
must_haves:
  truths:
    - "Each selected /projects/{slug} detail route has title, description, canonical URL, Open Graph, and Twitter metadata derived from curated project data."
    - "Each selected project detail route renders project-specific JSON-LD in static HTML before hydration."
    - "Generated sitemap output includes every selected project detail route and excludes unselected project records."
    - "Project detail social preview metadata resolves to a deterministic checked-in local static asset without runtime rendering."
    - "Static and release verifiers fail if project detail metadata, JSON-LD, sitemap coverage, local social asset mapping, or runtime GitHub/API restrictions regress."
  artifacts:
    - path: "src/domain/seo.ts"
      provides: "Pure project metadata, project JSON-LD, sitemap, robots, JSON-LD serialization, and static social preview helpers"
      exports: ["metadataForProject", "projectJsonLd", "jsonLdScriptContent", "sitemapXml", "robotsTxt"]
    - path: "src/domain/project-detail-routes.test.ts"
      provides: "Selected/unselected detail route, metadata, sitemap, and social preview unit coverage"
    - path: "src/domain/portfolio-surfaces.test.ts"
      provides: "Portfolio SEO, project JSON-LD, Person sameAs, sitemap, robots, and serialization unit coverage"
    - path: "src/routes/projects/[slug].tsx"
      provides: "Project detail route head tags and project JSON-LD script rendering before hydration"
    - path: "public/sitemap.xml"
      provides: "Checked-in generated sitemap containing selected project detail routes"
    - path: "public/robots.txt"
      provides: "Checked-in generated robots text matching pure helper output"
    - path: "scripts/verify-static.ts"
      provides: "Static output verification for project detail metadata, JSON-LD, sitemap, and local social preview asset mapping"
    - path: "scripts/verify-release.ts"
      provides: "Release semantic verifier requiring JSON-LD on project detail routes"
    - path: "scripts/verify-release.test.ts"
      provides: "Regression coverage for release verifier JSON-LD requirement on project detail routes"
  key_links:
    - from: "src/routes/projects/[slug].tsx"
      to: "src/domain/seo.ts"
      via: "projectJsonLd(selectedProject()) serialized with jsonLdScriptContent() in an application/ld+json script"
      pattern: "projectJsonLd\\(|jsonLdScriptContent\\("
    - from: "scripts/generate-static-metadata.ts"
      to: "src/domain/seo.ts"
      via: "sitemapXml() and robotsTxt() pure helper output"
      pattern: "sitemapXml\\(\\)|robotsTxt\\(\\)"
    - from: "scripts/verify-static.ts"
      to: "src/domain/seo.ts"
      via: "metadataForProject(), projectJsonLd(), sitemapXml(), and robotsTxt() expected output"
      pattern: "metadataForProject\\(|projectJsonLd\\(|sitemapXml\\("
    - from: "scripts/verify-release.ts"
      to: "project detail static HTML"
      via: "semanticFindingsForRoute() requiring application/ld+json on every route"
      pattern: "application\\/ld\\+json"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 12-2026-06-02T21-19-24
generated_at: 2026-06-02T21:35:43Z
---

<objective>
Complete Phase 12 so selected project story pages are shareable static pages with route-specific metadata, project structured data, sitemap coverage, and deterministic social preview support.

Purpose: Project detail pages already exist and render stories. This plan makes those routes discoverable and shareable without adding runtime GitHub/API calls, dynamic OG endpoints, or visitor-visible OpenLinks repetition.

Output: Updated pure SEO helpers and tests, project detail JSON-LD rendered before hydration, regenerated checked-in static metadata, stricter static/release verification, and a passing repo-native verification gate.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/12-project-metadata-sharing/12-CONTEXT.md
@.planning/phases/12-project-metadata-sharing/12-RESEARCH.md
@.planning/phases/10-project-detail-route-foundation/10-01-SUMMARY.md
@.planning/phases/11-project-story-page-ui/11-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/projects.ts
@src/domain/profile.ts
@src/domain/routes.ts
@src/domain/seo.ts
@src/routes/projects/[slug].tsx
@scripts/generate-static-metadata.ts
@scripts/verify-static.ts
@scripts/verify-release.ts

<standards_guidance>
- Repo-local guidance: keep dark-primary UI behavior intact; this phase should not add visual UI text or OpenLinks CTAs.
- Bright Builds sidecar: preserve functional-core/pure-helper SEO logic, TypeScript `maybe...` naming for nullish internal values, Arrange/Act/Assert test structure, and repo-native Bun verification.
- Pinned canonical standards loaded: architecture, code shape, verification, testing, and TypeScript/JavaScript at commit 05f8d7a6c9c2e157ec4f922a05273e72dab97676.
- OpenLinks skill applied in subtle metadata mode: keep OpenLinks discoverability through existing `profileSameAsLinks()` / `peterProfile.links` identity data and do not add repeated visible promotion to project detail pages.
</standards_guidance>

<interfaces>
Existing project selection and route contracts:
```ts
export type ProjectDetailPageProject = ProjectStory & {
  tier: "flagship";
  includeInProjectIndex: true;
  detail: ProjectDetailStory;
};

export function projectDetailPageProjects(
  projects?: readonly ProjectStory[],
): readonly ProjectDetailPageProject[];

export function maybeProjectDetailPageProjectBySlug(
  slug: string,
  projects?: readonly ProjectStory[],
): ProjectDetailPageProject | null;

export function projectDetailPath(project: Pick<ProjectStory, "slug">): string;
export function projectDetailRoutes(projects?: readonly ProjectStory[]): readonly string[];
export const prerenderRoutes: readonly string[];
```

Existing SEO/profile contracts:
```ts
export type PageMetadata = {
  title: string;
  description: string;
  canonical: string;
  openGraph: {
    title: string;
    description: string;
    url: string;
    type: "website";
    image: SocialImageMetadata;
  };
  twitter: {
    card: "summary_large_image";
    title: string;
    description: string;
    image: SocialImageMetadata;
  };
};

export function metadataForProject(project: ProjectStory, profile?: Profile): PageMetadata;
export function personJsonLd(profile?: Profile): PersonJsonLd;
export function projectItemListJsonLd(projects?: readonly ProjectStory[], profile?: Profile): ProjectItemListJsonLd;
export function jsonLdScriptContent(value: unknown): string;
export function profileSameAsLinks(profile: Profile): string[];
```

New contract to create:
```ts
export type ProjectJsonLd = {
  "@context": "https://schema.org";
  "@type": "SoftwareSourceCode";
  name: string;
  description: string;
  url: string;
  sameAs: string[];
  keywords: string;
  creator: PersonJsonLd;
  about: string[];
};

export function projectJsonLd(project: ProjectDetailPageProject, profile?: Profile): ProjectJsonLd;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend pure project SEO helpers and unit coverage</name>
  <read_first>
    - .planning/phases/12-project-metadata-sharing/12-CONTEXT.md
    - .planning/phases/12-project-metadata-sharing/12-RESEARCH.md
    - src/domain/seo.ts
    - src/domain/projects.ts
    - src/domain/profile.ts
    - src/domain/routes.ts
    - src/domain/project-detail-routes.test.ts
    - src/domain/portfolio-surfaces.test.ts
  </read_first>
  <files>src/domain/seo.ts, src/domain/project-detail-routes.test.ts, src/domain/portfolio-surfaces.test.ts</files>
  <behavior>
    - Test 1: every `projectDetailPageProjects()` record gets complete `metadataForProject()` title, description, canonical, Open Graph, Twitter, and `social/bright-builds-og.png` image fields per D-01, D-02, D-08, and META-01/META-04.
    - Test 2: new `projectJsonLd(project, peterProfile)` returns `SoftwareSourceCode` JSON-LD with project name, `oneLine`, canonical detail URL, direct project links in `sameAs`, combined tags/themes in `keywords`, story facts in `about`, and `creator.sameAs` containing both `https://github.com/pRizz` and `https://openlinks.us/` per D-04, D-05, D-06, and META-02.
    - Test 3: `sitemapXml()` default output includes `prerenderRoutes` / `projectDetailRoutes()` and excludes an unselected public project such as `/projects/open-bitcoin` per D-07 and META-03.
    - Test 4: `jsonLdScriptContent(projectJsonLd(...))` remains HTML-safe for values containing `<`.
  </behavior>
  <action>
    Update `src/domain/seo.ts` as the functional core. Add exported `ProjectJsonLd` and `projectJsonLd(project: ProjectDetailPageProject, profile: Profile = peterProfile)` using only checked-in curated project fields and profile data. Use `projectDetailPath(project)` for canonical URLs, `project.links.map((link) => link.href)` for `sameAs`, `[...project.themes, ...project.tags]` joined into a deterministic keyword string, and `profileSameAsLinks(profile)` through `personJsonLd(profile)` for the creator identity so OpenLinks remains discoverable through existing profile metadata per D-06. Include authored detail/story facts in `about` as plain strings without claiming schema fields not backed by curated data.

    Keep `metadataForProject()` pure and project-specific. It must continue to derive title, description, canonical, Open Graph, and Twitter fields from the project and `peterProfile.canonicalOrigin`; do not add runtime GitHub calls, route filesystem scanning, or dynamic image rendering. If the social image helper needs to be exported to reduce duplication, export a stable constant or helper whose value maps to `/social/bright-builds-og.png`.

    Change `sitemapXml()` so the default route set covers all prerendered paths, including selected detail routes. Prefer a path-based signature such as `sitemapXml(paths: readonly string[] = prerenderRoutes, profile: Profile = peterProfile)` if it avoids fake `SiteRoute` objects and import cycles. If an import cycle appears between `seo.ts` and `routes.ts`, keep `sitemapXml(paths, profile)` path-based and require callers/tests/generators to pass `prerenderRoutes`; do not weaken the selected-route coverage.

    Update `src/domain/project-detail-routes.test.ts` and `src/domain/portfolio-surfaces.test.ts` with focused Arrange/Act/Assert tests for the behaviors above. Keep tests one concern each and avoid broad snapshots.
  </action>
  <verify>
    <automated>bun run test -- src/domain/project-detail-routes.test.ts src/domain/portfolio-surfaces.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "export function projectJsonLd|export type ProjectJsonLd" src/domain/seo.ts` finds the new pure JSON-LD helper and type.
    - `rg "https://openlinks.us/|profileSameAsLinks|creator" src/domain/portfolio-surfaces.test.ts src/domain/project-detail-routes.test.ts` shows tests preserving subtle OpenLinks identity through metadata, not visible project-page copy.
    - `rg "open-bitcoin" src/domain/project-detail-routes.test.ts src/domain/portfolio-surfaces.test.ts` shows unselected exclusion coverage for default sitemap/detail behavior.
    - Focused domain tests pass with the command above.
  </acceptance_criteria>
  <done>Pure helpers and unit tests fully cover META-01 through META-04 without runtime GitHub/API dependency, dynamic OG rendering, or unselected project exposure.</done>
</task>

<task type="auto">
  <name>Task 2: Render project JSON-LD and regenerate checked-in static metadata</name>
  <read_first>
    - src/routes/projects/[slug].tsx
    - src/domain/seo.ts
    - src/domain/projects.ts
    - scripts/generate-static-metadata.ts
    - public/sitemap.xml
    - public/robots.txt
  </read_first>
  <files>src/routes/projects/[slug].tsx, public/sitemap.xml, public/robots.txt</files>
  <action>
    Update `src/routes/projects/[slug].tsx` to import `jsonLdScriptContent` and `projectJsonLd` from `../../domain/seo`. In the selected-project branch, render exactly one project-specific `<script type="application/ld+json">` before the main `<article>` and after head metadata is acceptable. Use Solid's HTML insertion pattern already accepted by the project, for example `<script type="application/ld+json">{jsonLdScriptContent(projectJsonLd(selectedProject()))}</script>` if Solid escapes safely for script contents, or the existing project-safe pattern if another route uses it. The not-found fallback must not emit project-specific JSON-LD.

    Keep the existing route head rendering and asset links. Do not introduce a broad shared head component refactor per D-03. Do not add new visible OpenLinks text or a new OpenLinks CTA to project detail pages; the creator identity belongs in structured metadata through `personJsonLd()` per D-06.

    Run `bun run generate:static-metadata` after the sitemap helper change. Commit the regenerated `public/sitemap.xml` and `public/robots.txt` only if their contents change. The sitemap must contain every selected route from `projectDetailRoutes()` and must not contain `/projects/open-bitcoin` or other unselected detail URLs.
  </action>
  <verify>
    <automated>bun run generate:static-metadata</automated>
    <automated>bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg "projectJsonLd|jsonLdScriptContent|application/ld\\+json" 'src/routes/projects/[slug].tsx'` shows project detail JSON-LD rendering wired to the pure helper.
    - `rg "<loc>https://www.brightbuilds.us/projects/openlinks</loc>" public/sitemap.xml` succeeds.
    - `! rg "<loc>https://www.brightbuilds.us/projects/open-bitcoin</loc>" public/sitemap.xml` succeeds.
    - `bun run build` prerenders selected project detail routes without adding runtime API output.
  </acceptance_criteria>
  <done>Selected project detail pages render project JSON-LD before hydration, and checked-in static metadata matches the pure helper output.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Tighten static and release verifiers, then run the full gate</name>
  <read_first>
    - scripts/verify-static.ts
    - scripts/verify-release.ts
    - scripts/verify-release.test.ts
    - src/domain/seo.ts
    - src/domain/projects.ts
    - src/domain/routes.ts
    - package.json
  </read_first>
  <files>scripts/verify-static.ts, scripts/verify-release.ts, scripts/verify-release.test.ts</files>
  <behavior>
    - Test 1: `semanticFindingsForRoute(routeFixture("/projects/openlinks", htmlWithoutJsonLd))` reports `JSON-LD` once project detail JSON-LD is no longer deferred per D-12.
    - Test 2: `semanticFindingsForRoute(routeFixture("/projects/openlinks", htmlWithJsonLd))` returns no JSON-LD finding when main, h1, and skip link are valid.
    - Static verifier behavior: every selected detail route checks route-specific metadata, project JSON-LD containing project name and canonical URL, local social preview asset mapping, sitemap inclusion, and unselected sitemap/detail exclusion per D-10 and D-11.
  </behavior>
  <action>
    Update `scripts/verify-static.ts` to import and use `projectJsonLd` alongside `metadataForProject`, `projectDetailRoutes`, `sitemapXml`, and `robotsTxt`. For each selected detail route, assert static HTML contains the same project metadata produced by `metadataForProject(project)`, an `application/ld+json` script containing at least `projectJsonLd(project).name`, `projectJsonLd(project).url`, and one profile sameAs URL, and local social image metadata that maps to `.output/public/social/bright-builds-og.png`. Keep the existing forbidden runtime GitHub/API residue checks and remote runtime visual asset checks.

    Extend static verification for sitemap behavior: assert generated `sitemap.xml` equals `sitemapXml()` output, contains every `projectDetailRoutes()` path, and does not contain `/projects/open-bitcoin` as a detail URL. Keep `robots.txt` equality to `robotsTxt()`.

    Update `scripts/verify-release.ts` by removing `isProjectDetailFoundationRoute()` and the conditional JSON-LD exception from `semanticFindingsForRoute()`. All routes, including `/projects/{slug}`, must require an `application/ld+json` script. Update `scripts/verify-release.test.ts` by replacing the old deferral test with the two behaviors listed above.

    Run the full verification sequence in this order: `bun run format`, `bun run check`, `bun run typecheck`, `bun run test -- src/domain/project-detail-routes.test.ts src/domain/portfolio-surfaces.test.ts scripts/verify-release.test.ts`, `bun run generate:static-metadata`, `bun run build`, `bun run verify:static`, `bun run verify:release`, and final `bun run verify`.
  </action>
  <verify>
    <automated>bun run format</automated>
    <automated>bun run check</automated>
    <automated>bun run typecheck</automated>
    <automated>bun run test -- src/domain/project-detail-routes.test.ts src/domain/portfolio-surfaces.test.ts scripts/verify-release.test.ts</automated>
    <automated>bun run generate:static-metadata</automated>
    <automated>bun run build</automated>
    <automated>bun run verify:static</automated>
    <automated>bun run verify:release</automated>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `! rg "isProjectDetailFoundationRoute|defers JSON-LD checks for project detail" scripts/verify-release.ts scripts/verify-release.test.ts` succeeds.
    - `rg "projectJsonLd|projectDetailRoutes|open-bitcoin|sitemapXml\\(" scripts/verify-static.ts` shows static verifier coverage for project JSON-LD, selected route inclusion, unselected detail exclusion, and pure sitemap equality.
    - `bun run verify:static` passes after `bun run build`.
    - `bun run verify:release` passes after `bun run build`.
    - Final `bun run verify` passes.
  </acceptance_criteria>
  <done>Release verification no longer tolerates missing project detail JSON-LD, and the aggregate repo gate proves the metadata/sharing contract is enforced.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated project data -> route metadata | Checked-in authored records become public title, description, canonical, Open Graph, Twitter, sitemap, and JSON-LD output. |
| profile identity -> structured data | Peter profile links, including OpenLinks, become `sameAs` identity signals in JSON-LD. |
| static helpers -> generated files | Pure helper output is written to `public/sitemap.xml`, `public/robots.txt`, and SolidStart static HTML. |
| built output -> crawlers/social parsers | Search and social clients consume HTML head tags, JSON-LD, sitemap XML, and social image URLs without app hydration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Information Disclosure | project metadata and verifier output | mitigate | Keep metadata/JSON-LD derived from checked-in curated data and profile records only per D-01; preserve `verify:no-github-runtime`, `scripts/verify-static.ts`, and `scripts/verify-release.ts` checks for GitHub API URLs, Octokit runtime residue, and token-like values. |
| T-12-02 | Tampering | JSON-LD script serialization | mitigate | Use `jsonLdScriptContent()` for `projectJsonLd()` script contents and unit-test escaping so malformed project strings cannot break script parsing or inject markup. |
| T-12-03 | Spoofing | creator/author structured data | mitigate | Populate creator identity through `personJsonLd(peterProfile)` / `profileSameAsLinks()` only; include OpenLinks subtly through existing profile sameAs metadata and do not invent unsupported identity claims. |
| T-12-04 | Tampering | sitemap route coverage | mitigate | Make sitemap generation use selected prerender/detail route helpers, add unit/static checks for all `projectDetailRoutes()`, and assert unselected `/projects/open-bitcoin` does not appear as a detail URL. |
| T-12-05 | Denial of Service | social preview assets | mitigate | Keep project detail social preview metadata mapped to checked-in `.output/public/social/bright-builds-og.png`; verify canonical local asset mapping and do not add runtime OG rendering or remote image dependencies. |
| T-12-06 | Repudiation | release semantics | mitigate | Remove the project detail JSON-LD release verifier exception so missing structured data fails `bun run verify:release` and final `bun run verify`. |
</threat_model>

<verification>
Run these commands in order after implementation:

```bash
bun run format
bun run check
bun run typecheck
bun run test -- src/domain/project-detail-routes.test.ts src/domain/portfolio-surfaces.test.ts scripts/verify-release.test.ts
bun run generate:static-metadata
bun run build
bun run verify:static
bun run verify:release
bun run verify
```

Before finishing, review `git diff -- . ':(exclude).planning/config.json'` and confirm no unrelated source, UI, dependency, or OpenLinks-visible CTA changes were introduced.
</verification>

<success_criteria>
- META-01: every selected project detail route has route-specific title, description, canonical, Open Graph, and Twitter metadata from `metadataForProject()`.
- META-02: every selected project detail route renders project-specific `SoftwareSourceCode` JSON-LD before hydration through `projectJsonLd()` and `jsonLdScriptContent()`.
- META-03: `sitemapXml()` / checked-in `public/sitemap.xml` includes every `projectDetailRoutes()` path and excludes unselected detail URLs.
- META-04: every project detail route uses deterministic local social preview metadata mapped to `public/social/bright-builds-og.png` without runtime rendering.
- `scripts/verify-static.ts`, `scripts/verify-release.ts`, and the focused tests enforce the metadata/sharing contract.
- Final `bun run verify` passes.
</success_criteria>

<output>
After completion, create `.planning/phases/12-project-metadata-sharing/12-01-SUMMARY.md`.
</output>
