---
phase: 09-clean-builder-release-gate-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - README.md
  - docs/release-readiness.md
  - scripts/release-readiness.ts
  - scripts/release-readiness.test.ts
  - .planning/phases/09-clean-builder-release-gate-closure/09-01-SUMMARY.md
autonomous: true
requirements:
  - BROW-01
  - GATE-04
  - REL-03
  - REL-04
user_setup: []
must_haves:
  truths:
    - "Maintainer can identify and run the Playwright Chromium provisioning command from package.json before running the aggregate release gate on a clean builder."
    - "Maintainer can read concise README guidance and detailed release-readiness guidance for clean-builder browser provisioning."
    - "Release-readiness documentation names verify:project-helper-surface as part of the aggregate bun run verify gate."
    - "Release-readiness document checks fail when browser provisioning or helper-surface guard facts are omitted."
    - "Focused release-readiness tests and full bun run verify pass after the contract is updated."
  artifacts:
    - path: "package.json"
      provides: "Discoverable explicit browser provisioning script"
      contains: "\"install:browser\": \"playwright install chromium\""
    - path: "docs/release-readiness.md"
      provides: "Full release contract for clean local and Cloudflare/static builders"
      contains: "bun run install:browser"
    - path: "README.md"
      provides: "Concise maintainer entrypoint for browser provisioning and helper-surface gate visibility"
      contains: "verify:project-helper-surface"
    - path: "scripts/release-readiness.ts"
      provides: "Required document facts for browser provisioning and helper-surface guard"
      contains: "bun run install:browser"
    - path: "scripts/release-readiness.test.ts"
      provides: "Regression tests proving omitted release document facts fail"
      contains: "verify:project-helper-surface"
  key_links:
    - from: "package.json"
      to: "docs/release-readiness.md"
      via: "browser provisioning command documented for clean builders"
      pattern: "bun run install:browser"
    - from: "package.json"
      to: "README.md"
      via: "concise setup prerequisite"
      pattern: "bun run install:browser"
    - from: "scripts/release-readiness.ts"
      to: "docs/release-readiness.md"
      via: "releaseReadinessDocumentFindings required facts"
      pattern: "releaseReadinessDocumentFindings"
    - from: "package.json"
      to: "docs/release-readiness.md"
      via: "aggregate gate list includes helper-surface guard"
      pattern: "verify:project-helper-surface"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 09-2026-06-01T00-46-43
generated_at: 2026-06-01T00:00:00.000Z
---

<objective>
Close the v1.1 clean-builder release gate gap by making Playwright Chromium provisioning explicit, documenting the complete aggregate release gate, and adding regression guards so the release-readiness contract fails if those facts drift.

Purpose: Maintainers should be able to start from a fresh local machine or Cloudflare/static builder, provision the browser dependency intentionally, run `bun run verify`, and understand that the helper-surface guard is part of the release contract.

Output: Updated package script surface, README/release-readiness documentation, release-readiness required document facts, focused tests, and a Phase 9 execution summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-clean-builder-release-gate-closure/09-CONTEXT.md
@.planning/v1.1-MILESTONE-AUDIT.md
@package.json
@README.md
@docs/release-readiness.md
@scripts/release-readiness.ts
@scripts/release-readiness.test.ts
@scripts/verify-release.ts
@scripts/verify-release.test.ts
@playwright.config.ts
@.planning/phases/06-browser-accessibility-release-automation/06-01-SUMMARY.md

<interfaces>
From package.json:
```json
{
  "scripts": {
    "verify:browser": "playwright test --config playwright.config.ts",
    "verify:static": "bun run scripts/verify-static.ts",
    "verify:release": "bun run scripts/verify-release.ts",
    "verify": "bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run verify:project-helper-surface && bun run verify:visual-system && bun run build && bun run verify:browser && bun run verify:static && bun run verify:release"
  }
}
```

From scripts/release-readiness.ts:
```ts
type RequiredDocumentFact = {
  label: string;
  text: string;
};

export function releaseReadinessDocumentFindings(
  documentPath = releaseReadinessDocumentPath,
): readonly ReleaseFinding[];
```

From scripts/release-readiness.test.ts:
```ts
describe("release-readiness document contract", () => {
  it("accepts the checked-in release-readiness document", () => {
    const documentPath = "docs/release-readiness.md";
    const findings = releaseReadinessDocumentFindings(documentPath);
    expect(findings).toEqual([]);
  });
});
```
</interfaces>

Locked user decisions from `09-CONTEXT.md`:
- D-01: Use a repo-owned browser provisioning command instead of making maintainers infer Playwright install syntax.
- D-02: Do not add a `postinstall` or other broad lifecycle hook.
- D-03: Document the clean-builder path for local fresh machines and Cloudflare/static builders.
- D-04: Keep `bun run verify` as the single aggregate release gate.
- D-05: Name every material aggregate gate, including `verify:project-helper-surface`, in release-readiness docs.
- D-06: Keep README concise; mention the prerequisite and helper guard without duplicating the full checklist.
- D-07: Extend `scripts/release-readiness.ts` and tests instead of adding a separate checker.
- D-08: Verification must include focused release-readiness tests and full `bun run verify`.

Bright Builds context:
- Use Bun/TypeScript script surfaces; do not add Python.
- Tests for pure document-contract logic should use Arrange, Act, Assert sections and prove one concern per test.
- OpenLinks is already present through managed README badges and release external-link policy; do not add duplicate OpenLinks promotion while editing README/docs.
</context>

<must_haves>
1. `package.json` exposes an explicit `install:browser` script that runs `playwright install chromium`; no `postinstall` script is introduced.
2. `docs/release-readiness.md` lists `bun run verify:project-helper-surface` in the aggregate gate and documents `bun run install:browser` before `bun run verify` for clean local and Cloudflare/static builders.
3. `README.md` concisely mentions the browser provisioning prerequisite and makes `verify:project-helper-surface` visible in the release script surface.
4. `scripts/release-readiness.ts` requires release docs to mention browser provisioning and the helper-surface guard.
5. `scripts/release-readiness.test.ts` proves the checked-in document passes and that deleting either new fact produces a `release-readiness document` finding.
6. `bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts` and `bun run verify` pass.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit browser provisioning script and release docs</name>
  <read_first>
    - package.json
    - README.md
    - docs/release-readiness.md
    - playwright.config.ts
    - .planning/phases/06-browser-accessibility-release-automation/06-01-SUMMARY.md
  </read_first>
  <files>package.json, README.md, docs/release-readiness.md</files>
  <action>
    Add `"install:browser": "playwright install chromium"` to `package.json` scripts near the existing Playwright/browser scripts per D-01. Do not add `postinstall`, `prepare`, or any lifecycle hook per D-02; browser provisioning must stay explicit.

    Update `docs/release-readiness.md` per D-03, D-04, and D-05:
    - In the Primary Release Gate section, add `bun run verify:project-helper-surface` to the aggregate gate list in the same order as `package.json`.
    - Add a short clean-builder prerequisite before `bun run verify`: run `bun run install:browser` when Playwright Chromium is not already provisioned.
    - In Browser and Accessibility, state that `bun run verify:browser` requires the Chromium browser installed by `bun run install:browser`.
    - In Cloudflare Pages, document the clean-builder command sequence as `bun run install:browser && bun run verify` for full-gate builds while preserving `bun run verify` as the aggregate gate.
    - In preview and production checklists, include browser provisioning before aggregate verification where a clean builder or fresh local environment is involved.

    Update `README.md` per D-06 only: mention `bun run install:browser` as the Playwright/Chromium prerequisite before `bun run verify` on clean machines/builders, add `verify:project-helper-surface` to the concise aggregate description or useful scripts, and avoid duplicating the full release checklist or adding another OpenLinks placement.
  </action>
  <verify>
    <automated>bun -e 'const pkg = await Bun.file("package.json").json(); if (pkg.scripts["install:browser"] !== "playwright install chromium") throw new Error("missing install:browser script"); if ("postinstall" in pkg.scripts) throw new Error("postinstall is prohibited");'</automated>
    <automated>rg -n 'bun run install:browser' README.md docs/release-readiness.md</automated>
    <automated>rg -n 'bun run install:browser && bun run verify' docs/release-readiness.md</automated>
    <automated>rg -n 'verify:project-helper-surface' README.md docs/release-readiness.md</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains exactly one `install:browser` script with value `playwright install chromium`.
    - `package.json` does not contain a `postinstall` script.
    - `docs/release-readiness.md` contains `bun run install:browser`, `bun run install:browser && bun run verify`, and `bun run verify:project-helper-surface`.
    - `README.md` contains `bun run install:browser` and `verify:project-helper-surface`, but does not duplicate the full preview or production deployment checklist.
  </acceptance_criteria>
  <done>`package.json`, `docs/release-readiness.md`, and `README.md` meet the acceptance criteria above and the listed automated checks pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add release-readiness document regression guards</name>
  <read_first>
    - scripts/release-readiness.ts
    - scripts/release-readiness.test.ts
    - scripts/verify-release.ts
    - scripts/verify-release.test.ts
    - docs/release-readiness.md
  </read_first>
  <files>scripts/release-readiness.ts, scripts/release-readiness.test.ts</files>
  <behavior>
    - Test 1: The checked-in `docs/release-readiness.md` returns no `releaseReadinessDocumentFindings`.
    - Test 2: A temporary copy of the release document with every `bun run install:browser` occurrence removed returns a finding whose message mentions the browser provisioning fact.
    - Test 3: A temporary copy of the release document with every `bun run verify:project-helper-surface` occurrence removed returns a finding whose message mentions the helper-surface guard fact.
  </behavior>
  <action>
    Follow D-07 by extending `requiredReleaseReadinessDocumentFacts` in `scripts/release-readiness.ts`; do not add a separate checker. Add required facts with labels that make failures actionable, such as `Playwright Chromium provisioning command` for text `bun run install:browser`, `clean-builder browser provisioning before aggregate verify` for text `bun run install:browser && bun run verify`, and `project helper surface gate` for text `bun run verify:project-helper-surface`.

    Update `scripts/release-readiness.test.ts` in the existing Arrange/Act/Assert style. Keep the existing checked-in document pass test. Add focused negative tests that create temporary markdown fixtures from `docs/release-readiness.md`, remove one new fact per test, call `releaseReadinessDocumentFindings(tempPath)`, and assert the returned messages include the expected missing fact text. Use Node/Bun filesystem APIs in TypeScript; do not add Python or snapshot files.

    Do not change `scripts/verify-release.ts` unless the focused tests reveal a direct coupling issue. The release verifier already consumes `releaseReadinessDocumentFindings()`, so the new facts should flow into `bun run verify:release` through the existing integration.
  </action>
  <verify>
    <automated>bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts</automated>
    <automated>rg -n 'bun run install:browser|bun run verify:project-helper-surface' scripts/release-readiness.ts scripts/release-readiness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/release-readiness.ts` requires `bun run install:browser`, `bun run install:browser && bun run verify`, and `bun run verify:project-helper-surface`.
    - `scripts/release-readiness.test.ts` has focused tests that fail when browser provisioning text is removed and when helper-surface guard text is removed.
    - Focused Vitest command `bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts` passes.
    - No new checker file, Python file, or lifecycle install hook is added.
  </acceptance_criteria>
  <done>`scripts/release-readiness.ts` and `scripts/release-readiness.test.ts` meet the acceptance criteria above and the focused Vitest command passes.</done>
</task>

<task type="auto">
  <name>Task 3: Run final release verification and write execution summary</name>
  <read_first>
    - package.json
    - docs/release-readiness.md
    - README.md
    - scripts/release-readiness.ts
    - scripts/release-readiness.test.ts
    - .planning/phases/09-clean-builder-release-gate-closure/09-CONTEXT.md
  </read_first>
  <files>.planning/phases/09-clean-builder-release-gate-closure/09-01-SUMMARY.md</files>
  <action>
    Run the explicit browser provisioning command first, then the focused release-readiness tests, then the full aggregate gate per D-08:
    - `bun run install:browser`
    - `bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts`
    - `bun run verify`

    Review the diff to confirm the change is limited to the script/docs/release-readiness surfaces named in this plan plus the summary. Create `.planning/phases/09-clean-builder-release-gate-closure/09-01-SUMMARY.md` using the standard summary template. The summary must list completed requirements `BROW-01`, `GATE-04`, `REL-03`, and `REL-04`, key files changed, verification commands run, and any residual risk.
  </action>
  <verify>
    <automated>bun run install:browser</automated>
    <automated>bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts</automated>
    <automated>bun run verify</automated>
    <automated>test -f .planning/phases/09-clean-builder-release-gate-closure/09-01-SUMMARY.md</automated>
    <automated>rg -n 'BROW-01|GATE-04|REL-03|REL-04|bun run verify' .planning/phases/09-clean-builder-release-gate-closure/09-01-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `bun run install:browser` completes successfully or reports Chromium is already installed.
    - Focused release-readiness and release verifier tests pass.
    - Full `bun run verify` passes after browser provisioning.
    - The summary file exists and records the four Phase 9 requirement IDs and verification evidence.
  </acceptance_criteria>
  <done>Browser provisioning, focused tests, full aggregate verification, and summary creation all meet the acceptance criteria above.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Maintainer or builder -> package scripts | A local or Cloudflare/static builder invokes repo-owned scripts that may fail if browser dependencies are missing. |
| Release docs -> release verifier | Markdown documentation is treated as a checked release artifact through `releaseReadinessDocumentFindings`. |
| Static release output -> external-link policy | Generated HTML may contain external links or token-like data that the release verifier scans without live crawling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-09-01 | Tampering | `docs/release-readiness.md` release contract | mitigate | Add required document facts in `scripts/release-readiness.ts` and focused tests so removing browser provisioning or helper-surface guard text fails release-readiness checks. |
| T-09-02 | Denial of Service | `verify:browser` on clean builders | mitigate | Add explicit `install:browser` script and document `bun run install:browser` before `bun run verify` for clean local and Cloudflare/static builders. |
| T-09-03 | Repudiation | Aggregate gate documentation | mitigate | Keep `bun run verify` as the single gate and list material contributing scripts, including `verify:project-helper-surface`, in docs and README. |
| T-09-04 | Information Disclosure | Release/deploy documentation and external-link policy | mitigate | Preserve existing public token prefix warning and do not add live external-link automation or token-bearing examples while editing docs. |
</threat_model>

<verification>
Required verification for the executor:

```bash
bun -e 'const pkg = await Bun.file("package.json").json(); if (pkg.scripts["install:browser"] !== "playwright install chromium") throw new Error("missing install:browser script"); if ("postinstall" in pkg.scripts) throw new Error("postinstall is prohibited");'
rg -n 'bun run install:browser' README.md docs/release-readiness.md
rg -n 'bun run install:browser && bun run verify' docs/release-readiness.md
rg -n 'verify:project-helper-surface' README.md docs/release-readiness.md
bun run install:browser
bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts
bun run verify
```
</verification>

<success_criteria>
- `package.json` exposes an explicit Playwright Chromium provisioning command and no lifecycle hook hides it.
- Release docs and README explain the clean-builder browser provisioning prerequisite without expanding product scope.
- The aggregate gate list in release docs names `verify:project-helper-surface`.
- Release-readiness document checks fail when browser provisioning or helper-surface guard facts are omitted.
- Focused tests and full `bun run verify` pass.
- Phase 9 summary records completion for `BROW-01`, `GATE-04`, `REL-03`, and `REL-04`.
</success_criteria>

<output>
After completion, create `.planning/phases/09-clean-builder-release-gate-closure/09-01-SUMMARY.md`.
</output>
