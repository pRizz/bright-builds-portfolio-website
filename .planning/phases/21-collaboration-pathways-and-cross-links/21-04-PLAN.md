---
phase: 21-collaboration-pathways-and-cross-links
plan: 04
type: execute
wave: 3
depends_on:
  - 21-02
  - 21-03
files_modified:
  - scripts/verify-static/expected-route-text.ts
  - tests/browser-release.playwright.ts
autonomous: true
requirements:
  - SYNTH-02
  - SYNTH-03
  - COLLAB-01
  - COLLAB-02
  - COLLAB-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-06-17T22-54-40
generated_at: 2026-06-17T23:11:34Z
must_haves:
  truths:
    - "Generated static HTML contains theme collaboration panel text and helper-derived action hrefs before hydration."
    - "Generated static HTML contains project and writing reciprocal theme links before hydration."
    - "Browser checks cover keyboard reachability plus desktop/mobile dark layout for the affected route family."
    - "Phase 21 verification does not add metadata, JSON-LD, sitemap, social-preview, release-readiness, or evidence-label assertions."
  artifacts:
    - path: "scripts/verify-static/expected-route-text.ts"
      provides: "Generated HTML expected text for collaboration and reciprocal theme links"
      contains: "relatedThemeExpectedTextsForProject"
    - path: "tests/browser-release.playwright.ts"
      provides: "Focused keyboard reachability checks for collaboration and reciprocal theme links"
      contains: "representativeThemeExternalCollaborationHref"
  key_links:
    - from: "scripts/verify-static/expected-route-text.ts"
      to: "src/domain/themes.ts"
      via: "collaborationActionsForTheme(), publicThemeEntriesForProject(), publicThemeEntriesForWritingEntry()"
      pattern: "collaborationActionsForTheme"
    - from: "tests/browser-release.playwright.ts"
      to: "built static route server"
      via: "Playwright focus traversal"
      pattern: "hasFocusedInternalPath"
---

<objective>
Add generated-output and focused browser coverage for the completed Phase 21 collaboration surfaces.

Purpose: Prove the new links and panels exist in static HTML and stay keyboard-reachable without adding Phase 22 metadata or Phase 23 release-contract claims.
Output: Static verifier expected text updates and focused Playwright assertions.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
@.planning/phases/20-theme-routes-and-dark-ui/20-03-SUMMARY.md
@.planning/phases/20-theme-routes-and-dark-ui/20-04-SUMMARY.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-02-SUMMARY.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-03-SUMMARY.md
@scripts/verify-static/expected-route-text.ts
@tests/browser-release.playwright.ts
@src/domain/themes.ts
@src/routes/themes/[slug].tsx
@src/routes/projects/[slug].tsx
@src/routes/writing/[slug].tsx

<interfaces>
From completed Phase 21 source:
```ts
export function collaborationActionsForTheme(theme: PublicThemeEntry): readonly ThemeCollaborationAction[];
export function publicThemeEntriesForProject(project: Pick<ProjectDetailPageProject, "slug">): readonly PublicThemeEntry[];
export function publicThemeEntriesForWritingEntry(entry: Pick<PublicWritingEntry, "slug">): readonly PublicThemeEntry[];
export function themeDetailPath(theme: Pick<ThemeRecord, "slug">): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend generated static text checks for collaboration links</name>
  <files>scripts/verify-static/expected-route-text.ts</files>
  <read_first>
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-02-SUMMARY.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-03-SUMMARY.md
    - scripts/verify-static/expected-route-text.ts
    - src/domain/themes.ts
    - src/routes/themes/[slug].tsx
    - src/routes/projects/[slug].tsx
    - src/routes/writing/[slug].tsx
  </read_first>
  <action>Update `scripts/verify-static/expected-route-text.ts` to import `collaborationActionsForTheme`, `publicThemeEntriesForProject`, and `publicThemeEntriesForWritingEntry` from `../../src/domain/themes`. In `themeDetailExpectedTexts(theme)`, add exact expected texts before `Related projects`: `Collaboration starting points`, `Where to start`, `theme.collaborationAngle`, and each `collaborationActionsForTheme(theme)` label plus `href="${escapeHtmlAttribute(action.href)}"`. For external actions also expect `target="_blank"` and `rel="${escapeHtmlAttribute(action.maybeRel ?? "noopener noreferrer")}"`. Add exported helpers:
```ts
export function relatedThemeExpectedTextsForProject(
  project: ProjectDetailPageProject,
): readonly string[] { ... }

export function relatedThemeExpectedTextsForWriting(
  entry: PublicWritingEntry,
): readonly string[] { ... }
```
Each helper returns `[]` when no related public themes exist; otherwise it returns `Related theme paths`, each theme title, each theme summary, `Explore theme`, and `href="${escapeHtmlAttribute(themeDetailPath(theme))}"`. Add `...relatedThemeExpectedTextsForProject(maybeProject)` to project detail expected texts after `...relatedWritingExpectedTexts`, and add `...relatedThemeExpectedTextsForWriting(entry)` to `writingDetailExpectedTexts(entry)` after related project expected texts. Do not modify `metadata-jsonld-verifier.ts`, sitemap verifier logic, social preview logic, release-readiness scripts, release evidence labels, or package scripts.</action>
  <acceptance_criteria>
    - `rg -n "collaborationActionsForTheme" scripts/verify-static/expected-route-text.ts` finds the import and helper use.
    - `rg -n "publicThemeEntriesForProject" scripts/verify-static/expected-route-text.ts` finds the import and helper use.
    - `rg -n "publicThemeEntriesForWritingEntry" scripts/verify-static/expected-route-text.ts` finds the import and helper use.
    - `rg -n "relatedThemeExpectedTextsForProject" scripts/verify-static/expected-route-text.ts` finds the exported project helper.
    - `rg -n "relatedThemeExpectedTextsForWriting" scripts/verify-static/expected-route-text.ts` finds the exported writing helper.
    - `rg -n "Collaboration starting points|Where to start|Related theme paths|Explore theme" scripts/verify-static/expected-route-text.ts` finds the exact expected text additions.
    - `git diff --exit-code -- scripts/verify-static/metadata-jsonld-verifier.ts scripts/verify-static/sitemap-assets-verifier.ts scripts/verify-release.ts scripts/release-readiness.ts package.json` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run test scripts/verify-static.test.ts && bun run build && bun run verify:static</automated>
  </verify>
  <done>Static verification proves generated HTML includes collaboration actions and reciprocal theme links while metadata, JSON-LD, sitemap, social preview, release docs, and package scripts remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Extend focused browser keyboard coverage for new links</name>
  <files>tests/browser-release.playwright.ts</files>
  <read_first>
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
    - .planning/phases/20-theme-routes-and-dark-ui/20-04-SUMMARY.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-02-SUMMARY.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-03-SUMMARY.md
    - tests/browser-release.playwright.ts
    - src/domain/themes.ts
    - src/domain/projects.ts
    - src/domain/writing.ts
  </read_first>
  <action>Update `tests/browser-release.playwright.ts` to import `collaborationActionsForTheme`, `publicThemeEntriesForProject`, `publicThemeEntriesForWritingEntry`, and `themeDetailPath`. Add `projectDetailPageProjects` to the project imports if needed. In the existing `keyboard focus reaches release-critical paths` test, after focusing the theme detail route, assert focus reaches one representative external collaboration action using a new helper `representativeThemeExternalCollaborationHref()` and a new predicate `hasFocusedHref(focusedTargets, expectedHref)`. After focusing a representative selected project detail route that has related themes, assert focus reaches `representativeProjectRelatedThemeRoute()`. After focusing a representative writing detail route that has related themes, assert focus reaches `representativeWritingRelatedThemeRoute()`. Add helpers:
```ts
function representativeThemeExternalCollaborationHref(): string { ... }
function representativeProjectRelatedThemeRoute(): string { ... }
function representativeWritingRelatedThemeRoute(): string { ... }
function hasFocusedHref(focusedTargets: readonly FocusSnapshot[], expectedHref: string): boolean { ... }
```
Each representative helper must derive from public helper output, throw a clear `Expected ...` error when no route/action exists, and use no copied slugs. Do not add release-readiness document checks, evidence label checks, metadata checks, sitemap checks, live external-link reachability, or new Playwright projects.</action>
  <acceptance_criteria>
    - `rg -n "representativeThemeExternalCollaborationHref" tests/browser-release.playwright.ts` finds the helper and assertion.
    - `rg -n "representativeProjectRelatedThemeRoute" tests/browser-release.playwright.ts` finds the helper and assertion.
    - `rg -n "representativeWritingRelatedThemeRoute" tests/browser-release.playwright.ts` finds the helper and assertion.
    - `rg -n "hasFocusedHref" tests/browser-release.playwright.ts` finds the predicate and use.
    - `rg -n "collaborationActionsForTheme|publicThemeEntriesForProject|publicThemeEntriesForWritingEntry|themeDetailPath" tests/browser-release.playwright.ts` finds helper-derived imports or uses.
    - `! rg -n "release-readiness|evidence label|metadata|JSON-LD|sitemap|reachability|newProject" tests/browser-release.playwright.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run build && bun run verify:browser</automated>
  </verify>
  <done>Browser verification proves keyboard focus reaches a theme collaboration external action plus reciprocal project and writing theme links, while existing axe and desktop/mobile dark layout loops cover the affected prerendered routes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built static output -> verifier expectations | Generated HTML must prove visitor-visible collaboration links before hydration. |
| browser focus traversal -> link reachability evidence | Keyboard checks verify anchors are reachable without relying on manual inspection. |
| external collaboration links -> browser tab boundary | Source/live links must keep `noopener noreferrer` semantics. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-04-01 | Information Disclosure | `expected-route-text.ts` helper additions | mitigate | Derive expected project/writing/theme relationship text from public helpers only; return `[]` for no related public themes. |
| T-21-04-02 | Spoofing/Tampering | Generated external anchors | mitigate | Static expected text includes `target="_blank"` and `rel="noopener noreferrer"` for external collaboration actions. |
| T-21-04-03 | Information Disclosure | Runtime fetch/token leakage | mitigate | Acceptance blocks release/link reachability scope, and existing `bun run verify:no-github-runtime` remains available through aggregate verification. |
| T-21-04-04 | Repudiation | Automated evidence boundaries | mitigate | This plan updates static text and focused browser checks only; acceptance requires no release-readiness or evidence-label edits. |
</threat_model>

<verification>
Run `bun run test scripts/verify-static.test.ts`, `bun run build`, `bun run verify:static`, `bun run verify:browser`, and final `bun run typecheck && bun run check && bun run test`. If any high-severity threat mitigation fails, stop before summary.
</verification>

<success_criteria>
- Static verifier expects `Collaboration starting points`, `Where to start`, collaboration action labels/hrefs, `Related theme paths`, and `Explore theme` for generated route HTML.
- Browser checks prove keyboard focus reaches one helper-derived theme external collaboration action and reciprocal theme links from project and writing detail routes.
- Phase 22 metadata/JSON-LD/sitemap/social-preview files and Phase 23 release-readiness/evidence-label files are unchanged by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/21-collaboration-pathways-and-cross-links/21-04-SUMMARY.md`.
</output>
