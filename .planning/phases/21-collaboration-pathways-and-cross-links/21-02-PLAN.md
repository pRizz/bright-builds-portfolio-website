---
phase: 21-collaboration-pathways-and-cross-links
plan: 02
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/routes/themes/[slug].tsx
  - tests/theme-detail-route.test.tsx
autonomous: true
requirements:
  - SYNTH-02
  - COLLAB-01
  - COLLAB-02
  - COLLAB-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-06-17T22-54-40
generated_at: 2026-06-17T23:11:34Z
must_haves:
  truths:
    - "Visitor can see a compact collaboration starting-point panel on each public theme detail page."
    - "Visitor can move from theme detail pages to selected project story links and public writing links."
    - "Visitor can inspect reviewed source/live links from related projects without OpenLinks becoming a generic primary CTA."
    - "Unknown or non-public theme fallback does not expose collaboration data."
  artifacts:
    - path: "src/routes/themes/[slug].tsx"
      provides: "Theme detail collaboration panel"
      contains: "Collaboration starting points"
    - path: "tests/theme-detail-route.test.tsx"
      provides: "SSR route assertions for collaboration panel and fallback safety"
      contains: "renders collaboration starting points on public theme detail routes"
  key_links:
    - from: "src/routes/themes/[slug].tsx"
      to: "src/domain/themes.ts"
      via: "collaborationActionsForTheme(theme)"
      pattern: "collaborationActionsForTheme"
    - from: "src/routes/themes/[slug].tsx"
      to: "external reviewed project links"
      via: "ThemeCollaborationAction.external and maybeRel"
      pattern: "target=.*_blank"
---

<objective>
Add the public theme detail collaboration panel over the Phase 21 domain helper.

Purpose: Turn theme pages into practical collaboration starting points per D-01 through D-04 while keeping OpenLinks low-intrusion per D-11 through D-13.
Output: Theme detail route panel and SSR route tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
@.planning/phases/20-theme-routes-and-dark-ui/20-02-SUMMARY.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-01-SUMMARY.md
@src/routes/themes/[slug].tsx
@tests/theme-detail-route.test.tsx
@src/styles/app.css
@src/components/SiteLayout.tsx

<interfaces>
From Plan 21-01, `src/domain/themes.ts` must export:
```ts
export type ThemeCollaborationAction = {
  kind: ThemeCollaborationActionKind;
  label: "Project story" | "Source" | "Live surface" | "Read note" | "Read essay" | "Contact path";
  href: string;
  external: boolean;
  maybeRel?: string;
  maybeProjectSlug?: string;
  maybeWritingSlug?: string;
};

export function collaborationActionsForTheme(
  theme: PublicThemeEntry,
  projects?: readonly ProjectStory[],
  entries?: readonly WritingEntry[],
): readonly ThemeCollaborationAction[];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render the theme collaboration starting-point panel</name>
  <files>src/routes/themes/[slug].tsx</files>
  <read_first>
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-01-SUMMARY.md
    - src/domain/themes.ts
    - src/routes/themes/[slug].tsx
    - src/routes/projects/[slug].tsx
    - src/components/SiteLayout.tsx
    - src/styles/app.css
  </read_first>
  <action>Import `collaborationActionsForTheme` and `type ThemeCollaborationAction` from `../../domain/themes`. In `ThemeArticle`, add `const collaborationActions = collaborationActionsForTheme(theme);` and render `<ThemeCollaborationPanel theme={theme} actions={collaborationActions} />` as the first child inside `<aside class="project-detail-aside" ...>`, before `RelatedProjectsPanel`. Add `ThemeCollaborationPanel(props: { theme: PublicThemeEntry; actions: readonly ThemeCollaborationAction[] })` with one `<section class="project-detail-panel visual-surface" aria-labelledby="collaboration-starting-points">`. Use exact heading text `Collaboration starting points`, exact label `Where to start`, and render `props.theme.collaborationAngle` before actions. Render actions as real anchors inside `<nav class="link-list" aria-label={`${props.theme.title} collaboration starting points`}>`. Each anchor uses `class="text-link surface-link"`, `href={action.href}`, `target={action.external ? "_blank" : undefined}`, and `rel={action.maybeRel ?? (action.external ? "noopener noreferrer" : undefined)}`. If `props.actions.length === 0`, render exact heading text `No collaboration paths yet` and exact body text `When reviewed project, source, live, or writing links are available for this theme, they will appear here.` inside the same panel without `empty-state` or nested card classes. Do not add `OpenLinks profile`, route metadata, JSON-LD, sitemap, search/filtering, runtime fetches, new CSS, new UI dependencies, or a primary OpenLinks CTA.</action>
  <acceptance_criteria>
    - `rg -n "collaborationActionsForTheme" 'src/routes/themes/[slug].tsx'` finds the import and call.
    - `rg -n "ThemeCollaborationPanel" 'src/routes/themes/[slug].tsx'` finds the component and usage.
    - `rg -n "Collaboration starting points" 'src/routes/themes/[slug].tsx'` finds the panel heading.
    - `rg -n "Where to start" 'src/routes/themes/[slug].tsx'` finds the collaboration angle label.
    - `rg -n "No collaboration paths yet" 'src/routes/themes/[slug].tsx'` finds the empty-state heading.
    - `rg -n "target=\\{action\\.external \\? \\\"_blank\\\" : undefined\\}" 'src/routes/themes/[slug].tsx'` finds the external target binding.
    - `rg -n "rel=\\{action\\.maybeRel \\?\\? \\(action\\.external \\? \\\"noopener noreferrer\\\" : undefined\\)\\}" 'src/routes/themes/[slug].tsx'` finds the rel fallback binding.
    - `! rg -n "OpenLinks profile|metadataForTheme|jsonLd|sitemap|fetch\\(|Search|Filter" 'src/routes/themes/[slug].tsx'` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Public theme detail pages render a compact helper-derived collaboration panel before related work panels, with rel-safe external anchors and no generic OpenLinks CTA.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove theme collaboration rendering and fallback safety</name>
  <files>tests/theme-detail-route.test.tsx</files>
  <read_first>
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
    - src/domain/themes.ts
    - src/routes/themes/[slug].tsx
    - tests/theme-detail-route.test.tsx
  </read_first>
  <behavior>
    - Public `agentic-engineering` theme SSR contains `Collaboration starting points`, `Where to start`, that theme's `collaborationAngle`, `Project story`, `Source`, `Live surface`, `Read note`, `/projects/opencode-cloud`, `https://github.com/pRizz/opencode-cloud`, and `/writing/agentic-engineering-workflows`.
    - Public `agentic-engineering` theme SSR does not contain `OpenLinks profile`.
    - Unknown theme fallback SSR does not contain collaboration panel text or action labels.
  </behavior>
  <action>Update `tests/theme-detail-route.test.tsx`. Rename or add a test named `renders collaboration starting points on public theme detail routes`. In that test, keep `mockedSlug = "agentic-engineering"`, render `ThemeDetail()`, and assert the exact strings and hrefs listed in the behavior block. Add `expect(html).not.toContain("OpenLinks profile")` for the public agentic theme route. In the existing fallback test's `forbiddenFallbackText` array, add exact forbidden strings `Collaboration starting points`, `Where to start`, `Project story`, `Source`, `Live surface`, `Contact path`, and `No collaboration paths yet`. Do not add browser tests, metadata assertions, sitemap assertions, or release-readiness assertions in this plan.</action>
  <acceptance_criteria>
    - `rg -n "renders collaboration starting points on public theme detail routes" tests/theme-detail-route.test.tsx` finds the test name.
    - `rg -n "Collaboration starting points" tests/theme-detail-route.test.tsx` finds public and fallback assertions.
    - `rg -n "Where to start" tests/theme-detail-route.test.tsx` finds public and fallback assertions.
    - `rg -n "https://github.com/pRizz/opencode-cloud" tests/theme-detail-route.test.tsx` finds the reviewed source assertion.
    - `rg -n "href=\\\"/projects/opencode-cloud\\\"" tests/theme-detail-route.test.tsx` finds the project story assertion.
    - `rg -n "href=\\\"/writing/agentic-engineering-workflows\\\"" tests/theme-detail-route.test.tsx` finds the writing assertion.
    - `rg -n "not\\.toContain\\(\\\"OpenLinks profile\\\"\\)" tests/theme-detail-route.test.tsx` finds the OpenLinks posture assertion.
  </acceptance_criteria>
  <verify>
    <automated>bun run test tests/theme-detail-route.test.tsx && bun run typecheck</automated>
  </verify>
  <done>SSR route tests prove the collaboration panel appears on public theme detail routes and remains absent from unknown fallback output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| route param -> theme lookup | Untrusted slug text crosses into public route rendering. |
| domain action data -> rendered anchors | Helper-derived links become visitor-clickable anchors. |
| OpenLinks identity surface -> theme collaboration UI | Existing identity placement must not become the generic theme CTA. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-02-01 | Information Disclosure | `ThemeDetail` fallback | mitigate | Continue gating with `maybePublicThemeEntryBySlug(params.slug ?? "")`; fallback test forbids theme slugs, status labels, and collaboration text. |
| T-21-02-02 | Spoofing/Tampering | `ThemeCollaborationPanel` anchors | mitigate | Bind `target="_blank"` only for `action.external` and bind `rel` from `action.maybeRel` or `"noopener noreferrer"` for external actions. |
| T-21-02-03 | Information Disclosure | Theme route source | mitigate | Acceptance blocks `fetch(`, metadata helpers, JSON-LD, sitemap, search/filtering, and generic OpenLinks profile copy in the route file. |
| T-21-02-04 | Repudiation | Collaboration destination labels | mitigate | Render exact destination-specific labels from the domain helper; route test asserts `Project story`, `Source`, `Live surface`, and `Read note`. |
</threat_model>

<verification>
Run `bun run test tests/theme-detail-route.test.tsx`, `bun run typecheck`, and the acceptance `rg` checks. Existing `bun run verify:browser` is not modified here; generated static verifier coverage is owned by Plan 21-04.
</verification>

<success_criteria>
- Theme detail pages show `Collaboration starting points` with `Where to start`, the theme collaboration angle, project story links, reviewed source/live links, and related writing links.
- Unknown theme fallback has no collaboration panel, no action labels, and no non-public theme leakage.
- OpenLinks remains absent as a generic theme CTA; any OpenLinks URL can only appear through existing related project data in the `open-identity` path.
</success_criteria>

<output>
After completion, create `.planning/phases/21-collaboration-pathways-and-cross-links/21-02-SUMMARY.md`.
</output>
