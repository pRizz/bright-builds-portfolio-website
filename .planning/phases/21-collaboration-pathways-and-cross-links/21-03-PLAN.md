---
phase: 21-collaboration-pathways-and-cross-links
plan: 03
type: execute
wave: 2
depends_on:
  - 21-01
files_modified:
  - src/routes/projects/[slug].tsx
  - src/routes/writing/[slug].tsx
  - tests/collaboration-route-links.test.tsx
autonomous: true
requirements:
  - SYNTH-03
  - COLLAB-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 21-2026-06-17T22-54-40
generated_at: 2026-06-17T23:11:34Z
must_haves:
  truths:
    - "Selected project detail pages show related theme paths only when public themes reference that project."
    - "Public writing detail pages show related theme paths only when public themes reference that writing entry."
    - "Project and writing narratives remain primary; related theme links are secondary panels or sections."
    - "Related theme links use stable `themeDetailPath()` hrefs and `Explore theme` labels."
  artifacts:
    - path: "src/routes/projects/[slug].tsx"
      provides: "Selected project detail related-theme panel"
      contains: "Related theme paths"
    - path: "src/routes/writing/[slug].tsx"
      provides: "Public writing detail related-theme section"
      contains: "Related theme paths"
    - path: "tests/collaboration-route-links.test.tsx"
      provides: "SSR route tests for reciprocal project/writing theme links"
      contains: "renders related theme links on a selected project detail route"
  key_links:
    - from: "src/routes/projects/[slug].tsx"
      to: "src/domain/themes.ts"
      via: "publicThemeEntriesForProject(project) and themeDetailPath(theme)"
      pattern: "publicThemeEntriesForProject"
    - from: "src/routes/writing/[slug].tsx"
      to: "src/domain/themes.ts"
      via: "publicThemeEntriesForWritingEntry(entry) and themeDetailPath(theme)"
      pattern: "publicThemeEntriesForWritingEntry"
---

<objective>
Add reciprocal theme cross-links to selected project and public writing detail pages.

Purpose: Let visitors move from project and writing surfaces back into clarifying theme paths per SYNTH-03 and D-08 through D-10 without adding broad index cards or navigation links.
Output: Secondary route panels and SSR route tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
@.planning/phases/20-theme-routes-and-dark-ui/20-02-SUMMARY.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-01-SUMMARY.md
@src/routes/projects/[slug].tsx
@src/routes/writing/[slug].tsx
@src/domain/themes.ts
@src/styles/app.css

<interfaces>
From Plan 21-01, `src/domain/themes.ts` must export:
```ts
export function publicThemeEntriesForProject(
  project: Pick<ProjectDetailPageProject, "slug">,
  themes?: readonly ThemeRecord[],
): readonly PublicThemeEntry[];

export function publicThemeEntriesForWritingEntry(
  entry: Pick<PublicWritingEntry, "slug">,
  themes?: readonly ThemeRecord[],
): readonly PublicThemeEntry[];

export function themeDetailPath(theme: Pick<ThemeRecord, "slug">): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add related theme links to selected project detail pages</name>
  <files>src/routes/projects/[slug].tsx</files>
  <read_first>
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-01-SUMMARY.md
    - src/domain/themes.ts
    - src/routes/projects/[slug].tsx
    - src/styles/app.css
  </read_first>
  <action>Import `publicThemeEntriesForProject`, `themeDetailPath`, and `type PublicThemeEntry` from `../../domain/themes`. Inside the selected project `<aside>`, render `<RelatedThemesPanel project={selectedProject()} />` after `<RelatedWritingPanel project={selectedProject()} />`. Add `RelatedThemesPanel(props: { project: ProjectDetailPageProject })` using `const relatedThemes = () => publicThemeEntriesForProject(props.project);`. Render nothing when `relatedThemes().length === 0`. When non-empty, render `<section class="project-detail-panel visual-surface" aria-labelledby="project-related-theme-paths">`, `<h2 id="project-related-theme-paths" class="card-title">Related theme paths</h2>`, `<div class="writing-related-grid">`, and one `<article class="surface-card">` per theme. Each theme item must include `<h3 class="card-title">{theme.title}</h3>`, `<p class="card-copy">{theme.summary}</p>`, and an anchor `<a class="text-link surface-link" href={themeDetailPath(theme)}>Explore theme</a>`. Do not add related theme links to `src/routes/projects/index.tsx`, home cards, navigation, footer, metadata, JSON-LD, or sitemap code.</action>
  <acceptance_criteria>
    - `rg -n "publicThemeEntriesForProject" 'src/routes/projects/[slug].tsx'` finds the import and helper use.
    - `rg -n "RelatedThemesPanel" 'src/routes/projects/[slug].tsx'` finds the component and usage.
    - `rg -n "project-related-theme-paths" 'src/routes/projects/[slug].tsx'` finds the section heading id.
    - `rg -n "Related theme paths" 'src/routes/projects/[slug].tsx'` finds the exact heading text.
    - `rg -n "Explore theme" 'src/routes/projects/[slug].tsx'` finds the exact action label.
    - `rg -n "themeDetailPath\\(theme\\)" 'src/routes/projects/[slug].tsx'` finds stable theme href generation.
    - `! rg -n "publicThemeEntriesForProject|Related theme paths|Explore theme" src/routes/projects/index.tsx src/routes/index.tsx src/components/SiteLayout.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Selected project detail pages conditionally render a secondary related-theme panel through helper-derived public theme entries.</done>
</task>

<task type="auto">
  <name>Task 2: Add related theme links to public writing detail pages</name>
  <files>src/routes/writing/[slug].tsx</files>
  <read_first>
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-UI-SPEC.md
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-01-SUMMARY.md
    - src/domain/themes.ts
    - src/routes/writing/[slug].tsx
    - src/styles/app.css
  </read_first>
  <action>Import `publicThemeEntriesForWritingEntry`, `themeDetailPath`, and `type PublicThemeEntry` from `../../domain/themes`. In `WritingArticle`, add `const relatedThemes = publicThemeEntriesForWritingEntry(entry);` after `const relatedProjects = relatedProjectDetailPageProjects(entry);`. After the existing related projects `<Show>` block, add a second `<Show when={relatedThemes.length > 0}>` block. The section must be `<section class="content-section" aria-labelledby="writing-related-theme-paths">`, with `<h2 id="writing-related-theme-paths" class="section-title">Related theme paths</h2>`, `<div class="writing-related-grid">`, and one `<article class="surface-card">` per theme. Each item must render theme title, theme summary, and `<a class="text-link surface-link" href={themeDetailPath(theme)}>Explore theme</a>`. Do not add related theme links to `src/routes/writing/index.tsx`, broad writing cards, navigation, footer, metadata, JSON-LD, or sitemap code.</action>
  <acceptance_criteria>
    - `rg -n "publicThemeEntriesForWritingEntry" 'src/routes/writing/[slug].tsx'` finds the import and helper use.
    - `rg -n "const relatedThemes = publicThemeEntriesForWritingEntry\\(entry\\)" 'src/routes/writing/[slug].tsx'` finds the related theme derivation.
    - `rg -n "writing-related-theme-paths" 'src/routes/writing/[slug].tsx'` finds the section heading id.
    - `rg -n "Related theme paths" 'src/routes/writing/[slug].tsx'` finds the exact heading text.
    - `rg -n "Explore theme" 'src/routes/writing/[slug].tsx'` finds the exact action label.
    - `rg -n "themeDetailPath\\(theme\\)" 'src/routes/writing/[slug].tsx'` finds stable theme href generation.
    - `! rg -n "publicThemeEntriesForWritingEntry|Related theme paths|Explore theme" src/routes/writing/index.tsx src/routes/index.tsx src/components/SiteLayout.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <done>Public writing detail pages conditionally render a secondary related-theme section after the article body and related project section.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add SSR route tests for reciprocal theme links</name>
  <files>tests/collaboration-route-links.test.tsx</files>
  <read_first>
    - .planning/phases/21-collaboration-pathways-and-cross-links/21-CONTEXT.md
    - src/routes/projects/[slug].tsx
    - src/routes/writing/[slug].tsx
    - src/domain/themes.ts
    - tests/collaboration-route-links.test.tsx (create)
    - tests/theme-detail-route.test.tsx
  </read_first>
  <behavior>
    - Rendering project slug `openlinks` contains `Related theme paths`, `Open identity`, `href="/themes/open-identity"`, and `Explore theme`.
    - Rendering project slug `win3bitcoin` omits `Related theme paths`.
    - Rendering writing slug `portable-identity-and-owned-surfaces` contains `Related projects` before `Related theme paths`, then `Open identity`, `href="/themes/open-identity"`, and `Explore theme`.
  </behavior>
  <action>Create `tests/collaboration-route-links.test.tsx`. Follow the existing `tests/theme-detail-route.test.tsx` pattern: mock `@solidjs/router` with a mutable `mockedSlug`, import `renderToString` from `solid-js/web`, and dynamically import `../src/routes/projects/[slug]` and `../src/routes/writing/[slug]` after the mock. Add tests named `renders related theme links on a selected project detail route`, `omits related theme links for selected projects without public theme references`, and `renders related theme links after related projects on a writing detail route`. Use exact string assertions from the behavior block. For the writing order assertion, compare `html.indexOf("Related projects")` and `html.indexOf("Related theme paths")` and expect the related project index to be greater than or equal to 0 and less than the related theme index. Do not add DOM test dependencies or new package scripts.</action>
  <acceptance_criteria>
    - `test -f tests/collaboration-route-links.test.tsx` succeeds.
    - `rg -n "renders related theme links on a selected project detail route" tests/collaboration-route-links.test.tsx` finds the project test.
    - `rg -n "omits related theme links for selected projects without public theme references" tests/collaboration-route-links.test.tsx` finds the omission test.
    - `rg -n "renders related theme links after related projects on a writing detail route" tests/collaboration-route-links.test.tsx` finds the writing test.
    - `rg -n "href=\\\"/themes/open-identity\\\"" tests/collaboration-route-links.test.tsx` finds the stable theme href assertion.
    - `rg -n "indexOf\\(\\\"Related projects\\\"\\)" tests/collaboration-route-links.test.tsx` finds the placement assertion.
  </acceptance_criteria>
  <verify>
    <automated>bun run test tests/collaboration-route-links.test.tsx && bun run typecheck</automated>
  </verify>
  <done>SSR route tests prove selected project and public writing reciprocal theme links render through stable theme detail paths and omit the project panel when no public theme references exist.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| project/writing route params -> public record lookup | Untrusted slugs select public project and writing surfaces. |
| public theme helpers -> cross-link UI | Helper output controls which theme records appear on project and writing routes. |
| route UI -> content graph navigation | New anchors influence visitor movement through the static content graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-03-01 | Information Disclosure | Project and writing related theme panels | mitigate | Use `publicThemeEntriesForProject()` and `publicThemeEntriesForWritingEntry()` from Plan 21-01; unit tests already prove non-public themes are excluded. |
| T-21-03-02 | Tampering | Theme href generation | mitigate | Generate all reciprocal links through `themeDetailPath(theme)` and assert `href="/themes/open-identity"` in SSR tests. |
| T-21-03-03 | Denial of Service | Route layout | accept | Static pages render small checked-in arrays only; no runtime queries, fetches, or user-controlled loops are introduced. |
| T-21-03-04 | Information Disclosure | Broad route surfaces | mitigate | Acceptance blocks reciprocal theme strings in home, project index, writing index, footer, and navigation files for this phase. |
</threat_model>

<verification>
Run `bun run test tests/collaboration-route-links.test.tsx`, `bun run typecheck`, and the acceptance `rg` checks. Generated static verifier coverage and browser checks are owned by Plan 21-04.
</verification>

<success_criteria>
- Selected project detail pages show `Related theme paths` only when public themes reference the selected project.
- Public writing detail pages show `Related theme paths` after article body and related projects when public themes reference the entry.
- Cross-links use `themeDetailPath()` and exact action label `Explore theme`.
- Broad index cards, navigation, footer, metadata, JSON-LD, sitemap, and release docs remain untouched by this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/21-collaboration-pathways-and-cross-links/21-03-SUMMARY.md`.
</output>
