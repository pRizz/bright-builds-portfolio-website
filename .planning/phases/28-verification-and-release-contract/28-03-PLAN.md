---
phase: 28-verification-and-release-contract
plan: 03
type: execute
wave: 2
depends_on: ["28-02"]
files_modified:
  - docs/release-readiness.md
  - scripts/release-readiness.ts
  - scripts/release-readiness.test.ts
autonomous: true
requirements: [VERIFY-02, VERIFY-04, VERIFY-05]
user_setup: []
must_haves:
  truths:
    - "Release-readiness docs preserve the clean-builder command `bun run install:browser && bun run verify`."
    - "Docs explain social preview generation, read-only social preview verification, static output social image/manifest verification, release budget enforcement, offline freshness reports, and manual social-card smoke checks."
    - "Docs and doc-fact tests state that hosted crawler validation, deployed preview/production smoke checks, current live GitHub state, and live external-link reachability are manual or explicit opt-in activities outside `bun run verify`."
    - "Aggregate verification tests prove `verify:social-previews` runs before `build` and `verify` excludes generation, freshness reports, GitHub sync, hosted smoke, live/network command strings, and browser install."
    - "OpenLinks remains described only as low-intrusion identity/external-link policy context, not as a primary route CTA or brand replacement."
  artifacts:
    - path: "docs/release-readiness.md"
      provides: "Human release contract for generation, verification, freshness, budgets, and manual smoke flow"
      contains: "bun run install:browser && bun run verify"
    - path: "scripts/release-readiness.ts"
      provides: "Required release-readiness document facts and manual checklist labels"
      exports: ["releaseReadinessDocumentFindings", "automatedReleaseReadinessEvidenceLabels", "manualReleaseChecklistLabels"]
    - path: "scripts/release-readiness.test.ts"
      provides: "Doc-fact and aggregate verify order/exclusion tests"
      contains: "verify:social-previews"
  key_links:
    - from: "scripts/release-readiness.ts"
      to: "docs/release-readiness.md"
      via: "requiredReleaseReadinessDocumentFacts regex checks"
      pattern: "requiredReleaseReadinessDocumentFacts"
    - from: "scripts/release-readiness.test.ts"
      to: "package.json"
      via: "aggregate verify script exact order and exclusion assertions"
      pattern: "packageJson\\.scripts\\.verify"
    - from: "docs/release-readiness.md"
      to: "scripts/verify-release.ts"
      via: "documents generated social preview per-image and total budget enforcement"
      pattern: "generated social preview"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 28-2026-06-22T15-45-43
generated_at: 2026-06-22T16:02:52Z
---

<objective>
Document and guard the Phase 28 release contract without overclaiming manual or live checks.

Purpose: Maintainers need one readable release guide and executable doc-fact tests that explain the static social preview workflow, freshness report, release budgets, and manual social-card smoke flow while preserving the local deterministic release boundary.
Output: Updated release-readiness documentation plus release-readiness tests and required document facts that guard the clean-builder command, aggregate verify order, advisory/manual boundaries, and low-intrusion OpenLinks posture.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/28-verification-and-release-contract/28-CONTEXT.md
@.planning/phases/28-verification-and-release-contract/28-RESEARCH.md
@.planning/phases/27-freshness-reports-and-reviewed-snapshot-policy/27-02-SUMMARY.md
@docs/release-readiness.md
@scripts/release-readiness.ts
@scripts/release-readiness.test.ts
@package.json

<interfaces>
From `scripts/release-readiness.ts`:
```ts
export function releaseReadinessDocumentFindings(
  documentPath?: string,
): readonly ReleaseFinding[];

export function automatedReleaseReadinessEvidenceLabels(): readonly string[];
export function manualReleaseChecklistLabels(): readonly string[];
```

From `package.json`:
```json
{
  "scripts": {
    "generate:social-previews": "bun run scripts/generate-social-previews.ts",
    "verify:social-previews": "bun run scripts/generate-social-previews.ts --check",
    "report:freshness": "bun run scripts/generate-freshness-report.ts",
    "verify": "bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run verify:project-helper-surface && bun run verify:visual-system && bun run verify:social-previews && bun run build && bun run verify:browser && bun run verify:static && bun run verify:release"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add social preview release-readiness facts and docs</name>
  <files>docs/release-readiness.md, scripts/release-readiness.ts, scripts/release-readiness.test.ts</files>
  <read_first>
    - `.planning/phases/28-verification-and-release-contract/28-CONTEXT.md`
    - `.planning/phases/28-verification-and-release-contract/28-RESEARCH.md`
    - `AGENTS.bright-builds.md`
    - `/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/SKILL.md`
    - `/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/references/surface-patterns.md`
    - `docs/release-readiness.md`
    - `scripts/release-readiness.ts`
    - `scripts/release-readiness.test.ts`
    - `scripts/social-previews/config.ts`
    - `scripts/verify-release.ts`
  </read_first>
  <behavior>
    - Test 1: `releaseReadinessDocumentFindings("docs/release-readiness.md")` returns `[]` only when docs contain exact command blocks for `bun run generate:social-previews`, `bun run verify:social-previews`, and `bun run install:browser && bun run verify` per D-01, D-18, and D-19.
    - Test 2: Removing `public/social/generated/manifest.json`, `.output/public/social/generated/manifest.json`, `generated social preview PNG total`, or `Manual social-card smoke check` from a fixture doc produces a `release-readiness document` finding per D-10, D-14, D-18, and D-20.
    - Test 3: Removing the sentence that hosted social-card validation, current live GitHub state, live external-link reachability, and preview/production smoke checks are outside `bun run verify` produces a `release-readiness document` finding per D-03, D-15, D-16, D-17, and D-20.
    - Test 4: Removing the low-intrusion OpenLinks sentence produces a `release-readiness document` finding, and the replacement text must keep OpenLinks as identity/external-link context rather than a primary release CTA per D-21.
  </behavior>
  <action>Update the release-readiness document and its executable fact checks per D-18 through D-21. In `docs/release-readiness.md`, add a `## Social Preview Assets` section after `## Static Output` and before `## Freshness Reports`. Include exact fenced command blocks for `bun run generate:social-previews` and `bun run verify:social-previews`. State that `generate:social-previews` writes reviewed checked-in PNGs and `public/social/generated/manifest.json`, while `verify:social-previews` is read-only check mode and runs before `bun run build` inside `bun run verify` per D-01 and D-02. State that `verify:static` checks generated HTML social image metadata, Twitter image parity, JSON-LD image parity, local PNG existence, dimensions, and `.output/public/social/generated/manifest.json` consistency per D-10. State that `verify:release` enforces the 250 KiB per-image budget and the 1 MiB generated social preview PNG total budget, and include the exact phrase `generated social preview PNG total` per D-14. Add a `Manual social-card smoke check` subsection explaining that after preview or production deployment, maintainers manually inspect social cards for representative project, writing, and theme URLs; hosted social-card validation, current live GitHub state, live external-link reachability, and preview/production smoke checks are not part of `bun run verify` per D-03, D-17, and D-20. Preserve freshness report severity language for `release blocker`, `needs review`, and `manual smoke`, and state that only deterministic local blockers fail local release gates per D-16. Preserve the existing `bun run install:browser && bun run verify` clean-builder command per D-19. Preserve low-intrusion OpenLinks posture with a sentence that OpenLinks remains identity/external-link policy context and is not a primary route CTA or brand replacement per D-21. In `scripts/release-readiness.ts`, add required document facts for all exact strings above. In `scripts/release-readiness.test.ts`, add fixture-removal tests for the new facts using Arrange/Act/Assert comments. Do not add OpenLinks promotion, dynamic OG endpoints, server functions, hosted crawler automation, live external-link release gates, GitHub sync automation, or new dependencies.</action>
  <verify>
    <automated>bun run test scripts/release-readiness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `docs/release-readiness.md` contains `## Social Preview Assets`
    - `docs/release-readiness.md` contains a fenced command block with `bun run generate:social-previews`
    - `docs/release-readiness.md` contains a fenced command block with `bun run verify:social-previews`
    - `docs/release-readiness.md` contains `public/social/generated/manifest.json`
    - `docs/release-readiness.md` contains `.output/public/social/generated/manifest.json`
    - `docs/release-readiness.md` contains `generated social preview PNG total`
    - `docs/release-readiness.md` contains `Manual social-card smoke check`
    - `docs/release-readiness.md` contains `not part of \`bun run verify\``
    - `docs/release-readiness.md` contains `not a primary route CTA or brand replacement`
    - `scripts/release-readiness.ts` contains `social preview generation command`
    - `scripts/release-readiness.ts` contains `generated preview manifest`
    - `scripts/release-readiness.ts` contains `manual social-card smoke check`
    - `scripts/release-readiness.test.ts` contains `reports missing social preview generation guidance`
    - `scripts/release-readiness.test.ts` contains `reports missing manual social-card smoke guidance`
    - `bun run test scripts/release-readiness.test.ts` exits 0
  </acceptance_criteria>
  <done>Release docs and doc-fact tests explain social preview generation/check/static/budget/manual-smoke flows while preserving the clean-builder command and low-intrusion OpenLinks posture.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Strengthen aggregate verify order and boundary guards</name>
  <files>scripts/release-readiness.test.ts</files>
  <read_first>
    - `.planning/phases/28-verification-and-release-contract/28-CONTEXT.md`
    - `package.json`
    - `scripts/release-readiness.test.ts`
    - `scripts/verify-no-github-runtime.ts`
    - `scripts/generate-freshness-report.ts`
    - `scripts/sync-github-metadata.ts`
  </read_first>
  <behavior>
    - Test 1: The aggregate script exactly remains `bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run verify:project-helper-surface && bun run verify:visual-system && bun run verify:social-previews && bun run build && bun run verify:browser && bun run verify:static && bun run verify:release` per D-01.
    - Test 2: `verifyScript.indexOf("bun run verify:social-previews")` is less than `verifyScript.indexOf("bun run build")` per D-02.
    - Test 3: `verifyScript` contains `bun run verify:no-github-runtime` and excludes `bun run generate:social-previews`, `bun run generate:static-metadata`, `bun run install:browser`, `report:freshness`, `freshness:live`, `smoke:hosted`, `sync:github-metadata`, `scripts/sync-github-metadata.ts`, `https://`, `http://`, `api.github.com`, `github.com/graphql`, `curl`, `wget`, and `fetch` per D-03, D-04, and D-20.
  </behavior>
  <action>Strengthen the existing aggregate release script contract test without changing `package.json`. In `scripts/release-readiness.test.ts`, keep the exact `expectedVerifyScript` assertion and add index-order assertions that `bun run verify:social-previews` appears before `bun run build`, `bun run build` appears before `bun run verify:browser`, `bun run verify:static` appears before `bun run verify:release`, and `bun run verify:release` is the final segment. Add explicit negative assertions for `curl`, `wget`, `fetch`, `http://`, and the existing live/hosted/sync/network strings. Keep `bun run install:browser` excluded from `verify` while docs preserve `bun run install:browser && bun run verify` as the clean-builder sequence per D-01 and D-19. Do not edit `package.json`; this task guards the existing contract rather than replacing it.</action>
  <verify>
    <automated>bun run test scripts/release-readiness.test.ts && bun run test scripts/verify-release.test.ts scripts/release-readiness.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/release-readiness.test.ts` contains `indexOf("bun run verify:social-previews")`
    - `scripts/release-readiness.test.ts` contains `indexOf("bun run build")`
    - `scripts/release-readiness.test.ts` contains `bun run verify:release`
    - `scripts/release-readiness.test.ts` contains `curl`
    - `scripts/release-readiness.test.ts` contains `wget`
    - `scripts/release-readiness.test.ts` contains `fetch`
    - `scripts/release-readiness.test.ts` contains `api.github.com`
    - `scripts/release-readiness.test.ts` contains `github.com/graphql`
    - `package.json` verify script remains byte-for-byte equal to the expected string in this task
    - `bun run test scripts/release-readiness.test.ts && bun run test scripts/verify-release.test.ts scripts/release-readiness.test.ts` exits 0
  </acceptance_criteria>
  <done>Aggregate release script tests prove deterministic social preview verification runs before build and that the release gate remains local, deterministic, and free of live/networked release checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs -> maintainer action | Release docs influence manual and automated release decisions and must not overclaim what local commands prove. |
| package script -> release gate | `package.json` script ordering determines the real local gate and must be guarded against hidden live, generation, or sync steps. |
| OpenLinks identity guidance -> release docs | Identity links must remain low-intrusion and not become primary brand or CTA guidance. |
| manual smoke checklist -> automated evidence | Manual social-card, deployment, hosted crawler, live GitHub, and live external-link checks must stay visibly separate from automated labels. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-28-03-01 | R | `docs/release-readiness.md` | mitigate | Required document facts and fixture-removal tests enforce exact local commands, manual boundaries, and clean-builder sequence. |
| T-28-03-02 | S | `scripts/release-readiness.test.ts` aggregate script guard | mitigate | Exact verify-string and order assertions prevent a different command from masquerading as the clean-builder release gate. |
| T-28-03-03 | I | release docs and labels | mitigate | Docs state hosted validators, current live GitHub state, live external-link reachability, and preview/production smoke checks are outside `bun run verify`. |
| T-28-03-04 | T | OpenLinks documentation posture | mitigate | Doc facts preserve OpenLinks as identity/external-link policy context and reject primary route CTA or brand-replacement wording. |
</threat_model>

<verification>
Run these after all tasks in this plan:

```bash
bun run test scripts/release-readiness.test.ts
bun run test scripts/verify-release.test.ts scripts/release-readiness.test.ts
bun run build
bun run verify:release
```
</verification>

<success_criteria>
This plan is complete when release-readiness docs and tests preserve `bun run install:browser && bun run verify`, explain generation/check/freshness/manual social-card smoke flows, guard aggregate verify ordering, and keep hosted/live/manual evidence outside automated local release claims.
</success_criteria>

<output>
After completion, create `.planning/phases/28-verification-and-release-contract/28-03-SUMMARY.md`
</output>
