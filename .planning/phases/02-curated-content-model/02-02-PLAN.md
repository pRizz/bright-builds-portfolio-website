---
phase: 02-curated-content-model
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified: [scripts/verify-curation.ts, scripts/verify-no-github-runtime.ts, scripts/verify-static.ts, package.json]
autonomous: true
requirements: [CUR-02, CUR-05, GH-01]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 2-2026-05-25T23-28-02
generated_at: 2026-05-25T23:52:23Z
must_haves:
  truths:
    - "bun run verify fails before build when curated registry hard errors exist."
    - "bun run verify fails before build when src contains visitor-path GitHub API, Octokit, or browser-exposed GitHub token mechanisms."
    - "Static verification proves generated HTML contains curated registry/profile content before hydration."
    - "Normal github.com repository links remain allowed."
  artifacts:
    - path: scripts/verify-curation.ts
      provides: "Bun verification shell for curation hard errors and warnings"
      contains: "validateProjectRegistry"
    - path: scripts/verify-no-github-runtime.ts
      provides: "Bun source guard for forbidden visitor-runtime GitHub mechanisms"
      contains: "api.github.com"
    - path: scripts/verify-static.ts
      provides: "Static HTML proof for route text plus curated/profile content"
      contains: "homeProjects"
    - path: package.json
      provides: "verify:curation, verify:no-github-runtime, and aggregate verify wiring"
      contains: "verify:no-github-runtime"
  key_links:
    - from: package.json
      to: scripts/verify-curation.ts
      via: "verify:curation script"
      pattern: "verify:curation"
    - from: package.json
      to: scripts/verify-no-github-runtime.ts
      via: "verify:no-github-runtime script"
      pattern: "verify:no-github-runtime"
    - from: scripts/verify-static.ts
      to: src/domain/projects.ts
      via: "homeProjects and visibleProjects imports"
      pattern: "homeProjects|visibleProjects"
---

<objective>
Wire curation and no-runtime-GitHub guarantees into repo verification and static output proof.

Purpose: Make Phase 2 enforceable from Bun scripts so curated content remains static, reviewed, and independent from live browser/runtime GitHub API calls.
Output: Curation verifier, GitHub runtime guard, package script wiring, and stronger static HTML verification.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-curated-content-model/02-CONTEXT.md
@.planning/phases/02-curated-content-model/02-RESEARCH.md
@.planning/phases/02-curated-content-model/02-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/projects.ts
@src/domain/project-validation.ts
@src/domain/profile.ts
@scripts/verify-static.ts
@package.json

<interfaces>
Expected exports from Plan 01:
```typescript
export declare const curatedProjects: readonly ProjectStory[];
export declare function homeProjects(projects?: readonly ProjectStory[]): readonly HomeProjectStory[];
export declare function visibleProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export declare function validateProjectRegistry(projects: readonly ProjectStory[]): {
  errors: readonly CurationIssue[];
  warnings: readonly CurationIssue[];
};
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add curation and no-runtime-GitHub verification scripts</name>
  <files>scripts/verify-curation.ts, scripts/verify-no-github-runtime.ts, package.json</files>
  <read_first>package.json, scripts/verify-static.ts, src/domain/projects.ts, src/domain/project-validation.ts, .planning/phases/02-curated-content-model/02-CONTEXT.md, .planning/phases/02-curated-content-model/02-RESEARCH.md</read_first>
  <action>Create scripts/verify-curation.ts that imports curatedProjects from ../src/domain/projects and validateProjectRegistry from ../src/domain/project-validation, prints each warning as `[curation warning] {slug}: {code} - {message}`, prints each error as `[curation error] {slug}: {code} - {message}`, exits with code 1 when errors.length > 0, and prints `Curated registry valid: {projectCount} projects, {warningCount} warnings.` on success. Create scripts/verify-no-github-runtime.ts using node:fs and node:path only; recursively scan `src/` TypeScript and TSX files, allow normal `https://github.com/...` links, and fail on these exact forbidden mechanisms per D-16 and GH-01: `api.github.com`, `github.com/graphql`, `api.github.com/graphql`, imports from `@octokit/`, dynamic imports of `@octokit/`, `GITHUB_TOKEN`, `VITE_*GITHUB*TOKEN`, `PUBLIC_*GITHUB*TOKEN`, and `SOLID_PUBLIC_*GITHUB*TOKEN`. Update package.json scripts to add `"verify:curation": "bun run scripts/verify-curation.ts"` and `"verify:no-github-runtime": "bun run scripts/verify-no-github-runtime.ts"`. Update `"verify"` to run `bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run build && bun run verify:static`. Do not add Zod, Python, Playwright, @octokit/*, optional GitHub sync, or generated snapshots per D-10, D-15, D-16, D-18, and GH-01.</action>
  <verify>
    <automated>bun run verify:curation && bun run verify:no-github-runtime && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg '"verify:curation": "bun run scripts/verify-curation.ts"' package.json` finds the curation script entry.
    - `rg '"verify:no-github-runtime": "bun run scripts/verify-no-github-runtime.ts"' package.json` finds the GitHub runtime guard entry.
    - `rg 'verify:curation && bun run verify:no-github-runtime && bun run build && bun run verify:static' package.json` proves both new guards run before build/static verification.
    - `rg 'validateProjectRegistry' scripts/verify-curation.ts` finds the pure validator import/use.
    - `rg 'api.github.com|@octokit|GITHUB_TOKEN' scripts/verify-no-github-runtime.ts` finds the forbidden-pattern definitions.
    - `bun run verify:curation && bun run verify:no-github-runtime && bun run typecheck` passes.
  </acceptance_criteria>
  <done>Repo verification fails on curation hard errors or forbidden visitor-runtime GitHub mechanisms before production build.</done>
</task>

<task type="auto">
  <name>Task 2: Extend static HTML verification for curated content</name>
  <files>scripts/verify-static.ts, package.json</files>
  <read_first>scripts/verify-static.ts, package.json, src/domain/projects.ts, src/domain/profile.ts, src/domain/routes.ts, src/routes/index.tsx, src/routes/projects.tsx</read_first>
  <action>Extend scripts/verify-static.ts so StaticRouteCheck supports `expectedTexts: readonly string[]` and `forbiddenTextPatterns?: readonly RegExp[]`. Keep the existing route HTML discovery behavior. For every route, keep asserting routeByPath(route).staticCheckText. For `/`, also assert peterProfile.name, peterProfile.handle, every homeProjects() project.name, and every homeProjects() project.oneLine. For `/projects`, assert every visibleProjects() project.name and every visibleProjects() project.oneLine. For all generated HTML files, assert exact stale or invented standalone repo hrefs are absent for `https://github.com/pRizz/openlinks`, `https://github.com/pRizz/win3bitcoin`, and `https://github.com/pRizz/open-bitcoin`; the Open Bitcoin check must not reject `https://github.com/pRizz/open-bitcoin-web-miner`. Keep the script filesystem-only; do not add a browser suite, network calls, Playwright, Python, Zod, Octokit, or GitHub sync code in this phase per D-15, D-17, D-18, and GH-01.</action>
  <verify>
    <automated>bun run build && bun run verify:static && bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg 'expectedTexts' scripts/verify-static.ts` finds multi-text static checks.
    - `rg 'forbiddenTextPatterns' scripts/verify-static.ts` finds exact stale-link pattern checks.
    - `rg 'homeProjects' scripts/verify-static.ts` finds home curated content checks.
    - `rg 'visibleProjects' scripts/verify-static.ts` finds project index curated content checks.
    - `rg 'pRizz/openlinks|pRizz/win3bitcoin|pRizz/open-bitcoin' scripts/verify-static.ts` finds forbidden stale repo assertions.
    - `bun run build && bun run verify:static` passes.
    - `bun run verify` passes.
  </acceptance_criteria>
  <done>Static verification proves route shell text, curated project/profile text, and stale-link absence from generated HTML.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| src/ source -> visitor bundle | Source code can introduce runtime GitHub API calls or token names that reach the browser. |
| checked-in registry -> build verification | Curated content quality must fail before build when hard errors exist. |
| generated HTML -> visitor | Visitors receive prerendered content before hydration. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-05 | Information Disclosure | scripts/verify-no-github-runtime.ts | mitigate | Fail verification on GITHUB_TOKEN, VITE_*GITHUB*TOKEN, PUBLIC_*GITHUB*TOKEN, and SOLID_PUBLIC_*GITHUB*TOKEN in src/. |
| T-02-06 | Denial of Service | src/ visitor paths | mitigate | Fail verification on api.github.com, github.com/graphql, api.github.com/graphql, and @octokit/* imports in src/. |
| T-02-07 | Tampering | scripts/verify-curation.ts | mitigate | Run validateProjectRegistry before build so hard curation errors block verify. |
| T-02-08 | Repudiation | scripts/verify-static.ts | mitigate | Static verifier prints deterministic failure messages naming the route/file and missing curated text. |
| T-02-09 | Spoofing | generated HTML | mitigate | Static verifier rejects stale or invented repo URLs for OpenLinks, Win3Bitco.in, and Open Bitcoin. |
</threat_model>

<verification>
Overall checks for this plan:
- [ ] bun run verify:curation
- [ ] bun run verify:no-github-runtime
- [ ] bun run build
- [ ] bun run verify:static
- [ ] bun run verify
- [ ] rg checks from task acceptance criteria pass
</verification>

<success_criteria>
- CUR-02: The aggregate verify command fails on curation hard errors.
- CUR-05: Static verification proves named curated project-story content appears in generated HTML.
- GH-01: Source and static verification prove complete portfolio content does not require visitor-runtime GitHub API calls.
</success_criteria>

<output>
After completion, create `.planning/phases/02-curated-content-model/02-02-SUMMARY.md`.
</output>
