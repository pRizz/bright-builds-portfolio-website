---
phase: 08-content-helper-surface-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/projects.ts
  - src/domain/foundation.test.ts
  - scripts/verify-project-helper-surface.ts
  - scripts/project-helper-surface.test.ts
  - package.json
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 8-2026-05-31T23-03-14
generated_at: "2026-05-31T23:18:12Z"
must_haves:
  truths:
    - "Maintainer can identify the supported curated project selector surface without reverse-engineering seed-era aliases."
    - "Runtime-facing project helper exports no longer expose projectSeeds, primaryProjectLink, or featuredProjects."
    - "Runtime portfolio and build-time verification source fails checked verification if it imports orphaned seed-era helper exports from src/domain/projects."
    - "Existing curated project ordering, filtering, anchor, label, and route behavior remains unchanged."
  artifacts:
    - path: "src/domain/projects.ts"
      provides: "Authoritative curatedProjects registry, documented supported selector surface, and no legacy helper exports"
      contains: "Maintainer-facing project data surface"
    - path: "src/domain/foundation.test.ts"
      provides: "Selector behavior and export-surface regression coverage"
      contains: "seed-era helper aliases"
    - path: "scripts/verify-project-helper-surface.ts"
      provides: "Dependency-free TypeScript import guard with pure scanner functions and CLI shell"
      exports:
        - "projectHelperSurfaceFindingsForSource"
        - "projectHelperSurfaceFindingsForFiles"
        - "runProjectHelperSurfaceVerification"
    - path: "scripts/project-helper-surface.test.ts"
      provides: "Import guard unit coverage for allowed selectors and forbidden legacy imports"
    - path: "package.json"
      provides: "verify:project-helper-surface script wired into bun run verify"
  key_links:
    - from: "package.json"
      to: "scripts/verify-project-helper-surface.ts"
      via: "verify:project-helper-surface script"
      pattern: "\"verify:project-helper-surface\": \"bun run scripts/verify-project-helper-surface.ts\""
    - from: "scripts/verify-project-helper-surface.ts"
      to: "src/domain/projects.ts"
      via: "TypeScript AST import detection"
      pattern: "ts\\.createSourceFile"
    - from: "src/routes/index.tsx"
      to: "src/domain/projects.ts"
      via: "named imports of supported selectors only"
      pattern: "homeProjects|currentFocusProjects|projectAnchorHref|projectLinkDisplayLabel"
    - from: "src/routes/projects.tsx"
      to: "src/domain/projects.ts"
      via: "named imports of curatedProjects and supported selectors only"
      pattern: "publicProjectIndexProjects|projectsByPlacement|hiddenExcludedProjects|writingProjects"
---

<objective>
Make the curated project helper surface intentional.

Purpose: Phase 8 removes seed-era helper ambiguity, documents the supported project selector API, and adds deterministic verification so runtime portfolio surfaces cannot drift back to legacy helper exports.

Output: One updated project data module, focused Vitest coverage, one dependency-free Bun/TypeScript import guard, and `package.json` verification wiring.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-content-helper-surface-cleanup/08-CONTEXT.md
@.planning/phases/08-content-helper-surface-cleanup/08-RESEARCH.md
@.planning/phases/07-release-gates-deploy-readiness/07-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/projects.ts
@src/domain/foundation.test.ts
@src/domain/portfolio-surfaces.test.ts
@src/routes/index.tsx
@src/routes/projects.tsx
@scripts/verify-no-github-runtime.ts
@scripts/verify-release.ts
@scripts/verify-release.test.ts
@package.json
@tsconfig.json

<interfaces>
Key existing contracts the executor must preserve:

From `src/domain/projects.ts`:
```ts
export type ProjectPlacement = "home" | "supporting" | "lab" | "archive" | "hidden";
export type ProjectTier = "flagship" | "supporting" | "lab" | "archive" | "excluded";
export type ProjectLinkKind = "repo" | "live" | "docs" | "article" | "related";

export type ProjectLink = {
  label: string;
  href: string;
  kind: ProjectLinkKind;
};

export type ProjectStory = {
  slug: string;
  name: string;
  placement: ProjectPlacement;
  tier: ProjectTier;
  includeOnHome: boolean;
  includeInProjectIndex: boolean;
  displayOrder: number;
  links: readonly [ProjectLink, ...ProjectLink[]];
};

export const curatedProjects: readonly ProjectStory[];
export function homeProjects(projects?: readonly ProjectStory[]): readonly HomeProjectStory[];
export function visibleProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function publicProjectIndexProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function hiddenExcludedProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function currentFocusProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function projectsByPlacement(
  placement: ProjectPlacement,
  projects?: readonly ProjectStory[],
): readonly ProjectStory[];
export function writingProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function projectAnchorHref(project: ProjectStory): string;
export function projectLinkDisplayLabel(link: ProjectLink): string;
```

Forbidden legacy names for this phase: `projectSeeds`, `primaryProjectLink`, `featuredProjects`.
</interfaces>
</context>

<decision_coverage>
| Decision | Plan Task | Coverage |
| --- | --- | --- |
| D-01 | Task 1 | Full: documents `curatedProjects` plus supported selectors as the maintainer-facing API. |
| D-02 | Task 1 | Full: removes `projectSeeds` and removes `featuredProjects` because it is only a compatibility alias. |
| D-03 | Task 1 | Full: removes `primaryProjectLink` because there is no current source consumer; no replacement selector is needed. |
| D-04 | Task 2 | Full: import guard fails runtime source imports of legacy names from `src/domain/projects`. |
| D-05 | Task 2 | Full: guard includes non-test `scripts/**/*.ts` and allows only authoritative registry or supported selectors. |
| D-06 | Task 2, Task 3 | Full: repo-owned Bun/TypeScript verifier, no new dependency, wired into `bun run verify`. |
| D-07 | Task 1 | Full: selector API documentation lives in `src/domain/projects.ts` beside the registry and selectors. |
| D-08 | Task 1, Task 2 | Full: Vitest coverage for selector/export behavior and import-guard behavior. |
| D-09 | Task 1, Task 3 | Full: no route/content expansion; verification proves visitor-facing project behavior remains unchanged. |
</decision_coverage>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Remove legacy helper exports and document supported selectors</name>
  <files>src/domain/projects.ts, src/domain/foundation.test.ts</files>
  <read_first>
    .planning/phases/08-content-helper-surface-cleanup/08-CONTEXT.md
    .planning/phases/08-content-helper-surface-cleanup/08-RESEARCH.md
    src/domain/projects.ts
    src/domain/foundation.test.ts
    src/domain/portfolio-surfaces.test.ts
    src/routes/index.tsx
    src/routes/projects.tsx
  </read_first>
  <behavior>
    - Test 1: `homeProjects()` still returns `openlinks`, `free-the-world`, `win3bitcoin`, `opencode-cloud`, `zeckendorf`, `mystic-ui` in display order.
    - Test 2: the module export surface includes `curatedProjects`, `homeProjects`, `visibleProjects`, `publicProjectIndexProjects`, `hiddenExcludedProjects`, `currentFocusProjects`, `projectsByPlacement`, `writingProjects`, `projectAnchorHref`, and `projectLinkDisplayLabel`.
    - Test 3: the module export surface does not include `projectSeeds`, `primaryProjectLink`, or `featuredProjects`.
  </behavior>
  <action>
    Update `src/domain/projects.ts` per D-01, D-02, D-03, D-07, and D-09. Add a concise TSDoc block near `curatedProjects` named "Maintainer-facing project data surface" that states `curatedProjects` is the authoritative checked-in registry and names the supported selector exports exactly as listed in D-01. Remove the `projectSeeds`, `primaryProjectLink`, and `featuredProjects` exports entirely; do not add a replacement for `primaryProjectLink` because current source has no consumer. Do not change project records, display order, placement, visibility flags, link values, `currentFocusProjectSlugs`, selector predicates, anchor generation, or link label behavior.

    Update `src/domain/foundation.test.ts` per D-08. Remove the `featuredProjects` import and compatibility test. Add a focused export-surface test, using `import * as projectSurface from "./projects"`, that asserts supported selector names exist and the legacy names `projectSeeds`, `primaryProjectLink`, and `featuredProjects` are absent. Keep the existing curated registry and home selector behavior tests intact.
  </action>
  <verify>
    <automated>bun run test -- src/domain/foundation.test.ts src/domain/portfolio-surfaces.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/projects.ts` contains the phrase `Maintainer-facing project data surface`.
    - `src/domain/projects.ts` no longer exports `projectSeeds`, `primaryProjectLink`, or `featuredProjects`.
    - `src/domain/foundation.test.ts` has an export-surface test covering both supported selectors and absent legacy names.
    - `bun run test -- src/domain/foundation.test.ts src/domain/portfolio-surfaces.test.ts` passes.
    - `bun run typecheck` passes.
  </acceptance_criteria>
  <done>Maintainers have an intentional documented project selector surface, the seed-era helper exports are gone, and existing project selector behavior is preserved by tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add project helper import-surface guard</name>
  <files>scripts/verify-project-helper-surface.ts, scripts/project-helper-surface.test.ts</files>
  <read_first>
    .planning/phases/08-content-helper-surface-cleanup/08-CONTEXT.md
    .planning/phases/08-content-helper-surface-cleanup/08-RESEARCH.md
    scripts/verify-no-github-runtime.ts
    scripts/verify-release.ts
    scripts/verify-release.test.ts
    tsconfig.json
    src/routes/index.tsx
    src/routes/projects.tsx
  </read_first>
  <behavior>
    - Test 1: allowed imports from `src/domain/projects` such as `curatedProjects`, `homeProjects`, `publicProjectIndexProjects`, and `type ProjectStory` produce no findings.
    - Test 2: named imports and aliased named imports of `projectSeeds`, `primaryProjectLink`, or `featuredProjects` produce findings that include file, line, import name, and message.
    - Test 3: namespace imports from `src/domain/projects` produce findings so `projects.projectSeeds` cannot bypass the named import check.
    - Test 4: re-exports of forbidden names from `src/domain/projects` produce findings.
    - Test 5: tests, declaration files, generated output, and `src/domain/projects.ts` itself are not guarded source paths.
  </behavior>
  <action>
    Create `scripts/verify-project-helper-surface.ts` per D-04, D-05, and D-06. Use the existing `typescript` dependency and `ts.createSourceFile` to parse imports and exports; do not use regex as the import parser and do not add dependencies. Export pure scanner functions named `projectHelperSurfaceFindingsForSource`, `projectHelperSurfaceFindingsForFiles`, and `runProjectHelperSurfaceVerification`. Keep filesystem traversal, console output, and exit behavior in the CLI shell guarded by `if (import.meta.main)`.

    The verifier must scan `src/**/*.ts`, `src/**/*.tsx`, and `scripts/**/*.ts`; exclude `*.test.ts`, `*.test.tsx`, `*.d.ts`, generated directories, and `src/domain/projects.ts`. It must resolve relative imports that point to `src/domain/projects` from route, component, domain, and script files. It must fail for `projectSeeds`, `primaryProjectLink`, and `featuredProjects` named imports or re-exports, and it must fail namespace imports from `src/domain/projects`. It must allow named imports of `curatedProjects`, supported selectors, and type-only project types.

    Create `scripts/project-helper-surface.test.ts` per D-08 with focused Vitest unit tests for the pure scanner functions. Use Arrange, Act, Assert comments in each test. Use source-string fixtures rather than writing temporary files.
  </action>
  <verify>
    <automated>bun run test -- scripts/project-helper-surface.test.ts && bun run scripts/verify-project-helper-surface.ts</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-project-helper-surface.ts` imports `typescript` and calls `ts.createSourceFile`.
    - `scripts/verify-project-helper-surface.ts` exports `projectHelperSurfaceFindingsForSource`, `projectHelperSurfaceFindingsForFiles`, and `runProjectHelperSurfaceVerification`.
    - `scripts/project-helper-surface.test.ts` includes cases for allowed selector imports, forbidden named imports, aliased forbidden imports, namespace imports, re-exports, and excluded paths.
    - `bun run test -- scripts/project-helper-surface.test.ts` passes.
    - `bun run scripts/verify-project-helper-surface.ts` passes against the current repository.
  </acceptance_criteria>
  <done>The repo has a deterministic import guard that blocks runtime and build-time source from depending on seed-era project helper names.</done>
</task>

<task type="auto">
  <name>Task 3: Wire helper-surface guard into aggregate verification</name>
  <files>package.json</files>
  <read_first>
    package.json
    .planning/phases/07-release-gates-deploy-readiness/07-01-SUMMARY.md
    scripts/verify-project-helper-surface.ts
    scripts/project-helper-surface.test.ts
  </read_first>
  <action>
    Update `package.json` per D-06 and D-09. Add `"verify:project-helper-surface": "bun run scripts/verify-project-helper-surface.ts"` next to the existing verification scripts. Add `bun run verify:project-helper-surface` to the aggregate `verify` script after `bun run verify:no-github-runtime` and before `bun run verify:visual-system`, preserving Phase 7's single-command release contract. Do not change dependencies or devDependencies, and do not modify `bun.lock`.
  </action>
  <verify>
    <automated>bun run verify:project-helper-surface && bun run test -- src/domain/foundation.test.ts src/domain/portfolio-surfaces.test.ts scripts/project-helper-surface.test.ts && bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `verify:project-helper-surface`.
    - The aggregate `verify` script includes `bun run verify:project-helper-surface` after `verify:no-github-runtime`.
    - `package.json` dependencies and devDependencies are unchanged.
    - `bun.lock` is unchanged.
    - `bun run verify` passes with the new helper-surface guard included.
  </acceptance_criteria>
  <done>The new import guard is part of the repo-native release verification path, and visitor-facing project behavior remains protected by the aggregate gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| maintainer source edits -> project data module | Maintainer-authored project records and helper imports influence static route output. |
| verifier CLI -> local filesystem | The Bun verifier reads source files and reports findings; it should not read generated output, secrets, or planning artifacts. |
| curated data -> prerendered visitor output | Project registry changes become public static HTML through existing route and SEO helpers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-08-01 | Tampering | `src/domain/projects.ts` export surface | mitigate | Remove legacy exports and add an export-surface test so seed-era aliases cannot remain available accidentally. |
| T-08-02 | Tampering | runtime/helper imports in `src/**/*.ts(x)` and `scripts/**/*.ts` | mitigate | Add TypeScript AST import guard that fails named, aliased, re-exported, and namespace imports of forbidden helper names from `src/domain/projects`. |
| T-08-03 | Information Disclosure | verifier filesystem scanning | mitigate | Restrict scanner to `src` and `scripts`, exclude generated output, tests, declarations, planning files, and the project module itself; do not introduce runtime network calls or tokens. |
| T-08-04 | Denial of Service | aggregate `bun run verify` | mitigate | Keep the guard dependency-free and path-scoped so it runs deterministically without external services. |
| T-08-05 | Spoofing / Elevation of Privilege / Repudiation | Phase 8 runtime behavior | accept | Phase 8 adds no authentication, session, authorization, user input, backend API, secret handling, or runtime network behavior. Verification must confirm no dependency or token-surface additions. |
</threat_model>

<verification>
Run the narrow checks as each task completes, then run the aggregate gate:

```bash
bun run test -- src/domain/foundation.test.ts src/domain/portfolio-surfaces.test.ts scripts/project-helper-surface.test.ts
bun run verify:project-helper-surface
bun run typecheck
bun run check
bun run verify
```

Also review the final diff to confirm only the planned files changed, `bun.lock` is untouched, and no visitor-facing project copy, ordering, route output, or dependency declarations changed.
</verification>

<success_criteria>
- DATA-01: `projectSeeds`, `primaryProjectLink`, and `featuredProjects` are removed as exports, and `src/domain/projects.ts` documents the supported selector API.
- DATA-02: Vitest coverage and `verify:project-helper-surface` prove runtime/build-time source cannot import orphaned helper exports from `src/domain/projects`.
- DATA-03: `bun run verify` includes the helper-surface guard so future curated project data edits keep using documented selector APIs.
- Existing home and projects route behavior remains unchanged under focused domain tests and aggregate release verification.
- No new dependencies, runtime network paths, token surfaces, backend behavior, or visitor-facing content expansion are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/08-content-helper-surface-cleanup/08-01-SUMMARY.md`.
</output>
