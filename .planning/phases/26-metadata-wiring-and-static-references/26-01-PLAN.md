---
phase: 26-metadata-wiring-and-static-references
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/seo.ts
  - src/domain/project-detail-routes.test.ts
  - src/domain/writing-metadata.test.ts
  - src/domain/portfolio-surfaces.test.ts
  - src/domain/foundation.test.ts
autonomous: true
requirements: [META-01, META-03, META-04, META-05]
user_setup: []
must_haves:
  truths:
    - "Covered project, writing, theme, and route-family index metadata resolve route-specific social preview images through the Phase 24 social preview helper."
    - "Home, about, contact, and non-covered generic route metadata continue to use the fallback social image."
    - "Project, writing, and theme detail JSON-LD image values match the corresponding Open Graph and Twitter image URL."
    - "Route files remain consumers of domain metadata helpers and do not receive generated social image paths."
  artifacts:
    - path: "src/domain/seo.ts"
      provides: "Route-aware social image metadata resolver, MIME type field, and JSON-LD image parity"
      exports: ["metadataForRoute", "metadataForProject", "metadataForWritingEntry", "metadataForTheme", "projectJsonLd", "writingBlogPostingJsonLd", "writingItemListJsonLd", "themeCollectionPageJsonLd"]
    - path: "src/domain/project-detail-routes.test.ts"
      provides: "Project detail metadata and SoftwareSourceCode JSON-LD route-specific image coverage"
    - path: "src/domain/writing-metadata.test.ts"
      provides: "Writing and theme metadata, ItemList, BlogPosting, and CollectionPage image parity coverage"
    - path: "src/domain/portfolio-surfaces.test.ts"
      provides: "Top-level route metadata coverage for covered index and generic fallback routes"
    - path: "src/domain/foundation.test.ts"
      provides: "Generic route fallback regression coverage"
  key_links:
    - from: "src/domain/seo.ts"
      to: "src/domain/social-previews.ts"
      via: "maybeSocialPreviewTargetForRoutePath(routePath) with SOCIAL_PREVIEW_FALLBACK_IMAGE fallback"
      pattern: "maybeSocialPreviewTargetForRoutePath"
    - from: "projectJsonLd/writingBlogPostingItemJsonLd/themeCollectionPageJsonLd"
      to: "metadata social image selection"
      via: "socialImageForRoutePath(routePath, profile).url"
      pattern: "image: socialImageForRoutePath"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-06-21T21-29-16
generated_at: 2026-06-21T21:46:48.979Z
---

<objective>
Add the pure route-aware SEO image resolver and JSON-LD parity contract for Phase 26.

Purpose: This makes the domain metadata layer the single source of truth for generated social preview asset selection before route components or static verification consume it.
Output: Updated `src/domain/seo.ts` plus focused domain tests proving route-specific image selection, generic fallback preservation, MIME metadata shape, and JSON-LD image parity.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-metadata-wiring-and-static-references/26-CONTEXT.md
@.planning/phases/26-metadata-wiring-and-static-references/26-RESEARCH.md
@.planning/phases/24-social-image-data-contract/24-CONTEXT.md
@.planning/phases/24-social-image-data-contract/24-01-SUMMARY.md
@.planning/phases/25-deterministic-static-image-generation/25-CONTEXT.md
@.planning/phases/25-deterministic-static-image-generation/25-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/typescript-javascript.md
@src/domain/seo.ts
@src/domain/social-previews.ts
@src/domain/project-detail-routes.test.ts
@src/domain/writing-metadata.test.ts
@src/domain/portfolio-surfaces.test.ts
@src/domain/foundation.test.ts

<interfaces>
From `src/domain/social-previews.ts`:
```typescript
export const SOCIAL_PREVIEW_DIMENSIONS = { width: 1200, height: 630 } as const;
export const SOCIAL_PREVIEW_FALLBACK_IMAGE = {
  assetPath: "/social/bright-builds-og.png",
  alt: "Peter Ryszkiewicz / pRizz and Bright Builds portfolio focus on AI, Bitcoin, open systems, and developer tooling.",
  dimensions: SOCIAL_PREVIEW_DIMENSIONS,
} as const;
export function maybeSocialPreviewTargetForRoutePath(routePath: string): SocialPreviewTarget | null;
```

From `src/domain/seo.ts` before this plan:
```typescript
export type SocialImageMetadata = {
  url: string;
  width: number;
  height: number;
  alt: string;
};
export function metadataForRoute(route: SiteRoute, profile?: Profile): PageMetadata;
export function metadataForProject(project: ProjectStory, profile?: Profile): PageMetadata;
export function metadataForWritingEntry(entry: PublicWritingEntry, profile?: Profile): PageMetadata;
export function metadataForTheme(theme: PublicThemeEntry, profile?: Profile): PageMetadata;
export function projectJsonLd(project: ProjectDetailPageProject, profile?: Profile): ProjectJsonLd;
export function writingBlogPostingJsonLd(entry: PublicWritingEntry, profile?: Profile): WritingBlogPostingJsonLd;
export function writingItemListJsonLd(entries?: readonly PublicWritingEntry[], profile?: Profile): WritingItemListJsonLd;
export function themeCollectionPageJsonLd(theme: PublicThemeEntry, profile?: Profile): ThemeCollectionPageJsonLd;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing domain tests for route-aware metadata and JSON-LD image parity</name>
  <files>src/domain/project-detail-routes.test.ts, src/domain/writing-metadata.test.ts, src/domain/portfolio-surfaces.test.ts, src/domain/foundation.test.ts</files>
  <read_first>
    .planning/phases/26-metadata-wiring-and-static-references/26-CONTEXT.md
    .planning/phases/26-metadata-wiring-and-static-references/26-RESEARCH.md
    src/domain/social-previews.ts
    src/domain/seo.ts
    src/domain/project-detail-routes.test.ts
    src/domain/writing-metadata.test.ts
    src/domain/portfolio-surfaces.test.ts
    src/domain/foundation.test.ts
  </read_first>
  <behavior>
    - Test covered project detail metadata: for each `projectDetailPageProjects()` record, derive `routePath = projectDetailPath(project)`, get `maybeSocialPreviewTargetForRoutePath(routePath)`, assert it is not null, and assert `metadataForProject(project, peterProfile).openGraph.image` has `url: peterProfile.canonicalOrigin + target.assetPath`, `width: 1200`, `height: 630`, `alt: target.alt`, and `mimeType: "image/png"` per D-01, D-03, D-06, D-08.
    - Test project JSON-LD parity: `projectJsonLd(project, peterProfile).image` must equal `metadataForProject(project, peterProfile).openGraph.image.url` per D-10.
    - Test covered writing and theme detail metadata and JSON-LD: use `writingDetailPath(entry)` and `themeDetailPath(theme)` with `maybeSocialPreviewTargetForRoutePath()`; assert metadata image fields and `writingBlogPostingJsonLd(entry).image`, `writingItemListJsonLd().itemListElement[*].item.image`, and `themeCollectionPageJsonLd(theme).image` match metadata image URLs per D-11 and D-12.
    - Test route-family indexes `/projects`, `/writing`, and `/themes`: `metadataForRoute(routeByPath(path), peterProfile).openGraph.image.url` must equal the route-specific target URL from `maybeSocialPreviewTargetForRoutePath(path)` per D-02.
    - Test generic fallback routes `/`, `/about`, and `/contact`: `metadataForRoute(routeByPath(path), peterProfile).openGraph.image.url` must equal `peterProfile.canonicalOrigin + SOCIAL_PREVIEW_FALLBACK_IMAGE.assetPath`, alt must equal `SOCIAL_PREVIEW_FALLBACK_IMAGE.alt`, and `mimeType` must be `"image/png"` per D-04 and D-07.
  </behavior>
  <action>
    Update existing tests rather than adding copied route-to-asset maps. Import `maybeSocialPreviewTargetForRoutePath` and `SOCIAL_PREVIEW_FALLBACK_IMAGE` from `./social-previews` where needed. Replace stale expectations that assert every route uses `/social/bright-builds-og.png` with helper-derived expectations for covered routes, while preserving explicit fallback assertions for `/`, `/about`, and `/contact`. Keep Arrange/Act/Assert comments in non-trivial tests per Bright Builds testing rules. Do not add generated filenames such as `openlinks-b31ca7ec626b.png` to tests; derive paths from `maybeSocialPreviewTargetForRoutePath()` so tests implement D-16 and META-05.
  </action>
  <verify>
    <automated>bun run test src/domain/project-detail-routes.test.ts src/domain/writing-metadata.test.ts src/domain/portfolio-surfaces.test.ts src/domain/foundation.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybeSocialPreviewTargetForRoutePath|SOCIAL_PREVIEW_FALLBACK_IMAGE" src/domain/project-detail-routes.test.ts src/domain/writing-metadata.test.ts src/domain/portfolio-surfaces.test.ts src/domain/foundation.test.ts` shows helper-derived expectations.
    - `rg -n "mimeType: \"image/png\"|\\.mimeType\\)\\.toBe\\(\"image/png\"\\)" src/domain/project-detail-routes.test.ts src/domain/writing-metadata.test.ts src/domain/portfolio-surfaces.test.ts src/domain/foundation.test.ts` shows MIME type coverage.
    - `rg -n "projectJsonLd\\(|writingBlogPostingJsonLd\\(|writingItemListJsonLd\\(|themeCollectionPageJsonLd\\(" src/domain/project-detail-routes.test.ts src/domain/writing-metadata.test.ts` shows JSON-LD image parity coverage.
    - `rg -n "bright-builds-og\\.png" src/domain/project-detail-routes.test.ts src/domain/writing-metadata.test.ts` does not show fallback expectations for covered detail routes.
  </acceptance_criteria>
  <done>Domain tests fail before implementation because covered routes expect generated social images and detail JSON-LD expects parity with Open Graph/Twitter image URLs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement route-aware social image metadata and JSON-LD parity</name>
  <files>src/domain/seo.ts</files>
  <read_first>
    .planning/phases/26-metadata-wiring-and-static-references/26-CONTEXT.md
    .planning/phases/26-metadata-wiring-and-static-references/26-RESEARCH.md
    src/domain/social-previews.ts
    src/domain/seo.ts
    src/domain/project-detail-routes.test.ts
    src/domain/writing-metadata.test.ts
    src/domain/portfolio-surfaces.test.ts
    src/domain/foundation.test.ts
  </read_first>
  <behavior>
    - `metadataForRoute(routeByPath("/projects" | "/writing" | "/themes"), peterProfile)` returns Open Graph and Twitter images for generated assets under `/social/generated/`.
    - `metadataForRoute(routeByPath("/" | "/about" | "/contact"), peterProfile)` returns the fallback `/social/bright-builds-og.png` image.
    - `metadataForProject`, `metadataForWritingEntry`, and `metadataForTheme` use the route-specific generated image for their route paths.
    - `openGraph.image` and `twitter.image` remain equal for every metadata helper and include `mimeType: "image/png"`.
    - Project `SoftwareSourceCode`, writing `BlogPosting` and writing ItemList entries, and theme `CollectionPage` JSON-LD `image` values use the same image URL as their corresponding metadata helper.
  </behavior>
  <action>
    In `src/domain/seo.ts`, import `maybeSocialPreviewTargetForRoutePath` and `SOCIAL_PREVIEW_FALLBACK_IMAGE` from `./social-previews`. Extend `SocialImageMetadata` with `mimeType: "image/png"`. Replace fallback-only `socialImageForProfile(profile)` usage with a pure `socialImageForRoutePath(routePath: string, profile: Profile): SocialImageMetadata` helper that sets `const maybeTarget = maybeSocialPreviewTargetForRoutePath(routePath)`, chooses `maybeTarget ?? SOCIAL_PREVIEW_FALLBACK_IMAGE`, and returns `{ url: profile.canonicalOrigin + image.assetPath, width: image.dimensions.width, height: image.dimensions.height, alt: image.alt, mimeType: "image/png" }`. Update `metadataForRoute` to pass `route.path`, `metadataForProject` to pass `projectDetailPath(project)`, `metadataForWritingEntry` to pass `writingDetailPath(entry)`, and `metadataForTheme` to pass `themeDetailPath(theme)` per D-01 through D-09. Add `image: string` to `ProjectJsonLd`, set `projectJsonLd(project, profile).image` to `socialImageForRoutePath(projectDetailPath(project), profile).url`, set `writingBlogPostingItemJsonLd(entry, profile).image` the same way using `writingDetailPath(entry)`, and set `themeCollectionPageJsonLd(theme, profile).image` using `themeDetailPath(theme)` per D-10 through D-13. Keep `personJsonLd()` and `profileSameAsLinks()` unchanged so OpenLinks remains low-intrusion identity metadata per D-05.
  </action>
  <verify>
    <automated>bun run test src/domain/project-detail-routes.test.ts src/domain/writing-metadata.test.ts src/domain/portfolio-surfaces.test.ts src/domain/foundation.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "maybeSocialPreviewTargetForRoutePath|SOCIAL_PREVIEW_FALLBACK_IMAGE" src/domain/seo.ts` shows the social preview contract import and resolver use.
    - `rg -n "mimeType: \"image/png\"" src/domain/seo.ts` shows the metadata MIME field is produced by the domain helper.
    - `rg -n "function socialImageForRoutePath\\(routePath: string, profile: Profile\\)" src/domain/seo.ts` shows one route-path image resolver.
    - `rg -n "image: socialImageForRoutePath\\(.*\\)\\.url" src/domain/seo.ts` shows JSON-LD image values derive from the same resolver.
    - `rg -n "const socialImagePath|const socialImageSize" src/domain/seo.ts` returns no stale fallback-only constants unless they are no longer used by metadata selection.
  </acceptance_criteria>
  <done>`src/domain/seo.ts` is the single domain entrypoint for route-aware social image metadata, and the focused domain tests plus typecheck pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Domain content -> metadata helpers | Checked-in project, writing, theme, route, and social preview records become crawler-visible metadata and JSON-LD. |
| Metadata helpers -> route/static consumers | Route components and static verifiers trust `PageMetadata` and JSON-LD helper outputs. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-01 | Spoofing | `socialImageForRoutePath()` in `src/domain/seo.ts` | mitigate | Build absolute image URLs only from `profile.canonicalOrigin` plus helper-derived local `assetPath`; tests assert canonical URLs for generated and fallback images. |
| T-26-02 | Information Disclosure | Covered route selection through `maybeSocialPreviewTargetForRoutePath()` | mitigate | Reuse Phase 24 public helper filtering; do not add route arrays or generated path maps in SEO tests or implementation. |
| T-26-03 | Tampering | JSON-LD image fields | mitigate | Set project, writing, and theme detail JSON-LD `image` values from `socialImageForRoutePath(routePath, profile).url`; tests assert parity with Open Graph/Twitter metadata. |
| T-26-04 | Repudiation | Metadata drift across helper consumers | mitigate | Unit tests cover covered route indexes, detail routes, generic fallback routes, and JSON-LD parity from the same helper-derived expectations. |
| T-26-05 | Information Disclosure | OpenLinks identity metadata | accept | Existing `Person.sameAs` remains low-intrusion identity context; this plan does not add OpenLinks as a primary brand or CTA. |
</threat_model>

<verification>
Run these after both tasks:

```bash
bun run test src/domain/project-detail-routes.test.ts src/domain/writing-metadata.test.ts src/domain/portfolio-surfaces.test.ts src/domain/foundation.test.ts
bun run typecheck
```
</verification>

<success_criteria>
- META-01: Metadata helpers select generated images for `/projects`, selected project detail routes, `/writing`, public writing detail routes, `/themes`, and public theme detail routes through `maybeSocialPreviewTargetForRoutePath()`.
- META-03: Project, writing, and theme detail JSON-LD `image` values equal the matching metadata image URL.
- META-04: `/`, `/about`, and `/contact` still use `SOCIAL_PREVIEW_FALLBACK_IMAGE.assetPath`.
- META-05: Route components do not receive generated asset paths or route-to-image maps from this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/26-metadata-wiring-and-static-references/26-01-SUMMARY.md`.
</output>
