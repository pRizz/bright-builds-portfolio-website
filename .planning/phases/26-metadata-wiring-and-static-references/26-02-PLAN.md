---
phase: 26-metadata-wiring-and-static-references
plan: 02
type: execute
wave: 2
depends_on: [26-01]
files_modified:
  - src/routes/index.tsx
  - src/routes/about.tsx
  - src/routes/contact.tsx
  - src/routes/projects/index.tsx
  - src/routes/projects/[slug].tsx
  - src/routes/writing/index.tsx
  - src/routes/writing/[slug].tsx
  - src/routes/themes/index.tsx
  - src/routes/themes/[slug].tsx
  - scripts/verify-static/metadata-jsonld-verifier.ts
  - scripts/verify-static.test.ts
autonomous: true
requirements: [META-02, META-05]
user_setup: []
must_haves:
  truths:
    - "Generated HTML exposes canonical social image metadata, including `og:image:type`, for every covered route before hydration."
    - "Generated HTML continues to expose fallback social image metadata for generic routes before hydration."
    - "Static verification rejects image URLs that are non-canonical, wrong for the route, missing from output, wrong-size, or not mirrored in JSON-LD detail records."
    - "Route components consume `PageMetadata` fields and do not hard-code generated social image paths or fallback image paths."
  artifacts:
    - path: "src/routes/*.tsx"
      provides: "Helper-derived `og:image:type` rendering beside existing Open Graph image tags"
    - path: "scripts/verify-static/metadata-jsonld-verifier.ts"
      provides: "Route-aware static HTML metadata, local asset, PNG dimension, and JSON-LD image parity checks"
    - path: "scripts/verify-static.test.ts"
      provides: "Import-safe verifier coverage for generated image acceptance and fallback preservation"
  key_links:
    - from: "src/routes/**/*.tsx"
      to: "src/domain/seo.ts"
      via: "metadata.openGraph.image.mimeType"
      pattern: "og:image:type"
    - from: "scripts/verify-static/metadata-jsonld-verifier.ts"
      to: "src/domain/social-previews.ts"
      via: "maybeSocialPreviewTargetForRoutePath(path) ?? SOCIAL_PREVIEW_FALLBACK_IMAGE"
      pattern: "maybeSocialPreviewTargetForRoutePath"
    - from: "scripts/verify-static/metadata-jsonld-verifier.ts"
      to: "scripts/verify-static/output.ts"
      via: "assertPngDimensions(outputRoot, assetPath, width, height)"
      pattern: "assertPngDimensions"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 26-2026-06-21T21-29-16
generated_at: 2026-06-21T21:46:48.979Z
---

<objective>
Render and verify helper-derived static social image references in generated HTML.

Purpose: This closes the crawler-facing contract by adding `og:image:type` route rendering and making static verification route-aware for generated and fallback images.
Output: Updated route head tags and static verifier checks that prove canonical image metadata and JSON-LD parity in prerendered output.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/26-metadata-wiring-and-static-references/26-CONTEXT.md
@.planning/phases/26-metadata-wiring-and-static-references/26-RESEARCH.md
@.planning/phases/26-metadata-wiring-and-static-references/26-01-SUMMARY.md
@.planning/phases/24-social-image-data-contract/24-01-SUMMARY.md
@.planning/phases/25-deterministic-static-image-generation/25-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@src/domain/seo.ts
@src/domain/social-previews.ts
@scripts/verify-static/metadata-jsonld-verifier.ts
@scripts/verify-static/output.ts
@scripts/verify-static.test.ts

<interfaces>
Expected `SocialImageMetadata` after Plan 26-01:
```typescript
export type SocialImageMetadata = {
  url: string;
  width: number;
  height: number;
  alt: string;
  mimeType: "image/png";
};
```

Static output helper available from `scripts/verify-static/output.ts`:
```typescript
export function assertPngDimensions(root: string, path: string, width: number, height: number): void;
```

Current static verifier entry point to update:
```typescript
export function assertMetadataImageMapsToLocalAsset(outputRoot: string, imageUrl: string): void;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render helper-derived `og:image:type` in every route head</name>
  <files>src/routes/index.tsx, src/routes/about.tsx, src/routes/contact.tsx, src/routes/projects/index.tsx, src/routes/projects/[slug].tsx, src/routes/writing/index.tsx, src/routes/writing/[slug].tsx, src/routes/themes/index.tsx, src/routes/themes/[slug].tsx</files>
  <read_first>
    .planning/phases/26-metadata-wiring-and-static-references/26-CONTEXT.md
    .planning/phases/26-metadata-wiring-and-static-references/26-RESEARCH.md
    .planning/phases/26-metadata-wiring-and-static-references/26-01-SUMMARY.md
    src/domain/seo.ts
    src/routes/index.tsx
    src/routes/about.tsx
    src/routes/contact.tsx
    src/routes/projects/index.tsx
    src/routes/projects/[slug].tsx
    src/routes/writing/index.tsx
    src/routes/writing/[slug].tsx
    src/routes/themes/index.tsx
    src/routes/themes/[slug].tsx
  </read_first>
  <action>
    In every route component that already renders `<Meta property="og:image" ...>`, add `<Meta property="og:image:type" content={metadata.openGraph.image.mimeType} />` immediately after the `og:image` tag per D-07. For `src/routes/projects/[slug].tsx`, use the existing signal shape: `<Meta property="og:image:type" content={pageMetadata().openGraph.image.mimeType} />`. Do not hard-code `"image/png"` in route files. Do not add `/social/generated/...` or `/social/bright-builds-og.png` literals to route files. Keep `twitter:card` as `summary_large_image` through the existing `metadata.twitter.card` field per D-09, and keep all route files consuming `metadataForRoute`, `metadataForProject`, `metadataForWritingEntry`, `metadataForTheme`, and JSON-LD helpers per D-03 and D-05.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -l 'property="og:image:type"' src/routes | sort | wc -l | tr -d ' '` outputs `9`.
    - `rg -n 'property="og:image:type" content=\\{[^}]*mimeType' src/routes` shows every new tag is helper-derived from metadata.
    - `rg -n '/social/(generated|bright-builds-og)' src/routes` returns no matches.
    - `rg -n 'summary_large_image' src/routes` returns no newly hard-coded route-level card values beyond existing helper-derived rendering.
  </acceptance_criteria>
  <done>All prerendered route surfaces render `og:image:type` from the domain metadata object without route-level social image path literals.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make static metadata verification route-aware for generated and fallback image assets</name>
  <files>scripts/verify-static/metadata-jsonld-verifier.ts, scripts/verify-static.test.ts</files>
  <read_first>
    .planning/phases/26-metadata-wiring-and-static-references/26-CONTEXT.md
    .planning/phases/26-metadata-wiring-and-static-references/26-RESEARCH.md
    .planning/phases/26-metadata-wiring-and-static-references/26-01-SUMMARY.md
    src/domain/seo.ts
    src/domain/social-previews.ts
    scripts/verify-static/metadata-jsonld-verifier.ts
    scripts/verify-static/output.ts
    scripts/verify-static.test.ts
    public/social/generated/manifest.json
  </read_first>
  <behavior>
    - Static verification asserts `og:image:type` equals `metadata.openGraph.image.mimeType` for every route per D-07.
    - For covered routes, `assertMetadataImageMapsToLocalAsset(outputRoot, routePath, imageUrl)` requires the exact asset path from `maybeSocialPreviewTargetForRoutePath(routePath)?.assetPath` per D-14 and D-15.
    - For generic routes, the same assertion requires `SOCIAL_PREVIEW_FALLBACK_IMAGE.assetPath` per D-04 and D-14.
    - For every accepted image URL, the verifier preserves canonical origin checks and calls `assertPngDimensions(outputRoot, assetPathWithoutLeadingSlash, expectedWidth, expectedHeight)` per D-15.
    - Project, writing, and theme detail JSON-LD verification includes the expected detail image URL from the matching domain helper per D-10 through D-12 and D-15.
  </behavior>
  <action>
    In `scripts/verify-static/metadata-jsonld-verifier.ts`, import `maybeSocialPreviewTargetForRoutePath` and `SOCIAL_PREVIEW_FALLBACK_IMAGE` from `../../src/domain/social-previews`, and import `assertPngDimensions` from `./output`. Update `assertMetadata()` to assert `<Meta property="og:image:type" content="${image.mimeType}">` in the static HTML beside existing `og:image`, width, height, and alt checks. Change `assertMetadataImageMapsToLocalAsset` to accept `(outputRoot: string, routePath: string, imageUrl: string)`. Inside it, keep `new URL(imageUrl)` and `url.origin === peterProfile.canonicalOrigin`, compute `const maybeTarget = maybeSocialPreviewTargetForRoutePath(routePath)`, compute `const expectedImage = maybeTarget ?? SOCIAL_PREVIEW_FALLBACK_IMAGE`, require `url.pathname === expectedImage.assetPath`, then call `assertPngDimensions(outputRoot, expectedImage.assetPath.replace(/^\\//, ""), expectedImage.dimensions.width, expectedImage.dimensions.height)`. Update callers in `assertMetadata()` to pass `path` for both Open Graph and Twitter images. Update `assertProjectJsonLd`, `assertWritingBlogPostingJsonLd`, and `assertThemeCollectionPageJsonLd` to include each expected JSON-LD `image` value in `assertJsonLdContains()`; use `projectJsonLd(project).image`, `writingBlogPostingJsonLd(entry).image`, and `themeCollectionPageJsonLd(theme).image` so the check stays helper-derived. In `scripts/verify-static.test.ts`, add import-safe tests that create a temporary static output root with a minimal valid 1200x630 PNG at one generated target path and the fallback path, then prove `assertMetadataImageMapsToLocalAsset(tempRoot, coveredRoutePath, canonicalGeneratedUrl)` accepts the generated asset, rejects the fallback for that covered route, and accepts the fallback for `/about`. Do not read manifest checksums or asset budgets in this phase; `verify:social-previews` and Phase 28 own broader manifest/budget release expansion.
  </action>
  <verify>
    <automated>bun run test scripts/verify-static.test.ts && bun run build && bun run verify:static</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'og:image:type' scripts/verify-static/metadata-jsonld-verifier.ts` shows static HTML verification for the MIME tag.
    - `rg -n 'maybeSocialPreviewTargetForRoutePath|SOCIAL_PREVIEW_FALLBACK_IMAGE|assertPngDimensions' scripts/verify-static/metadata-jsonld-verifier.ts` shows route-aware asset and PNG dimension checks.
    - `rg -n 'assertMetadataImageMapsToLocalAsset\\(outputRoot, path,' scripts/verify-static/metadata-jsonld-verifier.ts` shows both Open Graph and Twitter image checks pass the route path.
    - `rg -n '\\.image' scripts/verify-static/metadata-jsonld-verifier.ts | rg 'projectJsonLd|writingBlogPostingJsonLd|themeCollectionPageJsonLd|expectedJsonLd'` shows JSON-LD image parity is asserted for detail records.
    - `rg -n 'canonicalGeneratedUrl|coveredRoutePath|SOCIAL_PREVIEW_FALLBACK_IMAGE' scripts/verify-static.test.ts` shows import-safe generated/fallback verifier tests.
  </acceptance_criteria>
  <done>`bun run verify:static` proves generated HTML contains route-correct canonical social image metadata and JSON-LD detail image parity for generated and fallback assets.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route metadata helpers -> generated HTML | Solid route components convert helper-derived metadata into crawler-visible static `<head>` tags. |
| Generated HTML -> static verifier | Verification reads prerendered HTML and local copied public assets as release evidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-06 | Spoofing | Static `og:image` and `twitter:image` URLs | mitigate | Static verifier rejects non-`peterProfile.canonicalOrigin` image URLs and route-mismatched asset paths. |
| T-26-07 | Tampering | Generated social image asset references | mitigate | Static verifier maps covered route paths through `maybeSocialPreviewTargetForRoutePath()` and checks local PNG dimensions through `assertPngDimensions()`. |
| T-26-08 | Repudiation | Missing pre-hydration metadata | mitigate | `bun run build && bun run verify:static` checks generated HTML for `og:image`, `og:image:type`, dimensions, alt text, Twitter image tags, and JSON-LD image values before release. |
| T-26-09 | Information Disclosure | Route component hard-coded image paths | mitigate | Acceptance criteria grep route files for `/social/generated` and `/social/bright-builds-og` literals; route files may only render helper-derived metadata fields. |
| T-26-10 | Elevation of Privilege | JSON-LD script rendering | accept | No new script execution path is introduced; route components continue using `jsonLdScriptContent()` for JSON-LD escaping. |
</threat_model>

<verification>
Run these after both tasks:

```bash
bun run typecheck
bun run test scripts/verify-static.test.ts
bun run build
bun run verify:static
bun run verify
```
</verification>

<success_criteria>
- META-02: Generated HTML exposes absolute canonical `og:image`, `og:image:type`, `og:image:width`, `og:image:height`, `og:image:alt`, `twitter:image`, and `twitter:image:alt` for covered share routes before hydration.
- META-05: Route components render metadata fields only and do not hard-code generated or fallback social image paths.
- Static verification requires exact generated local assets for covered routes and exact fallback local asset for generic routes.
- Static verification checks JSON-LD image parity for project, writing, and theme detail routes.
</success_criteria>

<output>
After completion, create `.planning/phases/26-metadata-wiring-and-static-references/26-02-SUMMARY.md`.
</output>
