---
phase: 01-static-app-foundation-ui-shell
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [.gitignore, README.md, package.json, bun.lock, app.config.ts, tsconfig.json, biome.json, postcss.config.cjs, tailwind.config.ts, src/global.d.ts, src/entry-client.tsx, src/entry-server.tsx, src/app.tsx, src/styles/app.css, src/components/SiteLayout.tsx, src/routes/index.tsx, src/routes/about.tsx, src/routes/projects.tsx, src/routes/contact.tsx]
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-03]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260524T184659Z
generated_at: 2026-05-24T18:49:43Z
must_haves:
  truths:
    - "Bun package scripts exist for dev, build, format, format-check, lint/check, typecheck, test, and verify."
    - "SolidStart is configured for static output and explicitly prerenders /, /about, /projects, and /contact."
    - "Tailwind CSS 3.x imports Mystic UI through the supported withMysticUI setup, pinned to the requested GitHub commit."
  artifacts:
    - path: package.json
      contains: "github:pRizz/mystic-ui#d36017757708ed01ef2b3b47beb14f294726411c"
    - path: app.config.ts
      contains: "prerender"
    - path: tailwind.config.ts
      contains: "withMysticUI"
    - path: src/app.tsx
      contains: "FileRoutes"
  key_links:
    - from: src/app.tsx
      to: src/components/SiteLayout.tsx
      pattern: "SiteLayout"
    - from: tailwind.config.ts
      to: mystic-ui
      pattern: "withMysticUI"
---

<objective>
Scaffold the SolidStart static-first app foundation and visible UI shell.

Purpose: Establish a reproducible app/tooling baseline before content, motion, and GitHub enrichment work starts.
Output: Package scripts, framework config, Tailwind/Mystic setup, base layout, and simple static routes for the initial indexable route set.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/phases/01-static-app-foundation-ui-shell/01-CONTEXT.md
@.planning/phases/01-static-app-foundation-ui-shell/01-RESEARCH.md
@.planning/phases/01-static-app-foundation-ui-shell/01-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SolidStart package and static build config</name>
  <files>package.json, bun.lock, app.config.ts, tsconfig.json</files>
  <read_first>.planning/research/STACK.md, .planning/phases/01-static-app-foundation-ui-shell/01-RESEARCH.md</read_first>
  <action>Create a Bun-based package manifest using SolidStart, SolidJS, @solidjs/meta, Tailwind 3.x, Biome, TypeScript, Vitest, and the exact Mystic UI GitHub dependency github:pRizz/mystic-ui#d36017757708ed01ef2b3b47beb14f294726411c. Add scripts named dev, build, start, format, format:check, lint, check, typecheck, test, verify:static, and verify. Configure app.config.ts so SolidStart static output prerenders exactly /, /about, /projects, and /contact for this phase.</action>
  <verify>bun install; bun run typecheck</verify>
  <acceptance_criteria>
    - package.json contains "packageManager": "bun@1.3.14".
    - package.json contains the exact Mystic UI GitHub commit dependency.
    - app.config.ts contains routes for "/", "/about", "/projects", and "/contact".
  </acceptance_criteria>
  <done>The package installs and TypeScript can load the app configuration.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Tailwind 3 and Mystic UI styling</name>
  <files>tailwind.config.ts, postcss.config.cjs, src/styles/app.css</files>
  <read_first>.planning/research/STACK.md, AGENTS.bright-builds.md</read_first>
  <action>Add Tailwind 3 content scanning for src files, selector-based dark mode, PostCSS setup, and Mystic UI theme import in the app stylesheet. Use supported Mystic imports only: withMysticUI and mystic-ui/tailwind/theme.css. Keep the visual shell intentionally restrained and leave final polish for Phase 4.</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - tailwind.config.ts imports withMysticUI from a public mystic-ui path.
    - src/styles/app.css imports mystic-ui/tailwind/theme.css.
    - The build processes Tailwind without unsupported deep package imports.
  </acceptance_criteria>
  <done>Tailwind and Mystic setup compiles in the production build.</done>
</task>

<task type="auto">
  <name>Task 3: Add base layout and indexable route shell</name>
  <files>src/entry-client.tsx, src/entry-server.tsx, src/app.tsx, src/components/SiteLayout.tsx, src/routes/index.tsx, src/routes/about.tsx, src/routes/projects.tsx, src/routes/contact.tsx</files>
  <read_first>.planning/phases/01-static-app-foundation-ui-shell/01-UI-SPEC.md</read_first>
  <action>Create a minimal accessible app shell with skip link, header navigation, main landmark, footer, and route pages for home, about, projects, and contact. Use accurate placeholder wording about Peter Ryszkiewicz and the portfolio foundation, not Bright Builds template copy. Keep controls keyboard-reachable and avoid motion in Phase 1 except stable CSS hover states.</action>
  <verify>bun run build</verify>
  <acceptance_criteria>
    - src/app.tsx renders FileRoutes through SiteLayout.
    - Every base route has an h1 and route-specific title metadata.
    - The footer includes a low-intrusion OpenLinks identity link with rel="me noopener noreferrer".
  </acceptance_criteria>
  <done>The static route shell builds and has semantic navigation/content landmarks.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run format:check
- [ ] bun run check
- [ ] bun run typecheck
- [ ] bun run build
</verification>

<success_criteria>

- Package scripts and dependencies match the stack contract.
- SolidStart static prerender config includes all current base routes.
- Tailwind/Mystic setup uses the supported public consumer paths.
- Base routes render meaningful shell content without template residue.
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-app-foundation-ui-shell/01-01-SUMMARY.md`.
</output>
