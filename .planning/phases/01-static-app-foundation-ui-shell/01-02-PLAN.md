---
phase: 01-static-app-foundation-ui-shell
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified: [src/domain/routes.ts, src/domain/profile.ts, src/domain/projects.ts, src/domain/seo.ts, src/domain/foundation.test.ts, src/routes/index.tsx, src/routes/about.tsx, src/routes/projects.tsx, src/routes/contact.tsx, scripts/verify-static.ts, package.json, tsconfig.json]
autonomous: true
requirements: [FOUND-01, FOUND-02, FOUND-04]
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: phase-20260524T184659Z
generated_at: 2026-05-24T18:49:43Z
must_haves:
  truths:
    - "Route, profile, project, and SEO derivation live in pure TypeScript modules without DOM, network, or Solid runtime dependencies."
    - "Vitest tests cover route paths, profile identity links, featured project selection, and SEO derivation."
    - "A repo-owned static-output verifier proves generated HTML exists for /, /about, /projects, and /contact after build."
  artifacts:
    - path: src/domain/routes.ts
      contains: "siteRoutes"
    - path: src/domain/profile.ts
      contains: "Peter Ryszkiewicz"
    - path: src/domain/projects.ts
      contains: "featuredProjects"
    - path: src/domain/seo.ts
      contains: "metadataForRoute"
    - path: scripts/verify-static.ts
      contains: "expectedRoutes"
  key_links:
    - from: src/routes/projects.tsx
      to: src/domain/projects.ts
      pattern: "featuredProjects"
    - from: src/routes/index.tsx
      to: src/domain/seo.ts
      pattern: "metadataForRoute"
    - from: scripts/verify-static.ts
      to: src/domain/routes.ts
      pattern: "prerenderRoutes"
---

<objective>
Add testable pure domain seams and static build verification.

Purpose: Make Phase 1 prove more than a compiling shell: the route/content/SEO foundation must be testable without browser or framework runtime and the static output must be observable on disk.
Output: Pure TypeScript modules, focused Vitest coverage, and a static HTML verifier wired into package scripts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-static-app-foundation-ui-shell/01-CONTEXT.md
@.planning/phases/01-static-app-foundation-ui-shell/01-RESEARCH.md
@.planning/phases/01-static-app-foundation-ui-shell/01-UI-SPEC.md
@.planning/phases/01-static-app-foundation-ui-shell/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pure route/profile/project/SEO modules</name>
  <files>src/domain/routes.ts, src/domain/profile.ts, src/domain/projects.ts, src/domain/seo.ts</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/01-static-app-foundation-ui-shell/01-CONTEXT.md</read_first>
  <action>Create small pure TypeScript modules for the base route registry, Peter profile links, initial curated project seed data, featured project selection, route metadata, and JSON-LD person derivation. Do not call GitHub APIs, DOM globals, Solid signals, or browser APIs. Keep copy accurate but intentionally minimal because full curation is Phase 2.</action>
  <verify>bun run typecheck</verify>
  <acceptance_criteria>
    - src/domain/routes.ts exports prerenderRoutes and siteRoutes.
    - src/domain/profile.ts includes GitHub and OpenLinks identity links.
    - src/domain/projects.ts marks only curated seed projects as featured.
    - src/domain/seo.ts derives metadata from route/profile inputs without importing Solid.
  </acceptance_criteria>
  <done>Pure modules compile independently from the route components.</done>
</task>

<task type="auto">
  <name>Task 2: Consume pure modules from routes and tests</name>
  <files>src/domain/foundation.test.ts, src/routes/index.tsx, src/routes/about.tsx, src/routes/projects.tsx, src/routes/contact.tsx</files>
  <read_first>src/domain/routes.ts, src/domain/profile.ts, src/domain/projects.ts, src/domain/seo.ts</read_first>
  <action>Add Vitest coverage with Arrange, Act, Assert comments for route paths, profile links, featured projects, metadata derivation, and JSON-LD sameAs values. Update route components to use the pure metadata/content helpers where practical, while keeping the components simple and semantic.</action>
  <verify>bun run test; bun run typecheck</verify>
  <acceptance_criteria>
    - src/domain/foundation.test.ts contains tests for route, profile, project, and SEO behavior.
    - Route components call metadataForRoute or equivalent pure SEO helper.
    - Tests run without DOM, network, or Solid testing utilities.
  </acceptance_criteria>
  <done>The pure foundation behavior is covered by Vitest and route components compile against it.</done>
</task>

<task type="auto">
  <name>Task 3: Add static output verifier and full verify script</name>
  <files>scripts/verify-static.ts, package.json</files>
  <read_first>.planning/phases/01-static-app-foundation-ui-shell/01-RESEARCH.md</read_first>
  <action>Create scripts/verify-static.ts using Node/Bun filesystem APIs only. It must locate the SolidStart static output directory after bun run build, assert HTML exists for /, /about, /projects, and /contact, and check each file contains the expected route title or heading. Wire package.json verify:static to this script and verify to run format:check, check, typecheck, test, build, then verify:static.</action>
  <verify>bun run verify</verify>
  <acceptance_criteria>
    - scripts/verify-static.ts imports the route registry rather than duplicating route paths.
    - bun run verify fails if a required prerendered HTML file is missing.
    - bun run verify passes after the production build.
  </acceptance_criteria>
  <done>One command proves formatting, lint/check, type safety, tests, production build, and prerendered static HTML.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] bun run format:check
- [ ] bun run check
- [ ] bun run typecheck
- [ ] bun run test
- [ ] bun run build
- [ ] bun run verify:static
- [ ] bun run verify
</verification>

<success_criteria>

- Pure modules provide testable seams for FOUND-04.
- Unit tests exercise the route, profile, project, and SEO derivation behavior.
- Static HTML verification proves FOUND-02 from generated files on disk.
- The top-level verify command covers all Phase 1 quality gates.
</success_criteria>

<output>
After completion, create `.planning/phases/01-static-app-foundation-ui-shell/01-02-SUMMARY.md`.
</output>
