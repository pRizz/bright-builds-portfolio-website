---
phase: 29-archived-project-public-filter-guard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/projects.ts
  - src/domain/project-detail-routes.test.ts
  - src/domain/portfolio-surfaces.test.ts
  - src/domain/social-previews.test.ts
autonomous: true
requirements: [SHARE-02, VERIFY-01]
user_setup: []
must_haves:
  truths:
    - "Archived project records with `status: \"archived\"` cannot appear in public project index, selected project detail route, or social preview target output."
    - "Archived project records with `maturity: \"archived\"` cannot appear in public project index, selected project detail route, or social preview target output."
    - "`projectDetailPageProjects()`, `projectDetailRoutes()`, `maybeProjectDetailPageProjectBySlug()`, `visibleProjects()`, and `hiddenExcludedProjects()` share the same public project predicate."
    - "`socialPreviewTargets({ projects })` continues to derive project targets through `projectDetailPageProjects()` and does not add a parallel archived-project guard."
    - "Existing selected public projects and generated social preview assets remain stable unless a real default route target change is proven."
  artifacts:
    - path: "src/domain/projects.ts"
      provides: "Shared public project predicate used by public, detail, route, and hidden/excluded project selectors"
      contains: "project.status !== \"archived\""
    - path: "src/domain/project-detail-routes.test.ts"
      provides: "Regression tests for archived selected-looking project detail fixtures"
      contains: "archived-status-detail"
    - path: "src/domain/portfolio-surfaces.test.ts"
      provides: "Regression tests that archived projects are hidden/excluded public-surface records"
      contains: "archived-status-public-placement"
    - path: "src/domain/social-previews.test.ts"
      provides: "Regression tests that archived selected-looking project fixtures do not become social preview target routes"
      contains: "archived-status-detail"
  key_links:
    - from: "src/domain/projects.ts"
      to: "projectDetailPageProjects()"
      via: "isProjectDetailPageProject(project) calls isPublicProjectIndexProject(project)"
      pattern: "isPublicProjectIndexProject\\(project\\)"
    - from: "src/domain/projects.ts"
      to: "visibleProjects() and hiddenExcludedProjects()"
      via: "publicProjectIndexProjects(projects) and negated isPublicProjectIndexProject(project)"
      pattern: "hiddenExcludedProjects"
    - from: "src/domain/social-previews.ts"
      to: "src/domain/projects.ts"
      via: "socialPreviewTargets() calls projectDetailPageProjects(sources.projects)"
      pattern: "projectDetailPageProjects\\(sources\\.projects\\)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 29-2026-06-23T02-40-50
generated_at: 2026-06-23T02:47:48.736Z
---

<objective>
Close the archived project public-filter audit gap.

Purpose: Archived project records must not become public project detail pages, social preview targets, generated preview manifest inputs, metadata targets, static verification inputs, or release verification subjects.
Output: A tightened shared project selector plus focused regression coverage for archived `status` and archived `maturity` project fixtures.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/v1.5-MILESTONE-AUDIT.md
@.planning/phases/29-archived-project-public-filter-guard/29-CONTEXT.md
@.planning/phases/24-social-image-data-contract/24-01-SUMMARY.md
@.planning/phases/28-verification-and-release-contract/28-01-SUMMARY.md
@.planning/phases/28-verification-and-release-contract/28-02-SUMMARY.md
@.planning/phases/28-verification-and-release-contract/28-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/code-shape.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@src/domain/projects.ts
@src/domain/project-detail-routes.test.ts
@src/domain/portfolio-surfaces.test.ts
@src/domain/social-previews.ts
@src/domain/social-previews.test.ts
@package.json

<interfaces>
From `src/domain/projects.ts`:
```ts
export type ProjectMaturity = "active" | "stable" | "prototype" | "paused" | "archived";
export type ProjectStatus = "building" | "maintained" | "paused" | "archived" | "hidden";

export type ProjectStory = {
  slug: string;
  placement: ProjectPlacement;
  tier: ProjectTier;
  maturity: ProjectMaturity;
  status: ProjectStatus;
  includeOnHome: boolean;
  includeInProjectIndex: boolean;
  detail?: ProjectDetailStory;
};

export function visibleProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function hiddenExcludedProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function projectDetailPageProjects(projects?: readonly ProjectStory[]): readonly ProjectDetailPageProject[];
export function maybeProjectDetailPageProjectBySlug(
  slug: string,
  projects?: readonly ProjectStory[],
): ProjectDetailPageProject | null;
export function projectDetailRoutes(projects?: readonly ProjectStory[]): readonly string[];
export function projectDetailPath(project: Pick<ProjectStory, "slug">): string;
```

From `src/domain/social-previews.ts`:
```ts
export type SocialPreviewTargetSources = {
  projects?: readonly ProjectStory[];
  writingEntries?: readonly WritingEntry[];
  themes?: readonly ThemeRecord[];
};

export function socialPreviewTargets(
  sources?: SocialPreviewTargetSources,
): readonly SocialPreviewTarget[];
```

Current selector seam to change in `src/domain/projects.ts`:
```ts
function isPublicProjectIndexProject(project: ProjectStory): boolean {
  return (
    project.includeInProjectIndex &&
    project.placement !== "hidden" &&
    project.tier !== "excluded" &&
    project.status !== "hidden"
  );
}
```
</interfaces>

<decision_coverage>
| Decision | Plan | Task | Coverage |
|----------|------|------|----------|
| D-01 | 29-01 | 1 | Full: `status: "archived"` and `maturity: "archived"` are rejected by the public project predicate. |
| D-02 | 29-01 | 1 | Full: fix lands in `src/domain/projects.ts` so public, detail, route, and hidden/excluded selectors share it. |
| D-03 | 29-01 | 2 | Full: `src/domain/social-previews.ts` remains unchanged; tests prove composition through project detail helpers. |
| D-04 | 29-01 | 2 | Full: verification includes `bun run verify:social-previews` and checks generated assets remain unchanged. |
| D-05 | 29-01 | 1, 2 | Full: selected-looking `status: "archived"` fixture with detail data is excluded from route and social preview targets. |
| D-06 | 29-01 | 1, 2 | Full: selected-looking `maturity: "archived"` fixture with detail data is excluded from route and social preview targets. |
| D-07 | 29-01 | 1 | Full: portfolio surface tests prove archived projects enter `hiddenExcludedProjects()` and not visible/detail surfaces. |
| D-08 | 29-01 | 1, 2 | Full: new non-trivial tests use Arrange, Act, Assert comments. |
| D-09 | 29-01 | 2 | Full: social target flow remains `projectDetailPageProjects()` -> `socialPreviewTargets()` -> verification commands. |
| D-10 | 29-01 | 2 | Full: generation is avoided when default targets remain stable; executor must handle a genuine default target drift explicitly. |
| D-11 | 29-01 | 2 | Full: final summary and verification notes must name `SHARE-02`, `VERIFY-01`, `INT-01`, and `FLOW-01`. |
</decision_coverage>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten the shared public project selector</name>
  <files>src/domain/projects.ts, src/domain/project-detail-routes.test.ts, src/domain/portfolio-surfaces.test.ts</files>
  <read_first>
    - `.planning/phases/29-archived-project-public-filter-guard/29-CONTEXT.md`
    - `.planning/v1.5-MILESTONE-AUDIT.md`
    - `standards/core/architecture.md`
    - `standards/core/testing.md`
    - `standards/languages/typescript-javascript.md`
    - `src/domain/projects.ts`
    - `src/domain/project-detail-routes.test.ts`
    - `src/domain/portfolio-surfaces.test.ts`
    - `src/domain/project-validation.ts`
  </read_first>
  <behavior>
    - Test 1: `projectDetailRoutes(fixtureProjects)` returns only `/projects/visible-detail` when fixture input also includes selected-looking archived projects with `status: "archived"` and `maturity: "archived"` per D-01, D-05, and D-06.
    - Test 2: `maybeProjectDetailPageProjectBySlug("archived-status-detail", fixtureProjects)` and `maybeProjectDetailPageProjectBySlug("archived-maturity-detail", fixtureProjects)` both return `null` per D-02.
    - Test 3: `visibleProjects(fixtureProjects)` excludes archived status and archived maturity records, while `hiddenExcludedProjects(fixtureProjects)` includes their slugs per D-07.
    - Test 4: Existing selected public project fixture behavior remains unchanged per D-04.
  </behavior>
  <action>Implement the archived project guard at the shared selector layer per D-01 and D-02. In `src/domain/projects.ts`, update `isPublicProjectIndexProject(project)` so it still requires `project.includeInProjectIndex`, `project.placement !== "hidden"`, `project.tier !== "excluded"`, and `project.status !== "hidden"`, and also requires `project.status !== "archived"` and `project.maturity !== "archived"`. Do not change `curatedProjects`, `ProjectStatus`, `ProjectMaturity`, `isProjectDetailPageProject()`, or `src/domain/social-previews.ts`. In `src/domain/project-detail-routes.test.ts`, extend the existing public/detail exclusion coverage with two selected-looking flagship fixtures named `archived-status-detail` and `archived-maturity-detail`; each fixture must keep `includeInProjectIndex: true`, `tier: "flagship"`, authored `detail` data, and proof points so the only exclusion reason is archived status or maturity. Assert `projectDetailRoutes(fixtureProjects)` is exactly `["/projects/visible-detail"]`, and assert `maybeProjectDetailPageProjectBySlug()` returns `null` for both archived fixture slugs with the same fixture array. In `src/domain/portfolio-surfaces.test.ts`, extend the public surface fixture test with `archived-status-public-placement` using `status: "archived"` and `archived-maturity-public-placement` using `maturity: "archived"`; assert `visibleProjects(projects).map((project) => project.slug)` remains exactly `["public-supporting"]` and `hiddenExcludedProjects(projects).map((project) => project.slug)` includes `private-draft`, `excluded-public-placement`, `hidden-public-placement`, `archived-status-public-placement`, and `archived-maturity-public-placement` in display order. Add Arrange, Act, Assert comments to any new non-trivial tests per D-08.</action>
  <verify>
    <automated>bun run test src/domain/project-detail-routes.test.ts src/domain/portfolio-surfaces.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/projects.ts` contains `project.status !== "archived"`
    - `src/domain/projects.ts` contains `project.maturity !== "archived"`
    - `src/domain/project-detail-routes.test.ts` contains `archived-status-detail`
    - `src/domain/project-detail-routes.test.ts` contains `archived-maturity-detail`
    - `src/domain/project-detail-routes.test.ts` contains `maybeProjectDetailPageProjectBySlug("archived-status-detail"`
    - `src/domain/project-detail-routes.test.ts` contains `maybeProjectDetailPageProjectBySlug("archived-maturity-detail"`
    - `src/domain/portfolio-surfaces.test.ts` contains `archived-status-public-placement`
    - `src/domain/portfolio-surfaces.test.ts` contains `archived-maturity-public-placement`
    - `bun run test src/domain/project-detail-routes.test.ts src/domain/portfolio-surfaces.test.ts` exits 0
  </acceptance_criteria>
  <done>Archived project status and maturity records are rejected by the shared public project predicate, and project detail plus portfolio surface tests prove the selector contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Guard the social preview target flow and release checks</name>
  <files>src/domain/social-previews.test.ts</files>
  <read_first>
    - `.planning/phases/29-archived-project-public-filter-guard/29-CONTEXT.md`
    - `.planning/v1.5-MILESTONE-AUDIT.md`
    - `.planning/phases/24-social-image-data-contract/24-01-SUMMARY.md`
    - `.planning/phases/28-verification-and-release-contract/28-01-SUMMARY.md`
    - `.planning/phases/28-verification-and-release-contract/28-02-SUMMARY.md`
    - `.planning/phases/28-verification-and-release-contract/28-03-SUMMARY.md`
    - `standards/core/testing.md`
    - `src/domain/projects.ts`
    - `src/domain/social-previews.ts`
    - `src/domain/social-previews.test.ts`
    - `package.json`
  </read_first>
  <behavior>
    - Test 1: `socialPreviewTargets({ projects: fixtureProjects }).map((target) => target.routePath)` excludes `projectDetailPath(archivedStatusProject)` when the fixture has `status: "archived"`, `tier: "flagship"`, `includeInProjectIndex: true`, and authored `detail` data per D-03 and D-05.
    - Test 2: The same target list excludes `projectDetailPath(archivedMaturityProject)` when the fixture has `maturity: "archived"`, `tier: "flagship"`, `includeInProjectIndex: true`, and authored `detail` data per D-03 and D-06.
    - Test 3: The target list still includes the selected public project fixture and public writing/theme fixtures already covered by the test per D-04 and D-09.
    - Test 4: `src/domain/social-previews.ts` remains free of a separate `status === "archived"` or `maturity === "archived"` project guard per D-03.
  </behavior>
  <action>Extend social preview regression coverage without adding visibility logic to `src/domain/social-previews.ts` per D-03 and D-09. In the existing `filters fixture records through existing public project, writing, and theme selectors` test in `src/domain/social-previews.test.ts`, add `archivedStatusProject = makeProjectStory({ slug: "archived-status-detail", status: "archived", displayOrder: 60 })` and `archivedMaturityProject = makeProjectStory({ slug: "archived-maturity-detail", maturity: "archived", displayOrder: 70 })`. Include both projects in the `projects` fixture array passed to `socialPreviewTargets({ projects, writingEntries, themes })`. Keep their default fixture fields selected-looking: `placement: "home"`, `tier: "flagship"`, `includeInProjectIndex: true`, and non-empty `detail`. Update assertions so the expected route list still contains only `/projects`, `projectDetailPath(selectedProject)`, `/writing`, `writingDetailPath(publicWriting)`, `/themes`, and `themeDetailPath(publicTheme)`, and add explicit `not.toContain(projectDetailPath(archivedStatusProject))` and `not.toContain(projectDetailPath(archivedMaturityProject))` checks. Do not edit `src/domain/social-previews.ts`; the implementation must flow from `projectDetailPageProjects(sources.projects)` per D-03. After targeted tests pass, run `git status --short public/social/generated` before and after `bun run verify:social-previews`; expected output is empty both times because D-04 preserves selected public projects and default generated targets. If `bun run verify:social-previews` fails because the default route target list genuinely changed, run `bun run generate:social-previews`, inspect the generated diff, rerun `bun run verify:social-previews`, `bun run verify:static`, and `bun run verify:release`, and document the actual managed generated files in the summary per D-10. If generated files change without a default target change, fix the selector/test issue and leave `public/social/generated/**` untouched. In the final `29-01-SUMMARY.md`, explicitly name the closed audit gap IDs `SHARE-02`, `VERIFY-01`, `INT-01`, and `FLOW-01` per D-11.</action>
  <verify>
    <automated>bun run test src/domain/project-detail-routes.test.ts src/domain/social-previews.test.ts src/domain/portfolio-surfaces.test.ts && bun run verify:social-previews && bun run verify:static && bun run verify:release && bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/social-previews.test.ts` contains `archived-status-detail`
    - `src/domain/social-previews.test.ts` contains `archived-maturity-detail`
    - `src/domain/social-previews.test.ts` contains `projectDetailPath(archivedStatusProject)`
    - `src/domain/social-previews.test.ts` contains `projectDetailPath(archivedMaturityProject)`
    - `src/domain/social-previews.ts` still contains `projectDetailPageProjects(sources.projects)`
    - `src/domain/social-previews.ts` does not contain `status === "archived"`
    - `src/domain/social-previews.ts` does not contain `maturity === "archived"`
    - `git status --short public/social/generated` has no output unless `bun run verify:social-previews` first proved a genuine default target change
    - `bun run test src/domain/project-detail-routes.test.ts src/domain/social-previews.test.ts src/domain/portfolio-surfaces.test.ts && bun run verify:social-previews && bun run verify:static && bun run verify:release && bun run verify` exits 0
    - `.planning/phases/29-archived-project-public-filter-guard/29-01-SUMMARY.md` contains `SHARE-02`, `VERIFY-01`, `INT-01`, and `FLOW-01`
  </acceptance_criteria>
  <done>Social preview regression coverage proves archived selected-looking project records are filtered through the shared project selector, and release verification proves the downstream social preview/static/release contracts still pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| curated project data -> public selectors | Checked-in project records can contain hidden, excluded, archived, or otherwise non-public values; selectors decide what becomes public. |
| public selectors -> social preview targets | `projectDetailPageProjects()` output crosses into generated social preview target derivation and then into metadata/static/release verification. |
| generated asset state -> release evidence | `public/social/generated/**` must not churn unless helper-derived default target output genuinely changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-29-01 | I | `src/domain/projects.ts` public selector | mitigate | Reject `project.status === "archived"` and `project.maturity === "archived"` in `isPublicProjectIndexProject()` so archived records cannot leak into public/detail/social route surfaces. |
| T-29-02 | S | `src/domain/social-previews.ts` project target derivation | mitigate | Keep social previews deriving project targets from `projectDetailPageProjects(sources.projects)` and add tests proving no separate social-preview-only guard is needed. |
| T-29-03 | T | `public/social/generated/**` managed assets | mitigate | Run `bun run verify:social-previews` and check `git status --short public/social/generated`; regenerate only after a proven default target change. |
| T-29-04 | R | Phase audit closure evidence | mitigate | Final summary and verification notes must name closed gap IDs `SHARE-02`, `VERIFY-01`, `INT-01`, and `FLOW-01` with exact commands run. |
| T-29-05 | D | Aggregate release verification | mitigate | Run targeted Vitest, `bun run verify:social-previews`, `bun run verify:static`, `bun run verify:release`, and aggregate `bun run verify` before marking the phase complete. |
</threat_model>

<verification>
Overall phase verification:

```bash
bun run test src/domain/project-detail-routes.test.ts src/domain/social-previews.test.ts src/domain/portfolio-surfaces.test.ts
bun run verify:social-previews
bun run verify:static
bun run verify:release
bun run verify
git status --short public/social/generated
```

Expected results:
- The targeted Vitest command exits 0.
- `bun run verify:social-previews` exits 0.
- `bun run verify:static` exits 0.
- `bun run verify:release` exits 0.
- `bun run verify` exits 0.
- `git status --short public/social/generated` prints no output unless the executor documented a proven default target change and regenerated managed files per D-10.
</verification>

<success_criteria>
Phase 29 is complete when archived project `status` and archived project `maturity` are excluded from public/detail/social selector output, tests prove the archived fixture contract, generated social preview assets remain stable or are explicitly regenerated after proven target drift, and verification closes `SHARE-02`, `VERIFY-01`, `INT-01`, and `FLOW-01`.
</success_criteria>

<output>
After completion, create `.planning/phases/29-archived-project-public-filter-guard/29-01-SUMMARY.md`
</output>
