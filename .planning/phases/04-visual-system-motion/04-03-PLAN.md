---
phase: 04-visual-system-motion
plan: 03
type: execute
wave: 3
depends_on:
  - 04-01
  - 04-02
files_modified:
  - package.json
  - scripts/verify-static.ts
  - scripts/verify-visual-system.ts
autonomous: true
requirements:
  - EXP-05
  - MOTION-01
  - MOTION-02
  - MOTION-03
  - MOTION-04
  - MOTION-05
user_setup: []
must_haves:
  truths:
    - "Developer can run `bun run verify` and get static/source proof that visual and motion boundaries remain local, cleaned up, dark-primary, and dependency-light."
    - "Generated static HTML still preserves Phase 03 route copy, metadata, JSON-LD, project anchors, OpenLinks placement, local assets, sitemap, robots, and no-runtime-GitHub guarantees before hydration."
    - "Browser evidence covers `/`, `/projects`, `/about`, and `/contact` at 1440x900, 390x844, and 320x844 in dark mode with no horizontal overflow, text overlap, clipped controls, console errors, or header/content collisions."
    - "Browser evidence proves reduced motion disables non-essential motion, keyboard focus is visible/logical, and motion cleanup remains isolated to UI helpers."
  artifacts:
    - path: "scripts/verify-visual-system.ts"
      provides: "Source-level visual/motion/domain-boundary guard for Phase 04"
    - path: "package.json"
      provides: "`verify:visual-system` script and aggregate `bun run verify` integration"
    - path: "scripts/verify-static.ts"
      provides: "Static output guard for dark shell, local visual hooks/assets, route semantics, and reduced-motion CSS output"
    - path: ".planning/phases/04-visual-system-motion/04-03-SUMMARY.md"
      provides: "Executor evidence for aggregate verification, browser matrix, reduced motion, keyboard/focus, overflow/overlap, and cleanup proof"
  key_links:
    - from: "package.json"
      to: "scripts/verify-visual-system.ts"
      via: "verify:visual-system in aggregate verify"
      pattern: "verify:visual-system"
    - from: "scripts/verify-visual-system.ts"
      to: "src/components/ReactiveSurface.tsx"
      via: "cleanup token/source guard"
      pattern: "onCleanup|removeEventListener|cancelAnimationFrame|visibilitychange"
    - from: "scripts/verify-visual-system.ts"
      to: "src/domain"
      via: "forbidden UI/motion/DOM import scan"
      pattern: "src/domain"
    - from: "browser evidence"
      to: "Phase 04 requirements"
      via: "viewport, reduced-motion, keyboard, overlap, and overflow matrix"
      pattern: "1440x900|390x844|320x844|prefers-reduced-motion"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-05-26T17-22-53
generated_at: 2026-05-26T17:56:25.711Z
---

<objective>
Harden static/source verification and capture final browser evidence for the Phase 04 visual system and motion work.

Purpose: make EXP-05 and MOTION-01 through MOTION-05 observable through `bun run verify`, source/static guards, and required desktop/mobile/reduced-motion/keyboard visual checks.
Output: a repo-owned visual-system verifier, static-output checks, aggregate verify integration, and Phase 04 summary evidence for all required viewports and accessibility/motion gates.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-visual-system-motion/04-CONTEXT.md
@.planning/phases/04-visual-system-motion/04-RESEARCH.md
@.planning/phases/04-visual-system-motion/04-UI-SPEC.md
@.planning/phases/04-visual-system-motion/04-01-SUMMARY.md
@.planning/phases/04-visual-system-motion/04-02-SUMMARY.md
@.planning/phases/03-portfolio-surfaces-seo/03-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@package.json
@scripts/verify-static.ts
@src/styles/app.css
@src/components/ReactiveSurface.tsx
@src/components/visual-motion.ts
@src/routes/index.tsx
@src/routes/projects.tsx
@src/routes/about.tsx
@src/routes/contact.tsx
@src/domain/projects.ts
@src/domain/profile.ts
@src/domain/routes.ts
@src/domain/seo.ts

<interfaces>
From `package.json`:
```json
{
  "scripts": {
    "verify": "bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run build && bun run verify:static"
  }
}
```

Expected new script:
```json
{
  "scripts": {
    "verify:visual-system": "bun run scripts/verify-visual-system.ts"
  }
}
```

From Plan 02:
```ts
export function canRunDecorativeMotion(input: DecorativeMotionGateInput): boolean;
export function ReactiveSurface(props: ReactiveSurfaceProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add visual-system source guard to aggregate verification</name>
  <read_first>
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-01 through D-14.
    - `.planning/phases/04-visual-system-motion/04-RESEARCH.md` Validation Architecture, Security Domain, and Common Pitfalls.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Visual Assets, Motion Contract, Accessibility Contract, and Verification Contract.
    - `package.json`
    - `scripts/verify-no-github-runtime.ts`
    - `src/components/ReactiveSurface.tsx`
    - `src/components/visual-motion.ts`
    - `src/domain`
    - `src/styles/app.css`
  </read_first>
  <files>package.json, scripts/verify-visual-system.ts</files>
  <behavior>
    - Guard 1: the verifier exits non-zero if `src/domain/*` imports `solid-js`, `mystic-ui`, `src/components/*`, DOM globals, motion helpers, or animation frame APIs.
    - Guard 2: the verifier exits non-zero if `package.json` adds runtime motion/physics/3D dependencies such as `motion`, `gsap`, `three`, `matter-js`, `rapier`, or `@solid-primitives/spring`.
    - Guard 3: the verifier exits non-zero if `ReactiveSurface.tsx` exists without `onCleanup`, `removeEventListener`, `cancelAnimationFrame`, `visibilitychange`, and the pure `canRunDecorativeMotion` gate.
    - Guard 4: the verifier exits non-zero if route/style files introduce forbidden visual patterns such as `gradient-orb`, `bokeh`, `blur-3xl`, `from-purple`, `bg-white`, `bg-stone-50`, or `text-zinc-950`.
  </behavior>
  <action>
    Create `scripts/verify-visual-system.ts` as a Bun TypeScript verifier using Node filesystem/path APIs only. Model it after existing repo-owned verification scripts: clear helper functions, explicit failure messages, deterministic source scanning, no shelling out, no Python, no network, no new dependencies, and no hidden foreign-language programs inside strings.

    The script must:
    - Recursively scan `src/domain` and fail on UI/motion/DOM terms that would violate D-12 and MOTION-05: `solid-js`, `mystic-ui`, `src/components`, `window`, `document`, `navigator`, `matchMedia`, `requestAnimationFrame`, `addEventListener`, `ReactiveSurface`, `visual-motion`, and `onCleanup`.
    - Parse or read `package.json` and fail if runtime dependencies or devDependencies include new runtime animation/physics/3D packages: `motion`, `gsap`, `three`, `matter-js`, `rapier`, `@react-three/fiber`, `@solid-primitives/spring`, or package names containing `webgpu` unless this plan summary documents an explicit approved exception. No exception is expected for Phase 04.
    - If `src/components/ReactiveSurface.tsx` exists, fail unless it contains `onCleanup`, `removeEventListener`, `cancelAnimationFrame`, `visibilitychange`, `canRunDecorativeMotion`, `matchMedia`, and no `setInterval`, `setTimeout`, `ResizeObserver`, `MutationObserver`, `IntersectionObserver`, `from "motion"`, `gsap`, `three`, `matter-js`, or `rapier`.
    - Scan `src/styles/app.css` plus route/layout files and fail on forbidden visual-pattern strings: `gradient-orb`, `bokeh`, `blur-3xl`, `from-purple`, `via-purple`, `to-purple`, `bg-white`, `bg-stone-50`, `text-zinc-950`, `letter-spacing: -`, and remote CSS asset URLs matching `url("http`.
    - Log a concise success summary listing domain files scanned, motion dependency check, cleanup guard check, and visual-pattern check.

    Add package script `"verify:visual-system": "bun run scripts/verify-visual-system.ts"` and wire it into aggregate `"verify"` after `verify:no-github-runtime` and before `bun run build`. Preserve the existing `bun run verify` sequence otherwise.
  </action>
  <acceptance>
    - `rg "verify:visual-system" package.json` confirms aggregate verification has the new guard.
    - `rg "src/domain|ReactiveSurface|canRunDecorativeMotion|cancelAnimationFrame|removeEventListener|gradient-orb|@solid-primitives/spring|matter-js" scripts/verify-visual-system.ts` finds the required checks.
    - `bun run verify:visual-system` exits 0 after Plan 01 and Plan 02 work.
    - Temporarily inserting a forbidden string is not required; the script's behavior must be clear from code and final `bun run verify` must run it.
  </acceptance>
  <verify>
    <automated>bun run verify:visual-system && bun run format:check && bun run check && bun run typecheck</automated>
  </verify>
  <done>`bun run verify` now includes a deterministic visual/motion/domain-boundary source guard with no new dependencies or external services.</done>
</task>

<task type="auto">
  <name>Task 2: Harden static output checks for visual and reduced-motion invariants</name>
  <read_first>
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-01, D-03, D-07, D-10, D-11, and D-14.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Visual Assets, Reduced-motion and device gates, and Static preservation verification.
    - `.planning/phases/03-portfolio-surfaces-seo/03-03-SUMMARY.md` static verifier patterns and reduced-motion bug fix.
    - `scripts/verify-static.ts`
    - `src/domain/routes.ts`
    - `src/domain/seo.ts`
    - `src/styles/app.css`
  </read_first>
  <files>scripts/verify-static.ts</files>
  <action>
    Extend `scripts/verify-static.ts` only for static/generated-output invariants, not browser layout measurements. Preserve existing route text, metadata, JSON-LD, sitemap, robots, asset, residue, and no-runtime-GitHub checks.

    Add assertions that generated HTML and emitted assets preserve Phase 04 fundamentals:
    - Every prerendered route still renders the dark document/shell before hydration, such as `<html class="dark">` and `site-shell`/main landmark content.
    - The home route output contains the Phase 04 local visual hook, such as `brand-material`, while still containing live `Peter Ryszkiewicz`, `Browse projects`, `Now building`, and flagship story text before hydration.
    - Generated output references only local asset paths for runtime/decorative visual material. Allow canonical absolute metadata URLs that map to checked-in assets, such as `https://www.brightbuilds.us/social/bright-builds-og.png` in OG/Twitter metadata, but fail on remote decorative/runtime visual assets such as remote `<img>` visuals or CSS background/image URLs matching `url("http` in generated HTML or emitted CSS.
    - Emitted CSS includes `prefers-reduced-motion` and `:root *` or an equally specific selector for reduced transition/animation durations.
    - Generated HTML still contains the Phase 03 OpenLinks footer/about/contact placement, project anchors, Writing empty state or entries, hidden/excluded aggregate note, metadata, JSON-LD, sitemap, robots, and social/icon asset references.

    Do not add browser test packages, accessibility-audit packages, performance-audit packages, runtime dependencies, or HTML parsing dependencies. Keep the verifier filesystem-only and deterministic.
  </action>
  <acceptance>
    - `rg "brand-material|prefers-reduced-motion|:root \\*|site-shell|remote.*asset|OpenLinks profile" scripts/verify-static.ts` finds the new static checks.
    - `bun run build && bun run verify:static` passes.
    - `bun run verify:no-github-runtime && bun run verify:visual-system` passes after the static verifier change.
  </acceptance>
  <verify>
    <automated>bun run build && bun run verify:static && bun run verify:visual-system</automated>
  </verify>
  <done>Static verification now catches loss of dark shell, local visual hook/assets, reduced-motion CSS, route semantics, OpenLinks placement, project anchors, and existing SEO/static-output guarantees.</done>
</task>

<task type="auto">
  <name>Task 3: Run aggregate and browser evidence matrix</name>
  <read_first>
    - `AGENTS.md` Repo-Local Guidance for dark-primary visual verification.
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-01 through D-14.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Verification Contract.
    - `package.json`
    - `scripts/verify-visual-system.ts`
    - `scripts/verify-static.ts`
    - `src/components/ReactiveSurface.tsx`
    - `src/styles/app.css`
  </read_first>
  <files>.planning/phases/04-visual-system-motion/04-03-SUMMARY.md</files>
  <action>
    Run final aggregate verification with `bun run verify`. Then start the local dev server on an available localhost port using `bun run dev -- --host 127.0.0.1`. Use the Browser plugin or equivalent browser automation; do not add browser test dependencies unless the existing Browser path is unavailable and the summary documents why.

    Browser checks must cover `/`, `/projects`, `/about`, and `/contact` at exactly these viewports in dark mode: `1440x900`, `390x844`, and `320x844`. For every route and viewport, record evidence for:
    - `.dark` root and dark body/background rendering.
    - No horizontal overflow: `document.documentElement.scrollWidth <= document.documentElement.clientWidth`.
    - No incoherent text/control overlap or clipped CTA/link/chip/project title/contact URL text. Use DOM bounding-box inspection plus screenshots when useful; ignore the hidden skip link until focused.
    - Header/nav wrap, footer, route headings, cards, chips, and contact URLs remain readable.
    - Browser console has no errors or warnings attributable to Phase 04 code.

    Run reduced-motion verification in a real browser mode that emulates or forces `prefers-reduced-motion: reduce`. Confirm `matchMedia("(prefers-reduced-motion: reduce)").matches === true`, non-essential transitions/animations are collapsed, pointer-following/reactive effects are disabled, and all route content/links remain usable. Confirm coarse/small viewport behavior at mobile widths disables pointer-following effects while preserving tap/focus color/border feedback.

    Run keyboard/focus verification by tabbing through at least: skip link, brand, nav links, home `Browse projects` CTA, home current-focus/project links, `/projects` project anchors, about OpenLinks identity link, contact cards, and footer OpenLinks link. Focus rings must be visible, order must be logical, and no important content may require hover.

    Prove motion cleanup stays isolated by running `bun run verify:visual-system`, inspecting `src/components/ReactiveSurface.tsx` for `onCleanup`, `removeEventListener`, `cancelAnimationFrame`, `visibilitychange`, and `canRunDecorativeMotion`, and recording that `rg "ReactiveSurface|visual-motion|matchMedia|requestAnimationFrame|addEventListener|document|window" src/domain` returns no matches. If the browser tool can navigate between routes after pointer interaction, also navigate away and confirm no console errors occur.

    Record commands run, dev server URL/port, viewports, routes, reduced-motion result, keyboard/focus result, overflow/overlap result, cleanup/domain-boundary proof, and residual risks in `.planning/phases/04-visual-system-motion/04-03-SUMMARY.md`. Stop the dev server before completing the task.
  </action>
  <acceptance>
    - `bun run verify` exits 0.
    - Browser checks cover all four routes at `1440x900`, `390x844`, and `320x844`.
    - Browser checks confirm no horizontal overflow, no incoherent text/control overlap, no clipped important text, no Phase 04 console errors, and visible keyboard focus.
    - Reduced-motion browser check confirms non-essential motion is disabled and content remains fully usable.
    - Summary records cleanup proof, including `bun run verify:visual-system` and a domain scan with no motion/DOM matches.
    - Dev server is stopped before completion.
  </acceptance>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <done>Phase 04 has aggregate verification plus browser evidence for desktop/mobile dark rendering, reduced motion, keyboard/focus access, no overlap, no horizontal overflow, and isolated motion cleanup.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| source tree -> verification scripts | Local source code is scanned to enforce visual/motion/domain boundaries before build. |
| generated output -> static verifier | Static HTML/CSS/assets are trusted as proof of pre-hydration route behavior. |
| browser runtime -> verification evidence | Browser checks observe layout, focus, reduced motion, console, and overflow behavior that static checks cannot prove. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03-01 | Tampering | `scripts/verify-visual-system.ts` | mitigate | Add deterministic source scans for forbidden domain coupling, motion dependencies, missing cleanup tokens, forbidden visual motifs, and remote visual assets. |
| T-04-03-02 | Information Disclosure | aggregate verification and source/output scans | mitigate | Preserve `verify:no-github-runtime`, static forbidden token/API checks, and add visual verifier scans without reading secrets or environment variables. |
| T-04-03-03 | Denial of Service | browser/layout verification | mitigate | Keep Phase 04 browser checks local, bounded to four routes and three viewports, and stop the dev server after evidence is captured. |
| T-04-03-04 | Repudiation | Phase 04 visual evidence | mitigate | Record exact commands, routes, viewport sizes, reduced-motion result, keyboard/focus result, cleanup proof, and residual risks in the plan summary. |
</threat_model>

<verification>
Run `bun run verify:visual-system`, `bun run build && bun run verify:static`, and final `bun run verify`. Perform browser checks for `/`, `/projects`, `/about`, and `/contact` at `1440x900`, `390x844`, and `320x844`, with dark rendering, no-overflow/no-overlap, reduced-motion, keyboard/focus, console, and cleanup/domain-boundary evidence.
</verification>

<success_criteria>
`bun run verify` includes visual/motion boundary guards and passes, static output proves preserved route/SEO/local-asset/reduced-motion invariants, and the Phase 04 summary records required desktop/mobile/reduced-motion/keyboard/no-overlap/no-overflow/cleanup evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-system-motion/04-03-SUMMARY.md`.
</output>
