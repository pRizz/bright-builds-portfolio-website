---
phase: 04-visual-system-motion
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - src/components/visual-motion.ts
  - src/components/visual-motion.test.ts
  - src/components/ReactiveSurface.tsx
  - src/styles/app.css
  - src/routes/index.tsx
  - src/routes/projects.tsx
autonomous: true
requirements:
  - EXP-05
  - MOTION-02
  - MOTION-03
  - MOTION-04
  - MOTION-05
user_setup: []
must_haves:
  truths:
    - "Visitor on capable devices experiences restrained non-essential reactive polish on project/current-focus surfaces without content being delayed, hidden, or layout-shifted."
    - "Visitor using reduced motion, coarse pointer, small viewport, hidden tab, or low-power/save-data conditions receives the complete static site with non-essential motion disabled."
    - "Keyboard and touch users receive equivalent focus/tap feedback for every important interactive surface."
    - "Developer can verify every listener, animation frame, timer, or observer introduced for motion is cleaned up on unmount or inactivity and stays out of src/domain modules."
  artifacts:
    - path: "src/components/visual-motion.ts"
      provides: "Pure decorative-motion gate decisions for reduced motion, pointer, viewport, visibility, and low-power/save-data inputs"
      exports: ["canRunDecorativeMotion", "decorativeMotionMediaQueries"]
    - path: "src/components/visual-motion.test.ts"
      provides: "Vitest coverage for pure motion gate behavior"
    - path: "src/components/ReactiveSurface.tsx"
      provides: "The only Solid UI helper for optional pointer-reactive CSS custom property updates and cleanup"
    - path: "src/styles/app.css"
      provides: "Reactive surface/card CSS, hover/focus parity, and reduced/coarse/small viewport fallbacks"
    - path: "src/routes/index.tsx"
      provides: "Non-essential reactive wrapper for home visual/project surfaces"
    - path: "src/routes/projects.tsx"
      provides: "Non-essential reactive wrapper for project-card surfaces"
  key_links:
    - from: "src/components/ReactiveSurface.tsx"
      to: "src/components/visual-motion.ts"
      via: "pure gate before DOM listener/RAF setup"
      pattern: "canRunDecorativeMotion|decorativeMotionMediaQueries"
    - from: "src/components/ReactiveSurface.tsx"
      to: "Solid lifecycle cleanup"
      via: "onMount/onCleanup around addEventListener and requestAnimationFrame"
      pattern: "onCleanup|removeEventListener|cancelAnimationFrame"
    - from: "src/routes/index.tsx"
      to: "src/components/ReactiveSurface.tsx"
      via: "UI-only wrapper around non-essential visual surfaces"
      pattern: "ReactiveSurface"
    - from: "src/domain/*"
      to: "motion layer"
      via: "no dependency allowed"
      pattern: "no imports from src/components or DOM APIs in src/domain"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-05-26T17-22-53
generated_at: 2026-05-26T17:56:25.711Z
---

<objective>
Add restrained reactive motion as a UI-only enhancement with explicit reduced-motion, device, visibility, low-power, and cleanup gates.

Purpose: satisfy MOTION-02 through MOTION-05 without adding runtime animation dependencies, physics engines, route/page motion, scroll-jacking, or domain coupling.
Output: one pure motion-gate helper with tests, one small Solid `ReactiveSurface` helper with cleanup, CSS-only card/field effects, and limited wiring to non-essential home/project surfaces.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-visual-system-motion/04-CONTEXT.md
@.planning/phases/04-visual-system-motion/04-RESEARCH.md
@.planning/phases/04-visual-system-motion/04-UI-SPEC.md
@.planning/phases/04-visual-system-motion/04-01-SUMMARY.md
@.planning/phases/03-portfolio-surfaces-seo/03-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/styles/app.css
@src/routes/index.tsx
@src/routes/projects.tsx
@src/domain/projects.ts

<interfaces>
New `src/components/visual-motion.ts` contract to create:
```ts
export type DecorativeMotionGateInput = {
  prefersReducedMotion: boolean;
  hasFinePointer: boolean;
  isLargeViewport: boolean;
  isPageVisible: boolean;
  maybeSaveData?: boolean;
};
export function canRunDecorativeMotion(input: DecorativeMotionGateInput): boolean;
export const decorativeMotionMediaQueries: {
  reducedMotion: "(prefers-reduced-motion: reduce)";
  finePointer: "(pointer: fine)";
  largeViewport: "(min-width: 641px)";
};
```

New `src/components/ReactiveSurface.tsx` contract to create:
```tsx
import type { JSX } from "solid-js";

type ReactiveSurfaceProps = {
  children: JSX.Element;
  class?: string;
};

export function ReactiveSurface(props: ReactiveSurfaceProps): JSX.Element;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add pure decorative-motion gates</name>
  <read_first>
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-05, D-06, D-07, D-08, D-12, and D-14.
    - `.planning/phases/04-visual-system-motion/04-RESEARCH.md` Pure Motion Gate Helper and Common Pitfalls.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Motion Contract, Reduced-motion and device gates, and Accessibility Contract.
    - `AGENTS.bright-builds.md` nullable `maybe` naming and functional-core guidance.
    - `src/components/SiteLayout.tsx`
  </read_first>
  <files>src/components/visual-motion.ts, src/components/visual-motion.test.ts</files>
  <behavior>
    - Test 1: `canRunDecorativeMotion` returns true only when reduced motion is false, fine pointer is true, large viewport is true, page is visible, and `maybeSaveData` is not true.
    - Test 2: each disabled condition returns false independently: reduced motion, coarse/non-fine pointer, small viewport, hidden tab, and save-data/low-power signal.
    - Test 3: `decorativeMotionMediaQueries` exposes exact media query strings for reduced motion, fine pointer, and minimum 641px viewport.
  </behavior>
  <action>
    Create `src/components/visual-motion.ts` as a UI-focused pure helper, not a domain module. Export `DecorativeMotionGateInput`, `decorativeMotionMediaQueries`, and `canRunDecorativeMotion(input)`. The function must be deterministic and data-in/data-out only; it must not reference `window`, `document`, `navigator`, DOM APIs, Solid lifecycle APIs, timers, animation frames, or Mystic internals. Name the optional save-data/low-power field `maybeSaveData` to match the Bright Builds nullable naming rule.

    Create `src/components/visual-motion.test.ts` with focused Vitest tests and explicit Arrange, Act, Assert comments unless a table row is trivial. Keep one concern per test. These tests prove the reduced-motion, coarse pointer, small viewport, hidden-tab, and low-power/save-data gates from D-07 and D-08 before any Solid helper is implemented.
  </action>
  <acceptance>
    - `rg "DecorativeMotionGateInput|canRunDecorativeMotion|decorativeMotionMediaQueries|maybeSaveData" src/components/visual-motion.ts src/components/visual-motion.test.ts` finds the pure gate contract and tests.
    - `rg "window|document|navigator|requestAnimationFrame|addEventListener|onMount|onCleanup|mystic-ui" src/components/visual-motion.ts` returns no matches.
    - `rg "prefersReducedMotion|hasFinePointer|isLargeViewport|isPageVisible|maybeSaveData" src/components/visual-motion.test.ts` confirms all gate inputs are covered.
  </acceptance>
  <verify>
    <automated>bun run test -- src/components/visual-motion.test.ts && bun run typecheck</automated>
  </verify>
  <done>Decorative motion eligibility is a pure, tested UI helper with no DOM, Solid, timer, asset, Mystic, network, or domain dependency.</done>
</task>

<task type="auto">
  <name>Task 2: Create the single cleanup-safe ReactiveSurface helper</name>
  <read_first>
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-05, D-06, D-07, D-08, D-12, D-13, and D-14.
    - `.planning/phases/04-visual-system-motion/04-RESEARCH.md` Solid Cleanup Shell, Pattern 2, and Common Pitfalls.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Motion Contract and Performance limits.
    - `src/components/visual-motion.ts`
    - `src/styles/app.css`
  </read_first>
  <files>src/components/ReactiveSurface.tsx, src/styles/app.css</files>
  <action>
    Create `src/components/ReactiveSurface.tsx` as the only Solid helper for Phase 04 motion. It should render a semantic-neutral wrapper such as `<div class="reactive-surface ...">` around `props.children`; it must not create buttons, links, or hidden content. Use `onMount` and `onCleanup` from Solid.

    On mount, read browser state in the imperative shell: `matchMedia(decorativeMotionMediaQueries.reducedMotion)`, `matchMedia(decorativeMotionMediaQueries.finePointer)`, `matchMedia(decorativeMotionMediaQueries.largeViewport)`, `document.visibilityState === "visible"`, and optional `navigator.connection?.saveData` where available. Feed those booleans into `canRunDecorativeMotion`. If the gate is false, return without adding pointer listeners, timers, observers, or animation frames.

    If the gate is true, add at most one `pointermove` listener on the mounted wrapper and one `visibilitychange` listener on `document`. Use at most one `requestAnimationFrame` handle at a time to write CSS custom properties such as `--pointer-x` and `--pointer-y`; do not run layout reads inside the animation frame. Cancel any pending frame when the document becomes hidden and on cleanup. Remove every listener in `onCleanup`. Do not use `setInterval`, persistent timers, observers, runtime animation libraries, physics libraries, route/page transitions, or domain imports.

    Extend `src/styles/app.css` with `.reactive-surface` and `.reactive-card` CSS. Effects must be non-essential, low-opacity, and limited to transform, opacity, background, border-color, or box-shadow. Pair hover states with focus-within/focus-visible states. Disable pointer-following and transforms under reduced motion, coarse pointer, and max-width 640px media gates. Keep card lift at max `translateY(-2px)`, no scale above `1.01`, and no layout-affecting animation.
  </action>
  <acceptance>
    - `rg "onMount|onCleanup|canRunDecorativeMotion|decorativeMotionMediaQueries|matchMedia|visibilitychange|requestAnimationFrame|cancelAnimationFrame|removeEventListener" src/components/ReactiveSurface.tsx` confirms lifecycle gates and cleanup paths.
    - `rg "setInterval|setTimeout|ResizeObserver|MutationObserver|IntersectionObserver|from \\\"motion\\\"|gsap|three|matter-js|rapier|src/domain" src/components/ReactiveSurface.tsx` returns no matches.
    - `rg "reactive-surface|reactive-card|--pointer-x|--pointer-y|pointer: coarse|max-width: 640px|prefers-reduced-motion" src/styles/app.css` confirms CSS hooks and fallbacks.
    - `rg "scale\\(1\\.0[2-9]|scale\\(1\\.[1-9]|translateY\\(-[3-9]px|width [0-9].*transition|height [0-9].*transition" src/styles/app.css` returns no matches.
  </acceptance>
  <verify>
    <automated>bun run format:check && bun run check && bun run typecheck && bun run test -- src/components/visual-motion.test.ts</automated>
  </verify>
  <done>`ReactiveSurface` is the single UI-only motion helper, gates all decorative work before listener setup, and cleans up frames/listeners on unmount or hidden-tab inactivity.</done>
</task>

<task type="auto">
  <name>Task 3: Wire restrained reactive states to non-essential surfaces</name>
  <read_first>
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12, and D-14.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Motion Contract, Layout, Accessibility Contract, and Verification Contract.
    - `src/components/ReactiveSurface.tsx`
    - `src/routes/index.tsx`
    - `src/routes/projects.tsx`
    - `src/styles/app.css`
  </read_first>
  <files>src/routes/index.tsx, src/routes/projects.tsx, src/styles/app.css</files>
  <action>
    Import `ReactiveSurface` only into route/component layers that need the optional effect. Wrap non-essential visual groups, not individual semantic links in a way that changes focus order. Good targets are the home flagship story grid, the home current-focus panel, and/or the project section grid. Do not wrap the whole `main`, do not delay route content, do not hide content until hydration, and do not make the pointer effect necessary for reading or navigation.

    Apply `.reactive-card` or equivalent classes to story cards, focus rows, and project anchor cards so capable-pointer users get restrained border/highlight/lift feedback. Ensure every effect has a keyboard-visible equivalent through `:focus-within` or existing link focus states, and ensure touch/coarse pointer users still see static border/color feedback. Keep all important project links as native anchors with existing hrefs and rel values.

    Preserve all Phase 03 metadata, JSON-LD, sitemap/static verification text, route headings, project anchors, OpenLinks placement, hidden/excluded note, Writing empty state, and domain selectors. Do not modify `src/domain/*`, `package.json`, or add any dependency. The only motion logic in this plan should be `src/components/visual-motion.ts`, `src/components/ReactiveSurface.tsx`, route imports, and CSS.
  </action>
  <acceptance>
    - `rg "ReactiveSurface" src/routes/index.tsx src/routes/projects.tsx` confirms reactive wiring is limited to route/component layers.
    - `rg "reactive-card|focus-within|focus-visible|pointer: coarse|prefers-reduced-motion" src/styles/app.css` confirms hover/focus parity and device/reduced-motion fallbacks.
    - `rg "ReactiveSurface|visual-motion|matchMedia|requestAnimationFrame|addEventListener|document|window" src/domain` returns no matches.
    - `rg "motion|@solid-primitives/spring|gsap|three|matter-js|rapier" package.json` returns no matches.
    - `rg "metadataForRoute|jsonLdScriptContent|projectItemListJsonLd|OpenLinks profile|Some reviewed repositories stay hidden" src/routes/index.tsx src/routes/projects.tsx src/components/SiteLayout.tsx` confirms Phase 03 contracts are still present.
  </acceptance>
  <verify>
    <automated>bun run format:check && bun run check && bun run typecheck && bun run test && bun run build && bun run verify:static</automated>
  </verify>
  <done>Restrained reactive effects are wired only to non-essential UI surfaces, preserve static route content and anchors, provide keyboard/touch fallbacks, and keep domain modules dependency-free.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser capability APIs -> UI helper | `matchMedia`, visibility state, pointer events, and optional save-data signals control decorative behavior. |
| pointer events -> CSS variables | Untrusted high-frequency pointer input updates presentation-only custom properties. |
| route/component layer -> domain layer | UI motion must not create imports or behavioral dependencies inside pure domain modules. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Denial of Service | `ReactiveSurface` pointer/RAF work | mitigate | Gate setup with reduced-motion, fine-pointer, viewport, visibility, and save-data checks; keep one RAF handle; cancel on hidden tab and cleanup. |
| T-04-02-02 | Tampering | pointer CSS variable writes | mitigate | Write only presentation custom properties on the mounted wrapper and never mutate content, hrefs, dataset-backed domain values, or layout dimensions. |
| T-04-02-03 | Information Disclosure | motion/browser state helper | accept | The helper reads only local capability/visibility/save-data signals and sends nothing over network; no analytics or external services are introduced. |
| T-04-02-04 | Elevation of Privilege | route/component imports into domain | mitigate | Keep all motion helpers under `src/components/*` and add acceptance greps proving `src/domain/*` has no DOM, Solid lifecycle, Mystic, or motion imports. |
</threat_model>

<verification>
Run `bun run test -- src/components/visual-motion.test.ts`, `bun run format:check`, `bun run check`, `bun run typecheck`, `bun run test`, `bun run build`, `bun run verify:static`, and final `bun run verify` before completing this plan. Plan 03 performs browser reduced-motion, keyboard/focus, mobile, overlap, horizontal-overflow, and cleanup evidence capture.
</verification>

<success_criteria>
The site has restrained non-essential reactive motion on capable devices, full static usability under reduced/coarse/small/hidden/save-data conditions, tested pure motion gates, cleanup-safe Solid lifecycle code, no runtime animation dependency, and no domain coupling.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-system-motion/04-02-SUMMARY.md`.
</output>
