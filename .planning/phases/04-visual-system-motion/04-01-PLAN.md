---
phase: 04-visual-system-motion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/styles/app.css
  - src/components/SiteLayout.tsx
  - src/routes/index.tsx
  - src/routes/projects.tsx
  - src/routes/about.tsx
  - src/routes/contact.tsx
autonomous: true
requirements:
  - EXP-05
  - MOTION-01
  - MOTION-05
user_setup: []
must_haves:
  truths:
    - "Visitor sees a polished dark-primary Bright Builds-inspired visual system without unfinished template motifs, decorative gradient orbs, bokeh, or a one-note purple/slate palette."
    - "Visitor sees a local visual asset treatment in the real home surface while live semantic headings, route copy, and the Browse projects CTA remain the first-viewport focus."
    - "Visitor can use desktop, 390px mobile, and 320px mobile layouts without text overlap, horizontal overflow, layout jumps, or hover-only access to important content."
    - "Developer can confirm visual-system work stays in route/component/CSS layers and does not add UI, motion, DOM, or asset concerns to src/domain modules."
  artifacts:
    - path: "src/styles/app.css"
      provides: "Dark-primary visual system primitives, asset treatment, stable responsive dimensions, focus states, and reduced-motion-compatible CSS"
    - path: "src/components/SiteLayout.tsx"
      provides: "Stable shell/header/footer with low-intrusion OpenLinks placement"
    - path: "src/routes/index.tsx"
      provides: "Identity-first home route with local brand material and polished flagship/current-focus surfaces"
    - path: "src/routes/projects.tsx"
      provides: "Stable grouped project cards and anchors"
    - path: "src/routes/about.tsx"
      provides: "Stable theme surfaces with low-intrusion identity link"
    - path: "src/routes/contact.tsx"
      provides: "Stable collaboration cards and wrapping URLs"
  key_links:
    - from: "src/routes/index.tsx"
      to: "public/social/bright-builds-og.png"
      via: "decorative local brand-material CSS hook or aria-hidden visual surface"
      pattern: "brand-material|/social/bright-builds-og\\.png"
    - from: "src/routes/*.tsx"
      to: "src/styles/app.css"
      via: "shared visual-system classes instead of route-local one-off styling"
      pattern: "visual-|interactive-surface|brand-material|project-anchor-card"
    - from: "src/routes/*.tsx"
      to: "src/domain/*"
      via: "existing pure selectors and metadata helpers only"
      pattern: "peterProfile|homeProjects|projectItemListJsonLd|metadataForRoute"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 4-2026-05-26T17-22-53
generated_at: 2026-05-26T17:56:25.711Z
---

<objective>
Apply the Phase 04 dark-primary visual system, local asset treatment, and stable responsive layout primitives to the existing Phase 03 portfolio surfaces.

Purpose: deliver EXP-05, MOTION-01, and the visual half of MOTION-05 without changing curated content, route semantics, SEO contracts, OpenLinks priority, or domain boundaries.
Output: shared CSS primitives plus route/layout markup that feels like a polished Bright Builds successor while remaining semantic, local-asset-only, dark-primary, and stable on desktop and narrow mobile.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-visual-system-motion/04-CONTEXT.md
@.planning/phases/04-visual-system-motion/04-RESEARCH.md
@.planning/phases/04-visual-system-motion/04-UI-SPEC.md
@.planning/phases/03-portfolio-surfaces-seo/03-02-SUMMARY.md
@.planning/phases/03-portfolio-surfaces-seo/03-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/styles/app.css
@src/components/SiteLayout.tsx
@src/routes/index.tsx
@src/routes/projects.tsx
@src/routes/about.tsx
@src/routes/contact.tsx
@src/domain/projects.ts
@src/domain/profile.ts
@src/domain/routes.ts
@src/domain/seo.ts

<interfaces>
From `src/domain/projects.ts`:
```ts
export type ProjectStory = {
  slug: string;
  name: string;
  placement: ProjectPlacement;
  tier: ProjectTier;
  maturity: ProjectMaturity;
  status: ProjectStatus;
  themes: readonly string[];
  tags: readonly string[];
  role: string;
  oneLine: string;
  story: ProjectStoryDetails;
  links: readonly [ProjectLink, ...ProjectLink[]];
};
export function homeProjects(projects?: readonly ProjectStory[]): readonly HomeProjectStory[];
export function currentFocusProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function projectAnchorHref(project: ProjectStory): string;
export function projectLinkDisplayLabel(link: ProjectLink): string;
```

From `src/domain/profile.ts`:
```ts
export const peterProfile: Profile;
export function profileSameAsLinks(profile: Profile): string[];
```

From `src/domain/seo.ts`:
```ts
export const siteAssetLinks: readonly SiteAssetLink[];
export function metadataForRoute(route: SiteRoute, profile?: Profile): PageMetadata;
export function jsonLdScriptContent(value: unknown): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish dark-primary visual primitives and local brand material</name>
  <read_first>
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-01, D-02, D-03, D-05, D-09, D-10, D-11, D-12, and D-13.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Design System, Typography, Color, Layout, Visual Assets, Motion Contract, and Accessibility Contract.
    - `.planning/phases/03-portfolio-surfaces-seo/03-03-SUMMARY.md` reduced-motion and dark visual smoke evidence.
    - `src/styles/app.css`
    - `public/social/bright-builds-og.png`
    - `public/favicon.svg`
  </read_first>
  <files>src/styles/app.css</files>
  <action>
    Extend `src/styles/app.css` as the shared visual-system surface. Keep the site dark-primary per D-01: dominant `#050608`, secondary `#18181b`/`rgba(24,24,27,0.75)`, restrained emerald `#6ee7b7`, cyan `#67e8f9`, and amber `#fcd34d`. Do not introduce purple, purple-blue, bokeh, decorative orb, or stock-like background motifs per D-02.

    Add CSS primitives for the Phase 04 visual layer:
    - `.visual-stage` or equivalent unframed route-level composition hook for richer section spacing without wrapping whole pages in cards.
    - `.brand-material` using only checked-in local assets, preferably `url("/social/bright-builds-og.png")`, as a cropped/subtle decorative material. It must be non-blocking, low-opacity, unable to obscure text, and paired with route markup using `aria-hidden="true"` per D-03. Do not paste the social image as a literal screenshot card.
    - `.visual-surface`, `.interactive-surface`, and `.surface-link` style hooks for route cards/links with 8px radius max, 44px minimum interactive targets, visible cyan focus rings, and stable padding/gaps from the UI-SPEC spacing scale.
    - Stable card, chip, label, nav, footer, and link rules that use `min-width: 0`, `overflow-wrap: anywhere`, constrained grid tracks such as `minmax(0, 1fr)`, and no hover layout resizing per D-09 and D-10.
    - Hover and focus states that use transform/color/border/background only, max `translateY(-2px)`, no scale above `1.01`, and matching `:focus-visible`/`:focus-within` states so no important affordance is hover-only per D-05 and D-10.

    Preserve the existing high-specificity reduced-motion cascade using `:root *` selectors unless replacing it with an equivalent that keeps all non-essential transition/animation durations collapsed under `prefers-reduced-motion: reduce` per D-07. Do not add runtime motion dependencies, Three.js, WebGPU, Matter.js, Rapier, GSAP, remote image URLs, or source changes under `src/domain/*`.
  </action>
  <acceptance>
    - `rg "brand-material|visual-surface|interactive-surface|prefers-reduced-motion|:root \\*" src/styles/app.css` finds the new visual hooks and preserved reduced-motion guard.
    - `rg "/social/bright-builds-og\\.png|favicon\\.svg" src/styles/app.css src/routes/index.tsx` finds only local asset references for Phase 04 visual material.
    - `rg "gradient-orb|bokeh|blur-3xl|from-purple|via-purple|to-purple|bg-white|bg-stone-50|text-zinc-950|letter-spacing: -" src/styles/app.css` returns no matches.
    - `rg "matter-js|rapier|three|webgpu|gsap|from \\\"motion\\\"|@solid-primitives/spring" src package.json` returns no matches.
  </acceptance>
  <verify>
    <automated>bun run format:check && bun run check && bun run typecheck && bun run build && bun run verify:static</automated>
  </verify>
  <done>Shared CSS provides the Phase 04 dark-primary visual primitives, local brand-material treatment, stable layout constraints, focus states, and reduced-motion-compatible base styles without new runtime dependencies or domain changes.</done>
</task>

<task type="auto">
  <name>Task 2: Apply the visual shell to layout and home surfaces</name>
  <read_first>
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-01, D-03, D-04, D-09, D-10, D-11, D-12, and D-13.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Layout, Visual Assets, Copywriting Contract, and Accessibility Contract.
    - `/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/SKILL.md`
    - `src/components/SiteLayout.tsx`
    - `src/routes/index.tsx`
    - `src/styles/app.css`
    - `src/domain/profile.ts`
    - `src/domain/projects.ts`
  </read_first>
  <files>src/components/SiteLayout.tsx, src/routes/index.tsx, src/styles/app.css</files>
  <action>
    In `src/components/SiteLayout.tsx`, polish the existing header/footer shell with the shared visual primitives from Task 1 while keeping the same semantic landmarks, skip link, brand link, primary navigation, and footer OpenLinks placement. Preserve OpenLinks as one low-intrusion footer identity link labeled `OpenLinks profile` with `rel="me noopener noreferrer"` and do not add OpenLinks to the header, hero CTA, or repeated promotional surfaces per D-04.

    In `src/routes/index.tsx`, preserve the Phase 03 identity-first copy, route metadata, JSON-LD, `Browse projects` CTA, `Now building` panel, and flagship project stories. Add an `aria-hidden="true"` local brand-material element or equivalent CSS hook inside the home visual composition so the first viewport has real local visual material per D-03. The live H1, Peter/pRizz/Bright Builds context, CTA, and Now building surface must remain visible and semantic before hydration; the asset must not become the primary subject, obscure text, or push all following content below the fold per D-11.

    Apply shared `.visual-surface`, `.interactive-surface`, `.surface-link`, `.brand-material`, or equivalent classes to the home focus rows, flagship cards, project detail links, and CTA. Pair every hover treatment with focus-visible or focus-within treatment, keep card radius at 8px or less, keep interactive targets at least 44px, keep first-viewport composition unframed instead of wrapping the whole hero in a card, and keep typography at the UI-SPEC body/label/heading/display sizes without viewport-fluid scaling or negative letter spacing.
  </action>
  <acceptance>
    - `rg "brand-material|aria-hidden=\\\"true\\\"|Browse projects|Now building|homeProjects|currentFocusProjects" src/routes/index.tsx` confirms local asset treatment and existing home data wiring.
    - `rg "OpenLinks profile|rel=\\\"me noopener noreferrer\\\"" src/components/SiteLayout.tsx` confirms low-intrusion OpenLinks placement is preserved.
    - `rg "metadataForRoute|personJsonLd|jsonLdScriptContent|siteAssetLinks" src/routes/index.tsx` confirms route metadata and JSON-LD wiring remain intact.
    - `rg "tracking-\\[|letter-spacing: -|bg-white|bg-stone-50|text-zinc-950|OpenLinks.*Browse projects" src/components/SiteLayout.tsx src/routes/index.tsx src/styles/app.css` returns no matches.
  </acceptance>
  <verify>
    <automated>bun run format:check && bun run check && bun run typecheck && bun run build && bun run verify:static</automated>
  </verify>
  <done>The shared shell and home route show polished dark-primary visual hierarchy, local brand material, stable focus/touch states, and preserved semantic/SEO/OpenLinks contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Stabilize project, about, and contact responsive surfaces</name>
  <read_first>
    - `.planning/phases/04-visual-system-motion/04-CONTEXT.md` decisions D-01, D-02, D-04, D-09, D-10, D-11, D-12, D-13, and D-14.
    - `.planning/phases/04-visual-system-motion/04-UI-SPEC.md` Layout, Components, Accessibility Contract, and Verification Contract.
    - `src/routes/projects.tsx`
    - `src/routes/about.tsx`
    - `src/routes/contact.tsx`
    - `src/styles/app.css`
    - `src/domain/projects.ts`
    - `src/domain/profile.ts`
  </read_first>
  <files>src/routes/projects.tsx, src/routes/about.tsx, src/routes/contact.tsx, src/styles/app.css</files>
  <action>
    Apply the shared visual-system primitives to `/projects`, `/about`, and `/contact` without changing the Phase 03 content hierarchy, project anchors, metadata, JSON-LD, or profile/domain selectors. The work must preserve stable `/projects#slug` anchors, hidden/excluded aggregate note behavior, Writing empty state behavior, about theme cards, contact cards from `peterProfile.links`, and low-intrusion OpenLinks copy per D-04 and D-11.

    In `src/routes/projects.tsx`, make grouped project cards visually richer but layout-stable: no nested card wrappers, no card-inside-card composition, no absolute-positioned text labels over content, no hover-only content, no dynamic text that changes grid size on hover, and no animation of width/height/margins/grid tracks. Ensure project titles, long links, chips, and `Win3Bitco.in / Open Bitcoin Web Miner` wrap at 320px with `overflow-wrap: anywhere` or equivalent.

    In `src/routes/about.tsx` and `src/routes/contact.tsx`, apply the same dark-primary visual primitives, 44px focusable target rules, visible cyan focus rings, stable grid tracks, and text wrapping. Keep OpenLinks as the existing about/profile/contact identity link only; do not promote it above GitHub or Bright Builds. Do not add new route/content surfaces, metrics, fake case studies, skill bars, remote images, live repository data, or external services.
  </action>
  <acceptance>
    - `rg "projectItemListJsonLd|id=\\{props\\.project\\.slug\\}|/projects#|No reviewed projects in this group yet|Some reviewed repositories stay hidden" src/routes/projects.tsx` confirms project semantics and anchors remain.
    - `rg "overflow-wrap: anywhere|break-words|minmax\\(0, 1fr\\)|min-h-11|focus-visible:ring-cyan-300" src/styles/app.css` confirms text wrapping, stable grids, and focus/touch primitives.
    - `rg "Agentic engineering|OpenLinks identity hub|peterProfile\\.links|GitHub is the best place" src/routes/about.tsx src/routes/contact.tsx` confirms about/contact content and profile data wiring remain.
    - `rg "Selected works|Creative Director|Awwwards|Lorem ipsum|skill bar|api.github.com|@octokit|GITHUB_TOKEN|VITE_.*GITHUB.*TOKEN|bg-white|bg-stone-50|text-zinc-950" src/routes/projects.tsx src/routes/about.tsx src/routes/contact.tsx src/styles/app.css` returns no matches.
  </acceptance>
  <verify>
    <automated>bun run format:check && bun run check && bun run typecheck && bun run build && bun run verify:static</automated>
  </verify>
  <done>Projects, about, and contact routes use the shared visual system with stable responsive layout, keyboard/touch-accessible states, preserved semantics, and no forbidden visual/content/dependency regressions.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local assets -> route visual layer | Checked-in image/icon assets become visible decorative UI material. |
| route/domain content -> styled HTML | Curated copy and identity links become visually enhanced public HTML before hydration. |
| external links -> new tabs | Project, GitHub, OpenLinks, and Bright Builds links leave the site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | `src/routes/index.tsx` asset treatment | mitigate | Use only checked-in local assets, mark decorative material `aria-hidden`, and keep live semantic headings/copy/CTA visible before hydration. |
| T-04-01-02 | Spoofing | OpenLinks route/footer placement | mitigate | Preserve OpenLinks only in footer/about/contact identity surfaces with `rel="me noopener noreferrer"` and keep Bright Builds/GitHub/project CTAs primary. |
| T-04-01-03 | Information Disclosure | visual-system route changes | mitigate | Do not add runtime GitHub calls, Octokit imports, token names, remote asset URLs, analytics, or environment reads; acceptance greps enforce forbidden patterns. |
| T-04-01-04 | Denial of Service | visual CSS and layout | mitigate | Use stable CSS primitives, local assets, constrained grids, text wrapping, and no layout-affecting animation to prevent mobile overflow or expensive visual work. |
</threat_model>

<verification>
Run `bun run format:check`, `bun run check`, `bun run typecheck`, `bun run build`, `bun run verify:static`, and final `bun run verify` before completing the plan. Browser visual evidence is required in Plan 03 after motion and verification hardening land.
</verification>

<success_criteria>
The Phase 03 portfolio routes render as a polished dark-primary Bright Builds-inspired interface using local visual material, stable responsive primitives, low-intrusion OpenLinks placement, accessible focus/touch states, and pure-domain boundaries with no deferred feature creep.
</success_criteria>

<output>
After completion, create `.planning/phases/04-visual-system-motion/04-01-SUMMARY.md`.
</output>
