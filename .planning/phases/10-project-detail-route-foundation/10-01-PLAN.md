---
phase: 10-project-detail-route-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/projects.ts
  - src/domain/routes.ts
  - src/domain/seo.ts
  - src/routes/projects/index.tsx
  - src/routes/projects/[slug].tsx
  - src/domain/foundation.test.ts
  - src/domain/portfolio-surfaces.test.ts
  - src/domain/project-detail-routes.test.ts
  - scripts/verify-static.ts
  - scripts/verify-release.ts
  - scripts/verify-release.test.ts
  - .planning/phases/10-project-detail-route-foundation/10-01-SUMMARY.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - STORY-01
  - VERIFY-01
user_setup: []
must_haves:
  truths:
    - "Selected curated projects have deterministic /projects/{slug} detail paths derived from typed project data."
    - "Maintainers can author intro, technical shape, proof points, current status, and collaboration angle for selected detail projects."
    - "Static builds prerender every selected detail route and exclude unselected projects."
    - "Unit tests cover detail eligibility, path derivation, exclusion behavior, and metadata derivation."
    - "Repo verification passes without claiming Phase 12 JSON-LD or sitemap work."
  artifacts:
    - path: "src/domain/projects.ts"
      provides: "Project detail story data, selector, lookup, path, and route helpers"
      contains: "projectDetailRoutes"
    - path: "src/routes/projects/[slug].tsx"
      provides: "Static project detail route foundation"
      contains: "Project story"
    - path: "src/domain/project-detail-routes.test.ts"
      provides: "Focused route eligibility and metadata derivation tests"
      contains: "projectDetailRoutes"
    - path: "scripts/verify-static.ts"
      provides: "Static output checks for project detail route body and metadata"
      contains: "maybeProjectForDetailRoute"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 10-2026-06-02T20-30-24
generated_at: 2026-06-02T20:52:13Z
---

<objective>
Create the Phase 10 route foundation for selected project detail pages.

Purpose: Maintainers should be able to select project detail pages from typed curated data, prerender stable `/projects/{slug}` routes, and rely on tests that prove selected projects are included while unselected projects stay private.

Output: Typed detail story data and helpers, prerendered dynamic project detail route, initial project metadata derivation, focused tests, route-aware static/release verification updates, and Phase 10 GSD artifacts.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-project-detail-route-foundation/10-CONTEXT.md
@src/domain/projects.ts
@src/domain/routes.ts
@src/domain/seo.ts
@src/routes/projects/index.tsx
@scripts/verify-static.ts
@scripts/verify-release.ts
@tests/browser-release.playwright.ts
</context>

<must_haves>
1. `ProjectStory` supports typed detail-page story content for selected projects.
2. `projectDetailPageProjects()`, `maybeProjectDetailPageProjectBySlug()`, `projectDetailPath()`, and `projectDetailRoutes()` are supported helper exports.
3. `prerenderRoutes` includes selected project detail routes and excludes unselected projects.
4. `src/routes/projects/[slug].tsx` renders static body content for selected project routes and a safe not-found fallback for unselected slugs.
5. Unit tests prove selected slugs, excluded slugs, path derivation, route derivation, and project metadata derivation.
6. `bun run verify` passes.
</must_haves>

<tasks>

<task type="auto">
  <name>Task 1: Add typed detail story data and route helpers</name>
  <files>src/domain/projects.ts, src/domain/routes.ts, src/domain/seo.ts</files>
  <action>
    Add a typed `ProjectDetailStory` content field to selected flagship project records. Add selector and route helpers for detail-page projects, then include those routes in `prerenderRoutes`. Add initial `metadataForProject()` derivation for Phase 10 tests without expanding sitemap or JSON-LD behavior.
  </action>
  <verify>
    <automated>bun run test -- src/domain/project-detail-routes.test.ts src/domain/foundation.test.ts src/domain/portfolio-surfaces.test.ts</automated>
  </verify>
  <done>Typed detail data and helpers exist, selected routes are derived, and focused tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Add project detail route foundation</name>
  <files>src/routes/projects/index.tsx, src/routes/projects/[slug].tsx</files>
  <action>
    Move the project index route to nested `projects/index.tsx` and add a `[slug].tsx` detail route that renders selected project story content before hydration. Keep navigation changes minimal: retain existing project-index anchors and include a back-to-index link from detail pages.
  </action>
  <verify>
    <automated>bun run build</automated>
    <automated>bun run verify:static</automated>
  </verify>
  <done>Build output prerenders `/projects/:slug` pages for selected projects and static verification passes.</done>
</task>

<task type="auto">
  <name>Task 3: Update release verification and finalize</name>
  <files>scripts/verify-static.ts, scripts/verify-release.ts, scripts/verify-release.test.ts, .planning/phases/10-project-detail-route-foundation/10-01-SUMMARY.md</files>
  <action>
    Make static verification route-aware for project detail pages. Keep release semantic checks active for new route HTML, while deferring project-specific JSON-LD findings for detail routes until Phase 12. Run the full aggregate gate and record the execution summary.
  </action>
  <verify>
    <automated>bun run verify</automated>
    <automated>git diff --check</automated>
  </verify>
  <done>Full repo verification passes and Phase 10 summary records completed requirements and residual Phase 11-13 work.</done>
</task>

</tasks>

---

*Phase: 10-project-detail-route-foundation*
*Plan: 10-01*
