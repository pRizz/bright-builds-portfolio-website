---
phase: 23-theme-verification-and-release-contract
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - scripts/release-readiness.ts
  - scripts/release-readiness.test.ts
  - scripts/verify-release.test.ts
  - docs/release-readiness.md
  - README.md
autonomous: true
requirements:
  - VERIFY-02
  - VERIFY-03
  - VERIFY-04
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-06-18T04-28-20
generated_at: 2026-06-18T04:56:49Z
must_haves:
  truths:
    - "The aggregate `bun run verify` script runs `bun run verify:release` last after `bun run verify:static`."
    - "The aggregate `bun run verify` script remains non-mutating and does not run `generate:static-metadata` or `install:browser`."
    - "Release-readiness checks require theme route coverage, theme static coverage, theme browser coverage, and the current representative theme smoke route."
    - "Release evidence labels include `theme route coverage` and do not claim hosted audits, live link crawling, full WCAG certification, screenshot baselines, or manual review as automated evidence."
    - "README and release-readiness docs identify `bun run install:browser && bun run verify` as the clean-builder sequence with theme coverage included."
  artifacts:
    - path: "package.json"
      provides: "Aggregate verify ordering"
      contains: "bun run verify:static && bun run verify:release"
    - path: "scripts/release-readiness.ts"
      provides: "Required document facts and automated release evidence labels"
      exports:
        - "releaseReadinessDocumentFindings"
        - "releaseReadinessEvidenceLabels"
    - path: "scripts/release-readiness.test.ts"
      provides: "Focused tests for theme release-readiness facts and aggregate verify script composition"
      contains: "theme route coverage"
    - path: "scripts/verify-release.test.ts"
      provides: "Release evidence label guard"
      contains: "theme route coverage"
    - path: "docs/release-readiness.md"
      provides: "Checked-in release contract"
      contains: "theme route coverage"
    - path: "README.md"
      provides: "Developer release command guidance"
      contains: "release-readiness verifier"
  key_links:
    - from: "package.json"
      to: "scripts/verify-release.ts"
      via: "`verify` script calls `bun run verify:release` last"
      pattern: "verify:static && bun run verify:release"
    - from: "scripts/release-readiness.ts"
      to: "src/domain/themes.ts"
      via: "`themeDetailRoutes()[0]` representative smoke route"
      pattern: "themeDetailRoutes\\(\\)\\[0\\]"
    - from: "scripts/verify-release.ts"
      to: "scripts/release-readiness.ts"
      via: "`releaseEvidenceLabels()` spreads `releaseReadinessEvidenceLabels()`"
      pattern: "releaseReadinessEvidenceLabels\\(\\)"
    - from: "scripts/release-readiness.ts"
      to: "docs/release-readiness.md"
      via: "required document facts"
      pattern: "theme route coverage"
---

<objective>
Make the aggregate release contract truthful for theme coverage.

Purpose: Phase 23 needs the release gate, release-readiness document checks, evidence labels, and public docs to agree about what automated local theme verification actually runs.
Output: Updated aggregate verify script, theme release-readiness facts, theme evidence labels, README/release-readiness wording, and focused tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-theme-verification-and-release-contract/23-CONTEXT.md
@.planning/phases/23-theme-verification-and-release-contract/23-RESEARCH.md
@.planning/phases/23-theme-verification-and-release-contract/23-UI-SPEC.md
@.planning/milestones/v1.3-phases/17-writing-verification-and-release-contract/17-01-SUMMARY.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-04-SUMMARY.md
@.planning/phases/22-theme-metadata-and-structured-data/22-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@package.json
@scripts/release-readiness.ts
@scripts/release-readiness.test.ts
@scripts/verify-release.ts
@scripts/verify-release.test.ts
@tests/browser-release.playwright.ts
@src/domain/themes.ts
@docs/release-readiness.md
@README.md

<interfaces>
From `scripts/release-readiness.ts`:
```ts
export function releaseReadinessDocumentFindings(
  documentPath = releaseReadinessDocumentPath,
): readonly ReleaseFinding[];

export function releaseReadinessEvidenceLabels(): readonly string[];
```

From `scripts/verify-release.ts`:
```ts
export function releaseEvidenceLabels(): readonly string[] {
  return [
    "contrast/readability",
    "focus-visible",
    "focus state",
    "image alt",
    "interactive motion surfaces",
    "reduced-motion",
    ...releaseReadinessEvidenceLabels(),
  ];
}
```

From `src/domain/themes.ts`:
```ts
export function themeDetailRoutes(themes: readonly ThemeRecord[] = curatedThemes): readonly string[];
```

Existing browser evidence in `tests/browser-release.playwright.ts`:
```ts
for (const route of prerenderRoutes) {
  test(`axe has no violations on ${route}`, async ({ page }, testInfo) => { /* ... */ });
  test(`dark layout has no overflow or obvious overlap on ${route}`, async ({ page }, testInfo) => { /* ... */ });
}

function representativeThemeDetailRoute(): string {
  const maybeRoute = themeDetailRoutes()[0];
  if (!maybeRoute) {
    throw new Error("Expected at least one public theme detail route for release coverage.");
  }
  return maybeRoute;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add theme release facts, evidence labels, and aggregate verify ordering</name>
  <files>package.json, scripts/release-readiness.ts, scripts/release-readiness.test.ts, scripts/verify-release.test.ts</files>
  <read_first>
    - package.json
    - scripts/release-readiness.ts
    - scripts/release-readiness.test.ts
    - scripts/verify-release.ts
    - scripts/verify-release.test.ts
    - src/domain/themes.ts
    - .planning/phases/23-theme-verification-and-release-contract/23-CONTEXT.md
    - .planning/phases/23-theme-verification-and-release-contract/23-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: Removing `theme route coverage` from the release-readiness document fixture reports `Release-readiness document is missing theme route coverage: theme route coverage.`
    - Test 2: Removing `theme metadata, JSON-LD, sitemap, related project links, related writing links, collaboration links, and forbidden runtime residue coverage` reports the missing theme static coverage fact.
    - Test 3: Removing `theme axe, desktop/mobile dark layout, representative keyboard, and representative reduced-motion coverage` reports the missing theme browser coverage fact.
    - Test 4: Removing the representative theme route from `themeDetailRoutes()[0]` reports `Release-readiness document is missing representative theme smoke route: /themes/agentic-engineering.`
    - Test 5: `releaseReadinessEvidenceLabels()` and `releaseEvidenceLabels()` include `theme route coverage` immediately after `writing route coverage`.
    - Test 6: `package.json` `scripts.verify` ends with `bun run verify:static && bun run verify:release` and does not include `bun run generate:static-metadata` or `bun run install:browser`.
  </behavior>
  <action>
    First add the failing tests, then implement the code and package script changes.

    In `scripts/release-readiness.test.ts`, import `themeDetailRoutes` from `../src/domain/themes`. Add a local `representativeThemeDetailRoute()` helper matching the existing writing helper style: read `themeDetailRoutes()[0]`, throw `Expected at least one public theme detail route for release coverage.` when absent, and return the route. Add focused tests for the four required theme document facts listed in the behavior block. Add a package-script composition test that parses `package.json`, asserts the exact `verify` script value is `bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run verify:project-helper-surface && bun run verify:visual-system && bun run build && bun run verify:browser && bun run verify:static && bun run verify:release`, and asserts that script does not contain `bun run generate:static-metadata` or `bun run install:browser` per D-13, D-14, and D-15.

    In `scripts/release-readiness.ts`, import `themeDetailRoutes` from `../src/domain/themes`. Extend `requiredReleaseReadinessDocumentFacts` with these exact facts per D-09 and D-10: label `theme route coverage` text `theme route coverage`; label `theme static coverage` text `theme metadata, JSON-LD, sitemap, related project links, related writing links, collaboration links, and forbidden runtime residue coverage`; label `theme browser coverage` text `theme axe, desktop/mobile dark layout, representative keyboard, and representative reduced-motion coverage`; label `representative theme smoke route` text from a new `representativeThemeDetailRoute()` helper. Add `theme route coverage` to `releaseReadinessEvidenceLabels()` immediately after `writing route coverage` per D-11. Do not add manual external-link smoke checks, preview deployment checks, post-deploy checks, hosted audits, live reachability, screenshot baselines, full WCAG certification, dynamic Open Graph routes, runtime fetches, or OpenLinks primary-CTA language to automated labels per D-07, D-08, D-11, and D-12.

    In `scripts/verify-release.test.ts`, update the expected `releaseEvidenceLabels()` array to include `theme route coverage` immediately after `writing route coverage`, while preserving the existing negative assertions for `external suite`, `hosted audit`, `network`, and `live link`.

    In `package.json`, append `&& bun run verify:release` to `scripts.verify` after `bun run verify:static`. Do not add `bun run generate:static-metadata`, `bun run install:browser`, live link crawling, or any mutating generation step to `scripts.verify` per D-14, D-15, and D-16.
  </action>
  <acceptance_criteria>
    - `node -e 'const fs = require("node:fs"); const pkg = JSON.parse(fs.readFileSync("package.json", "utf8")); const expected = "bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run verify:project-helper-surface && bun run verify:visual-system && bun run build && bun run verify:browser && bun run verify:static && bun run verify:release"; if (pkg.scripts.verify !== expected) process.exit(1); if (pkg.scripts.verify.includes("generate:static-metadata") || pkg.scripts.verify.includes("install:browser")) process.exit(1);'` exits 0.
    - `rg -n 'theme route coverage' scripts/release-readiness.ts scripts/release-readiness.test.ts scripts/verify-release.test.ts` finds matches.
    - `rg -n 'theme metadata, JSON-LD, sitemap, related project links, related writing links, collaboration links, and forbidden runtime residue coverage' scripts/release-readiness.ts scripts/release-readiness.test.ts` finds matches.
    - `rg -n 'theme axe, desktop/mobile dark layout, representative keyboard, and representative reduced-motion coverage' scripts/release-readiness.ts scripts/release-readiness.test.ts` finds matches.
    - `rg -n 'themeDetailRoutes' scripts/release-readiness.ts scripts/release-readiness.test.ts` finds matches.
    - `rg -n 'not\\.toContain\\(\"hosted audit\"\\)|not\\.toContain\\(\"network\"\\)|not\\.toContain\\(\"live link\"\\)' scripts/verify-release.test.ts` finds the anti-overclaim assertions.
  </acceptance_criteria>
  <verify>
    <automated>bun run test scripts/release-readiness.test.ts scripts/verify-release.test.ts && bun run typecheck</automated>
  </verify>
  <done>`verify` runs release verification last without mutating generated files, theme release-readiness facts are code-enforced, and automated evidence labels name `theme route coverage` without overclaiming manual or hosted checks.</done>
</task>

<task type="auto">
  <name>Task 2: Update release-readiness and README wording for theme coverage</name>
  <files>docs/release-readiness.md, README.md</files>
  <read_first>
    - docs/release-readiness.md
    - README.md
    - package.json
    - scripts/release-readiness.ts
    - .planning/phases/23-theme-verification-and-release-contract/23-CONTEXT.md
    - .planning/phases/23-theme-verification-and-release-contract/23-UI-SPEC.md
  </read_first>
  <action>
    Update `docs/release-readiness.md` so the Primary Release Gate, Static Output, SEO and Static Metadata, Browser and Accessibility, Cloudflare Pages, Preview Deployment Checklist, and Production Deployment Checklist sections name theme coverage alongside existing project and writing coverage per D-09, D-10, D-17, and D-18.

    The document must contain these exact strings because `scripts/release-readiness.ts` will enforce them: `theme route coverage`; `theme metadata, JSON-LD, sitemap, related project links, related writing links, collaboration links, and forbidden runtime residue coverage`; `theme axe, desktop/mobile dark layout, representative keyboard, and representative reduced-motion coverage`; `/themes/agentic-engineering`. Keep manual external-link smoke checks, preview deployment checks, post-deploy checks, and hosted audits as checklist prose only. Do not describe them as automated evidence per D-07 and D-11. Keep OpenLinks wording low-intrusion and policy-oriented; do not turn OpenLinks into a primary theme CTA per D-12.

    Update `README.md` Build and Release Checks so it states the aggregate gate builds static output, runs browser/accessibility checks, verifies static prerendered routes, metadata, and theme route coverage, and then runs the release-readiness verifier. Keep the clean-builder guidance as `bun run install:browser` before `bun run verify`; do not imply `verify` installs browsers or mutates generated metadata per D-14 and D-15.
  </action>
  <acceptance_criteria>
    - `rg -n 'theme route coverage' docs/release-readiness.md README.md` finds matches.
    - `rg -n 'theme metadata, JSON-LD, sitemap, related project links, related writing links, collaboration links, and forbidden runtime residue coverage' docs/release-readiness.md` finds a match.
    - `rg -n 'theme axe, desktop/mobile dark layout, representative keyboard, and representative reduced-motion coverage' docs/release-readiness.md` finds a match.
    - `rg -n '/themes/agentic-engineering' docs/release-readiness.md` finds a match.
    - `rg -n 'bun run install:browser && bun run verify' docs/release-readiness.md` finds matches.
    - `rg -n 'complete WCAG certification|visual-regression baselines|screenshot approval|hosted Lighthouse audits.*automated|live external-link reachability.*automated' docs/release-readiness.md README.md` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run test scripts/release-readiness.test.ts && bun run typecheck</automated>
  </verify>
  <done>Checked-in release docs and README accurately describe the aggregate gate, clean-builder browser prerequisite, and automated theme coverage without claiming manual or hosted checks as automated evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| package script to local release environment | `bun run verify` controls which local gates run before release and must not hide browser installation or mutating generation. |
| checked-in docs to release verifier | `docs/release-readiness.md` is treated as release evidence by code-owned required facts. |
| generated static output to release evidence | Release labels summarize automated checks over `.output/public` and must not overstate what ran. |
| generated anchors to external origins | External links are static output from curated data and are verified by policy, not live crawling. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-01 | Information Disclosure | `package.json` aggregate `verify` and release scanners | mitigate | Keep `verify:no-github-runtime`, `verify:static`, and `verify:release` in the aggregate gate; do not add token-dependent runtime checks or public env-token guidance. |
| T-23-02 | Repudiation | `releaseReadinessEvidenceLabels()` and `releaseEvidenceLabels()` | mitigate | Add `theme route coverage` only after static/browser/release checks exist; preserve tests rejecting `hosted audit`, `network`, and `live link` overclaims. |
| T-23-03 | Denial of Service | `scripts.verify` in `package.json` | mitigate | Keep browser installation explicit through `bun run install:browser`; do not hide Playwright installation or network crawling inside `verify`. |
| T-23-04 | Tampering | `docs/release-readiness.md` required facts | mitigate | Enforce exact theme coverage facts and representative public theme route through `releaseReadinessDocumentFindings()` tests. |
| T-23-05 | Information Disclosure | external-link policy checks | mitigate | Keep sensitive query key detection and redacted finding messages; do not add live external-link reachability output that could leak tokens or third-party response details. |
</threat_model>

<verification>
Plan-level verification:

1. `bun run test scripts/release-readiness.test.ts scripts/verify-release.test.ts`
2. `bun run typecheck`
3. `bun run build`
4. `bun run verify:release`
5. After Plan 02 is also merged, run the phase-level proof `bun run install:browser && bun run verify` per D-17, D-18, and D-19.
</verification>

<success_criteria>
- `package.json` `scripts.verify` is exactly ordered with `bun run verify:release` last.
- `scripts/release-readiness.ts` requires theme route/static/browser/smoke-route facts and emits `theme route coverage`.
- `scripts/release-readiness.test.ts` and `scripts/verify-release.test.ts` guard the theme facts, labels, and non-mutating aggregate script.
- `docs/release-readiness.md` and `README.md` describe theme coverage as part of the clean-builder aggregate release path.
- No task adds runtime network dependencies, dynamic OG routes, live external-link crawling, browser installation inside `verify`, or generated metadata mutation inside `verify`.
</success_criteria>

<output>
After completion, create `.planning/phases/23-theme-verification-and-release-contract/23-01-SUMMARY.md`.
</output>
