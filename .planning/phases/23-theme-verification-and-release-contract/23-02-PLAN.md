---
phase: 23-theme-verification-and-release-contract
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/verify-static/run-static-verification.ts
  - scripts/verify-static.test.ts
autonomous: true
requirements:
  - VERIFY-01
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 23-2026-06-18T04-28-20
generated_at: 2026-06-18T04:56:49Z
must_haves:
  truths:
    - "Passing static verification terminal output explicitly names `theme route coverage`."
    - "Static verification remains helper-derived from `prerenderRoutes`, `themeDetailRoutes()`, public theme helpers, and existing metadata/sitemap verifier functions."
    - "Static verification continues to check generated theme HTML, metadata, JSON-LD, sitemap inclusion/exclusion, collaboration links, social fallback, fallback safety, and forbidden runtime residue without adding network checks."
  artifacts:
    - path: "scripts/verify-static/run-static-verification.ts"
      provides: "Static verification success summary"
      exports:
        - "staticVerificationSummary"
        - "runStaticVerification"
      contains: "theme route coverage"
    - path: "scripts/verify-static.test.ts"
      provides: "Import-safe static verifier contract tests"
      contains: "writing route coverage, theme route coverage"
  key_links:
    - from: "scripts/verify-static/run-static-verification.ts"
      to: "scripts/verify-static/expected-route-text.ts"
      via: "`expectedRoutes` controls route parity"
      pattern: "expectedRoutes"
    - from: "scripts/verify-static.test.ts"
      to: "src/domain/themes.ts"
      via: "`themeDetailRoutes()` and `maybeThemeForDetailRoute()` coverage assertions"
      pattern: "themeDetailRoutes"
    - from: "scripts/verify-static/sitemap-assets-verifier.ts"
      to: "public/sitemap.xml"
      via: "`assertSitemapThemeCoverage()` and `assertNoPrerenderedThemeRoute()`"
      pattern: "assertSitemapThemeCoverage"
---

<objective>
Make static verifier evidence explicitly name theme route coverage.

Purpose: Static verification already proves generated theme route content and crawler surfaces; Phase 23 needs the terminal evidence and focused tests to say that truth directly.
Output: Updated `staticVerificationSummary()` wording and import-safe tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/23-theme-verification-and-release-contract/23-CONTEXT.md
@.planning/phases/23-theme-verification-and-release-contract/23-RESEARCH.md
@.planning/phases/23-theme-verification-and-release-contract/23-UI-SPEC.md
@.planning/milestones/v1.3-phases/18-static-verifier-modularization/18-01-SUMMARY.md
@.planning/phases/21-collaboration-pathways-and-cross-links/21-04-SUMMARY.md
@.planning/phases/22-theme-metadata-and-structured-data/22-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/testing.md
@standards/core/verification.md
@standards/languages/typescript-javascript.md
@scripts/verify-static/run-static-verification.ts
@scripts/verify-static.test.ts
@scripts/verify-static/expected-route-text.ts
@scripts/verify-static/metadata-jsonld-verifier.ts
@scripts/verify-static/sitemap-assets-verifier.ts
@src/domain/themes.ts

<interfaces>
From `scripts/verify-static/run-static-verification.ts`:
```ts
export function staticVerificationSummary(options: {
  routeCount: number;
  outputRoot: string;
}): string;

export function runStaticVerification(): void;
```

Existing route parity test in `scripts/verify-static.test.ts`:
```ts
const routes = expectedRoutes.map((check) => check.route);
const themeRoutes = themeDetailRoutes();
expect(routes).toEqual(prerenderRoutes);
for (const route of [...projectRoutes, ...writingRoutes, ...themeRoutes]) {
  expect(routes).toContain(route);
}
```

Existing theme coverage exports in `scripts/verify-static/sitemap-assets-verifier.ts`:
```ts
export function assertSitemapThemeCoverage(outputRoot: string, sitemapText: string): void;
export function assertNoPrerenderedThemeRoute(outputRoot: string, route: string): void;
export function assertThemeFallbackSource(sourceText: string): void;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add theme route coverage to static verifier success evidence</name>
  <files>scripts/verify-static/run-static-verification.ts, scripts/verify-static.test.ts</files>
  <read_first>
    - scripts/verify-static/run-static-verification.ts
    - scripts/verify-static.test.ts
    - scripts/verify-static/expected-route-text.ts
    - scripts/verify-static/metadata-jsonld-verifier.ts
    - scripts/verify-static/sitemap-assets-verifier.ts
    - src/domain/themes.ts
    - .planning/phases/23-theme-verification-and-release-contract/23-CONTEXT.md
    - .planning/phases/23-theme-verification-and-release-contract/23-RESEARCH.md
    - .planning/phases/23-theme-verification-and-release-contract/23-UI-SPEC.md
  </read_first>
  <behavior>
    - Test 1: `staticVerificationSummary({ routeCount: 16, outputRoot: ".output/public" })` returns `Verified 16 prerendered routes, metadata, JSON-LD, writing route coverage, theme route coverage, assets, sitemap, and robots in .output/public.`
    - Test 2: Existing helper-derived coverage tests still prove `expectedRoutes` equals `prerenderRoutes` and contains every `themeDetailRoutes()` value.
    - Test 3: Existing verifier export tests still include `assertSitemapThemeCoverage`, `assertNoPrerenderedThemeRoute`, and `assertThemeFallbackSource`.
  </behavior>
  <action>
    First update the `scripts/verify-static.test.ts` summary test expectation and test name so it requires `writing route coverage, theme route coverage` in that order. Use `routeCount: 16` in the fixture to match the current Phase 22 generated route count, but keep `staticVerificationSummary()` generic by interpolating `options.routeCount`.

    Then update `staticVerificationSummary()` in `scripts/verify-static/run-static-verification.ts` so the returned string is exactly `Verified ${options.routeCount} prerendered routes, metadata, JSON-LD, writing route coverage, theme route coverage, assets, sitemap, and robots in ${options.outputRoot}.` per D-01 and D-02.

    Do not add a parsed JSON-LD framework, copied theme slug list, screenshot baseline, Playwright dependency, live external-link reachability, runtime fetch, token-dependent check, dynamic Open Graph route, or `generate:static-metadata` call per D-01, D-03, D-04, D-08, and D-15. Leave the actual generated-output checks in `expected-route-text.ts`, `metadata-jsonld-verifier.ts`, and `sitemap-assets-verifier.ts` helper-derived.
  </action>
  <acceptance_criteria>
    - `rg -n 'writing route coverage, theme route coverage' scripts/verify-static/run-static-verification.ts scripts/verify-static.test.ts` finds matches.
    - `rg -n 'themeDetailRoutes|maybeThemeForDetailRoute|assertSitemapThemeCoverage|assertNoPrerenderedThemeRoute|assertThemeFallbackSource' scripts/verify-static.test.ts` finds matches.
    - `rg -n 'fetch\\(|playwright|generate:static-metadata|hosted audit|live link|screenshot' scripts/verify-static/run-static-verification.ts scripts/verify-static.test.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run test scripts/verify-static.test.ts && bun run build && bun run verify:static</automated>
  </verify>
  <done>`bun run verify:static` prints static verifier success evidence that explicitly includes `theme route coverage`, while the coverage itself remains helper-derived and non-mutating.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| theme registry to static output | Public theme helpers decide which routes are generated and verified. |
| generated `.output/public` to static verifier | Static verification scans built HTML, assets, sitemap, and source fallbacks for release blockers. |
| static verifier terminal output to release evidence | The summary communicates what automated static coverage actually ran. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-23-06 | Information Disclosure | theme route static output | mitigate | Preserve helper-derived public theme route checks, sitemap exclusion checks, fallback-source checks, and forbidden runtime residue scans. |
| T-23-07 | Tampering | `staticVerificationSummary()` | mitigate | Guard exact summary wording with `scripts/verify-static.test.ts` so evidence cannot silently drop theme coverage. |
| T-23-08 | Repudiation | static verifier evidence | mitigate | Name only automated static checks that run locally; do not add hosted audit, live link, screenshot, or manual review wording. |
| T-23-09 | Denial of Service | `verify:static` | mitigate | Keep checks local and non-mutating; avoid network crawling, browser automation, or generated metadata writes inside static verification. |
</threat_model>

<verification>
Plan-level verification:

1. `bun run test scripts/verify-static.test.ts`
2. `bun run build`
3. `bun run verify:static`
4. After Plan 01 is also merged, run the phase-level proof `bun run install:browser && bun run verify` per D-17, D-18, and D-19, and include the local `bun --version` used in final evidence. Do not require upgrading local Bun by default; upgrade only if verification fails due to a Bun version mismatch or if the user explicitly requests exact local parity with `packageManager: bun@1.3.14`.
</verification>

<success_criteria>
- Static verifier success output includes `theme route coverage`.
- Static verifier tests still prove route parity through `prerenderRoutes`, `themeDetailRoutes()`, and theme sitemap/fallback verifier exports.
- No task adds parsed JSON-LD framework work, copied theme slug lists, live network checks, screenshot baselines, runtime fetches, token-dependent checks, or mutating static metadata generation.
</success_criteria>

<output>
After completion, create `.planning/phases/23-theme-verification-and-release-contract/23-02-SUMMARY.md`.
</output>
