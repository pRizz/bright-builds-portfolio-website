---
phase: 31-static-topic-routes
plan: 03
type: execute
wave: 3
depends_on: [31-01, 31-02]
files_modified:
  - scripts/verify-static/expected-route-text.ts
  - scripts/verify-static/metadata-jsonld-verifier.ts
  - scripts/verify-static/sitemap-assets-verifier.ts
  - scripts/verify-static/config.ts
  - tests/browser-release.playwright.ts
  - .planning/phases/31-static-topic-routes/31-VERIFICATION.md
autonomous: true
requirements: [DISC-01, DISC-02, DISC-03, DISC-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 31-2026-06-30T22-33-56
generated_at: 2026-06-30T22:55:00Z
must_haves:
  truths:
    - "Static verification proves topic route HTML, metadata, JSON-LD, sitemap coverage, fallback source, and unknown-topic exclusion."
    - "Browser release checks cover `/topics` and at least one representative topic detail route for axe, dark desktop/mobile layout, keyboard focus, and reduced motion."
    - "The aggregate `bun run verify` passes after Phase 31 implementation."
    - "Phase verification status is `passed` only after clean repo-native verification evidence."
  artifacts:
    - path: "scripts/verify-static/expected-route-text.ts"
      provides: "Expected pre-hydration topic route text and topic route resolver"
    - path: "scripts/verify-static/metadata-jsonld-verifier.ts"
      provides: "Topic metadata and JSON-LD verification in static output"
    - path: "scripts/verify-static/sitemap-assets-verifier.ts"
      provides: "Topic sitemap coverage, unknown-route exclusion, and fallback-source checks"
    - path: "tests/browser-release.playwright.ts"
      provides: "Representative topic keyboard and reduced-motion browser coverage"
    - path: ".planning/phases/31-static-topic-routes/31-VERIFICATION.md"
      provides: "Phase-level verification report with passed status"
---

<objective>
Prove Phase 31 end to end with static, sitemap, metadata, JSON-LD, browser, and aggregate verification, then write the GSD verification artifact required by the wrapper commit/push gate.
</objective>

<context>
@.planning/phases/31-static-topic-routes/31-CONTEXT.md
@.planning/phases/31-static-topic-routes/31-RESEARCH.md
@.planning/phases/31-static-topic-routes/31-UI-SPEC.md
@.planning/phases/31-static-topic-routes/31-01-PLAN.md
@.planning/phases/31-static-topic-routes/31-02-PLAN.md
@scripts/verify-static/expected-route-text.ts
@scripts/verify-static/metadata-jsonld-verifier.ts
@scripts/verify-static/sitemap-assets-verifier.ts
@scripts/verify-static/config.ts
@tests/browser-release.playwright.ts
@package.json
</context>

<threat_model>
ASVS level: 1
Block on: high

Threats:
- False-positive completion from route code without static output proof. Mitigation: extend `verify:static` to inspect built `.output/public` HTML for topic routes, metadata, sitemap entries, and unknown-route exclusion.
- Accessibility regression missed by unit/static checks. Mitigation: extend Playwright keyboard/reduced-motion representative coverage and rely on existing route-loop axe/layout checks.
- Social image contract drift. Mitigation: static metadata verification must confirm topic routes use the expected fallback/generated image path from `maybeSocialPreviewTargetForRoutePath(routePath) ?? SOCIAL_PREVIEW_FALLBACK_IMAGE`.
- Wrapper push on dirty or failing phase. Mitigation: write `31-VERIFICATION.md` with `status: passed` only after `bun run verify` passes.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend static route expected text and metadata verification for topics</name>
  <files>scripts/verify-static/expected-route-text.ts, scripts/verify-static/metadata-jsonld-verifier.ts</files>
  <read_first>
    scripts/verify-static/expected-route-text.ts
    scripts/verify-static/metadata-jsonld-verifier.ts
    scripts/verify-static/html-assertions.ts
    src/domain/topics.ts
    src/domain/seo.ts
    src/routes/topics/index.tsx
    src/routes/topics/[slug].tsx
  </read_first>
  <action>
    Update `scripts/verify-static/expected-route-text.ts`:
    - import `PublicTopic`, `maybePublicTopicBySlug`, `publicTopics`, `topicDetailPath`, and `topicDetailRoutes`
    - add `maybeTopicForDetailRoute(route: string): PublicTopic | null`
    - check `maybeTopicForDetailRoute(route)` before top-level route fallback
    - for `/topics`, expected text must include route static text, `Topic discovery`, `Topics`, topic labels, topic summaries/count labels, `Explore topic`, and topic detail `href`s
    - for topic detail routes, expected text must include `Back to topics`, `Topic`, topic label, grouped headings `Projects`, `Writing`, `Theme paths`, reference titles, summaries, action labels, and reference `href`s

    Update `scripts/verify-static/metadata-jsonld-verifier.ts`:
    - import topic metadata/JSON-LD helpers and topic route resolver
    - if `maybeTopicForDetailRoute(routePath)` returns a topic, assert `metadataForTopic()` and `topicCollectionPageJsonLd()`
    - for route `/topics`, assert `topicItemListJsonLd(publicTopics())`
    - preserve existing project/writing/theme/top-level behavior
  </action>
  <verify>
    <automated>bun run verify:static</automated>
  </verify>
  <acceptance_criteria>
    - `rg "maybeTopicForDetailRoute|topicDetailRoutes|topicDetailPath" scripts/verify-static/expected-route-text.ts` finds topic route resolution.
    - `rg "Topic discovery|Explore topic|Back to topics|Theme paths" scripts/verify-static/expected-route-text.ts` finds expected topic copy.
    - `rg "metadataForTopic|topicCollectionPageJsonLd|topicItemListJsonLd" scripts/verify-static/metadata-jsonld-verifier.ts` finds topic metadata/JSON-LD verification.
    - `bun run build && bun run verify:static` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Extend sitemap, unknown-topic fallback, and static-output exclusion checks</name>
  <files>scripts/verify-static/sitemap-assets-verifier.ts, scripts/verify-static/config.ts</files>
  <read_first>
    scripts/verify-static/sitemap-assets-verifier.ts
    scripts/verify-static/config.ts
    src/routes/topics/[slug].tsx
    src/domain/topics.ts
    src/domain/seo.ts
  </read_first>
  <action>
    Update static verification:
    - add topic detail route source path to `scripts/verify-static/config.ts` if source-path constants live there
    - add `assertSitemapTopicCoverage(root: string)` in `sitemap-assets-verifier.ts`
    - assert sitemap contains `/topics` and every `topicDetailRoutes()` entry
    - assert sitemap does not contain `/topics/unknown-topic`
    - add `assertNoPrerenderedTopicRoute(root, route)` and call it for `/topics/unknown-topic`
    - add `assertTopicFallbackSource()` that reads `src/routes/topics/[slug].tsx` and checks for `maybePublicTopicBySlug(params.slug ?? "")`, `metadataForFallbackPage({`, `title: "No public topic here | Topics | Bright Builds"`, `canonicalPath: "/topics"`, `No public topic here`, `Browse topics`, and `href="/topics"`
    - call topic coverage and fallback checks from `assertSitemapAssetsAndRobots()`
  </action>
  <verify>
    <automated>bun run build && bun run verify:static</automated>
  </verify>
  <acceptance_criteria>
    - `rg "assertSitemapTopicCoverage|assertNoPrerenderedTopicRoute|assertTopicFallbackSource" scripts/verify-static/sitemap-assets-verifier.ts` finds topic checks.
    - `rg "/topics/unknown-topic|No public topic here \\| Topics \\| Bright Builds|canonicalPath: \\\"/topics\\\"" scripts/verify-static/sitemap-assets-verifier.ts` finds unknown-route and fallback checks.
    - `bun run build && bun run verify:static` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Extend browser coverage and write phase verification report</name>
  <files>tests/browser-release.playwright.ts, .planning/phases/31-static-topic-routes/31-VERIFICATION.md</files>
  <read_first>
    tests/browser-release.playwright.ts
    src/domain/topics.ts
    .planning/phases/31-static-topic-routes/31-CONTEXT.md
    .planning/phases/31-static-topic-routes/31-01-PLAN.md
    .planning/phases/31-static-topic-routes/31-02-PLAN.md
    .planning/phases/31-static-topic-routes/31-03-PLAN.md
    package.json
  </read_first>
  <action>
    Update `tests/browser-release.playwright.ts`:
    - import `publicTopics` and `topicDetailPath`
    - add a representative topic detail route helper that returns `topicDetailPath(publicTopics()[0])` and throws if no topic exists
    - extend keyboard focus assertions so focus reaches `/topics` from primary nav and reaches one representative topic detail route from `/topics`
    - extend reduced-motion representative routes to include `/topics` and the representative topic detail route
    - rely on the existing `for (const route of prerenderRoutes)` loop for axe and dark desktop/mobile layout coverage

    Run final verification:
    - `bun run format:check`
    - `bun run check`
    - `bun run typecheck`
    - `bun run test`
    - `bun run verify:curation`
    - `bun run verify:no-github-runtime`
    - `bun run verify:project-helper-surface`
    - `bun run verify:visual-system`
    - `bun run verify:social-previews`
    - `bun run build`
    - `bun run verify:browser`
    - `bun run verify:static`
    - `bun run verify:release`
    - `bun run verify`

    After all required checks pass, write `.planning/phases/31-static-topic-routes/31-VERIFICATION.md` with YAML frontmatter containing `status: passed`, `phase: 31-static-topic-routes`, `generated_by: gsd-verifier`, `lifecycle_mode: yolo`, `phase_lifecycle_id: 31-2026-06-30T22-33-56`, and sections summarizing evidence for DISC-01, DISC-02, DISC-03, DISC-05, static verification, browser verification, and residual risks.
  </action>
  <verify>
    <automated>bun run verify</automated>
  </verify>
  <acceptance_criteria>
    - `rg "publicTopics|topicDetailPath|/topics" tests/browser-release.playwright.ts` finds topic browser coverage.
    - `bun run verify` passes.
    - `test -f .planning/phases/31-static-topic-routes/31-VERIFICATION.md` succeeds.
    - `rg "^status: passed" .planning/phases/31-static-topic-routes/31-VERIFICATION.md` finds passed status.
    - `rg "DISC-01|DISC-02|DISC-03|DISC-05|bun run verify" .planning/phases/31-static-topic-routes/31-VERIFICATION.md` finds requirement and aggregate verification evidence.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:

```bash
bun run verify
```
</verification>

<success_criteria>
- Static output includes `/topics` and all public topic details.
- Static output excludes `/topics/unknown-topic`.
- Topic route metadata, JSON-LD, sitemap entries, and social image references are verified from built HTML.
- Browser checks cover topic routes for axe, desktop/mobile dark layout, keyboard focus, and reduced motion.
- `31-VERIFICATION.md` has `status: passed` only after `bun run verify` passes.
</success_criteria>
