---
phase: 31-static-topic-routes
plan: 02
type: execute
wave: 2
depends_on: [31-01]
files_modified:
  - src/components/TopicChip.tsx
  - src/routes/projects/index.tsx
  - src/routes/projects/[slug].tsx
  - src/routes/writing/index.tsx
  - src/routes/writing/[slug].tsx
  - src/routes/themes/index.tsx
  - src/routes/themes/[slug].tsx
  - src/routes/topics/index.tsx
  - src/routes/topics/[slug].tsx
  - src/domain/seo.ts
  - src/domain/topic-metadata.test.ts
  - src/styles/app.css
autonomous: true
requirements: [DISC-03, DISC-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 31-2026-06-30T22-33-56
generated_at: 2026-06-30T22:55:00Z
must_haves:
  truths:
    - "Topic chips link only when labels resolve to canonical topics through the Phase 30 helper surface."
    - "Unsupported labels remain inert chips and do not generate fabricated topic paths."
    - "Topic index and detail pages expose helper-derived metadata and JSON-LD."
    - "Topic metadata uses the existing fallback social image contract until Phase 35 adds generated topic preview assets."
    - "OpenLinks remains low-intrusion through existing footer and Person sameAs metadata."
  artifacts:
    - path: "src/components/TopicChip.tsx"
      provides: "Reusable safe topic chip rendering"
    - path: "src/domain/seo.ts"
      provides: "Topic metadata plus topic ItemList and CollectionPage JSON-LD helpers"
    - path: "src/domain/topic-metadata.test.ts"
      provides: "Focused metadata and JSON-LD coverage for topic routes"
---

<objective>
Add safe topic-linked chips across public surfaces and helper-derived topic metadata/JSON-LD while preserving non-leaking unsupported label behavior and the existing social-image fallback contract.
</objective>

<context>
@.planning/phases/31-static-topic-routes/31-CONTEXT.md
@.planning/phases/31-static-topic-routes/31-RESEARCH.md
@.planning/phases/31-static-topic-routes/31-UI-SPEC.md
@src/domain/topics.ts
@src/domain/seo.ts
@src/routes/projects/index.tsx
@src/routes/projects/[slug].tsx
@src/routes/writing/index.tsx
@src/routes/writing/[slug].tsx
@src/routes/themes/index.tsx
@src/routes/themes/[slug].tsx
@src/routes/topics/index.tsx
@src/routes/topics/[slug].tsx
@src/styles/app.css
</context>

<threat_model>
ASVS level: 1
Block on: high

Threats:
- Raw label route fabrication. Mitigation: `TopicChip` must call `maybeTopicRecordForLabel()` and must never slugify labels directly.
- Hidden-content signal through chip behavior. Mitigation: unresolved labels stay inert chips with no reason-specific copy, no tooltip, no disabled state, and no fabricated link.
- Metadata drift. Mitigation: topic route metadata and JSON-LD must be helper-derived and covered by unit/static checks.
- OpenLinks over-promotion. Mitigation: do not add OpenLinks CTAs to topic route cards; preserve only existing footer/Person sameAs placement.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add reusable safe topic chip rendering</name>
  <files>src/components/TopicChip.tsx, src/styles/app.css</files>
  <read_first>
    src/domain/topics.ts
    src/routes/projects/index.tsx
    src/routes/writing/index.tsx
    src/styles/app.css
    .planning/phases/31-static-topic-routes/31-UI-SPEC.md
  </read_first>
  <action>
    Create `src/components/TopicChip.tsx` exporting:

    ```ts
    export function TopicChip(props: { label: string; class?: string }) { ... }
    export function TopicChipList(props: { labels: readonly string[]; ariaLabel: string }) { ... }
    ```

    `TopicChip` must call `maybeTopicRecordForLabel(props.label)`. If a topic exists, render an `<a>` with `href={topicDetailPath(topic)}` and classes `chip topic-chip-link`. If no topic exists, render `<span class="chip">`. Do not slugify raw labels directly. Do not add disabled state, reason text, or tooltip for unresolved labels.

    `TopicChipList` must render a `<ul class="label-row" aria-label={props.ariaLabel}>` with each label in an `<li>` containing `TopicChip`.

    Add minimal CSS:
    - `.topic-chip-link` must preserve chip visual density
    - include visible `:hover` and `:focus-visible` states using existing emerald/cyan colors
    - no light-first classes or new palette
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/components/TopicChip.tsx` succeeds.
    - `rg "maybeTopicRecordForLabel|topicDetailPath" src/components/TopicChip.tsx` finds safe helper usage.
    - `rg "replace\\(|toLowerCase\\(|slugify" src/components/TopicChip.tsx` returns no raw label slugification.
    - `rg "topic-chip-link" src/styles/app.css src/components/TopicChip.tsx` finds chip styling and usage.
    - `bun run typecheck` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Replace raw public label chips with safe topic chips</name>
  <files>src/routes/projects/index.tsx, src/routes/projects/[slug].tsx, src/routes/writing/index.tsx, src/routes/writing/[slug].tsx, src/routes/themes/index.tsx, src/routes/themes/[slug].tsx, src/routes/topics/index.tsx, src/routes/topics/[slug].tsx</files>
  <read_first>
    src/components/TopicChip.tsx
    src/routes/projects/index.tsx
    src/routes/projects/[slug].tsx
    src/routes/writing/index.tsx
    src/routes/writing/[slug].tsx
    src/routes/themes/index.tsx
    src/routes/themes/[slug].tsx
    src/routes/topics/index.tsx
    src/routes/topics/[slug].tsx
  </read_first>
  <action>
    Update existing public label chip rows:
    - Project index cards: render `TopicChipList` for `props.project.themes` and `props.project.tags`, while keeping existing tier/placement/status chips inert.
    - Project detail facts: render `TopicChipList` for `selectedProject().themes` and `selectedProject().tags`, while keeping `sourceType` inert.
    - Writing index and writing detail: render `TopicChipList` for `entry.topics` and `entry.tags`, while keeping writing kind/date chips inert.
    - Theme index and detail: render `TopicChip` for public theme title/proof point labels only where they are intended as topic labels; keep audience/proof point chips inert if they are not canonical public labels.
    - Topic index/detail: render canonical topic labels as links only when linking to a different canonical topic is useful; avoid self-links on the active topic detail page.

    Preserve existing `aria-label` text for label rows. Do not make tier, status, date, source type, audience, or count chips topic links unless they are actual canonical topic labels.
  </action>
  <verify>
    <automated>bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg "TopicChip|TopicChipList" src/routes/projects src/routes/writing src/routes/themes src/routes/topics` finds route usage.
    - `rg "\\.map\\(\\(label\\).*href=\\{.*label" src/routes` returns no direct raw-label topic href construction.
    - `rg "sourceType|status|maturity|Published|Updated" src/routes` still finds inert non-topic metadata labels.
    - `bun run typecheck` passes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add topic metadata and JSON-LD helpers</name>
  <files>src/domain/seo.ts, src/domain/topic-metadata.test.ts, src/routes/topics/index.tsx, src/routes/topics/[slug].tsx</files>
  <read_first>
    src/domain/seo.ts
    src/domain/writing-metadata.test.ts
    src/domain/portfolio-surfaces.test.ts
    src/domain/topics.ts
    src/routes/topics/index.tsx
    src/routes/topics/[slug].tsx
  </read_first>
  <action>
    Update `src/domain/seo.ts` with:
    - `TopicItemListJsonLd`
    - `TopicCollectionPageJsonLd`
    - `metadataForTopic(topic: PublicTopic, profile: Profile = peterProfile): PageMetadata`
    - `topicItemListJsonLd(topics: readonly PublicTopic[] = publicTopics(), profile: Profile = peterProfile): TopicItemListJsonLd`
    - `topicCollectionPageJsonLd(topic: PublicTopic, profile: Profile = peterProfile): TopicCollectionPageJsonLd`

    `metadataForTopic()` must use:
    - title `${topic.label} | Topics | Bright Builds`
    - description derived from public reference counts, e.g. `${topic.label} connects ${N} public project, writing, and theme references from Peter Ryszkiewicz's work.`
    - canonical `${profile.canonicalOrigin}${topic.canonicalPath}`
    - Open Graph and Twitter fields matching existing `PageMetadata`
    - existing fallback social image behavior through `socialImageForRoutePath()`

    `topicItemListJsonLd()` must create an `ItemList` of topic `CollectionPage` items.
    `topicCollectionPageJsonLd()` must create a `CollectionPage` with `name`, `description`, `url`, `mainEntityOfPage`, `image`, `creator`, `keywords`, `about`, and `hasPart` entries for public references. For references:
    - project references use `SoftwareSourceCode`
    - writing references use `BlogPosting`
    - theme references use `CollectionPage`

    Add `src/domain/topic-metadata.test.ts` covering metadata, fallback image parity, ItemList ordering, CollectionPage `hasPart`, Person sameAs preservation, and safe JSON-LD serialization.

    Update `src/routes/topics/index.tsx` to render topic ItemList JSON-LD.
    Update `src/routes/topics/[slug].tsx` to use `metadataForTopic()` and `topicCollectionPageJsonLd()` for public topics and fallback metadata for missing topics.
  </action>
  <verify>
    <automated>bun run test src/domain/topic-metadata.test.ts && bun run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg "metadataForTopic|topicItemListJsonLd|topicCollectionPageJsonLd" src/domain/seo.ts` finds all helper exports.
    - `rg "TopicItemListJsonLd|TopicCollectionPageJsonLd" src/domain/seo.ts` finds topic JSON-LD types.
    - `rg "metadataForTopic|topicCollectionPageJsonLd" 'src/routes/topics/[slug].tsx'` finds detail route metadata/JSON-LD usage.
    - `rg "topicItemListJsonLd" src/routes/topics/index.tsx` finds index JSON-LD usage.
    - `rg "OpenLinks|sameAs" src/domain/topic-metadata.test.ts` finds identity preservation coverage.
    - `bun run test src/domain/topic-metadata.test.ts` passes.
    - `bun run typecheck` passes.
  </acceptance_criteria>
</task>

</tasks>

<verification>
Run:

```bash
bun run test src/domain/topic-metadata.test.ts
bun run typecheck
```
</verification>

<success_criteria>
- Project, writing, theme, and topic surfaces link only validated canonical topic labels.
- Unsupported labels remain inert and non-leaking.
- Topic routes expose canonical helper-derived metadata and JSON-LD.
- Topic social image references use the existing fallback contract.
- No new OpenLinks promotion is added to topic cards or CTAs.
</success_criteria>
