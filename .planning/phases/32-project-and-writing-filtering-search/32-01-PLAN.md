---
phase: 32-project-and-writing-filtering-search
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/topics.ts
  - src/domain/topics.test.ts
  - src/domain/content-search.ts
  - src/domain/content-search.test.ts
autonomous: true
requirements: [FIND-01, FIND-02, FIND-05]
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 32-2026-07-03T01-12-38
generated_at: 2026-07-03T01:43:57Z
must_haves:
  truths:
    - "Project and writing search/filter behavior is deterministic, pure, and tested before route UI wiring."
    - "Search/filter helpers compose public selectors and never index hidden, draft, archived, excluded, unsupported, or internal-only fields."
    - "Project facets include canonical topics, tier, public status, and visitor-facing source labels."
    - "Writing facets include kind, canonical topics, raw public tags, and checked-in published/updated year labels."
    - "No search dependency, hosted search, runtime fetch, query-param state, hash state, localStorage, or sessionStorage is introduced."
  artifacts:
    - path: "src/domain/topics.ts"
      provides: "Safe public project tier and writing updated-date fields on PublicContentReference"
      exports: ["PublicContentReference", "publicContentReferences"]
    - path: "src/domain/topics.test.ts"
      provides: "Regression coverage for safe public reference fields"
    - path: "src/domain/content-search.ts"
      provides: "Pure deterministic content search, facet derivation, scoring, and result counts"
      exports:
        - "CONTENT_SEARCH_WEIGHTS"
        - "normalizeContentSearchQuery"
        - "contentFacetGroupsForKind"
        - "searchContentReferences"
    - path: "src/domain/content-search.test.ts"
      provides: "Focused Vitest coverage for normalization, facets, scoring, stable order, and public eligibility"
  key_links:
    - from: "src/domain/content-search.ts"
      to: "src/domain/topics.ts"
      via: "PublicContentReference and publicContentReferences()"
      pattern: "publicContentReferences"
    - from: "src/domain/content-search.test.ts"
      to: "src/domain/projects.ts and src/domain/writing.ts"
      via: "fixture/public selector behavior"
      pattern: "publicProjectIndexProjects|publicWritingEntries"
---

<objective>
Add the deterministic Phase 32 search/filter domain model for public projects and writing.

Purpose: Give route components one tested functional core for in-page narrowing while preserving the static-first, public-only content contract from Phase 30.
Output: Safe public reference facet fields plus `src/domain/content-search.ts` and focused Vitest tests.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/32-project-and-writing-filtering-search/32-CONTEXT.md
@.planning/phases/32-project-and-writing-filtering-search/32-RESEARCH.md
@.planning/phases/30-content-discovery-foundation/30-01-SUMMARY.md
@.planning/phases/31-static-topic-routes/31-01-SUMMARY.md
@.planning/phases/31-static-topic-routes/31-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards/core/architecture.md
@standards/core/testing.md
@standards/languages/typescript-javascript.md
@src/domain/topics.ts
@src/domain/topics.test.ts
@src/domain/projects.ts
@src/domain/writing.ts
@package.json

<interfaces>
From `src/domain/topics.ts`:
```ts
export type PublicContentReference =
  | (PublicContentReferenceBase & {
      kind: "project";
      projectStatus: ProjectStatus;
      projectSourceType: ProjectSourceType;
    })
  | (PublicContentReferenceBase & {
      kind: "writing";
      writingKind: WritingKind;
      maybePublishedOn?: string;
    })
  | (PublicContentReferenceBase & {
      kind: "theme";
      themeStatus: ThemeStatus;
    });

export function publicContentReferences(
  sources: TopicReferenceSources = {},
): readonly PublicContentReference[];
```

From `src/domain/projects.ts`:
```ts
export type ProjectTier = "flagship" | "supporting" | "lab" | "archive" | "excluded";
export type ProjectStatus = "building" | "maintained" | "paused" | "archived" | "hidden";
export type ProjectSourceType =
  | "original"
  | "fork"
  | "repro"
  | "playground"
  | "generated"
  | "profile"
  | "support"
  | "concept";

export function publicProjectIndexProjects(
  projects?: readonly ProjectStory[],
): readonly ProjectStory[];
```

From `src/domain/writing.ts`:
```ts
export type WritingKind = "note" | "essay";
export type WritingEntry = {
  maybePublishedOn?: string;
  maybeUpdatedOn?: string;
  topics: readonly [string, ...string[]];
  tags: readonly string[];
};

export function publicWritingEntries(
  entries?: readonly WritingEntry[],
): readonly PublicWritingEntry[];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend public reference facets safely</name>
  <files>src/domain/topics.ts, src/domain/topics.test.ts</files>
  <read_first>
    src/domain/topics.ts
    src/domain/topics.test.ts
    src/domain/projects.ts
    src/domain/writing.ts
    .planning/phases/32-project-and-writing-filtering-search/32-CONTEXT.md
    .planning/phases/32-project-and-writing-filtering-search/32-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: a public project reference exposes `projectTier` from a public `ProjectStory` and does not expose hidden/internal fields.
    - Test 2: a public writing reference exposes `maybeUpdatedOn` only when the checked-in writing entry has that value.
    - Test 3: public reference filtering still excludes hidden, draft, archived, unsupported, excluded, and selected-looking archived fixtures.
  </behavior>
  <action>
    Update `src/domain/topics.ts` per D-01, D-04, D-05, D-06, D-11, and D-14:
    - import `type ProjectTier` from `./projects`
    - add `projectTier: ProjectTier` to the `kind: "project"` branch of `PublicContentReference`
    - set `projectTier: project.tier` in `referenceForProject()`
    - add `maybeUpdatedOn?: string` to the `kind: "writing"` branch of `PublicContentReference`
    - set `maybeUpdatedOn: entry.maybeUpdatedOn` in `referenceForWriting()`
    - do not add raw registry records, hidden flags, draft reasons, archive reasons, curation diagnostics, source URLs, or internal-only fields to `PublicContentReference`

    Update `src/domain/topics.test.ts` before implementation if the test is not already present. Extend the safe public fields test so the project assertion includes `projectTier: "flagship"` and the writing assertion includes `maybeUpdatedOn` for a fixture that has that field. Keep the existing hidden/draft/archived/excluded filtering test intact.
  </action>
  <verify>
    <automated>bun run test src/domain/topics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg "projectTier: ProjectTier|projectTier: project\\.tier" src/domain/topics.ts` finds the safe project tier field and assignment.
    - `rg "maybeUpdatedOn\\?: string|maybeUpdatedOn: entry\\.maybeUpdatedOn" src/domain/topics.ts` finds the safe writing updated-date field and assignment.
    - `rg "projectTier|maybeUpdatedOn" src/domain/topics.test.ts` finds regression coverage.
    - `rg "reason|statusReason|hidden|draft|archived" src/domain/topics.ts` does not show new public reference fields with those names.
    - `bun run test src/domain/topics.test.ts` passes.
  </acceptance_criteria>
  <done>Public reference envelopes expose the exact safe fields required for Phase 32 facets without weakening existing public eligibility behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add deterministic content search and facet helpers</name>
  <files>src/domain/content-search.ts, src/domain/content-search.test.ts</files>
  <read_first>
    src/domain/content-search.ts
    src/domain/content-search.test.ts
    src/domain/topics.ts
    src/domain/topics.test.ts
    src/domain/projects.ts
    src/domain/writing.ts
    standards/core/architecture.md
    standards/core/testing.md
    standards/languages/typescript-javascript.md
    .planning/phases/32-project-and-writing-filtering-search/32-CONTEXT.md
    .planning/phases/32-project-and-writing-filtering-search/32-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `normalizeContentSearchQuery("  Open-Web, AI!!  ")` returns `["open", "web", "ai"]`.
    - Test 2: project facet groups are exactly `Topics`, `Tier`, `Status`, and `Source`, with IDs such as `topic:ai`, `project-tier:flagship`, `project-status:building`, and `project-source:original`.
    - Test 3: project source labels are visitor-facing, including `Original`, `Fork / promoted work`, `Prototype / playground`, and no raw internal taxonomy-only copy.
    - Test 4: writing facet groups are exactly `Kind`, `Topics`, `Tags`, and `Year`, with IDs such as `writing-kind:note`, `topic:open-web`, `writing-tag:identity`, `writing-year:published:2026`, and `writing-year:updated:2026`.
    - Test 5: selected facets are OR within one group and AND across different groups.
    - Test 6: query scoring orders title matches before canonical topic matches, canonical topic matches before public facet/source-label matches, and public facet/source-label matches before summary matches.
    - Test 7: equal-score results tie-break by `displayOrder`, then `kind`, then `slug`.
    - Test 8: when query and facets are empty, `searchContentReferences()` returns all references for the requested kind in original public display order with `active: false`.
    - Test 9: helper defaults use `publicContentReferences()` and do not require callers to import raw curated registries.
  </behavior>
  <action>
    Create `src/domain/content-search.ts` per D-01 through D-05, D-09, D-11, D-14, D-17, D-23, and D-24. Do not add Fuse, MiniSearch, hosted search, semantic search, typo tolerance, autocomplete, generated indexes, visitor-runtime fetches, URL APIs, hash APIs, `localStorage`, or `sessionStorage` per D-03, D-17, and FIND-05.

    Export these exact types and helpers:
    - `export type ContentSearchKind = "project" | "writing";`
    - `export type ContentFacetGroupId = "topics" | "project-tier" | "project-status" | "project-source" | "writing-kind" | "writing-tag" | "writing-year";`
    - `export type ContentFacet = { id: string; groupId: ContentFacetGroupId; label: string; count: number };`
    - `export type ContentFacetGroup = { id: ContentFacetGroupId; label: string; facets: readonly ContentFacet[] };`
    - `export type ContentSearchInput = { references?: readonly PublicContentReference[]; kind: ContentSearchKind; query?: string; selectedFacetIds?: readonly string[] };`
    - `export type ContentSearchResult = { reference: PublicContentReference; score: number; matchedFacetIds: readonly string[] };`
    - `export type ContentSearchSummary = { active: boolean; totalCount: number; visibleCount: number; results: readonly ContentSearchResult[] };`
    - `export const CONTENT_SEARCH_WEIGHTS = { title: 100, canonicalTopic: 70, publicFacet: 55, sourceLabel: 40, summary: 20 } as const;`
    - `export function normalizeContentSearchQuery(query: string): readonly string[]`
    - `export function contentFacetGroupsForKind(kind: ContentSearchKind, references: readonly PublicContentReference[] = publicContentReferences()): readonly ContentFacetGroup[]`
    - `export function searchContentReferences(input: ContentSearchInput): ContentSearchSummary`

    Implement normalization exactly as: trim, lowercase, remove straight/curly apostrophes, replace every non-`[a-z0-9]` run with a single space, split on whitespace, and drop empty tokens.

    Implement matching:
    - restrict references to `kind`
    - query tokens must all match at least one indexed public field
    - indexed fields are `title`, `summary`, canonical topic `label` and `slug`, source label `label`, and public facet labels/IDs
    - score each token by the highest matching field weight for that token, then sum token scores
    - selected facets match OR within the same `groupId` and AND across different `groupId` values
    - sort by score descending, then `displayOrder` ascending, then `kind` ascending, then `slug` ascending

    Implement facet labels:
    - project tiers: `Flagship`, `Supporting`, `Lab / Prototype`, `Archive`
    - project statuses: `Building`, `Maintained`, `Paused`
    - project sources: `Original`, `Fork / promoted work`, `Reproduction / reference`, `Prototype / playground`, `Generated experiment`, `Profile / identity`, `Supporting infrastructure`, `Concept / proposal`
    - writing kinds: `Note`, `Essay`
    - writing years: `Published YYYY` from `maybePublishedOn`, `Updated YYYY` from `maybeUpdatedOn`
    - topic facets must use canonical topic slugs from `reference.canonicalTopics` and never slugify raw labels
    - tag facets for writing use the checked-in public `sourceLabels` with `kind: "writing-tag"` and preserve the raw public tag text as the visitor-facing label
  </action>
  <verify>
    <automated>bun run test src/domain/content-search.test.ts src/domain/topics.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/domain/content-search.ts` succeeds.
    - `rg "CONTENT_SEARCH_WEIGHTS|normalizeContentSearchQuery|contentFacetGroupsForKind|searchContentReferences" src/domain/content-search.ts` finds all required exports.
    - `rg "title: 100|canonicalTopic: 70|publicFacet: 55|sourceLabel: 40|summary: 20" src/domain/content-search.ts` finds the explicit weights.
    - `rg "Fork / promoted work|Prototype / playground|Published \\\\${year}|Updated \\\\${year}" src/domain/content-search.ts src/domain/content-search.test.ts` finds visitor-facing facet/date labels.
    - `rg "Fuse|MiniSearch|URLSearchParams|location\\.search|localStorage|sessionStorage|fetch\\(" src/domain/content-search.ts` returns no matches.
    - `bun run test src/domain/content-search.test.ts src/domain/topics.test.ts` passes.
  </acceptance_criteria>
  <done>The pure content search/filter model is implemented with deterministic scoring, safe facets, stable ordering, and focused unit coverage.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run focused domain gates</name>
  <files>src/domain/content-search.ts, src/domain/content-search.test.ts, src/domain/topics.ts, src/domain/topics.test.ts</files>
  <read_first>
    package.json
    src/domain/content-search.ts
    src/domain/content-search.test.ts
    src/domain/topics.ts
    src/domain/topics.test.ts
    .planning/phases/32-project-and-writing-filtering-search/32-CONTEXT.md
  </read_first>
  <action>
    Run the focused verification for Plan 32-01 per D-24 and D-27:
    - `bun run test src/domain/content-search.test.ts src/domain/topics.test.ts`
    - `bun run typecheck`
    - `bun run check`

    If any command fails, fix only the files owned by this plan. Do not start project or writing route UI wiring in this plan.
  </action>
  <verify>
    <automated>bun run test src/domain/content-search.test.ts src/domain/topics.test.ts && bun run typecheck && bun run check</automated>
  </verify>
  <acceptance_criteria>
    - Terminal evidence shows `bun run test src/domain/content-search.test.ts src/domain/topics.test.ts` passes.
    - Terminal evidence shows `bun run typecheck` passes.
    - Terminal evidence shows `bun run check` passes.
    - `git diff -- src/routes/projects/index.tsx src/routes/writing/index.tsx tests/browser-release.playwright.ts scripts/verify-static/expected-route-text.ts` is empty after this plan.
  </acceptance_criteria>
  <done>Domain behavior is verified independently before either route consumes it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checked-in registries -> public search references | Raw project, writing, theme, and topic data is narrowed into visitor-safe reference envelopes. |
| Visitor query/facet input -> in-memory UI state | Untrusted text and selected facet IDs influence local filtering only. |
| Domain helper -> route rendering | Route components consume helper output and render it through JSX text nodes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-32-01-01 | Information Disclosure | `src/domain/topics.ts` | mitigate | Compose `publicProjectIndexProjects()`, `publicWritingEntries()`, and `publicThemeEntries()` only; tests must prove hidden/draft/archived/excluded/unsupported fixtures are absent. |
| T-32-01-02 | Tampering / Information Disclosure | `normalizeContentSearchQuery()` and route consumers | mitigate | Treat query text as inert tokens, never as HTML or route input; no `innerHTML`, URL construction, or runtime fetch behavior. |
| T-32-01-03 | Denial of Service / Information Disclosure | `searchContentReferences()` | mitigate | Keep state in memory only and do not emit query params, hash state, localStorage, sessionStorage, sitemap routes, or canonical URL changes. |
| T-32-01-04 | Information Disclosure | Facet derivation | mitigate | Topic facets use `reference.canonicalTopics`; writing tag facets use public writing source labels; no raw label slugification or diagnostic reason fields. |
| T-32-01-05 | Repudiation | Search ranking | mitigate | Export explicit weights and cover ordering/tie-breaks in unit tests so ranking decisions are explainable. |
</threat_model>

<verification>
Run:

```bash
bun run test src/domain/content-search.test.ts src/domain/topics.test.ts
bun run typecheck
bun run check
```
</verification>

<success_criteria>
- `src/domain/content-search.ts` exports the agreed helper contract and uses only public references.
- Unit tests prove normalization, facets, scoring, stable ordering, public eligibility, and no dependency/search-index behavior.
- Project tier and writing updated-year data are exposed only as safe public reference fields.
- No route UI, URL state, runtime fetch, sitemap, feed, related-work, social-preview, or release-evidence scope is implemented in this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/32-project-and-writing-filtering-search/32-01-SUMMARY.md`.
</output>
