---
phase: 07-release-gates-deploy-readiness
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/release-readiness.md
  - README.md
  - scripts/release-readiness.ts
  - scripts/release-readiness.test.ts
  - scripts/verify-release.ts
  - scripts/verify-release.test.ts
autonomous: true
requirements:
  - GATE-02
  - GATE-03
  - GATE-04
  - REL-01
  - REL-02
  - REL-03
  - REL-04
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 7-2026-05-31T22-21-18
generated_at: 2026-05-31T22:23:06.258Z

must_haves:
  truths:
    - "`bun run verify` remains the aggregate release command and includes browser, static metadata, and release-readiness gates."
    - "`bun run verify:release` fails when generated external anchors are not covered by the explicit external-link policy."
    - "`bun run verify:release` rejects non-HTTPS external anchors and sensitive query keys without printing token values."
    - "A checked-in release-readiness document covers Cloudflare Pages/static deployment assumptions, build command, output directory, runtime pins, preview checks, post-deploy smoke checks, and token-safety guidance."
    - "`bun run verify:release` fails when the release-readiness document omits required deployment or external-link policy facts."
    - "SEO/static metadata coverage remains documented as part of the aggregate release contract through `bun run verify:static`."
    - "Performance/best-practices coverage remains documented as deterministic static output budgets in `bun run verify:release`."
  artifacts:
    - path: "scripts/release-readiness.ts"
      provides: "Pure release-readiness policy helpers"
      contains: "externalLinkFindingsForRoutes"
    - path: "scripts/release-readiness.test.ts"
      provides: "Unit tests for release-readiness policy and docs validation"
      contains: "release-readiness"
    - path: "docs/release-readiness.md"
      provides: "Cloudflare/static release contract and checklist"
      contains: "Cloudflare Pages"
    - path: "scripts/verify-release.ts"
      provides: "Post-build release verifier integration"
      contains: "releaseReadinessDocumentFindings"
    - path: "README.md"
      provides: "Maintainer-facing release command summary"
      contains: "release-readiness"
  key_links:
    - from: "scripts/verify-release.ts"
      to: "scripts/release-readiness.ts"
      via: "imports release-readiness helpers"
      pattern: "from \"./release-readiness\""
    - from: "package.json"
      to: "tests/browser-release.playwright.ts"
      via: "aggregate verify runs verify:browser"
      pattern: "bun run verify:browser"
    - from: "docs/release-readiness.md"
      to: "package.json"
      via: "documents exact aggregate command"
      pattern: "bun run verify"
---

<objective>
Create a checked release-readiness contract for SEO/static metadata, performance budgets, external-link policy, and Cloudflare/static deployment assumptions.

Purpose: Maintainers need one reliable release command and one checked document that explains what is automated, what is manual, and how to avoid fragile external checks or token leaks.
Output: release-readiness helper module, tests, release verifier integration, release-readiness documentation, and README updates.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-release-gates-deploy-readiness/07-CONTEXT.md
@.planning/phases/07-release-gates-deploy-readiness/07-RESEARCH.md
@.planning/phases/06-browser-accessibility-release-automation/06-01-SUMMARY.md
@package.json
@scripts/verify-static.ts
@scripts/verify-release.ts
@scripts/verify-release.test.ts
@tests/browser-release.playwright.ts
@README.md
@CONTRIBUTING.md
</context>

<threat_model>
This phase changes release automation and documentation only. It must not introduce visitor runtime network calls, secrets, or third-party live checks in the local release gate.

Threats:
- Token leakage through external-link diagnostics; mitigate by redacting query values and reporting only origin/path plus sensitive key names.
- Fragile release failures from live third-party checks; mitigate by enforcing policy coverage locally and documenting manual smoke checks.
- Drift between docs and scripts; mitigate by having `verify:release` fail when required release-readiness document facts are missing.
- Overgrown release verifier; mitigate by moving new policy logic into `scripts/release-readiness.ts`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add release-readiness policy helpers and tests</name>
  <files>scripts/release-readiness.ts, scripts/release-readiness.test.ts, scripts/verify-release.test.ts</files>
  <read_first>scripts/verify-release.ts, scripts/verify-release.test.ts, package.json</read_first>
  <action>Create `scripts/release-readiness.ts` with pure helpers for external-link policy coverage, HTTPS enforcement, sensitive query-key rejection, required primary external href presence, release-readiness document validation, and release-readiness evidence labels. Create `scripts/release-readiness.test.ts` with focused Vitest tests for accepted links, uncovered origins, non-HTTPS links, sensitive query keys, missing required primary links, and document validation. Update existing release verifier tests only where labels or moved helper behavior require it.</action>
  <verify>bun run test -- scripts/release-readiness.test.ts scripts/verify-release.test.ts</verify>
  <acceptance_criteria>
    - `scripts/release-readiness.ts` exports `externalLinkFindingsForRoutes`.
    - `scripts/release-readiness.ts` exports `releaseReadinessDocumentFindings`.
    - `scripts/release-readiness.test.ts` contains Arrange/Act/Assert sections.
    - Unit tests prove token-like query values are not emitted in findings.
  </acceptance_criteria>
  <done>Release-readiness policy logic is isolated and unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate release-readiness checks and docs</name>
  <files>scripts/verify-release.ts, docs/release-readiness.md, README.md</files>
  <read_first>scripts/verify-release.ts, scripts/release-readiness.ts, README.md, CONTRIBUTING.md</read_first>
  <action>Import the new release-readiness helpers into `scripts/verify-release.ts`, remove the old inline external-link presence helper, add document findings to the release findings array, and include release-readiness evidence labels in release output. Add `docs/release-readiness.md` covering the exact aggregate command, SEO/static metadata gate, browser/accessibility gate, static budget performance gate, external-link policy, Cloudflare Pages build command/output/runtime pins/environment expectations, preview deploy checklist, post-deploy smoke checks, and token safety. Update `README.md` to mention `verify:browser`, the release-readiness document, and the expanded aggregate release gate.</action>
  <verify>bun run build && bun run verify:release</verify>
  <acceptance_criteria>
    - `scripts/verify-release.ts` imports from `./release-readiness`.
    - `scripts/verify-release.ts` includes `releaseReadinessDocumentFindings()` in release findings.
    - `docs/release-readiness.md` contains `Cloudflare Pages`, `bun run verify`, `bun run build`, `.output/public`, `BUN_VERSION=1.3.14`, and `Manual external-link smoke check`.
    - `README.md` links to `docs/release-readiness.md`.
  </acceptance_criteria>
  <done>`verify:release` enforces release-readiness documentation and policy coverage.</done>
</task>

<task type="auto">
  <name>Task 3: Run full verification and document completion</name>
  <files>.planning/phases/07-release-gates-deploy-readiness/07-01-SUMMARY.md, .planning/phases/07-release-gates-deploy-readiness/07-VERIFICATION.md</files>
  <read_first>.planning/phases/07-release-gates-deploy-readiness/07-01-PLAN.md, package.json</read_first>
  <action>Run `bun run format`, `bun run check`, `bun run typecheck`, `bun run test`, `bun run build`, `bun run verify:release`, and `bun run verify`. Create `07-01-SUMMARY.md` recording completed requirements GATE-02, GATE-03, GATE-04, REL-01, REL-02, REL-03, and REL-04. Create `07-VERIFICATION.md` proving every must-have truth and artifact from this plan, with lifecycle provenance matching `7-2026-05-31T22-21-18`.</action>
  <verify>bun run verify && node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 7 --require-plans --require-verification</verify>
  <acceptance_criteria>
    - `bun run verify` exits 0.
    - `07-01-SUMMARY.md` contains `requirements-completed`.
    - `07-VERIFICATION.md` contains `status: passed`.
    - Lifecycle validation exits valid.
  </acceptance_criteria>
  <done>Phase 7 is verified and ready for strict wrapper commit/push.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run format`
- [ ] `bun run check`
- [ ] `bun run typecheck`
- [ ] `bun run test`
- [ ] `bun run build`
- [ ] `bun run verify:release`
- [ ] `bun run verify`
- [ ] `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" verify lifecycle 7 --require-plans --require-verification`
</verification>

<success_criteria>
- Maintainers can run one aggregate release command, `bun run verify`, for browser, accessibility, SEO/static metadata, performance budgets, external-link policy, deployment documentation, and static checks.
- External links are covered by explicit policy without live third-party network checks.
- Token-sensitive external-link failures are reported without leaking values.
- Cloudflare/static deployment assumptions are documented and checked by release verification.
- Phase 7 requirements are recorded as completed in the summary and verification artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/07-release-gates-deploy-readiness/07-01-SUMMARY.md` and `.planning/phases/07-release-gates-deploy-readiness/07-VERIFICATION.md`.
</output>
