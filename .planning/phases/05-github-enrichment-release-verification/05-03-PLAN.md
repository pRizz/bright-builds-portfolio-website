---
phase: 05-github-enrichment-release-verification
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - scripts/verify-release.ts
  - scripts/verify-release.test.ts
  - package.json
  - README.md
  - CONTRIBUTING.md
  - .planning/ROADMAP.md
  - .planning/phases/05-github-enrichment-release-verification/05-RELEASE-EVIDENCE.md
autonomous: true
requirements:
  - GH-04
  - VER-01
  - VER-02
  - VER-03
  - VER-04
  - VER-05
user_setup: []
must_haves:
  truths:
    - "Developer can run one release verifier that composes static output, token-safety, link, accessibility-semantics, and budget checks."
    - "Documentation records setup, build, release, deployment, curation, and optional GitHub metadata refresh rules without exposing token values or public token prefixes."
    - "Final release evidence records automated verification plus desktop, mobile, keyboard, and reduced-motion browser evidence."
  artifacts:
    - path: "scripts/verify-release.ts"
      provides: "Dependency-free release verifier over built `.output/public`"
      contains: "Release verification passed"
    - path: "scripts/verify-release.test.ts"
      provides: "Unit tests for release verifier pattern and budget helpers"
      contains: "GitHub token-like value"
    - path: "README.md"
      provides: "Local setup, build, release, deployment, and metadata refresh documentation"
      contains: "bun run verify:release"
    - path: "CONTRIBUTING.md"
      provides: "Curation maintenance and release verification guidance"
      contains: "GITHUB_METADATA_TOKEN"
    - path: ".planning/phases/05-github-enrichment-release-verification/05-RELEASE-EVIDENCE.md"
      provides: "Final Phase 5 release evidence matrix"
      contains: "Browser Evidence"
  key_links:
    - from: "package.json"
      to: "scripts/verify-release.ts"
      via: "`verify:release` script after `verify:static`"
      pattern: "verify:release"
    - from: "scripts/verify-release.ts"
      to: ".output/public"
      via: "post-build filesystem scanner"
      pattern: ".output/public"
    - from: "README.md"
      to: "scripts/sync-github-metadata.ts"
      via: "`bun run sync:github-metadata` documentation"
      pattern: "sync:github-metadata"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-05-27T11-26-21
generated_at: 2026-05-27T11:53:11Z
---

<objective>
Add release verification, documentation, final evidence, and directly related planning-drift cleanup.

Purpose: Phase 5 completion must prove the static production site is token-safe, accessible enough for release checks, performant by project budgets, SEO-valid, documented, and ready to ship.
Output: Release verifier script/tests, aggregate verification wiring, README/CONTRIBUTING updates, release evidence artifact, and Phase 4 roadmap drift correction.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
@.planning/phases/05-github-enrichment-release-verification/05-RESEARCH.md
@.planning/phases/05-github-enrichment-release-verification/05-UI-SPEC.md
@.planning/phases/04-visual-system-motion/04-03-SUMMARY.md
@.planning/phases/05-github-enrichment-release-verification/05-02-SUMMARY.md
@scripts/verify-static.ts
@scripts/verify-no-github-runtime.ts
@scripts/verify-visual-system.ts
@scripts/sync-github-metadata.ts
@package.json
@README.md
@CONTRIBUTING.md
@src/domain/projects.ts
@src/domain/github-metadata.ts

<interfaces>
Existing aggregate verify in `package.json`:
```json
"verify": "bun run format:check && bun run check && bun run typecheck && bun run test && bun run verify:curation && bun run verify:no-github-runtime && bun run verify:visual-system && bun run build && bun run verify:static"
```

Release verifier must extend the chain to:
```json
"verify:release": "bun run scripts/verify-release.ts",
"verify": "... && bun run build && bun run verify:static && bun run verify:release"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add dependency-free release verifier and aggregate script wiring</name>
  <files>scripts/verify-release.ts, scripts/verify-release.test.ts, package.json</files>
  <read_first>
    - scripts/verify-static.ts
    - scripts/verify-no-github-runtime.ts
    - scripts/verify-visual-system.ts
    - package.json
    - src/domain/projects.ts
    - src/domain/github-metadata.ts
    - .planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
    - .planning/phases/05-github-enrichment-release-verification/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test: forbidden built-output patterns catch `api.github.com`, `github.com/graphql`, `@octokit/`, `GITHUB_TOKEN`, `GITHUB_METADATA_TOKEN`, public GitHub token env names, `github_pat_...`, and `gh[pousr]_...` token-like values.
    - Test: internal link checker accepts links whose route file and target anchor exist and rejects missing route or anchor targets.
    - Test: static budget helper reports route HTML, JS, CSS, and asset sizes and fails when configured thresholds are exceeded.
    - Test: semantic checker requires one `<main>`, one route `<h1>`, a skip link, JSON-LD on indexable routes, and no visitor-facing GitHub metadata maintenance error copy.
  </behavior>
  <action>Create `scripts/verify-release.ts` as a dependency-free Bun TypeScript release verifier over `.output/public` per D-14, D-15, and D-16. It must run only after `bun run build && bun run verify:static`; fail if no `.output/public` HTML exists. Scan built `.html`, `.js`, `.css`, `.json`, `.txt`, and `.svg` files for forbidden runtime/API/token patterns: `api.github.com`, `github.com/graphql`, `@octokit/`, `GITHUB_TOKEN`, `GITHUB_METADATA_TOKEN`, `VITE_*GITHUB*TOKEN`, `PUBLIC_*GITHUB*TOKEN`, `SOLID_PUBLIC_*GITHUB*TOKEN`, `github_pat_`, and `gh[pousr]_` token-like values per D-11 and D-12. Add static release heuristics for internal links and anchors, primary external link presence, one main landmark, one h1 per route, skip link, JSON-LD presence, reduced-motion CSS output, no remote runtime visual assets, and hard-fail budgets based on research assumptions: route HTML <= 75 KB, total uncompressed client JS <= 150 KB, total CSS <= 100 KB, and `social/bright-builds-og.png` <= 250 KB. Print measured sizes and finish with exact text `Release verification passed`. Add `scripts/verify-release.test.ts` for pure helpers and wire `"verify:release": "bun run scripts/verify-release.ts"` into `package.json` after `verify:static` in the aggregate `verify` command. Do not add Playwright, axe, Lighthouse, Octokit, Python, or new npm packages.</action>
  <acceptance_criteria>
    - `rg '"verify:release": "bun run scripts/verify-release.ts"|verify:static && bun run verify:release' package.json` returns release script wiring.
    - `rg 'Release verification passed|\\.output/public|api\\.github\\.com|github\\.com/graphql|@octokit/|GITHUB_METADATA_TOKEN|VITE_\\[A-Z0-9_\\]\\*GITHUB|PUBLIC_\\[A-Z0-9_\\]\\*GITHUB|SOLID_PUBLIC_\\[A-Z0-9_\\]\\*GITHUB|github_pat_|gh\\[pousr\\]_' scripts/verify-release.ts` returns all release-scan patterns.
    - `rg '75 \\* 1024|150 \\* 1024|100 \\* 1024|250 \\* 1024|internal link|skip link|JSON-LD|reduced-motion|one h1' scripts/verify-release.ts scripts/verify-release.test.ts` returns budget and semantic checks.
    - `rg '@playwright|axe-core|lighthouse|@octokit|python' package.json scripts/verify-release.ts scripts/verify-release.test.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- scripts/verify-release.test.ts && bun run build && bun run verify:release && bun run verify</automated>
  </verify>
  <done>`bun run verify` includes release verification after static verification, and the release verifier fails on token/runtime leaks, broken internal links/anchors, missing static semantics, or budget regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Update release, curation, deployment, and metadata refresh docs</name>
  <files>README.md, CONTRIBUTING.md</files>
  <read_first>
    - README.md
    - CONTRIBUTING.md
    - package.json
    - scripts/sync-github-metadata.ts
    - scripts/verify-release.ts
    - .planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
    - AGENTS.md
    - AGENTS.bright-builds.md
    - standards-overrides.md
  </read_first>
  <action>Update only repo-local sections outside managed Bright Builds blocks. In `README.md`, document local setup (`bun install`, `bun run dev`), production build (`bun run build`), aggregate release gate (`bun run verify`), direct release verifier (`bun run verify:release`), static output proof surface `.output/public`, deployment assumption that a static host serves `.output/public`, optional metadata refresh (`bun run sync:github-metadata` and `bun run sync:github-metadata:strict`), and optional non-public `GITHUB_METADATA_TOKEN`. In `CONTRIBUTING.md`, add curation maintenance rules that `src/domain/projects.ts` controls authored copy, tier, ordering, placement, themes, tags, and curated links; GitHub metadata remains advisory; direct repo links drive enrichment; related links do not; OpenLinks remains low-intrusion in footer/about/contact/profile and metadata. State that docs must never include token values and must not recommend `VITE_*`, `PUBLIC_*`, or `SOLID_PUBLIC_*` GitHub token prefixes per D-13, D-18, and D-19.</action>
  <acceptance_criteria>
    - `rg 'bun install|bun run dev|bun run build|bun run verify|bun run verify:release|\\.output/public|sync:github-metadata|sync:github-metadata:strict|GITHUB_METADATA_TOKEN' README.md` returns all setup/release/refresh docs.
    - `rg 'src/domain/projects.ts|advisory|direct repo links|related links|OpenLinks|low-intrusion|VITE_\\*|PUBLIC_\\*|SOLID_PUBLIC_\\*' CONTRIBUTING.md` returns curation, OpenLinks, and token-prefix guidance.
    - `rg 'ghp_|github_pat_|BEGIN|paste your token|VITE_.*GITHUB.*TOKEN|PUBLIC_.*GITHUB.*TOKEN|SOLID_PUBLIC_.*GITHUB.*TOKEN' README.md CONTRIBUTING.md` returns no matches; do not include any concrete token value.
    - `rg 'bright-builds-rules-readme-badges:begin|bright-builds-rules-readme-badges:end|bright-builds-rules-contributing:begin|bright-builds-rules-contributing:end' README.md CONTRIBUTING.md` still returns managed block markers.
  </acceptance_criteria>
  <verify>
    <automated>bun run format:check && bun run check && bun run verify:release</automated>
  </verify>
  <done>Docs explain setup, release gates, deployment assumptions, curated authority, optional metadata refresh, token-safe env naming, and low-intrusion OpenLinks placement without editing managed blocks or exposing secrets.</done>
</task>

<task type="auto">
  <name>Task 3: Record final release evidence and fix direct roadmap drift</name>
  <files>.planning/phases/05-github-enrichment-release-verification/05-RELEASE-EVIDENCE.md, .planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/phases/04-visual-system-motion/04-03-SUMMARY.md
    - .planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
    - package.json
    - scripts/verify-release.ts
  </read_first>
  <action>Run the final automated release gate `bun run verify`. Start the local production-equivalent dev server with `bun run dev -- --host 127.0.0.1` or the repo-supported equivalent, use available browser automation without adding dependencies, and record evidence in `.planning/phases/05-github-enrichment-release-verification/05-RELEASE-EVIDENCE.md`. Evidence must cover `/`, `/projects`, `/about`, and `/contact` at `1440x900`, `390x844`, and `320x844`; `.dark` root and body background `rgb(5, 6, 8)`; no horizontal overflow; no detected text/control overlap; no console errors; reduced-motion with `matchMedia("(prefers-reduced-motion: reduce)").matches === true`; keyboard Tab focus coverage for skip link, nav, primary project/contact links, metadata homepage links when present, and footer OpenLinks profile. Stop the dev server and prove no listener remains on the chosen port. In `.planning/ROADMAP.md`, fix the directly related Phase 4 drift from `**Plans**: 2/3 plans complete` to `**Plans**: 3/3 plans complete` per D-20 without changing unrelated roadmap content.</action>
  <acceptance_criteria>
    - `rg 'Release Evidence|Automated Verification|bun run verify|Browser Evidence|1440x900|390x844|320x844|prefers-reduced-motion|keyboard|OpenLinks profile|no horizontal overflow|no console errors' .planning/phases/05-github-enrichment-release-verification/05-RELEASE-EVIDENCE.md` returns all final evidence sections and route matrix details.
    - `rg '\\*\\*Plans\\*\\*: 3/3 plans complete' .planning/ROADMAP.md` returns the corrected Phase 4 plan count.
    - `rg '\\*\\*Plans\\*\\*: 2/3 plans complete' .planning/ROADMAP.md` returns no matches.
    - `rg 'Release verification passed|Verified .* prerendered routes|No visitor-runtime GitHub API' .planning/phases/05-github-enrichment-release-verification/05-RELEASE-EVIDENCE.md` returns command-output summaries from the final release gate.
  </acceptance_criteria>
  <verify>
    <automated>bun run verify && rg 'Browser Evidence|1440x900|390x844|320x844|prefers-reduced-motion|keyboard' .planning/phases/05-github-enrichment-release-verification/05-RELEASE-EVIDENCE.md && rg '\\*\\*Plans\\*\\*: 3/3 plans complete' .planning/ROADMAP.md</automated>
  </verify>
  <done>Final release evidence exists, aggregate verification passes, browser evidence covers the required routes/viewports/accessibility fallbacks, the dev server is stopped, and the Phase 4 roadmap plan-count drift is corrected.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| built output -> release verifier | Generated HTML/JS/CSS/assets are scanned for runtime dependencies, token leakage, semantics, links, and budgets. |
| docs -> developer/operator | README and CONTRIBUTING tell future maintainers how to handle setup, refresh, release, and tokens. |
| browser evidence -> release record | Browser observations become release-readiness evidence without claiming a dependency-backed axe/Lighthouse result. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03-01 | I | `.output/public` scanner | mitigate | Hard-fail on GitHub API endpoints, GitHub clients, `GITHUB_METADATA_TOKEN`, public token env names, and GitHub token-like values before release. |
| T-05-03-02 | D | Internal links and anchors | mitigate | Release verifier scans generated static route links and anchors so broken primary navigation/project anchors fail the release gate. |
| T-05-03-03 | R/I | Accessibility and performance claims | mitigate | Name scripted checks as static release heuristics, record browser evidence separately, and do not claim axe/Lighthouse coverage because those dependencies are deferred. |
| T-05-03-04 | I | README/CONTRIBUTING token docs | mitigate | Docs mention only optional non-public `GITHUB_METADATA_TOKEN`, forbid public token prefixes, and include no token values. |
| T-05-03-05 | T | `.planning/ROADMAP.md` drift fix | mitigate | Change only the exact Phase 4 `**Plans**: 2/3 plans complete` line to `3/3`, verified by grep. |
</threat_model>

<verification>
Run `bun run test -- scripts/verify-release.test.ts && bun run build && bun run verify:release && bun run verify`, then verify release evidence and roadmap drift with the task `rg` commands.
</verification>

<success_criteria>
The production build has a repo-owned release gate, documentation explains release and optional metadata refresh safely, final evidence covers automated and browser checks, and directly related roadmap drift is fixed.
</success_criteria>

<output>
After completion, create `.planning/phases/05-github-enrichment-release-verification/05-03-SUMMARY.md`.
</output>
