---
phase: 05-github-enrichment-release-verification
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - scripts/sync-github-metadata.ts
  - scripts/sync-github-metadata.test.ts
  - package.json
  - src/domain/github-metadata.snapshot.json
  - src/routes/index.tsx
  - src/routes/projects.tsx
  - src/styles/app.css
  - scripts/verify-static.ts
autonomous: true
requirements:
  - GH-02
  - GH-03
  - GH-04
  - VER-01
  - VER-02
  - VER-03
  - VER-04
user_setup: []
must_haves:
  truths:
    - "Developer can run a Bun script to refresh public GitHub metadata for direct curated repository links with optional non-public token support."
    - "Visitors see compact dark-primary GitHub metadata only when the checked-in snapshot has available data."
    - "Generated static HTML proves enrichment renders before hydration while empty/unavailable metadata stays omitted."
  artifacts:
    - path: "scripts/sync-github-metadata.ts"
      provides: "Native fetch GitHub REST metadata refresh and snapshot writer"
      contains: "GITHUB_METADATA_TOKEN"
    - path: "src/domain/github-metadata.snapshot.json"
      provides: "Checked-in refreshed metadata snapshot"
      contains: "\"status\""
    - path: "src/routes/projects.tsx"
      provides: "Project-card metadata row rendering"
      contains: "GitHub repository metadata"
    - path: "src/routes/index.tsx"
      provides: "Home story-card metadata row and optional metadata homepage link"
      contains: "maybeGitHubHomepageLinkForProject"
    - path: "scripts/verify-static.ts"
      provides: "Static output proof for pre-hydration metadata enrichment"
      contains: "GitHub repository metadata"
  key_links:
    - from: "scripts/sync-github-metadata.ts"
      to: "src/domain/projects.ts"
      via: "direct `kind: repo` project links"
      pattern: "kind === \"repo\""
    - from: "src/routes/projects.tsx"
      to: "src/domain/github-metadata.ts"
      via: "maybeGitHubMetadataForProject and gitHubMetadataFactsForProject"
      pattern: "gitHubMetadataFactsForProject"
    - from: "scripts/verify-static.ts"
      to: ".output/public"
      via: "post-build generated HTML checks"
      pattern: "GitHub repository metadata"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-05-27T11-26-21
generated_at: 2026-05-27T11:53:11Z
---

<objective>
Add the optional GitHub metadata refresh script and render compact static enrichment in existing project cards.

Purpose: The portfolio can use public GitHub facts as secondary context without adding runtime GitHub dependency, token exposure, new UI routes, or a raw repository mirror.
Output: Sync script, refreshed checked-in snapshot, project-card enrichment UI, CSS primitives, package script, and static output checks.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
@.planning/phases/05-github-enrichment-release-verification/05-RESEARCH.md
@.planning/phases/05-github-enrichment-release-verification/05-UI-SPEC.md
@.planning/phases/05-github-enrichment-release-verification/05-01-SUMMARY.md
@src/domain/github-metadata.ts
@src/domain/github-metadata.snapshot.json
@src/domain/projects.ts
@src/routes/index.tsx
@src/routes/projects.tsx
@src/styles/app.css
@scripts/verify-no-github-runtime.ts
@scripts/verify-static.ts
@package.json

<interfaces>
From Plan 01 `src/domain/github-metadata.ts`:
```ts
export function maybeGitHubMetadataForProject(
  project: ProjectStory,
  snapshot?: GitHubMetadataSnapshot,
): AvailableGitHubRepositoryMetadata | null;

export function gitHubMetadataFactsForProject(
  project: ProjectStory,
  snapshot?: GitHubMetadataSnapshot,
): readonly GitHubMetadataFact[];

export function maybeGitHubHomepageLinkForProject(
  project: ProjectStory,
  snapshot?: GitHubMetadataSnapshot,
): ProjectLink | null;
```

Existing project-card placement in `src/routes/projects.tsx`:
```tsx
<p class="card-copy">{props.project.oneLine}</p>
{props.variant === "flagship" ? <div class="story-stack">...</div> : null}
<ul class="label-row" aria-label={`${props.project.name} labels`}>...</ul>
<div class="link-list">...</div>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add native-fetch metadata sync script</name>
  <files>scripts/sync-github-metadata.ts, scripts/sync-github-metadata.test.ts, package.json, src/domain/github-metadata.snapshot.json</files>
  <read_first>
    - scripts/verify-curation.ts
    - scripts/verify-no-github-runtime.ts
    - scripts/generate-static-metadata.ts
    - src/domain/projects.ts
    - src/domain/github-metadata.ts
    - src/domain/github-metadata.snapshot.json
    - package.json
    - .planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
    - .planning/phases/05-github-enrichment-release-verification/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test: pagination parser extracts `rel="next"` URLs from a GitHub `Link` header and stops when no next link exists.
    - Test: GitHub repository JSON maps to snapshot fields `stars`, `forks`, `primaryLanguage`, `topics`, `pushedAt`, `isArchived`, `isFork`, `isTemplate`, `homepageUrl`, `repositoryUrl`, and `syncedAt`.
    - Test: HTTP 404, 403 rate limit, 301/302 moved, and other errors map to `status: "unavailable"` with reasons `missing`, `rate-limited`, `moved`, and `error`.
    - Test: direct `kind: "repo"` project links are selected and `kind: "related"` links are ignored.
  </behavior>
  <action>Create `scripts/sync-github-metadata.ts` as a Bun TypeScript script using native `fetch` per D-03. Use GitHub REST URLs `https://api.github.com/repos/{owner}/{repo}` and `https://api.github.com/repos/{owner}/{repo}/topics`, headers `Accept: application/vnd.github+json` and `X-GitHub-Api-Version: 2026-03-10`, optional non-public `process.env.GITHUB_METADATA_TOKEN`, and `redirect: "manual"` so moved repositories become unavailable records per D-05 and research Pitfall 4. Export pure helper functions used by `scripts/sync-github-metadata.test.ts`; guard execution with `if (import.meta.main)`. Write the snapshot to `src/domain/github-metadata.snapshot.json` with stable two-space JSON and a concise changed/available/unavailable summary. Add package scripts `"sync:github-metadata": "bun run scripts/sync-github-metadata.ts"` and `"sync:github-metadata:strict": "bun run scripts/sync-github-metadata.ts --strict"`. Run `bun run sync:github-metadata` once so the checked-in snapshot contains current public metadata or explicit unavailable records for every direct curated repo link. Do not add Octokit, GraphQL, scheduled CI refresh, browser token env names, or any dependency.</action>
  <acceptance_criteria>
    - `rg '"sync:github-metadata": "bun run scripts/sync-github-metadata.ts"|"sync:github-metadata:strict": "bun run scripts/sync-github-metadata.ts --strict"' package.json` returns both scripts.
    - `rg 'GITHUB_METADATA_TOKEN|X-GitHub-Api-Version|2026-03-10|redirect: "manual"|/topics|rel="next"|if \\(import\\.meta\\.main\\)' scripts/sync-github-metadata.ts` returns all required sync-script mechanisms.
    - `rg '@octokit|github\\.com/graphql|VITE_.*GITHUB.*TOKEN|PUBLIC_.*GITHUB.*TOKEN|SOLID_PUBLIC_.*GITHUB.*TOKEN' scripts/sync-github-metadata.ts package.json` returns no matches.
    - `rg '"status": "available"|"status": "unavailable"|"stars"|"forks"|"primaryLanguage"|"topics"|"pushedAt"|"isArchived"|"isFork"|"isTemplate"|"repositoryUrl"|"syncedAt"' src/domain/github-metadata.snapshot.json` returns snapshot field matches.
    - `rg 'pagination parser|status: "unavailable"|kind: "related"|rate-limited|moved' scripts/sync-github-metadata.test.ts` returns the required test coverage labels.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- scripts/sync-github-metadata.test.ts src/domain/github-metadata.test.ts && bun run sync:github-metadata && bun run verify:no-github-runtime && bun run typecheck</automated>
  </verify>
  <done>Developer can refresh the checked-in snapshot with native Bun fetch, optional `GITHUB_METADATA_TOKEN`, GitHub pagination support, explicit unavailable statuses, and no new dependency or visitor-runtime token path.</done>
</task>

<task type="auto">
  <name>Task 2: Render compact dark-primary metadata in existing project cards</name>
  <files>src/routes/index.tsx, src/routes/projects.tsx, src/styles/app.css</files>
  <read_first>
    - src/routes/index.tsx
    - src/routes/projects.tsx
    - src/styles/app.css
    - src/domain/github-metadata.ts
    - src/domain/projects.ts
    - .planning/phases/05-github-enrichment-release-verification/05-UI-SPEC.md
    - AGENTS.md
  </read_first>
  <action>In `src/routes/projects.tsx` and `src/routes/index.tsx`, import `Show` where needed plus `gitHubMetadataFactsForProject`, `maybeGitHubMetadataForProject`, and `maybeGitHubHomepageLinkForProject`. Render a metadata row only when `maybeGitHubMetadataForProject(project)` returns available metadata. Place the row after authored copy and flagship story content, before curated label chips and link rows, exactly per UI-SPEC. Use a `dl` or accessible list with `aria-label="GitHub repository metadata"` and textual labels only; do not add icons or animations. Append an enriched homepage link after curated links only when `maybeGitHubHomepageLinkForProject(project)` returns a non-duplicate link; keep existing curated `repo`, `live`, and `docs` links unchanged per D-08. Add `.github-meta-row`, `.github-meta-chip`, `.github-meta-label`, and `.github-meta-value` in `src/styles/app.css` using dark-primary surfaces, `font-size: 14px`, `font-weight: 400` for values, flex-wrap, `min-width: 0`, `overflow-wrap: anywhere`, 6px chip radius, and no light-first classes per D-06, D-07, and the UI-SPEC. Preserve OpenLinks footer/about/contact/profile placement with no new OpenLinks promotion.</action>
  <acceptance_criteria>
    - `rg 'aria-label="GitHub repository metadata"|gitHubMetadataFactsForProject|maybeGitHubMetadataForProject|maybeGitHubHomepageLinkForProject' src/routes/index.tsx src/routes/projects.tsx` returns matches in the route files.
    - `rg 'github-meta-row|github-meta-chip|github-meta-label|github-meta-value|overflow-wrap: anywhere|min-width: 0|flex-wrap' src/styles/app.css` returns the metadata CSS hooks and wrapping safeguards.
    - `rg 'bg-white|bg-stone-50|text-zinc-950' src/routes/index.tsx src/routes/projects.tsx src/styles/app.css` returns no matches.
    - `rg 'counter|setInterval|requestAnimationFrame' src/routes/index.tsx src/routes/projects.tsx` returns no matches.
    - `rg 'Project details|Open source|Live site|Live docs' src/routes/index.tsx src/routes/projects.tsx` still returns existing curated link labels.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck && bun run build</automated>
  </verify>
  <done>Home and projects route cards show available GitHub metadata as compact secondary dark-primary context, omit missing metadata, preserve authored project content, and do not alter OpenLinks placement.</done>
</task>

<task type="auto">
  <name>Task 3: Extend static output checks for pre-hydration enrichment</name>
  <files>scripts/verify-static.ts</files>
  <read_first>
    - scripts/verify-static.ts
    - src/domain/github-metadata.ts
    - src/domain/github-metadata.snapshot.json
    - src/routes/index.tsx
    - src/routes/projects.tsx
    - .planning/phases/05-github-enrichment-release-verification/05-UI-SPEC.md
  </read_first>
  <action>Extend `scripts/verify-static.ts` so post-build checks prove enriched GitHub metadata renders in generated HTML before hydration when available. Import `gitHubMetadataFactsForProject`, `maybeGitHubMetadataForProject`, and `maybeGitHubHomepageLinkForProject`. For `/projects`, assert each visible project with available metadata includes `aria-label="GitHub repository metadata"` and the compact fact values returned by the pure helper. For `/`, assert each home project with available metadata includes the same row and any non-duplicate metadata homepage link returned by `maybeGitHubHomepageLinkForProject`. Assert visitor HTML does not contain maintenance/error copy `No GitHub metadata yet` or `GitHub metadata refresh failed` because unavailable metadata must be omitted per D-05 and UI-SPEC. Keep existing Phase 03/04 metadata, OpenLinks, reduced-motion, local-asset, and no-runtime-GitHub assertions intact.</action>
  <acceptance_criteria>
    - `rg 'gitHubMetadataFactsForProject|maybeGitHubMetadataForProject|maybeGitHubHomepageLinkForProject|GitHub repository metadata|No GitHub metadata yet|GitHub metadata refresh failed' scripts/verify-static.ts` returns all new static verifier hooks.
    - `rg 'api\\.github\\.com|github\\.com/graphql|@octokit/|GITHUB_TOKEN|VITE_\\[A-Z0-9_\\]\\*GITHUB' scripts/verify-static.ts` still returns the existing forbidden runtime/token output patterns.
    - `rg 'OpenLinks profile|prefers-reduced-motion|assertNoRemoteRuntimeVisualAssets|sitemapXml|robotsTxt' scripts/verify-static.ts` still returns existing Phase 03/04 checks.
  </acceptance_criteria>
  <verify>
    <automated>bun run build && bun run verify:static && bun run verify:no-github-runtime</automated>
  </verify>
  <done>Static verification proves GitHub enrichment appears only from checked-in snapshot data before hydration and unavailable metadata stays invisible to visitors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local env -> sync script | Optional `GITHUB_METADATA_TOKEN` is read only by the Bun script. |
| GitHub REST -> snapshot file | Public GitHub JSON crosses into checked-in static data. |
| snapshot -> route HTML | Snapshot metadata renders into static visitor HTML. |
| route HTML -> browser | Visitors receive built HTML/JS/CSS/assets without script-time secrets. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | I | `scripts/sync-github-metadata.ts` | mitigate | Read only non-public `GITHUB_METADATA_TOKEN`; forbid `VITE_*`, `PUBLIC_*`, and `SOLID_PUBLIC_*` token names; keep token handling out of `src/`. |
| T-05-02-02 | T/I | GitHub REST JSON mapping | mitigate | Parse repository responses into typed available/unavailable records, render strings as text, never use raw HTML, and test error-status mapping. |
| T-05-02-03 | D | GitHub outage/rate limit | mitigate | Write explicit unavailable records and keep route rendering based on the checked-in snapshot so build/runtime rendering does not require live GitHub. |
| T-05-02-04 | I | Static route output | mitigate | Extend `verify-static` and keep `verify:no-github-runtime` so built output contains no GitHub API endpoints, GitHub clients, or public token names. |
| T-05-02-05 | T/R | Project-card presentation | mitigate | Render metadata after authored story content and before labels/links; tests and static checks preserve curated authority and prevent metadata from replacing curated links. |
</threat_model>

<verification>
Run `bun run test -- scripts/sync-github-metadata.test.ts src/domain/github-metadata.test.ts && bun run sync:github-metadata && bun run verify:no-github-runtime && bun run typecheck && bun run build && bun run verify:static`.
</verification>

<success_criteria>
Developer can refresh a checked-in GitHub snapshot with Bun native fetch, and generated static HTML shows compact advisory metadata only where available while preserving dark-primary UI, curated copy, OpenLinks placement, and no-runtime-GitHub guarantees.
</success_criteria>

<output>
After completion, create `.planning/phases/05-github-enrichment-release-verification/05-02-SUMMARY.md`.
</output>
