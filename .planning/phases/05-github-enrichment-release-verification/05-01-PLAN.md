---
phase: 05-github-enrichment-release-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/github-metadata.snapshot.json
  - src/domain/github-metadata.ts
  - src/domain/github-metadata.test.ts
autonomous: true
requirements:
  - GH-02
  - GH-03
  - VER-01
user_setup: []
must_haves:
  truths:
    - "Developer has a checked-in GitHub metadata snapshot contract that lets the site render when GitHub is unavailable."
    - "Pure domain helpers can attach available GitHub metadata to curated projects without changing authored copy, ordering, tier, placement, or links."
    - "Unavailable, missing, moved, private, or rate-limited repository metadata is represented explicitly and omitted from visitor rendering."
  artifacts:
    - path: "src/domain/github-metadata.snapshot.json"
      provides: "Runtime GitHub metadata snapshot source"
      contains: "\"schemaVersion\""
    - path: "src/domain/github-metadata.ts"
      provides: "Pure GitHub metadata types, URL parsing, enrichment lookup, fact formatting, and homepage-link derivation"
      exports:
        - "gitHubMetadataSnapshot"
        - "parseGitHubRepositoryUrl"
        - "maybeGitHubMetadataForProject"
        - "gitHubMetadataFactsForProject"
        - "maybeGitHubHomepageLinkForProject"
    - path: "src/domain/github-metadata.test.ts"
      provides: "Vitest coverage for parser, fallback, direct-repo-only enrichment, and curated-authority boundaries"
      contains: "does not use related links for enrichment"
  key_links:
    - from: "src/domain/github-metadata.ts"
      to: "src/domain/projects.ts"
      via: "ProjectStory and ProjectLink types"
      pattern: "import type .*ProjectStory"
    - from: "src/domain/github-metadata.ts"
      to: "src/domain/github-metadata.snapshot.json"
      via: "resolveJsonModule static import"
      pattern: "github-metadata.snapshot.json"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 5-2026-05-27T11-26-21
generated_at: 2026-05-27T11:53:11Z
---

<objective>
Create the static GitHub metadata runtime contract and pure domain helpers.

Purpose: Phase 5 enrichment must remain advisory and static. Authored curation in `src/domain/projects.ts` stays authoritative while GitHub metadata is parsed, typed, and omitted safely when unavailable.
Output: Checked-in snapshot file, pure helper module, and focused Vitest coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
@.planning/phases/05-github-enrichment-release-verification/05-RESEARCH.md
@.planning/phases/05-github-enrichment-release-verification/05-UI-SPEC.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/projects.ts
@src/domain/project-validation.ts
@src/domain/project-validation.test.ts
@src/domain/portfolio-surfaces.test.ts
@tsconfig.json

<interfaces>
From `src/domain/projects.ts`:
```ts
export type ProjectLinkKind = "repo" | "live" | "docs" | "article" | "related";
export type ProjectLink = { label: string; href: string; kind: ProjectLinkKind };
export type ProjectStory = {
  slug: string;
  name: string;
  placement: ProjectPlacement;
  tier: ProjectTier;
  themes: readonly string[];
  tags: readonly string[];
  oneLine: string;
  links: readonly [ProjectLink, ...ProjectLink[]];
};
export function projectLinkDisplayLabel(link: ProjectLink): string;
```

Required new contract from this plan:
```ts
export type GitHubRepositoryMetadata =
  | {
      status: "available";
      slug: string;
      owner: string;
      repo: string;
      repositoryUrl: string;
      homepageUrl: string | null;
      stars: number;
      forks: number;
      primaryLanguage: string | null;
      topics: readonly string[];
      pushedAt: string | null;
      isArchived: boolean;
      isFork: boolean;
      isTemplate: boolean;
      syncedAt: string;
    }
  | {
      status: "unavailable";
      slug: string;
      owner: string;
      repo: string;
      repositoryUrl: string;
      reason: "missing" | "private" | "moved" | "rate-limited" | "error";
      httpStatus?: number;
      message: string;
      syncedAt: string;
    };
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add snapshot contract and repository URL parser</name>
  <files>src/domain/github-metadata.snapshot.json, src/domain/github-metadata.ts, src/domain/github-metadata.test.ts</files>
  <read_first>
    - src/domain/projects.ts
    - src/domain/project-validation.ts
    - src/domain/project-validation.test.ts
    - tsconfig.json
    - .planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
    - .planning/phases/05-github-enrichment-release-verification/05-RESEARCH.md
  </read_first>
  <behavior>
    - Test: `parseGitHubRepositoryUrl("https://github.com/pRizz/open-links")` returns owner `pRizz`, repo `open-links`, and canonical repository URL `https://github.com/pRizz/open-links`.
    - Test: parser rejects non-GitHub URLs, GitHub profile URLs without owner/repo, and malformed URLs by returning `null`.
    - Test: snapshot import exposes top-level `schemaVersion`, `syncedAt`, and `repositories` without needing network, filesystem, DOM, or Solid runtime.
  </behavior>
  <action>Create `src/domain/github-metadata.snapshot.json` with exact top-level keys `schemaVersion`, `syncedAt`, and `repositories`; initialize it as a valid empty snapshot with `schemaVersion: 1`, `syncedAt: "1970-01-01T00:00:00.000Z"`, and `repositories: []` so checked-in runtime fallback exists per D-02. Create `src/domain/github-metadata.ts` as a pure module importing that JSON and exporting `gitHubMetadataSnapshot`, `GitHubMetadataSnapshot`, `GitHubRepositoryMetadata`, `GitHubMetadataFact`, `ParsedGitHubRepositoryUrl`, and `parseGitHubRepositoryUrl`. Keep the module free of `fetch`, `process.env`, `api.github.com`, `GITHUB_TOKEN`, `GITHUB_METADATA_TOKEN`, `VITE_`, `PUBLIC_`, `SOLID_PUBLIC_`, DOM globals, and Solid imports per D-01, D-02, D-10, and D-13. Write failing tests first in `src/domain/github-metadata.test.ts`, then implement until the tests pass.</action>
  <acceptance_criteria>
    - `rg '"schemaVersion"|"syncedAt"|"repositories"' src/domain/github-metadata.snapshot.json` returns all three snapshot keys.
    - `rg 'export type GitHubRepositoryMetadata|export type GitHubMetadataSnapshot|export const gitHubMetadataSnapshot|export function parseGitHubRepositoryUrl' src/domain/github-metadata.ts` returns matches for all listed exports.
    - `rg 'parseGitHubRepositoryUrl\\("https://github.com/pRizz/open-links"\\)|rejects non-GitHub URLs|valid empty snapshot' src/domain/github-metadata.test.ts` returns the parser and snapshot tests.
    - `rg 'fetch\\(|process\\.env|api\\.github\\.com|GITHUB_TOKEN|GITHUB_METADATA_TOKEN|VITE_|PUBLIC_|SOLID_PUBLIC_|from "solid-js"' src/domain/github-metadata.ts src/domain/github-metadata.snapshot.json` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/domain/github-metadata.test.ts && bun run typecheck</automated>
  </verify>
  <done>The repo has a checked-in JSON snapshot contract and pure parser exports; tests prove GitHub URL parsing and static snapshot import behavior without network or frontend runtime dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add curated-authority enrichment helpers</name>
  <files>src/domain/github-metadata.ts, src/domain/github-metadata.test.ts</files>
  <read_first>
    - src/domain/github-metadata.ts
    - src/domain/github-metadata.test.ts
    - src/domain/projects.ts
    - src/domain/portfolio-surfaces.test.ts
    - .planning/phases/05-github-enrichment-release-verification/05-CONTEXT.md
    - .planning/phases/05-github-enrichment-release-verification/05-UI-SPEC.md
  </read_first>
  <behavior>
    - Test: `maybeGitHubMetadataForProject(project, snapshot)` returns available metadata only when the project has a direct `kind: "repo"` GitHub link and matching available snapshot entry.
    - Test: unavailable snapshot entries return `null` and never throw during lookup.
    - Test: the `open-bitcoin` record with `kind: "related"` source link does not receive enrichment from `open-bitcoin-web-miner`.
    - Test: metadata facts use compact labels `Stars`, `Forks`, `Language`, `Updated`, `Archived`, `Fork`, `Template`, and up to three topics after removing duplicates from curated `themes` and `tags`.
    - Test: `maybeGitHubHomepageLinkForProject` returns `null` when the homepage URL duplicates an existing curated `live`, `docs`, or `repo` link.
  </behavior>
  <action>Extend `src/domain/github-metadata.ts` with `maybeGitHubMetadataForProject`, `gitHubMetadataFactsForProject`, and `maybeGitHubHomepageLinkForProject`. Use direct `kind: "repo"` links only per D-01, D-05, and the research recommendation for Open Bitcoin. Format stars and forks as text values suitable for static rendering, format pushed dates as `Updated Mon YYYY`, omit missing facts, include true-only repository flags, and return at most three topic facts after case-insensitive duplicate removal against curated `themes` and `tags` per D-04, D-06, D-08, and D-09. Do not mutate `ProjectStory`, do not change `src/domain/projects.ts`, and do not let metadata change placement, display order, curation tier, authored copy, or curated links.</action>
  <acceptance_criteria>
    - `rg 'export function maybeGitHubMetadataForProject|export function gitHubMetadataFactsForProject|export function maybeGitHubHomepageLinkForProject' src/domain/github-metadata.ts` returns all three helper exports.
    - `rg 'kind === "repo"|kind: "related"|does not use related links for enrichment|omits unavailable metadata|duplicate curated' src/domain/github-metadata.test.ts` returns matches proving direct-repo-only and unavailable fallback behavior.
    - `rg 'Stars|Forks|Language|Updated|Archived|Template|Fork' src/domain/github-metadata.ts src/domain/github-metadata.test.ts` returns compact fact labels.
    - `rg '\\.(displayOrder|placement|tier|oneLine|curationReason)\\s*=' src/domain/github-metadata.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/domain/github-metadata.test.ts src/domain/project-validation.test.ts src/domain/portfolio-surfaces.test.ts && bun run typecheck</automated>
  </verify>
  <done>Pure helpers attach optional available metadata, omit unavailable metadata, keep related links out of enrichment, and preserve all curated authority fields.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in snapshot -> domain helpers | JSON data is parsed into typed GitHub metadata before route components can consume it. |
| GitHub metadata -> curated project story | Advisory external metadata is joined to authoritative local curation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01-01 | T/I | `src/domain/github-metadata.snapshot.json` | mitigate | Model available/unavailable entries as a discriminated union and unit-test unavailable entries so invalid or missing metadata cannot crash rendering. |
| T-05-01-02 | I | `src/domain/github-metadata.ts` | mitigate | Keep domain helpers free of `fetch`, `process.env`, GitHub API endpoints, public token prefixes, and Solid/DOM imports; enforce with task acceptance `rg` checks. |
| T-05-01-03 | T/R | `maybeGitHubMetadataForProject` | mitigate | Match only direct `kind: "repo"` links and test that metadata never overrides curated copy, links, order, tier, or placement. |
| T-05-01-04 | D | Metadata lookup fallback | mitigate | Return `null` for unavailable/missing metadata and render omission downstream instead of throwing. |
</threat_model>

<verification>
Run `bun run test -- src/domain/github-metadata.test.ts src/domain/project-validation.test.ts src/domain/portfolio-surfaces.test.ts && bun run typecheck`.
</verification>

<success_criteria>
Phase 5 has a typed static GitHub metadata contract, checked-in runtime snapshot source, and pure enrichment helpers with tests proving advisory behavior and unavailable fallback.
</success_criteria>

<output>
After completion, create `.planning/phases/05-github-enrichment-release-verification/05-01-SUMMARY.md`.
</output>
