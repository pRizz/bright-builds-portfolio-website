---
phase: 06-browser-accessibility-release-automation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
  - package.json
  - bun.lock
  - playwright.config.ts
  - scripts/serve-static-output.ts
  - src/routes/index.tsx
  - tests/browser-release.playwright.ts
autonomous: true
requirements:
  - BROW-01
  - BROW-02
  - BROW-03
  - BROW-04
  - GATE-01
user_setup: []
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 6-2026-05-31T21-25-37
generated_at: 2026-05-31T21:25:32.082Z

must_haves:
  truths:
    - "Maintainer can run `bun run verify:browser` against built `.output/public` output."
    - "Browser checks cover every route in `prerenderRoutes` on desktop and mobile dark rendering."
    - "Browser checks fail on horizontal overflow or obvious text/control overlap."
    - "Browser checks verify reduced-motion mode disables nonessential hover/pointer motion."
    - "Browser checks verify keyboard focus reaches primary navigation, project links, and collaboration/contact paths."
    - "Browser checks run route-scoped accessibility scans and report violations clearly."
  artifacts:
    - path: "playwright.config.ts"
      provides: "Playwright browser release configuration"
      contains: "chromium-desktop"
    - path: "scripts/serve-static-output.ts"
      provides: "Bun static server for `.output/public`"
      contains: ".output/public"
    - path: "tests/browser-release.playwright.ts"
      provides: "Browser and accessibility release checks"
      contains: "AxeBuilder"
    - path: "package.json"
      provides: "Browser verification script wiring"
      contains: "verify:browser"
  key_links:
    - from: "tests/browser-release.playwright.ts"
      to: "src/domain/routes.ts"
      via: "imports prerenderRoutes"
      pattern: "from \"../src/domain/routes\""
    - from: "playwright.config.ts"
      to: "scripts/serve-static-output.ts"
      via: "webServer command"
      pattern: "serve:static"
    - from: "package.json"
      to: "playwright.config.ts"
      via: "verify:browser script"
      pattern: "playwright test --config playwright.config.ts"
---

<objective>
Add repeatable browser and accessibility release automation for the static portfolio.

Purpose: Maintainers need checked-in browser evidence for dark desktop/mobile layout, reduced-motion behavior, keyboard focus reachability, and accessibility scans before release.
Output: Playwright config, static output server, browser-release spec, dependency pins, and package scripts.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-browser-accessibility-release-automation/06-CONTEXT.md
@.planning/phases/06-browser-accessibility-release-automation/06-RESEARCH.md
@package.json
@app.config.ts
@src/domain/routes.ts
@src/components/SiteLayout.tsx
@src/components/ReactiveSurface.tsx
@src/styles/app.css
@scripts/verify-release.ts
</context>

<threat_model>
No network-facing runtime code is added. The new Bun static server is a local release-check helper only and must serve files from `.output/public` without exposing arbitrary filesystem paths. Browser tests must not include secrets, external tokens, or authenticated requests.

Threats:
- Path traversal in `scripts/serve-static-output.ts`; mitigate by normalizing request paths and only serving resolved files under `.output/public`.
- Browser tests leaking secrets in failure output; mitigate by avoiding environment-variable dumps and only reporting route/project/selector diagnostics.
- Third-party accessibility dependency drift; mitigate by pinning exact versions in `package.json` and `bun.lock`.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add browser verification tooling and static server</name>
  <files>.gitignore, package.json, bun.lock, playwright.config.ts, scripts/serve-static-output.ts</files>
  <read_first>package.json, app.config.ts, src/domain/routes.ts</read_first>
  <action>Add `@playwright/test@1.60.0` and `@axe-core/playwright@4.11.3` as dev dependencies. Add package scripts: `"serve:static": "bun run scripts/serve-static-output.ts"` and `"verify:browser": "playwright test --config playwright.config.ts"`. Update aggregate `"verify"` so it runs `bun run verify:browser` after `bun run build` and before existing static/release checks. Add `test-results/` and `playwright-report/` to `.gitignore`. Create `playwright.config.ts` with `testDir: "./tests"`, `testMatch: /browser-release\\.playwright\\.ts/`, a `webServer` that runs `bun run serve:static` at `http://127.0.0.1:4173`, and projects named `chromium-desktop`, `chromium-mobile`, and `chromium-reduced-motion`. Create `scripts/serve-static-output.ts` using `Bun.serve` to serve `.output/public`, reject path traversal, map route paths to `index.html`, and serve common emitted asset MIME types.</action>
  <verify>bun run typecheck</verify>
  <acceptance_criteria>
    - `package.json` contains `"verify:browser": "playwright test --config playwright.config.ts"`.
    - `package.json` contains `"serve:static": "bun run scripts/serve-static-output.ts"`.
    - `package.json` aggregate `verify` contains `bun run build && bun run verify:browser && bun run verify:static`.
    - `playwright.config.ts` contains `chromium-desktop`, `chromium-mobile`, and `chromium-reduced-motion`.
    - `scripts/serve-static-output.ts` contains `.output/public` and `Bun.serve`.
    - `.gitignore` contains `test-results/` and `playwright-report/`.
  </acceptance_criteria>
  <done>Browser tooling and static server are wired into package scripts and typecheck cleanly.</done>
</task>

<task type="auto">
  <name>Task 2: Add browser release checks</name>
  <files>tests/browser-release.playwright.ts</files>
  <read_first>src/domain/routes.ts, src/components/SiteLayout.tsx, src/components/ReactiveSurface.tsx, src/styles/app.css, scripts/verify-release.ts</read_first>
  <action>Create `tests/browser-release.playwright.ts`. Import `expect` and `test` from `@playwright/test`, `AxeBuilder` from `@axe-core/playwright`, and `prerenderRoutes` from `../src/domain/routes`. For each route, run accessibility checks with AxeBuilder and fail with route-scoped violation details. On desktop and mobile projects, assert the root element has `.dark`, the document has no horizontal overflow beyond 1px, and visible text/interactive elements do not overlap beyond a 4px tolerance. Verify keyboard focus by pressing `Tab` up to 40 times on `/`, collecting visible focused hrefs/text, and requiring `/`, `/about`, `/projects`, `/contact`, at least one `/projects#...` link, and `https://openlinks.us/`. On the `chromium-reduced-motion` project, navigate to `/`, hover an interactive surface and assert its computed transform remains `none`, then dispatch pointer movement over `.reactive-surface` and assert `--pointer-x` and `--pointer-y` remain unchanged.</action>
  <verify>bun run build && bun run verify:browser</verify>
  <acceptance_criteria>
    - `tests/browser-release.playwright.ts` contains `AxeBuilder`.
    - `tests/browser-release.playwright.ts` imports `prerenderRoutes`.
    - `tests/browser-release.playwright.ts` contains `horizontal overflow`.
    - `tests/browser-release.playwright.ts` contains `obvious overlap`.
    - `tests/browser-release.playwright.ts` contains `keyboard focus`.
    - `tests/browser-release.playwright.ts` contains `reduced-motion`.
  </acceptance_criteria>
  <done>Browser release spec proves route accessibility, dark layout, reduced motion, and keyboard reachability against static output.</done>
</task>

<task type="auto">
  <name>Task 3: Resolve browser-discovered home landmark issue</name>
  <files>src/routes/index.tsx</files>
  <read_first>src/routes/index.tsx, tests/browser-release.playwright.ts</read_first>
  <action>If `bun run verify:browser` reports `landmark-complementary-is-top-level` on `/`, remove the invalid nested complementary landmark by changing the home page focus panel wrapper from `<aside class="focus-panel visual-surface reactive-card">` to `<div class="focus-panel visual-surface reactive-card">` and the matching closing tag from `</aside>` to `</div>`. Keep all classes, content, links, and list semantics unchanged.</action>
  <verify>bun run build && bun run verify:browser</verify>
  <acceptance_criteria>
    - `src/routes/index.tsx` does not contain `<aside class="focus-panel visual-surface reactive-card">`.
    - `src/routes/index.tsx` contains `<div class="focus-panel visual-surface reactive-card">`.
    - `bun run verify:browser` exits 0.
  </acceptance_criteria>
  <done>Home page accessibility landmark semantics pass the browser accessibility suite without changing visitor-facing content.</done>
</task>

<task type="auto">
  <name>Task 4: Run full verification and document plan completion</name>
  <files>.planning/phases/06-browser-accessibility-release-automation/06-01-SUMMARY.md</files>
  <read_first>.planning/phases/06-browser-accessibility-release-automation/06-01-PLAN.md, package.json</read_first>
  <action>Run `bun run verify`. If Playwright reports missing browsers, install the required Chromium browser through Playwright's standard installer and rerun `bun run verify`. Create `06-01-SUMMARY.md` with frontmatter copying `requirements-completed: [BROW-01, BROW-02, BROW-03, BROW-04, GATE-01]`, `generated_by: gsd-execute-plan`, `lifecycle_mode: yolo`, and `phase_lifecycle_id: 6-2026-05-31T21-25-37`.</action>
  <verify>bun run verify</verify>
  <acceptance_criteria>
    - `bun run verify` exits 0.
    - `06-01-SUMMARY.md` contains `requirements-completed`.
    - `06-01-SUMMARY.md` contains `phase_lifecycle_id: 6-2026-05-31T21-25-37`.
  </acceptance_criteria>
  <done>Full repo verification passes and plan summary records Phase 6 evidence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bun run format:check`
- [ ] `bun run check`
- [ ] `bun run typecheck`
- [ ] `bun run test`
- [ ] `bun run build`
- [ ] `bun run verify:browser`
- [ ] `bun run verify`
</verification>

<success_criteria>
- Maintainer can run `bun run verify:browser` after `bun run build`.
- Browser automation covers all `prerenderRoutes`.
- Desktop and mobile dark rendering checks fail on overflow or obvious overlap.
- Reduced-motion browser behavior is verified.
- Keyboard focus reaches primary nav, project links, and collaboration/contact paths.
- Axe accessibility checks run with clear route-scoped failure output.
</success_criteria>

<output>
After completion, create `.planning/phases/06-browser-accessibility-release-automation/06-01-SUMMARY.md`
</output>
