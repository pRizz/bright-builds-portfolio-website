---
phase: 15-writing-routes-and-dark-ui
plan: 04
type: execute
wave: 3
depends_on:
  - 15-02
  - 15-03
files_modified:
  - scripts/verify-static.ts
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ROUTE-04
  - READ-01
  - READ-02
  - READ-03
  - LINK-02
must_haves:
  truths:
    - "Clean static builds emit /writing and every public writing detail route before hydration."
    - "Static verification checks writing index, writing detail, related project links, and project related-writing links."
    - "Static verification does not add Phase 16 writing metadata/JSON-LD or Phase 17 release evidence label scope."
    - "Browser verification covers new writing routes through existing prerenderRoutes-derived axe and dark layout loops."
  artifacts:
    - path: "scripts/verify-static.ts"
      provides: "Writing route static output assertions"
      contains: "maybeWritingForDetailRoute"
  key_links:
    - from: "scripts/verify-static.ts"
      to: "src/domain/routes.ts"
      via: "prerenderRoutes"
      pattern: "prerenderRoutes"
    - from: "scripts/verify-static.ts"
      to: "src/domain/writing.ts"
      via: "publicWritingEntries, writingDetailRoutes, maybePublicWritingEntryBySlug"
      pattern: "maybePublicWritingEntryBySlug|writingDetailRoutes"
    - from: "tests/browser-release.playwright.ts"
      to: "src/domain/routes.ts"
      via: "existing route-derived loops"
      pattern: "for \\(const route of prerenderRoutes\\)"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-06-13T16-56-50
generated_at: 2026-06-13T17:17:54.013Z
---

<objective>
Prove Phase 15 writing routes through static and browser verification.

Purpose: Ensure generated HTML contains the new writing route content and excludes non-public/unsafe route output without expanding Phase 16 metadata or Phase 17 release-label scope.
Output: Updated `scripts/verify-static.ts` and recorded verification evidence in the plan summary.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-writing-routes-and-dark-ui/15-CONTEXT.md
@.planning/phases/15-writing-routes-and-dark-ui/15-RESEARCH.md
@.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md
@.planning/phases/15-writing-routes-and-dark-ui/15-02-SUMMARY.md
@.planning/phases/15-writing-routes-and-dark-ui/15-03-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@scripts/verify-static.ts
@tests/browser-release.playwright.ts
@src/domain/writing.ts
@src/domain/routes.ts
@src/routes/writing/index.tsx
@src/routes/writing/[slug].tsx
@src/routes/projects/[slug].tsx

<interfaces>
Existing static verifier flow:

```ts
export const expectedRoutes: StaticRouteCheck[] = prerenderRoutes.map((route) => ({
  route,
  expectedTexts: ["OpenLinks profile", ...expectedTextsForRoute(route)],
  forbiddenTextPatterns: generatedOutputForbiddenPatterns,
}));
```

Add a writing branch before `topLevelRouteForPath(route)` is used. Preserve existing project-detail metadata/JSON-LD assertions and top-level route metadata assertions. Do not add writing-specific detail metadata, `BlogPosting`, writing JSON-LD, sitemap-specific assertions beyond existing `assertOutputTextEquals(outputRoot, "sitemap.xml", sitemapXml())`, release-readiness docs, or release evidence labels.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add static verifier coverage for writing routes and cross-links</name>
  <files>scripts/verify-static.ts</files>
  <read_first>
    - `scripts/verify-static.ts` for current route expected-text branching, dynamic project route branch, forbidden text patterns, and metadata assertion loop.
    - `src/domain/writing.ts` for `curatedWriting`, `publicWritingEntries()`, `maybePublicWritingEntryBySlug()`, `writingDetailPath()`, `writingDetailRoutes()`, and `relatedProjectDetailPageProjects()`.
    - `src/routes/writing/index.tsx` for exact index copy and card labels from Plan 15-02.
    - `src/routes/writing/[slug].tsx` for exact detail/fallback/related-project copy from Plan 15-02.
    - `src/routes/projects/[slug].tsx` for exact related-writing panel copy from Plan 15-03.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-CONTEXT.md` section "SEO And Release Boundary".
  </read_first>
  <action>Update `scripts/verify-static.ts` only. Import `curatedWriting`, `maybePublicWritingEntryBySlug`, `publicWritingEntries`, `relatedProjectDetailPageProjects`, `writingDetailPath`, `writingDetailRoutes`, type `PublicWritingEntry`, type `WritingBodyBlock`, and type `WritingEntry` from `../src/domain/writing`. Add generated output forbidden patterns for `href="javascript:` and `href="data:`. Add `maybeWritingForDetailRoute(route: string): PublicWritingEntry | null` with prefix `"/writing/"` and lookup `maybePublicWritingEntryBySlug(route.slice(detailRoutePrefix.length))`. In `expectedTextsForRoute(route)`, add a writing detail branch before the top-level route branch. The `/writing` branch must assert `routeStaticCheckText(route)`, `Notes and essays`, `Writing`, `Curated notes on agentic engineering, open systems, identity, and practical web software.`, and for every `publicWritingEntries()` entry: title, summary, `href="${writingDetailPath(entry)}"`, `Read note` or `Read essay`, visible date text, topics, tags, and related project count text when relationships exist. The writing detail branch must assert `Back to writing`, entry kind label, title, summary, visible date text, every section heading, paragraph/callout/list/link block text, `href` attributes for link blocks, and related selected project name, one-line summary, `Project details`, and project detail hrefs from `relatedProjectDetailPageProjects(entry)`. In the existing project-detail branch, add related writing expected texts for `publicWritingEntriesForProject` equivalent behavior by filtering `publicWritingEntries()` where `entry.relatedProjectSlugs.includes(maybeProject.slug)`: assert `Related writing`, entry title, summary, action label, and writing detail href only when at least one related writing entry exists. In the bottom metadata assertion loop, add `const maybeWriting = maybeWritingForDetailRoute(check.route); if (maybeWriting) { continue; }` before `topLevelRouteForPath(check.route)` so writing detail pages are not forced through top-level metadata assertions. Add `assertWritingDetailRouteCoverage(root)` that iterates `for (const route of writingDetailRoutes()) { routeHtmlPath(root, route); }` and call it after route assertions. Add `assertNoPrerenderedWritingRoute(root, route)` and after sitemap assertions call it for every non-published `curatedWriting` entry via `writingDetailPath(entry)` plus sentinel route `"/writing/unknown-writing-slug"`. Do not add `BlogPosting`, `metadataForWriting`, `writingJsonLd`, release-readiness docs, or release evidence labels.</action>
  <verify>
    <automated>bun run build && bun run verify:static</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'maybeWritingForDetailRoute|writingDetailExpectedTexts|assertWritingDetailRouteCoverage|assertNoPrerenderedWritingRoute' scripts/verify-static.ts` finds writing static verifier helpers.
    - `rg -n 'maybePublicWritingEntryBySlug|publicWritingEntries|writingDetailPath|writingDetailRoutes|relatedProjectDetailPageProjects|curatedWriting' scripts/verify-static.ts` finds writing helper imports/usages.
    - `rg -n 'href="javascript:|href="data:' scripts/verify-static.ts` finds generated output forbidden patterns.
    - `rg -n 'Notes and essays|Back to writing|Related writing' scripts/verify-static.ts` finds writing expected text coverage.
    - `rg -n 'BlogPosting|metadataForWriting|writingJsonLd|releaseReadinessEvidenceLabels|docs/release-readiness' scripts/verify-static.ts` exits 1.
    - `bun run build` exits 0.
    - `bun run verify:static` exits 0.
  </acceptance_criteria>
  <done>`verify:static` proves `/writing`, public writing details, project cross-links, and non-public writing output exclusion without taking Phase 16 or Phase 17 scope.</done>
</task>

<task type="auto">
  <name>Task 2: Run route-derived browser and aggregate verification evidence</name>
  <files>None - verification-only task; write evidence in 15-04-SUMMARY.md</files>
  <read_first>
    - `package.json` for the existing verification scripts.
    - `tests/browser-release.playwright.ts` for existing `for (const route of prerenderRoutes)` axe/layout coverage.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md` section "Verification Contract".
    - `.planning/phases/15-writing-routes-and-dark-ui/15-CONTEXT.md` section "SEO And Release Boundary".
  </read_first>
  <action>Run the repo-native verification sequence after Task 1: `bun run format:check`, `bun run check`, `bun run typecheck`, `bun run test`, `bun run build`, `bun run verify:static`, and `bun run verify:browser`. If Playwright browsers are missing, run `bun run install:browser` once and rerun `bun run verify:browser`. Confirm generated static files exist for `.output/public/writing/index.html`, `.output/public/writing/agentic-engineering-workflows/index.html`, and `.output/public/writing/portable-identity-and-owned-surfaces/index.html`. Use Playwright or the Browser plugin to visually inspect dark desktop viewport `1280x900` and mobile viewport `390x844` for `/writing`, `/writing/agentic-engineering-workflows`, and `/projects/openlinks`. Record in `15-04-SUMMARY.md` whether each route/viewport had horizontal overflow, text/control overlap, unreadable contrast, missing focus rings, or reduced-motion-only regressions. Do not edit `tests/browser-release.playwright.ts`, release-readiness docs, release evidence labels, or Phase 16 metadata/JSON-LD helpers in this task.</action>
  <verify>
    <automated>bun run format:check && bun run check && bun run typecheck && bun run test && bun run build && bun run verify:static && bun run verify:browser</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .output/public/writing/index.html` exits 0 after `bun run build`.
    - `test -f .output/public/writing/agentic-engineering-workflows/index.html` exits 0 after `bun run build`.
    - `test -f .output/public/writing/portable-identity-and-owned-surfaces/index.html` exits 0 after `bun run build`.
    - `bun run format:check` exits 0.
    - `bun run check` exits 0.
    - `bun run typecheck` exits 0.
    - `bun run test` exits 0.
    - `bun run verify:static` exits 0.
    - `bun run verify:browser` exits 0.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-04-SUMMARY.md` contains `/writing`, `/writing/agentic-engineering-workflows`, `/projects/openlinks`, `1280x900`, and `390x844`.
  </acceptance_criteria>
  <done>Static and browser evidence proves Phase 15 routes are generated, readable, and covered by route-derived checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| helper-derived route list -> static output | Route helpers determine generated files and verification coverage. |
| generated HTML -> release confidence | Static verifier output becomes evidence that visitor-facing content is present and unsafe/private content is absent. |
| route-derived browser tests -> UI evidence | Existing Playwright loops consume `prerenderRoutes` and produce axe/layout confidence for new routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-04-01 | I | `.output/public/writing/*` | mitigate | Verify public writing output exists and non-published/unknown writing routes do not produce static HTML. |
| T-15-04-02 | T | generated HTML links | mitigate | Add forbidden output checks for `href="javascript:` and `href="data:` in generated HTML. |
| T-15-04-03 | R | `scripts/verify-static.ts` evidence | mitigate | Assert exact writing index, detail, and cross-link text before hydration so release evidence is reproducible from CLI output. |
| T-15-04-04 | I | runtime secret/API leakage | mitigate | Preserve existing generated-output forbidden patterns for `api.github.com`, `github.com/graphql`, `@octokit/`, `GITHUB_TOKEN`, and `VITE_*GITHUB*TOKEN`; add no runtime fetches. |
| T-15-04-05 | D | route-derived browser coverage | mitigate | Run existing `verify:browser` after writing routes enter `prerenderRoutes`, covering axe and dark layout loops for all generated writing routes. |
</threat_model>

<verification>
Final phase verification:

```bash
bun run format:check
bun run check
bun run typecheck
bun run test
bun run build
bun run verify:static
bun run verify:browser
```

If browser binaries are missing:

```bash
bun run install:browser
bun run verify:browser
```
</verification>

<success_criteria>
- `bun run build` emits `/writing` and every public writing detail route.
- `bun run verify:static` asserts writing index/detail content, related project links, project related-writing links, unsafe href absence, and non-public writing output exclusion.
- `bun run verify:browser` passes with writing routes included through existing `prerenderRoutes` loops.
- Summary evidence records desktop and mobile dark visual inspection for `/writing`, one writing detail route, and one selected project detail page with related writing.
- No Phase 16 writing detail metadata/JSON-LD/sitemap-specific assertion work or Phase 17 release-readiness label expansion is added.
</success_criteria>

<output>
After completion, create `.planning/phases/15-writing-routes-and-dark-ui/15-04-SUMMARY.md`.
</output>
