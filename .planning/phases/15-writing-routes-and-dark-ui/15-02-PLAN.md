---
phase: 15-writing-routes-and-dark-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/routes/writing/index.tsx
  - src/routes/writing/[slug].tsx
  - src/styles/app.css
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - READ-01
  - READ-02
  - READ-03
must_haves:
  truths:
    - "Visitor can open /writing and see public writing entries in helper-provided order."
    - "Visitor can open each public /writing/{slug} route and read static title, summary, metadata, sections, and body blocks before hydration."
    - "Unknown writing slugs show a non-leaking fallback with a Browse writing path."
    - "Writing pages use dark-primary responsive layout with stable wrapping for long titles, chips, links, and body text."
  artifacts:
    - path: "src/routes/writing/index.tsx"
      provides: "/writing index route"
      contains: "Notes and essays"
    - path: "src/routes/writing/[slug].tsx"
      provides: "/writing/{slug} detail route and unknown-slug fallback"
      contains: "Back to writing"
    - path: "src/styles/app.css"
      provides: "Focused writing layout/readability classes"
      contains: ".writing-list"
  key_links:
    - from: "src/routes/writing/index.tsx"
      to: "src/domain/writing.ts"
      via: "publicWritingEntries() and writingDetailPath(entry)"
      pattern: "publicWritingEntries|writingDetailPath"
    - from: "src/routes/writing/[slug].tsx"
      to: "src/domain/writing.ts"
      via: "maybePublicWritingEntryBySlug(params.slug ?? \"\")"
      pattern: "maybePublicWritingEntryBySlug"
    - from: "src/routes/writing/[slug].tsx"
      to: "src/domain/projects.ts"
      via: "relatedProjectDetailPageProjects(entry)"
      pattern: "relatedProjectDetailPageProjects"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-06-13T16-56-50
generated_at: 2026-06-13T17:17:54.013Z
---

<objective>
Build the public writing index and writing detail UI.

Purpose: Let visitors browse and read curated writing in the dark-primary interface without adding a content pipeline or runtime data dependency.
Output: SolidStart route files under `src/routes/writing/` plus focused CSS for writing readability.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-writing-routes-and-dark-ui/15-CONTEXT.md
@.planning/phases/15-writing-routes-and-dark-ui/15-RESEARCH.md
@.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md
@.planning/phases/15-writing-routes-and-dark-ui/15-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/writing.ts
@src/domain/routes.ts
@src/routes/projects/index.tsx
@src/routes/projects/[slug].tsx
@src/styles/app.css

<interfaces>
Plan 15-01 must provide:

```ts
export function publicWritingEntries(): readonly PublicWritingEntry[];
export function maybePublicWritingEntryBySlug(slug: string): PublicWritingEntry | null;
export function writingDetailPath(entry: Pick<WritingEntry, "slug">): string;
export function relatedProjectDetailPageProjects(
  entry: Pick<WritingEntry, "relatedProjectSlugs">,
): readonly ProjectDetailPageProject[];
```

Use SolidStart file routes:

```text
src/routes/writing/index.tsx -> /writing
src/routes/writing/[slug].tsx -> /writing/:slug
```

Do not create `metadataForWriting`, `BlogPosting`, writing JSON-LD, sitemap-specific assertions, browser release label changes, RSS, search, pagination, newsletter, comments, CMS/admin, MDX, or runtime content fetches.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create the /writing index route</name>
  <files>src/routes/writing/index.tsx</files>
  <read_first>
    - `src/routes/projects/index.tsx` for route metadata, asset link, `ReactiveSurface`, card, chip, and empty-state patterns.
    - `src/domain/writing.ts` for `publicWritingEntries()`, `writingDetailPath()`, and `relatedProjectDetailPageProjects()`.
    - `src/domain/routes.ts` for `routeByPath("/writing")` from Plan 15-01.
    - `src/styles/app.css` for dark-first surface/link/chip classes.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md` sections "`/writing` Index", "Interaction Contract", and "Copywriting Contract".
  </read_first>
  <action>Create `src/routes/writing/index.tsx`. Import `Link as HeadLink`, `Meta`, and `Title` from `@solidjs/meta`; `For` and `Show` from `solid-js`; `ReactiveSurface` from `../../components/ReactiveSurface`; `publicWritingEntries`, `relatedProjectDetailPageProjects`, `writingDetailPath`, and type `PublicWritingEntry` from `../../domain/writing`; `routeByPath` from `../../domain/routes`; and `metadataForRoute`, `siteAssetLinks` from `../../domain/seo`. Set `const route = routeByPath("/writing")`, `const metadata = metadataForRoute(route)`, and `const writingEntries = publicWritingEntries()`. Render the existing route metadata pattern for title, description, canonical, asset links, Open Graph, and Twitter tags using `metadata`; this is allowed only for the `/writing` index. Render a compact `.page-intro` with exact eyebrow `Notes and essays`, H1 `Writing`, and lead `Curated notes on agentic engineering, open systems, identity, and practical web software.`. Render `writingEntries` in helper order inside `<ReactiveSurface class="writing-list">`. Each card must be an `<article class="writing-card project-anchor-card interactive-surface reactive-card">` with a title link to `writingDetailPath(entry)`, summary, kind label `Note` or `Essay`, date label `Published {Month D, YYYY}` when `maybePublishedOn` exists or `Updated {Month D, YYYY}` when only `maybeUpdatedOn` exists, wrapped topic/tag chips, related count text `1 related project` or `{N} related projects` when `relatedProjectDetailPageProjects(entry).length > 0`, and action link text `Read note` or `Read essay`. If there are no public entries, render one `<div class="empty-state visual-surface">` with heading `No public writing yet` and body `Published notes will appear here after they are added to the curated writing registry.`. Do not add search, tag archives, pagination, RSS, comments, newsletter, CMS, MDX, or runtime fetches.</action>
  <verify>
    <automated>bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/routes/writing/index.tsx` exits 0.
    - `rg -n 'Notes and essays|<h1 class="page-title">Writing</h1>|Curated notes on agentic engineering, open systems, identity, and practical web software\\.' src/routes/writing/index.tsx` finds the required intro copy.
    - `rg -n 'publicWritingEntries\\(\\)|writingDetailPath\\(entry\\)|relatedProjectDetailPageProjects\\(entry\\)' src/routes/writing/index.tsx` finds helper usage.
    - `rg -n 'No public writing yet|Published notes will appear here after they are added to the curated writing registry\\.' src/routes/writing/index.tsx` finds the empty-state copy.
    - `rg -n 'Read note|Read essay|1 related project|related projects' src/routes/writing/index.tsx` finds required card labels.
    - `rg -n 'fetch\\(|api\\.github|GITHUB_TOKEN|Markdown|MDX|rss|newsletter|pagination|search' src/routes/writing/index.tsx` exits 1.
    - `bun run typecheck` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <done>`/writing` renders public entries from `publicWritingEntries()` with the approved first-viewport browsing surface and no runtime content dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Create the /writing/{slug} detail route</name>
  <files>src/routes/writing/[slug].tsx</files>
  <read_first>
    - `src/routes/projects/[slug].tsx` for dynamic route fallback, `useParams()`, detail layout, and project-link patterns.
    - `src/domain/writing.ts` for body block, public lookup, path, and related project helper types.
    - `src/domain/projects.ts` for `projectDetailPath()` and selected project detail fields.
    - `src/styles/app.css` for `page-intro`, `detail-status-row`, `visual-surface`, `link-list`, `chip`, and `notice-panel` classes.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md` sections "`/writing/{slug}` Detail" and "Unknown Writing Slug".
  </read_first>
  <action>Create `src/routes/writing/[slug].tsx`. Import `useParams` from `@solidjs/router`; `For` and `Show` from `solid-js`; `maybePublicWritingEntryBySlug`, `relatedProjectDetailPageProjects`, type `PublicWritingEntry`, and type `WritingBodyBlock` from `../../domain/writing`; and `projectDetailPath` from `../../domain/projects`. Resolve `const entry = () => maybePublicWritingEntryBySlug(params.slug ?? "")`. For fallback, render a `.page-intro` that does not echo `params.slug`, draft titles, hidden titles, or private content; use exact H1 `No public writing here`, lead `Return to the writing index to browse published notes.`, and link text `Browse writing` to `/writing`. For found entries, render one `<article class="writing-article">` with a back link `Back to writing`, eyebrow `Note` or `Essay`, single H1 from `entry.title`, lead from `entry.summary`, metadata chips for kind/date/topics/tags, sections as H2 headings, and body blocks directly from typed `sections`. Render block kinds exactly: `paragraph` as `<p class="writing-body-copy">`, `list` as `<ul class="writing-body-list">`, `callout` as `<p class="writing-callout notice-panel visual-surface">`, and `link` as `<a class="text-link surface-link">`. For link blocks, set `target="_blank"` and `rel="noopener noreferrer"` only when `href` starts with `https://`; internal `/` and `#` hrefs must not get `target="_blank"`. Render related projects only when `relatedProjectDetailPageProjects(entry).length > 0`; each related project item must show project name, one-line summary, and a `Project details` link to `projectDetailPath(project)`. Do not use `innerHTML`, Markdown, MDX, parser packages, or runtime fetches. Do not add writing-specific detail metadata, JSON-LD, or sitemap logic in this phase.</action>
  <verify>
    <automated>bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `test -f 'src/routes/writing/[slug].tsx'` exits 0.
    - `rg -n 'useParams|maybePublicWritingEntryBySlug\\(params\\.slug \\?\\? ""\\)' 'src/routes/writing/[slug].tsx'` finds the dynamic public lookup.
    - `rg -n 'No public writing here|Return to the writing index to browse published notes\\.|Browse writing' 'src/routes/writing/[slug].tsx'` finds the non-leaking fallback copy.
    - `rg -n 'Back to writing|writing-body-copy|writing-body-list|writing-callout notice-panel visual-surface|WritingBodyBlock' 'src/routes/writing/[slug].tsx'` finds article and typed block rendering.
    - `rg -n 'target="_blank"|rel="noopener noreferrer"|href\\.startsWith\\("https://"\\)' 'src/routes/writing/[slug].tsx'` finds safe external link behavior.
    - `rg -n 'relatedProjectDetailPageProjects\\(entry\\)|Project details|projectDetailPath\\(project\\)' 'src/routes/writing/[slug].tsx'` finds related project linking.
    - `rg -n 'innerHTML|Markdown|MDX|BlogPosting|metadataForWriting|writingJsonLd|fetch\\(' 'src/routes/writing/[slug].tsx'` exits 1.
    - `bun run typecheck` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <done>Each public writing slug renders a static readable article, unknown/non-public slugs render a non-leaking fallback, and related selected projects link forward from writing details.</done>
</task>

<task type="auto">
  <name>Task 3: Add focused writing readability CSS</name>
  <files>src/styles/app.css</files>
  <read_first>
    - `src/styles/app.css` for existing component layer, dark-primary colors, card radius, reduced-motion guards, and text wrapping patterns.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md` sections "Typography", "Color", "Spacing Scale", and "Interaction Contract".
    - `AGENTS.md` Repo-Local Guidance for dark-primary UI and light-first utility exceptions.
  </read_first>
  <action>Add writing-specific CSS inside the existing `@layer components` block in `src/styles/app.css`. Use only dark-first colors and existing shared class composition. Add `.writing-list { @apply mt-8 grid gap-4; grid-template-columns: repeat(auto-fit, minmax(min(100%, 20rem), 1fr)); }`. Add `.writing-card { min-width: 0; overflow-wrap: anywhere; }`. Add `.writing-article { @apply mt-8 max-w-4xl; min-width: 0; }`. Add `.writing-body { @apply mt-8 grid gap-6; }`. Add `.writing-section { @apply grid gap-4; min-width: 0; }`. Add `.writing-body-copy { @apply m-0 text-zinc-300; font-size: 16px; line-height: 1.5; overflow-wrap: anywhere; }`. Add `.writing-body-list { @apply m-0 grid gap-4 pl-5 text-zinc-300; font-size: 16px; line-height: 1.5; overflow-wrap: anywhere; }`. Add `.writing-callout { @apply mt-0; }`. Add `.writing-related-grid { @apply mt-4 grid gap-4; grid-template-columns: repeat(auto-fit, minmax(min(100%, 18rem), 1fr)); }`. Do not add `bg-white`, `bg-stone-50`, `text-zinc-950`, negative letter spacing, viewport-scaled font sizes, card radius above 8px, or new motion selectors outside the existing reduced-motion guards.</action>
  <verify>
    <automated>bun run format:check && bun run check && bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n '\\.writing-list|\\.writing-card|\\.writing-article|\\.writing-body|\\.writing-section|\\.writing-body-copy|\\.writing-body-list|\\.writing-callout|\\.writing-related-grid' src/styles/app.css` finds all writing classes.
    - `rg -n 'repeat\\(auto-fit, minmax\\(min\\(100%, 20rem\\), 1fr\\)\\)|repeat\\(auto-fit, minmax\\(min\\(100%, 18rem\\), 1fr\\)\\)' src/styles/app.css` finds stable responsive grid constraints.
    - `rg -n 'font-size: 16px;|line-height: 1\\.5;|overflow-wrap: anywhere;' src/styles/app.css` finds the writing readability rules.
    - `rg -n 'bg-white|bg-stone-50|text-zinc-950' src/routes/writing src/styles/app.css` exits 1.
    - `bun run format:check` exits 0.
    - `bun run check` exits 0.
    - `bun run typecheck` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <done>Writing pages have dark-primary responsive layout and stable text wrapping without light-first utilities or new motion behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| route params -> writing detail lookup | Untrusted URL slug controls which static detail component branch renders. |
| checked-in writing body -> browser DOM | Authored body blocks render as visitor-readable HTML. |
| authored link href -> browser navigation | Link blocks and project links can navigate visitors away from the site. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-02-01 | I | `src/routes/writing/[slug].tsx` fallback | mitigate | Resolve through `maybePublicWritingEntryBySlug(params.slug ?? "")` and render fallback copy that does not echo the requested slug or any draft/hidden content. |
| T-15-02-02 | T | writing body renderer | mitigate | Render typed body text as JSX text nodes and never use `innerHTML`, Markdown, MDX, or parser output. |
| T-15-02-03 | I | external writing links | mitigate | Set `target="_blank"` plus `rel="noopener noreferrer"` for `https://` link blocks and rely on Plan 15-01 href validation for allowed protocols. |
| T-15-02-04 | D | responsive writing layout | mitigate | Add stable grid constraints, `min-width: 0`, and `overflow-wrap: anywhere` classes; verify with build and browser checks in Plan 15-04. |
</threat_model>

<verification>
Run after all tasks:

```bash
bun run format:check
bun run check
bun run typecheck
bun run build
```
</verification>

<success_criteria>
- `/writing` renders the approved writing browsing surface and only public entries.
- `/writing/{slug}` renders typed body sections directly and provides `Back to writing`.
- Unknown/non-public writing slugs render the approved fallback and do not reveal slug/private content.
- Related selected projects render on writing detail pages only when relationships exist.
- CSS remains dark-primary, responsive, and reduced-motion compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/15-writing-routes-and-dark-ui/15-02-SUMMARY.md`.
</output>
