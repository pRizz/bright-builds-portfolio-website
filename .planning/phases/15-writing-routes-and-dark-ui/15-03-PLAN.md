---
phase: 15-writing-routes-and-dark-ui
plan: 03
type: execute
wave: 2
depends_on:
  - 15-01
files_modified:
  - src/routes/projects/[slug].tsx
autonomous: true
requirements:
  - READ-03
  - LINK-02
must_haves:
  truths:
    - "Selected project detail pages show related writing only when public writing references that selected project."
    - "Project records do not gain reciprocal writing fields."
    - "Related writing cards show title, kind/date metadata, summary, and Read note or Read essay links."
  artifacts:
    - path: "src/routes/projects/[slug].tsx"
      provides: "Related writing panel on selected project details"
      contains: "Related writing"
  key_links:
    - from: "src/routes/projects/[slug].tsx"
      to: "src/domain/writing.ts"
      via: "publicWritingEntriesForProject(selectedProject())"
      pattern: "publicWritingEntriesForProject"
    - from: "src/routes/projects/[slug].tsx"
      to: "src/domain/writing.ts"
      via: "writingDetailPath(entry)"
      pattern: "writingDetailPath"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 15-2026-06-13T16-56-50
generated_at: 2026-06-13T17:17:54.013Z
---

<objective>
Add related writing links to selected project detail pages.

Purpose: Complete the bidirectional visitor path between project stories and writing without duplicating relationship data on project records.
Output: A conditional related writing panel in `src/routes/projects/[slug].tsx`.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-writing-domain-foundation/14-01-SUMMARY.md
@.planning/phases/15-writing-routes-and-dark-ui/15-CONTEXT.md
@.planning/phases/15-writing-routes-and-dark-ui/15-RESEARCH.md
@.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md
@.planning/phases/15-writing-routes-and-dark-ui/15-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/routes/projects/[slug].tsx
@src/domain/writing.ts
@src/domain/projects.ts
@src/styles/app.css

<interfaces>
Plan 15-01 must provide:

```ts
export function publicWritingEntriesForProject(
  project: Pick<ProjectDetailPageProject, "slug">,
): readonly PublicWritingEntry[];

export function writingDetailPath(entry: Pick<WritingEntry, "slug">): string;
```

Existing project detail route shape:

```tsx
<Show when={project()} fallback={...}>
  {(selectedProject) => (
    <article class="content-section">
      <div class="project-detail-layout">
        <section class="project-detail-story visual-surface" />
        <aside class="project-detail-aside" />
      </div>
    </article>
  )}
</Show>
```

Do not edit `src/domain/projects.ts`, add `relatedWriting` or `relatedWritingSlugs` fields to project records, touch the project index, or add Phase 17 release labels.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render related writing on selected project details</name>
  <files>src/routes/projects/[slug].tsx</files>
  <read_first>
    - `src/routes/projects/[slug].tsx` for current selected project detail layout, aside panels, action links, and fallback.
    - `src/domain/writing.ts` for `publicWritingEntriesForProject()` and `writingDetailPath()`.
    - `src/domain/projects.ts` for `ProjectDetailPageProject`, `ProjectStory`, and selected project detail fields.
    - `src/styles/app.css` for `project-detail-panel`, `surface-card`, `link-list`, `card-copy`, `card-title`, and chip classes.
    - `.planning/phases/15-writing-routes-and-dark-ui/15-UI-SPEC.md` section "Project Detail Related Writing".
  </read_first>
  <action>Update `src/routes/projects/[slug].tsx` only. Import `publicWritingEntriesForProject`, `writingDetailPath`, and type `PublicWritingEntry` from `../../domain/writing`; also import type `ProjectDetailPageProject` from `../../domain/projects`. Add a `RelatedWritingPanel(props: { project: ProjectDetailPageProject })` component after `GitHubMetadataRow`. Inside it, set `const relatedWriting = () => publicWritingEntriesForProject(props.project)`. Return `<Show when={relatedWriting().length > 0}>` with a `<section class="project-detail-panel visual-surface" aria-labelledby="related-writing">`, `<h2 id="related-writing" class="card-title">Related writing</h2>`, and a container that renders each entry as a repeated `.surface-card` item. Each item must show the entry title, kind/date metadata text (`Note`, `Essay`, `Published {Month D, YYYY}`, or `Updated {Month D, YYYY}`), summary, and a link with `href={writingDetailPath(entry)}` and label `Read note` or `Read essay`. Insert `<RelatedWritingPanel project={selectedProject()} />` inside the existing `project-detail-aside`, after `Project actions`. Keep the panel omitted when `relatedWriting().length === 0`. Do not add project-record fields, project index badges, release docs, or metadata changes.</action>
  <verify>
    <automated>bun run typecheck && bun run build</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n 'publicWritingEntriesForProject|writingDetailPath|PublicWritingEntry|ProjectDetailPageProject' 'src/routes/projects/[slug].tsx'` finds required imports/usages.
    - `rg -n 'function RelatedWritingPanel|Related writing|relatedWriting\\(\\)\\.length > 0' 'src/routes/projects/[slug].tsx'` finds the conditional panel.
    - `rg -n 'Read note|Read essay|Published |Updated ' 'src/routes/projects/[slug].tsx'` finds required related writing labels.
    - `rg -n '<RelatedWritingPanel project=\\{selectedProject\\(\\)\\}' 'src/routes/projects/[slug].tsx'` finds the aside insertion.
    - `rg -n 'relatedWriting|relatedWritingSlugs' src/domain/projects.ts` exits 1.
    - `bun run typecheck` exits 0.
    - `bun run build` exits 0.
  </acceptance_criteria>
  <done>Selected project detail pages derive and render related public writing links from the writing registry, and project records stay unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| writing registry -> project detail page | Authored writing relationships determine which writing links appear on selected project detail pages. |
| project detail route -> visitor navigation | Visitors follow related writing links from project pages to writing routes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-15-03-01 | I | `RelatedWritingPanel` | mitigate | Use `publicWritingEntriesForProject()` so draft, hidden, and archived writing never appears on project detail pages. |
| T-15-03-02 | T | project relationship source | mitigate | Do not add reciprocal fields to project records; derive from the writing registry to avoid stale duplicated relationship data. |
| T-15-03-03 | R | visitor link provenance | mitigate | Render title, kind/date metadata, summary, and explicit `Read note`/`Read essay` labels so generated HTML evidence can prove the relationship source. |
| T-15-03-04 | E | runtime API/secrets | accept | This plan adds static links only and no runtime fetches, API routes, secrets, or external services. |
</threat_model>

<verification>
Run after the task:

```bash
bun run typecheck
bun run build
```
</verification>

<success_criteria>
- Related writing appears on selected project detail pages only when public writing references that project.
- Related writing items include title, kind/date metadata, summary, and `Read note` or `Read essay`.
- `src/domain/projects.ts` remains free of reciprocal related-writing fields.
- The project index remains unchanged.
</success_criteria>

<output>
After completion, create `.planning/phases/15-writing-routes-and-dark-ui/15-03-SUMMARY.md`.
</output>
