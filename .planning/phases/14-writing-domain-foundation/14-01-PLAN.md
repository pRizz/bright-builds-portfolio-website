---
phase: 14-writing-domain-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/writing.ts
  - src/domain/writing.test.ts
autonomous: true
requirements:
  - WRITE-01
  - WRITE-02
  - WRITE-03
  - WRITE-04
  - LINK-01
must_haves:
  truths:
    - "Maintainer can define typed checked-in writing entries without runtime content dependencies."
    - "Public writing helpers return only published entries in deterministic display order."
    - "Writing detail paths derive as /writing/{slug} without wiring global routes."
    - "Writing entries can carry related selected project slugs."
  artifacts:
    - path: "src/domain/writing.ts"
      provides: "Writing entry types, curatedWriting registry, public selectors, path helpers, and selected-project relationship resolver"
      exports:
        - "curatedWriting"
        - "publicWritingEntries"
        - "maybePublicWritingEntryBySlug"
        - "writingDetailPath"
        - "writingDetailRoutes"
        - "relatedProjectDetailPageProjects"
    - path: "src/domain/writing.test.ts"
      provides: "Vitest coverage for registry shape, public eligibility, path derivation, nullable lookup, and relationship resolution"
  key_links:
    - from: "src/domain/writing.ts"
      to: "src/domain/projects.ts"
      via: "maybeProjectDetailPageProjectBySlug"
      pattern: "maybeProjectDetailPageProjectBySlug"
    - from: "src/domain/writing.test.ts"
      to: "src/domain/writing.ts"
      via: "direct imports of public helper exports"
      pattern: "publicWritingEntries|writingDetailRoutes|maybePublicWritingEntryBySlug"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-06-03T13-56-52
generated_at: 2026-06-03T14:08:15Z
---

<objective>
Create the Phase 14 writing domain contract.

Purpose: Give downstream route, metadata, and verification phases a pure checked-in writing data surface they can trust.
Output: `src/domain/writing.ts` and focused Vitest tests proving the supported helper surface.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-writing-domain-foundation/14-CONTEXT.md
@.planning/phases/14-writing-domain-foundation/14-RESEARCH.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/projects.ts
@src/domain/project-detail-routes.test.ts
@src/domain/foundation.test.ts

<interfaces>
From `src/domain/projects.ts`, reuse the selected project detail contract:

```ts
export type ProjectDetailPageProject = ProjectStory & {
  tier: "flagship";
  includeInProjectIndex: true;
  detail: ProjectDetailStory;
};

export function maybeProjectDetailPageProjectBySlug(
  slug: string,
  projects?: readonly ProjectStory[],
): ProjectDetailPageProject | null;
```

Do not import from or edit `src/domain/routes.ts`, `src/domain/seo.ts`, `src/routes/*`, `tests/*browser*`, `scripts/verify-static.ts`, `scripts/verify-release.ts`, or release documentation in this plan.
</interfaces>
</context>

<must_haves>
## Decision Coverage

| Decision | Coverage |
| --- | --- |
| D-01, D-02 | `src/domain/writing.ts` owns a repo-authored TypeScript registry and pure helpers with no runtime API, CMS, MDX, feed, or parser dependency. |
| D-03, D-07, D-08 | `WritingEntry`, `WritingSection`, and `WritingBodyBlock` contain the exact fields and simple block kinds named in the tasks. |
| D-04, D-05, D-06 | Public helpers filter to `status: "published"`, sort by `displayOrder`, use `maybe...` nullable naming, and derive `/writing/{slug}` paths. |
| D-09, D-10 | Writing records own `relatedProjectSlugs`, and `relatedProjectDetailPageProjects()` resolves only selected project detail pages. |
| D-12 | `src/domain/writing.test.ts` covers helper eligibility, path derivation, nullable lookup, public exclusion, relationship resolution, and supported runtime exports. |

Deferred work explicitly excluded: no `/writing` route files, no global `prerenderRoutes` wiring, no UI components, no metadata, no JSON-LD, no sitemap, no browser checks, no RSS/search/CMS/MDX, and no project-page related-writing UI.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create writing registry types and authored seed entries</name>
  <files>src/domain/writing.ts, src/domain/writing.test.ts</files>
  <read_first>
    - `src/domain/projects.ts` for the checked-in registry style, exported type placement, `curatedProjects`, and supported helper surface comment.
    - `src/domain/foundation.test.ts` for runtime export-surface test style and Arrange/Act/Assert comments.
    - `.planning/phases/14-writing-domain-foundation/14-CONTEXT.md` decisions D-01, D-02, D-03, D-07, and D-08.
  </read_first>
  <behavior>
    - Test 1: `curatedWriting.map((entry) => entry.slug)` contains exactly `agentic-engineering-workflows` and `portable-identity-and-owned-surfaces` in that order.
    - Test 2: every checked-in writing entry has non-empty `title`, `summary`, `topics`, and `sections`.
    - Test 3: runtime exports include `curatedWriting` and exclude broad namespace-only or legacy names such as `writingSeeds`, `featuredWriting`, and `primaryWritingEntry`.
  </behavior>
  <action>
    Write failing tests first in `src/domain/writing.test.ts` using `describe("curated writing registry", ...)` and explicit `// Arrange`, `// Act`, `// Assert` comments per the Bright Builds testing standard.

    Then create `src/domain/writing.ts` per D-01 and D-02. Export these exact types: `WritingStatus`, `WritingKind`, `WritingBodyBlock`, `WritingSection`, `WritingEntry`, and `PublicWritingEntry`. Use these exact unions: `WritingStatus = "published" | "draft" | "hidden" | "archived"` and `WritingKind = "note" | "essay"`. Use this exact block union per D-07 and D-08:

    ```ts
    export type WritingBodyBlock =
      | { kind: "paragraph"; text: string }
      | { kind: "list"; items: readonly [string, ...string[]] }
      | { kind: "callout"; text: string }
      | { kind: "link"; label: string; href: string };
    ```

    Define `WritingSection` as `{ heading: string; blocks: readonly [WritingBodyBlock, ...WritingBodyBlock[]] }`. Define `WritingEntry` with exact fields `slug`, `title`, `summary`, `status`, `kind`, `maybePublishedOn?`, `maybeUpdatedOn?`, `displayOrder`, `topics`, `tags`, `relatedProjectSlugs`, and `sections`. Use `maybePublishedOn` and `maybeUpdatedOn` names because the values are optional per Bright Builds maybe-naming guidance.

    Add a maintainer-facing comment above `curatedWriting` listing the supported runtime helper exports that Task 2 will complete: `curatedWriting`, `publicWritingEntries`, `maybePublicWritingEntryBySlug`, `writingDetailPath`, `writingDetailRoutes`, and `relatedProjectDetailPageProjects`.

    Export `curatedWriting` with exactly these two authored public entries:

    - `agentic-engineering-workflows`: title `Agentic engineering workflows`; summary `Notes on keeping AI-assisted development inspectable, tested, and tied to repo-owned evidence.`; status `published`; kind `note`; `maybePublishedOn: "2026-06-03"`; `displayOrder: 10`; topics `["Agentic engineering"]`; tags `["ai", "developer-tools"]`; related project slugs `["opencode-cloud", "free-the-world"]`; one section headed `Working thesis` with a paragraph block `Agentic engineering works best when agents produce small, reviewable changes against a shared contract instead of treating automation as a black box.` and a list block with items `Keep decisions in planning artifacts.`, `Prefer pure domain helpers before UI wiring.`, and `Treat verification output as the source of release confidence.`.
    - `portable-identity-and-owned-surfaces`: title `Portable identity and owned surfaces`; summary `A note on why owned web identity should stay visible, low-friction, and independent of any single social platform.`; status `published`; kind `note`; `maybePublishedOn: "2026-06-03"`; `displayOrder: 20`; topics `["Open web"]`; tags `["identity", "open-web"]`; related project slugs `["openlinks"]`; one section headed `Identity should be inspectable` with a paragraph block `Owned web surfaces make identity easier to verify because the canonical signal is not trapped inside a single platform profile.` and a callout block `OpenLinks should stay a low-intrusion identity signal, not the main portfolio call to action.`.

    Do not add package dependencies, content loaders, runtime fetches, route files, UI files, metadata helpers, sitemap behavior, or global prerender wiring in this task.
  </action>
  <verify>
    <automated>bun run test src/domain/writing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `src/domain/writing.ts` exports `curatedWriting`, `WritingStatus`, `WritingKind`, `WritingBodyBlock`, `WritingSection`, `WritingEntry`, and `PublicWritingEntry`.
    - `curatedWriting` is declared with `as const satisfies readonly WritingEntry[]`.
    - The only checked-in writing slugs are `agentic-engineering-workflows` and `portable-identity-and-owned-surfaces`.
    - `relatedProjectSlugs` exists on every `WritingEntry`; no project record receives a related-writing field.
    - `bun run test src/domain/writing.test.ts` passes after implementation.
  </acceptance_criteria>
  <done>The writing registry compiles, seed entries are authored static data, and tests prove the registry shape without adding any runtime content dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add public writing selectors, paths, nullable lookup, and selected-project resolver</name>
  <files>src/domain/writing.ts, src/domain/writing.test.ts</files>
  <read_first>
    - `src/domain/projects.ts` functions `projectDetailPageProjects`, `maybeProjectDetailPageProjectBySlug`, `projectDetailPath`, `projectDetailRoutes`, and `projectStoryHref`.
    - `src/domain/project-detail-routes.test.ts` tests for selected project eligibility, path derivation, and nullable lookup.
    - `.planning/phases/14-writing-domain-foundation/14-CONTEXT.md` decisions D-04, D-05, D-06, D-09, D-10, and D-12.
  </read_first>
  <behavior>
    - Test 1: `publicWritingEntries(customEntries)` returns only `status: "published"` entries and sorts by ascending `displayOrder`.
    - Test 2: `maybePublicWritingEntryBySlug("public-slug", customEntries)` returns the public entry, while draft, hidden, archived, and unknown slugs return `null`.
    - Test 3: `writingDetailPath(entry)` returns `/writing/{slug}`, and `writingDetailRoutes(customEntries)` returns paths only for public entries.
    - Test 4: `relatedProjectDetailPageProjects(entry)` resolves selected project detail slugs such as `openlinks` and filters unselected or unknown slugs from the resolver output.
  </behavior>
  <action>
    Extend `src/domain/writing.test.ts` with `describe("writing public helper surface", ...)` and failing tests for the behaviors above before editing production code.

    In `src/domain/writing.ts`, import `curatedProjects`, `maybeProjectDetailPageProjectBySlug`, and type-only `ProjectDetailPageProject` plus `ProjectStory` from `./projects`.

    Export exactly these helper functions per D-04, D-05, D-06, D-09, and D-10:

    - `publicWritingEntries(entries: readonly WritingEntry[] = curatedWriting): readonly PublicWritingEntry[]`
    - `maybePublicWritingEntryBySlug(slug: string, entries: readonly WritingEntry[] = curatedWriting): PublicWritingEntry | null`
    - `writingDetailPath(entry: Pick<WritingEntry, "slug">): string`
    - `writingDetailRoutes(entries: readonly WritingEntry[] = curatedWriting): readonly string[]`
    - `relatedProjectDetailPageProjects(entry: Pick<WritingEntry, "relatedProjectSlugs">, projects: readonly ProjectStory[] = curatedProjects): readonly ProjectDetailPageProject[]`

    Keep the public eligibility predicate private. It must treat only `status === "published"` as public. Route `maybePublicWritingEntryBySlug()` and `writingDetailRoutes()` through `publicWritingEntries()` so draft, hidden, and archived entries cannot leak through alternate helper paths.

    Implement a private `sortWriting<TEntry extends WritingEntry>(entries: readonly TEntry[]): readonly TEntry[]` that sorts by ascending `displayOrder`. Implement `relatedProjectDetailPageProjects()` by iterating `entry.relatedProjectSlugs`, calling `maybeProjectDetailPageProjectBySlug(slug, projects)`, and returning only non-null selected project detail records.

    Do not edit `src/domain/routes.ts`; `writingDetailRoutes()` is a domain helper only in this phase.
  </action>
  <verify>
    <automated>bun run test src/domain/writing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `publicWritingEntries()` returns `agentic-engineering-workflows` then `portable-identity-and-owned-surfaces` for the checked-in registry.
    - `maybePublicWritingEntryBySlug()` returns `PublicWritingEntry | null` and uses the `maybe` prefix.
    - `writingDetailRoutes()` derives `["/writing/agentic-engineering-workflows", "/writing/portable-identity-and-owned-surfaces"]` for the checked-in registry.
    - `relatedProjectDetailPageProjects(curatedWriting[0]).map((project) => project.slug)` returns `["opencode-cloud", "free-the-world"]`.
    - No file outside `src/domain/writing.ts` and `src/domain/writing.test.ts` is modified by this plan.
  </acceptance_criteria>
  <done>Downstream phases have a tested writing helper contract, and non-public writing entries remain excluded from public helper output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Maintainer-authored registry -> public helper output | Checked-in writing data crosses into helper functions that downstream public routes will consume. |
| Writing related slugs -> selected project story records | Authored relationship strings are resolved against selected public project detail eligibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-01 | Information Disclosure | `publicWritingEntries`, `maybePublicWritingEntryBySlug`, `writingDetailRoutes` | mitigate | Centralize public filtering on `status === "published"` and test draft, hidden, and archived exclusion in `src/domain/writing.test.ts`. |
| T-14-02 | Tampering | `writingDetailPath` | mitigate | Derive paths only from authored slugs here; Plan 02 validates lowercase URL-safe slug shape and duplicate slugs before curation passes. |
| T-14-03 | Information Disclosure | `relatedProjectDetailPageProjects` | mitigate | Resolve related slugs through `maybeProjectDetailPageProjectBySlug()` so helper output contains selected project detail pages only. |
| T-14-04 | Tampering | `WritingBodyBlock` | mitigate | Use typed text/link/list/callout blocks and do not add raw HTML, Markdown/MDX parsing, runtime fetches, or external content dependencies. |
</threat_model>

<verification>
Run these checks after both tasks in this plan:

```bash
bun run test src/domain/writing.test.ts
bun run typecheck
```

Do not run browser, static HTML, sitemap, metadata, or release checks for this plan because no visitor route exists in Phase 14.
</verification>

<success_criteria>
- `src/domain/writing.ts` is the only new production domain file in this plan.
- `src/domain/writing.test.ts` proves registry shape, public filtering, route-path derivation, nullable lookup, selected-project resolver behavior, and supported runtime exports.
- No route, UI, metadata, JSON-LD, sitemap, release, browser, RSS/search/CMS/MDX, or package dependency changes are included.
</success_criteria>

<output>
After completion, create `.planning/phases/14-writing-domain-foundation/14-01-SUMMARY.md`.
</output>
