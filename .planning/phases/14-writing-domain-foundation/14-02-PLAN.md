---
phase: 14-writing-domain-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 14-01
files_modified:
  - src/domain/writing-validation.ts
  - src/domain/writing-validation.test.ts
  - scripts/verify-curation.ts
autonomous: true
requirements:
  - WRITE-03
  - WRITE-04
  - LINK-01
  - LINK-03
must_haves:
  truths:
    - "Curation validation fails for invalid writing slugs, duplicate slugs, missing required fields, missing body content, and invalid related project slugs."
    - "Related project validation accepts only selected public project detail pages."
    - "The existing curation verification command fails on writing validation errors and succeeds for the checked-in registry."
  artifacts:
    - path: "src/domain/writing-validation.ts"
      provides: "Structured writing validation issues, registry validation, and assertion helper"
      exports:
        - "validateWritingEntry"
        - "validateWritingRegistry"
        - "writingCurationErrors"
        - "writingCurationWarnings"
        - "assertValidCuratedWriting"
    - path: "src/domain/writing-validation.test.ts"
      provides: "Vitest coverage for writing validation issue codes and checked-in registry acceptance"
    - path: "scripts/verify-curation.ts"
      provides: "Curation command that validates both project and writing registries"
  key_links:
    - from: "src/domain/writing-validation.ts"
      to: "src/domain/writing.ts"
      via: "curatedWriting and WritingEntry"
      pattern: "validateWritingRegistry"
    - from: "src/domain/writing-validation.ts"
      to: "src/domain/projects.ts"
      via: "maybeProjectDetailPageProjectBySlug"
      pattern: "unsupported_related_project"
    - from: "scripts/verify-curation.ts"
      to: "src/domain/writing-validation.ts"
      via: "validateWritingRegistry"
      pattern: "validateWritingRegistry\\(curatedWriting"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 14-2026-06-03T13-56-52
generated_at: 2026-06-03T14:08:15Z
---

<objective>
Create the Phase 14 writing validation and curation gate integration.

Purpose: Make the checked-in writing registry trustworthy before any public route, metadata, or release verifier consumes it.
Output: `src/domain/writing-validation.ts`, focused validation tests, and an expanded `bun run verify:curation` guard.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-writing-domain-foundation/14-CONTEXT.md
@.planning/phases/14-writing-domain-foundation/14-RESEARCH.md
@.planning/phases/14-writing-domain-foundation/14-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/writing.ts
@src/domain/writing.test.ts
@src/domain/project-validation.ts
@src/domain/project-validation.test.ts
@src/domain/projects.ts
@src/domain/project-detail-routes.test.ts
@scripts/verify-curation.ts
@package.json

<interfaces>
From Plan 01 `src/domain/writing.ts`, use these contracts:

```ts
export type WritingEntry = {
  slug: string;
  title: string;
  summary: string;
  status: WritingStatus;
  kind: WritingKind;
  maybePublishedOn?: string;
  maybeUpdatedOn?: string;
  displayOrder: number;
  topics: readonly [string, ...string[]];
  tags: readonly string[];
  relatedProjectSlugs: readonly string[];
  sections: readonly [WritingSection, ...WritingSection[]];
};

export const curatedWriting: readonly WritingEntry[];
```

From `src/domain/project-validation.ts`, mirror the structured issue pattern:

```ts
export type CurationIssue = {
  severity: "error" | "warning";
  code: string;
  slug: string;
  message: string;
};
```
</interfaces>
</context>

<must_haves>
## Decision Coverage

| Decision | Coverage |
| --- | --- |
| D-11 | `unsupported_related_project` validation fails for unknown, hidden, excluded, unselected, and unsupported project slugs by using `maybeProjectDetailPageProjectBySlug()`. |
| D-12 | Tests cover duplicate/invalid slugs, missing title, missing summary, missing body content, public/draft helper behavior through Plan 01 tests, path derivation through Plan 01 tests, nullable lookup through Plan 01 tests, and related project slug integrity in this plan. |
| D-13 | `WritingCurationIssue` carries `severity`, `code`, `slug`, and `message`; assertion errors format `code: slug: message`. |
| D-14 | `scripts/verify-curation.ts` runs writing validation because this plan creates a runnable writing validation guard; no browser, static HTML, sitemap, metadata, or release checks are expanded. |

Deferred work explicitly excluded: no `/writing` route files, no global `prerenderRoutes` wiring, no UI components, no metadata, no JSON-LD, no sitemap, no browser checks, no release-readiness docs, no RSS/search/CMS/MDX, and no project-page related-writing UI.
</must_haves>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add structured writing registry validation</name>
  <files>src/domain/writing-validation.ts, src/domain/writing-validation.test.ts</files>
  <read_first>
    - `src/domain/writing.ts` from Plan 01 for `WritingEntry`, `curatedWriting`, and body block names.
    - `src/domain/project-validation.ts` for issue shape, registry validation return shape, and assertion formatting.
    - `src/domain/project-validation.test.ts` for Arrange/Act/Assert test style and checked-in registry acceptance test.
    - `src/domain/projects.ts` for `maybeProjectDetailPageProjectBySlug()` selected-project eligibility.
    - `.planning/phases/14-writing-domain-foundation/14-CONTEXT.md` decisions D-11, D-12, and D-13.
  </read_first>
  <behavior>
    - Test 1: duplicate slugs produce an error with code `duplicate_slug` on the second entry.
    - Test 2: malformed slugs such as `Bad Slug` produce an error with code `invalid_slug`.
    - Test 3: blank title, blank summary, no tags or topics, and empty sections produce `missing_title`, `missing_summary`, `missing_tags_or_topics`, and `missing_body`.
    - Test 4: related project slugs `missing-project`, `open-bitcoin`, and a custom hidden/excluded project slug produce `unsupported_related_project`.
    - Test 5: `validateWritingRegistry(curatedWriting).errors` has length `0`.
    - Test 6: `assertValidCuratedWriting()` throws formatted lines containing `unsupported_related_project: test-writing:` when invalid relationships are passed.
  </behavior>
  <action>
    Write failing tests first in `src/domain/writing-validation.test.ts` using `describe("writing curation validation", ...)`, one concern per test, and explicit `// Arrange`, `// Act`, `// Assert` comments.

    Create `src/domain/writing-validation.ts` per D-11, D-12, and D-13. Export exact types `WritingCurationIssueSeverity`, `WritingCurationErrorCode`, `WritingCurationWarningCode`, `WritingCurationIssueCode`, `WritingCurationIssue`, and `WritingRegistryValidation`.

    Define `WritingCurationErrorCode` as this exact union:

    ```ts
    | "duplicate_slug"
    | "invalid_slug"
    | "duplicate_display_order"
    | "missing_title"
    | "missing_summary"
    | "missing_tags_or_topics"
    | "missing_body"
    | "empty_section_heading"
    | "empty_body_block"
    | "invalid_date"
    | "unsupported_related_project"
    ```

    Define `WritingCurationWarningCode` as `never` unless implementation discovers a concrete warning case already required by the tests. Keep every listed validation problem at severity `error`.

    Export exact functions:

    - `validateWritingEntry(entry: WritingEntry, projects: readonly ProjectStory[] = curatedProjects): readonly WritingCurationIssue[]`
    - `validateWritingRegistry(entries: readonly WritingEntry[] = curatedWriting, projects: readonly ProjectStory[] = curatedProjects): WritingRegistryValidation`
    - `writingCurationErrors(entries: readonly WritingEntry[] = curatedWriting, projects: readonly ProjectStory[] = curatedProjects): readonly WritingCurationIssue[]`
    - `writingCurationWarnings(entries: readonly WritingEntry[] = curatedWriting, projects: readonly ProjectStory[] = curatedProjects): readonly WritingCurationIssue[]`
    - `assertValidCuratedWriting(entries: readonly WritingEntry[] = curatedWriting, projects: readonly ProjectStory[] = curatedProjects): void`

    Use slug regex `^[a-z0-9]+(?:-[a-z0-9]+)*$`. Treat duplicate slugs and duplicate display orders as registry-level errors on the later duplicate entry. Treat a date as valid only when the optional date string matches `YYYY-MM-DD`; validate both `maybePublishedOn` and `maybeUpdatedOn` when present. Treat body content as missing when `sections.length === 0`, a section has no blocks, or every block in the entry is blank. Treat paragraph and callout text as blank when `.trim()` is empty; list blocks are invalid when any item trims empty; link blocks are invalid when either `label` or `href` trims empty.

    Validate `relatedProjectSlugs` by calling `maybeProjectDetailPageProjectBySlug(slug, projects)`. When it returns `null`, emit code `unsupported_related_project` with message `Related project "${slug}" must resolve to a selected project detail page.` This covers unknown, hidden, excluded, unselected, and unsupported slugs per D-11.

    `assertValidCuratedWriting()` must throw exactly one `Error` whose message joins error lines as `${issue.code}: ${issue.slug}: ${issue.message}`.
  </action>
  <verify>
    <automated>bun run test src/domain/writing-validation.test.ts src/domain/writing.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `validateWritingRegistry(curatedWriting).errors` returns `[]`.
    - Tests assert the exact issue codes `duplicate_slug`, `invalid_slug`, `missing_title`, `missing_summary`, `missing_tags_or_topics`, `missing_body`, and `unsupported_related_project`.
    - Related project validation imports and uses `maybeProjectDetailPageProjectBySlug`, not `publicProjectIndexProjects`.
    - `assertValidCuratedWriting()` formats errors as `code: slug: message`.
    - No route, UI, metadata, sitemap, browser, release, or package files are modified by this task.
  </acceptance_criteria>
  <done>Writing validation is pure, structured, unit-tested, and rejects invalid related project slugs against selected project detail eligibility.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add writing validation to the curation verification command</name>
  <files>scripts/verify-curation.ts</files>
  <read_first>
    - `scripts/verify-curation.ts` for current project registry warning/error output and process exit behavior.
    - `src/domain/writing-validation.ts` for `validateWritingRegistry`.
    - `src/domain/writing.ts` for `curatedWriting`.
    - `package.json` for the existing `verify:curation` and aggregate `verify` scripts.
    - `.planning/phases/14-writing-domain-foundation/14-CONTEXT.md` decision D-14.
  </read_first>
  <behavior>
    - Command behavior: `bun run verify:curation` runs both `validateProjectRegistry(curatedProjects)` and `validateWritingRegistry(curatedWriting)`.
    - Error behavior: any project or writing error prints before `process.exit(1)`.
    - Success behavior: a clean registry prints `Curated registries valid: {projectCount} projects, {writingCount} writing entries, {warningCount} warnings.`
  </behavior>
  <action>
    Update `scripts/verify-curation.ts` per D-14 after Task 1 creates `validateWritingRegistry()`. Import `curatedWriting` from `../src/domain/writing` and `validateWritingRegistry` from `../src/domain/writing-validation`.

    Keep existing project validation behavior, then add writing validation beside it. Print project warnings and errors using the existing line format. Print writing warnings and errors with these exact prefixes:

    - `[curation warning] writing/${warning.slug}: ${warning.code} - ${warning.message}`
    - `[curation error] writing/${error.slug}: ${error.code} - ${error.message}`

    Combine project and writing errors before deciding whether to exit. If the combined error count is greater than zero, print all errors and call `process.exit(1)`. On success, print exactly:

    ```ts
    `Curated registries valid: ${curatedProjects.length} projects, ${curatedWriting.length} writing entries, ${warningCount} warnings.`
    ```

    Do not change `package.json`; `verify:curation` already points to this script. Do not expand `bun run verify`, static verification, browser verification, release verification, or release-readiness docs in this phase.
  </action>
  <verify>
    <automated>bun run verify:curation</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/verify-curation.ts` imports `curatedWriting` and `validateWritingRegistry`.
    - `bun run verify:curation` exits `0` for the checked-in registries.
    - Successful output contains `Curated registries valid:` and `writing entries`.
    - `package.json` remains unchanged in this plan.
    - No browser, static HTML, sitemap, metadata, release-readiness, route, UI, RSS/search/CMS/MDX, or package dependency changes are included.
  </acceptance_criteria>
  <done>The existing curation command validates writing data and fails the command on writing curation errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
| --- | --- |
| Checked-in writing registry -> validation guard | Authored static data crosses into a script used by maintainers and release checks. |
| Authored related project slugs -> selected project detail eligibility | String slugs are checked against project detail helper output before they can be trusted by downstream links. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
| --- | --- | --- | --- | --- |
| T-14-05 | Tampering | `validateWritingRegistry` | mitigate | Fail on duplicate/invalid slugs, duplicate display orders, missing required fields, missing body content, invalid dates, and unsupported related project slugs. |
| T-14-06 | Information Disclosure | `unsupported_related_project` validation | mitigate | Resolve every related slug with `maybeProjectDetailPageProjectBySlug()` so hidden, excluded, unselected, and unknown projects cannot pass validation. |
| T-14-07 | Repudiation | `scripts/verify-curation.ts` output | mitigate | Prefix writing curation warnings/errors with `writing/{slug}` and keep structured issue codes in output. |
| T-14-08 | Tampering | Curation script dependency surface | mitigate | Import only checked-in domain modules; do not add runtime fetches, CMS clients, MDX parsers, file globs, package dependencies, or route/static/browser/release verifiers. |
</threat_model>

<verification>
Run these checks after both tasks in this plan:

```bash
bun run test src/domain/writing-validation.test.ts src/domain/writing.test.ts
bun run verify:curation
bun run typecheck
```

Do not run browser, static HTML, sitemap, metadata, or release checks for this plan because Phase 14 adds only domain data and validation.
</verification>

<success_criteria>
- `src/domain/writing-validation.ts` exposes structured validation helpers matching the existing project validation pattern.
- `src/domain/writing-validation.test.ts` proves invalid writing data fails with exact issue codes and checked-in writing data passes.
- `scripts/verify-curation.ts` validates both project and writing registries without changing package scripts.
- No route, UI, metadata, JSON-LD, sitemap, release, browser, RSS/search/CMS/MDX, or package dependency changes are included.
</success_criteria>

<output>
After completion, create `.planning/phases/14-writing-domain-foundation/14-02-SUMMARY.md`.
</output>
