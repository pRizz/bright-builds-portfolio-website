---
phase: 03-portfolio-surfaces-seo
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/projects.ts
  - src/domain/routes.ts
  - src/domain/seo.ts
  - src/domain/portfolio-surfaces.test.ts
autonomous: true
requirements:
  - EXP-01
  - EXP-04
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-05
user_setup: []
must_haves:
  truths:
    - "Developer can derive flagship project story copy and current-focus selections from checked-in TypeScript data."
    - "Developer can derive route metadata, social image metadata, sitemap XML, robots text, Person JSON-LD, and project ItemList JSON-LD from pure helpers."
    - "OpenLinks remains a profile sameAs identity link without becoming the primary portfolio brand."
  artifacts:
    - path: "src/domain/projects.ts"
      provides: "Flagship story details and current-focus selector"
      exports: ["ProjectStoryDetails", "currentFocusProjects", "projectAnchorHref"]
    - path: "src/domain/seo.ts"
      provides: "Pure metadata, sitemap, robots, and JSON-LD helpers"
      exports: ["metadataForRoute", "personJsonLd", "projectItemListJsonLd", "sitemapXml", "robotsTxt", "jsonLdScriptContent"]
    - path: "src/domain/portfolio-surfaces.test.ts"
      provides: "Unit coverage for Phase 3 story and SEO derivation"
  key_links:
    - from: "src/domain/projects.ts"
      to: "src/domain/seo.ts"
      via: "visible project ItemList input"
      pattern: "projectItemListJsonLd.*visibleProjects"
    - from: "src/domain/seo.ts"
      to: "src/domain/profile.ts"
      via: "canonical origin and sameAs derivation"
      pattern: "peterProfile|profileSameAsLinks"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-05-26T10-37-25
generated_at: 2026-05-26T11:10:47Z
---

<objective>
Create the pure Phase 3 content and metadata contracts that route components and static verification will consume.

Purpose: keep portfolio story logic, SEO derivation, sitemap/robots output, and structured data framework-free per D-05, D-12, and Bright Builds functional-core guidance.
Output: typed project story details, current-focus selectors, route/social metadata helpers, structured data helpers, sitemap/robots helpers, and focused Vitest coverage.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-portfolio-surfaces-seo/03-CONTEXT.md
@.planning/phases/03-portfolio-surfaces-seo/03-RESEARCH.md
@.planning/phases/03-portfolio-surfaces-seo/03-UI-SPEC.md
@.planning/phases/02-curated-content-model/02-01-SUMMARY.md
@.planning/phases/02-curated-content-model/02-02-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/domain/projects.ts
@src/domain/profile.ts
@src/domain/routes.ts
@src/domain/seo.ts
@src/domain/foundation.test.ts

<interfaces>
From `src/domain/projects.ts`:
```ts
export type ProjectPlacement = "home" | "supporting" | "lab" | "archive" | "hidden";
export type ProjectTier = "flagship" | "supporting" | "lab" | "archive" | "excluded";
export type ProjectStory = {
  slug: string;
  name: string;
  placement: ProjectPlacement;
  tier: ProjectTier;
  maturity: ProjectMaturity;
  status: ProjectStatus;
  themes: readonly string[];
  tags: readonly string[];
  role: string;
  oneLine: string;
  curationReason: string;
  links: readonly [ProjectLink, ...ProjectLink[]];
};
export function homeProjects(projects?: readonly ProjectStory[]): readonly HomeProjectStory[];
export function visibleProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function primaryProjectLink(project: ProjectStory): ProjectLink;
```

From `src/domain/profile.ts`:
```ts
export const peterProfile: Profile;
export function profileSameAsLinks(profile: Profile): string[];
```

From `src/domain/routes.ts`:
```ts
export const siteRoutes: readonly SiteRoute[];
export const prerenderRoutes: string[];
export function routeByPath(path: string): SiteRoute;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add flagship story details and current-focus selectors</name>
  <read_first>
    - `.planning/phases/03-portfolio-surfaces-seo/03-CONTEXT.md` decisions D-01 through D-08.
    - `.planning/phases/03-portfolio-surfaces-seo/03-UI-SPEC.md` Surface Contract for Home and Projects.
    - `.planning/phases/02-curated-content-model/02-CONTEXT.md` decisions D-01 through D-20.
    - `src/domain/projects.ts`
    - `src/domain/project-validation.ts`
  </read_first>
  <files>src/domain/projects.ts, src/domain/portfolio-surfaces.test.ts</files>
  <behavior>
    - Test 1: `homeProjects()` returns exactly 6 flagship records and every record has non-empty `story.problem`, `story.approach`, and `story.whyItMatters`.
    - Test 2: `currentFocusProjects()` returns slugs `openlinks`, `free-the-world`, `win3bitcoin`, `open-bitcoin`, and `opencode-cloud` in that order.
    - Test 3: `projectAnchorHref(project)` returns `/projects#${project.slug}` for every visible project.
  </behavior>
  <action>
    In `src/domain/projects.ts`, add `ProjectStoryDetails = { problem: string; approach: string; whyItMatters: string }` and add `story: ProjectStoryDetails` to `ProjectStory`. Populate every existing `curatedProjects` record with concrete story copy; the six home/flagship records must use these exact values per D-05 and D-06:

    - `openlinks`: problem `Personal identity is scattered across profiles, badges, and owned sites.`; approach `Package identity links and verification hints into a portable OpenLinks surface that other sites can reference.`; whyItMatters `It gives collaborators one stable place to verify Peter's current web presence.`
    - `free-the-world`: problem `Useful AI coordination work often gets trapped inside closed workflows.`; approach `Explore open-source software and agent workflows that keep human agency at the center.`; whyItMatters `It frames Peter's AI work around practical freedom, not novelty for its own sake.`
    - `win3bitcoin`: problem `Bitcoin proof-of-work is hard to make tangible in the browser.`; approach `Use an open web mining experiment to turn everyday hardware into a visible proof-of-work playground.`; whyItMatters `It connects Bitcoin mechanics with an inspectable web experiment people can try.`
    - `opencode-cloud`: problem `Agentic development tools are still too tied to one local machine.`; approach `Prototype a cloud-hostable workbench for running opencode-style workflows in reproducible environments.`; whyItMatters `It points toward practical agent infrastructure that teams can share and audit.`
    - `zeckendorf`: problem `Interesting Bitcoin-adjacent math ideas often stay disconnected from usable software.`; approach `Turn Zeckendorf representation into a focused computational experiment.`; whyItMatters `It shows Peter's interest in mathematical tools that can become practical systems.`
    - `mystic-ui`: problem `SolidJS projects need design primitives that fit the local stack without React adapters.`; approach `Maintain a Peter-owned Mystic UI fork and consume it through a pinned SolidJS and Tailwind setup.`; whyItMatters `It keeps the portfolio interface stack aligned with the tools Peter actually uses.`

    For supporting and lab records, write one concise sentence per `story` field using only the existing `oneLine`, `curationReason`, role, status, and links as source material. Do not add new project claims, metrics, clients, skill bars, live GitHub metadata, search/filtering, per-project routes, or per-project social images.

    Add `currentFocusProjects(projects = curatedProjects)` returning the five slugs named above in display order when present in the registry. Add `projectAnchorHref(project)` returning `/projects#${project.slug}` per D-07. Add `projectLinkDisplayLabel(link)` that maps `kind: "repo"` to `Open source`, keeps `Live site`, `Live docs`, `Docs`, `Article`, and `Related source` when supplied, and otherwise returns the reviewed label. Export all three helpers.

    Create `src/domain/portfolio-surfaces.test.ts` with Arrange/Act/Assert comments. Keep tests pure: import only `src/domain/*`, no Solid runtime, DOM, filesystem, or network.
  </action>
  <acceptance_criteria>
    - `rg "export type ProjectStoryDetails|currentFocusProjects|projectAnchorHref|projectLinkDisplayLabel" src/domain/projects.ts` finds all four exports.
    - `rg "Personal identity is scattered|Bitcoin proof-of-work is hard|SolidJS projects need design primitives" src/domain/projects.ts` finds the exact flagship story copy.
    - `rg "openlinks.*free-the-world.*win3bitcoin.*open-bitcoin.*opencode-cloud" src/domain/portfolio-surfaces.test.ts` confirms the current-focus order is asserted.
    - `rg "api.github.com|@octokit|GITHUB_TOKEN|VITE_.*GITHUB.*TOKEN" src/domain/projects.ts src/domain/portfolio-surfaces.test.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/domain/portfolio-surfaces.test.ts && bun run typecheck</automated>
  </verify>
  <done>Flagship story data, current-focus selection, anchor URL derivation, and link-label derivation are pure, exported, and covered by focused tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend pure SEO, structured data, sitemap, and robots helpers</name>
  <read_first>
    - `.planning/phases/03-portfolio-surfaces-seo/03-CONTEXT.md` decisions D-10 through D-15.
    - `.planning/phases/03-portfolio-surfaces-seo/03-UI-SPEC.md` SEO And Social Preview Contract.
    - `/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/SKILL.md`
    - `/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/references/surface-patterns.md`
    - `src/domain/profile.ts`
    - `src/domain/routes.ts`
    - `src/domain/seo.ts`
    - `src/domain/portfolio-surfaces.test.ts`
  </read_first>
  <files>src/domain/routes.ts, src/domain/seo.ts, src/domain/portfolio-surfaces.test.ts</files>
  <behavior>
    - Test 1: every `siteRoutes` record derives title, description, canonical, OG title/description/url/type/image, Twitter card/title/description/image, and canonical HTTPS URL.
    - Test 2: `personJsonLd()` includes `https://github.com/pRizz` and `https://openlinks.us/` in `sameAs`.
    - Test 3: `projectItemListJsonLd()` emits `/projects#${slug}` URLs and ordered ItemList positions for visible projects.
    - Test 4: `sitemapXml()` and `robotsTxt()` derive from `siteRoutes`, `prerenderRoutes`, and `peterProfile.canonicalOrigin`.
  </behavior>
  <action>
    In `src/domain/routes.ts`, update route copy so Phase 3 route metadata is visitor-facing:
    - home title `Peter Ryszkiewicz / pRizz | Bright Builds`; description `Portfolio for Peter Ryszkiewicz / pRizz, an agentic engineer building across AI, Bitcoin, open systems, developer tooling, and practical web experiments.`; heading `Peter Ryszkiewicz`; staticCheckText `Peter Ryszkiewicz / pRizz builds practical software through Bright Builds across AI, Bitcoin, open systems, developer tooling, and web experiments.`
    - about title `About Peter Ryszkiewicz / pRizz | Bright Builds`; description `Themes behind Peter Ryszkiewicz's work across agentic engineering, open source, Bitcoin, web tooling, and creative experiments.`; heading `About Peter`; staticCheckText `Agentic engineering, open source, Bitcoin, web tooling, and creative experiments are the connective tissue.`
    - projects title `Curated Projects | Peter Ryszkiewicz`; description `Flagship, supporting, lab, and archived project stories curated from Peter Ryszkiewicz's work without mirroring every repository.`; heading `Curated Projects`; staticCheckText `Flagship, supporting, lab, and archive work are separated by reviewed registry placement.`
    - contact title `Contact Peter Ryszkiewicz / pRizz | Bright Builds`; description `Collaboration links for Peter Ryszkiewicz, with GitHub first and OpenLinks as a low-intrusion identity hub.`; heading `Contact and Collaboration`; staticCheckText `Start with GitHub, or use OpenLinks to verify Peter's current identity links.`

    In `src/domain/seo.ts`, keep all helpers pure. Add `SocialImageMetadata`, `SiteAssetLink`, `ProjectItemListJsonLd`, and extend `PageMetadata` with:
    - `openGraph.image.url` `/social/bright-builds-og.png`
    - `openGraph.image.width` `1200`
    - `openGraph.image.height` `630`
    - `openGraph.image.alt` `Peter Ryszkiewicz / pRizz and Bright Builds portfolio focus on AI, Bitcoin, open systems, and developer tooling.`
    - matching `twitter.image` with card `summary_large_image`

    Export `siteAssetLinks` containing `/favicon.svg`, `/icon-192.png`, and `/apple-touch-icon.png` with the correct `rel` values for route head consumers. Export `jsonLdScriptContent(value)` that returns `JSON.stringify(value).replace(/</g, "\\u003c")`.

    Add `projectItemListJsonLd(projects = visibleProjects(), profile = peterProfile)` returning Schema.org `ItemList` with `itemListElement` entries ordered by `visibleProjects()`. Each item must include `@type: "ListItem"`, `position`, and `item` with `@type: "SoftwareSourceCode"`, `name`, `description: project.oneLine`, `url: ${profile.canonicalOrigin}/projects#${project.slug}`, and `sameAs` set to all project link hrefs.

    Add `sitemapXml(routes = siteRoutes, profile = peterProfile)` that emits XML for every `siteRoutes` path using the profile canonical origin. Add `robotsTxt(profile = peterProfile)` returning:
    `User-agent: *`
    `Allow: /`
    `Sitemap: https://www.brightbuilds.us/sitemap.xml`

    Extend `src/domain/portfolio-surfaces.test.ts` for all behaviors above. Do not add dependencies, DOM APIs, network calls, runtime GitHub calls, optional GitHub enrichment, or per-project routes.
  </action>
  <acceptance_criteria>
    - `rg "SocialImageMetadata|siteAssetLinks|projectItemListJsonLd|sitemapXml|robotsTxt|jsonLdScriptContent" src/domain/seo.ts` finds all exports.
    - `rg "/social/bright-builds-og.png|og:image|summary_large_image|ItemList|SoftwareSourceCode|Sitemap: https://www.brightbuilds.us/sitemap.xml" src/domain/seo.ts src/domain/portfolio-surfaces.test.ts` finds the expected metadata contract.
    - `rg "Peter Ryszkiewicz / pRizz builds practical software through Bright Builds" src/domain/routes.ts` finds the identity-first home static text.
    - `rg "api.github.com|@octokit|process\\.env|GITHUB_TOKEN|VITE_.*GITHUB.*TOKEN" src/domain/seo.ts src/domain/routes.ts src/domain/portfolio-surfaces.test.ts` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run test -- src/domain/portfolio-surfaces.test.ts src/domain/foundation.test.ts && bun run typecheck</automated>
  </verify>
  <done>Route copy and all SEO/structured-data/static-metadata helpers derive from pure domain data and have focused tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| checked-in registry -> JSON-LD | Curated strings become script text consumed by crawlers before hydration. |
| route/profile data -> canonical/sitemap | Internal route data becomes public crawler directives and canonical URLs. |
| profile identity links -> sameAs | Self-owned identity links become structured identity claims. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Tampering | `jsonLdScriptContent` in `src/domain/seo.ts` | mitigate | Escape `<` as `\u003c` and unit test script-safe serialization before route components use JSON-LD. |
| T-03-01-02 | Spoofing | canonical and sitemap helpers | mitigate | Derive all canonical URLs from `peterProfile.canonicalOrigin` and test `https://www.brightbuilds.us` output. |
| T-03-01-03 | Information Disclosure | SEO/domain helpers | mitigate | Keep helpers free of `process.env`, GitHub API endpoints, Octokit imports, and browser token names; acceptance criteria grep for those patterns. |
| T-03-01-04 | Spoofing | OpenLinks identity metadata | mitigate | Use `profileSameAsLinks` so OpenLinks appears as an identity hub beside GitHub, not as the site brand or primary CTA. |
</threat_model>

<verification>
Run `bun run test -- src/domain/portfolio-surfaces.test.ts src/domain/foundation.test.ts`, `bun run typecheck`, and `bun run verify:no-github-runtime`.
</verification>

<success_criteria>
The executor can import pure story, current-focus, metadata, JSON-LD, sitemap, and robots helpers from `src/domain/*` with no Solid, DOM, filesystem, network, or GitHub runtime dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/03-portfolio-surfaces-seo/03-01-SUMMARY.md`.
</output>
