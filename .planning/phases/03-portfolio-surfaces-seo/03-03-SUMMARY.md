---
phase: 03-portfolio-surfaces-seo
plan: 03
subsystem: static-seo-assets
tags: [solidstart, seo, static-verification, public-assets, dark-ui]

requires:
  - phase: 03-01
    provides: "Typed profile, route, SEO, OpenLinks, sitemap, robots, JSON-LD, and curated project helpers."
  - phase: 03-02
    provides: "Static-rendered home, projects, about, and contact surfaces with dark-primary UI and visible OpenLinks identity."
provides:
  - "Checked-in favicon, app icons, social preview image, sitemap, and robots files."
  - "Rerunnable Bun metadata generator for sitemap and robots output."
  - "Static output verifier covering route metadata, JSON-LD, public assets, project anchors, residue absence, and no-runtime-GitHub boundaries."
  - "Dark desktop/mobile visual smoke evidence across all Phase 3 routes."
affects: [04-visual-system-motion, 05-release-verification, seo, accessibility]

tech-stack:
  added: []
  patterns:
    - "Generated public metadata files from pure domain helpers."
    - "Dependency-free PNG dimension checks using IHDR bytes."
    - "Filesystem-only static output verification for prerendered HTML and public assets."

key-files:
  created:
    - scripts/generate-static-metadata.ts
    - public/favicon.svg
    - public/icon-192.png
    - public/apple-touch-icon.png
    - public/social/bright-builds-og.png
    - public/sitemap.xml
    - public/robots.txt
  modified:
    - package.json
    - scripts/verify-static.ts
    - src/styles/app.css

key-decisions:
  - "Keep sitemap and robots checked in but generated from pure SEO helpers, with static verification failing on drift."
  - "Use checked-in static icon and social PNG assets instead of adding runtime image generation or remote asset dependencies."
  - "Keep static verification dependency-free and filesystem-only so `bun run verify` remains deterministic."
  - "Use `!important` only inside the reduced-motion media query because utility transition classes otherwise defeat the accessibility contract."

patterns-established:
  - "Public SEO artifacts should be generated by Bun scripts and verified against shared domain helpers."
  - "Static verifiers should assert pre-hydration HTML, metadata, structured data, and public assets from `.output/public`."
  - "Reduced-motion verification should run in a real browser with the media query forced on."

requirements-completed:
  - PROF-04
  - CUR-04
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - SEO-01
  - SEO-02
  - SEO-03
  - SEO-04
  - SEO-05
generated_by: gsd-execute-plan
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-05-26T10-37-25
generated_at: 2026-05-26T12:34:31Z

duration: 21min
completed: 2026-05-26
---

# Phase 03 Plan 03: Static SEO Assets and Output Verification Summary

**Static SEO assets, generated sitemap/robots files, and deterministic build-output verification for the Phase 3 portfolio surfaces**

## Performance

- **Duration:** 21 min
- **Started:** 2026-05-26T12:14:00Z
- **Completed:** 2026-05-26T12:34:31Z
- **Tasks:** 3
- **Files modified:** 10

## Accomplishments

- Added checked-in public SEO assets: SVG favicon, 192x192 icon, 180x180 touch icon, 1200x630 social preview PNG, `sitemap.xml`, and `robots.txt`.
- Added `scripts/generate-static-metadata.ts` and `bun run generate:static-metadata` so sitemap and robots output comes from the shared SEO helpers.
- Extended `scripts/verify-static.ts` to prove route metadata, Open Graph/Twitter tags, JSON-LD, project anchors, Writing group empty/data state, hidden/excluded note visibility, OpenLinks identity, public asset dimensions, generated sitemap/robots drift, template residue absence, and no runtime GitHub API/token leakage.
- Ran full aggregate verification and browser smoke checks for dark desktop, mobile, minimum-width mobile, keyboard focus, console health, and reduced-motion behavior.

## Task Commits

Each task was committed atomically:

1. **Task 1: Add public assets and generated sitemap/robots files** - `26c7b7f` (feat)
2. **Task 2 RED: Add failing static verifier contract** - `4763a8a` (test)
3. **Task 2 GREEN: Extend static output verification** - `a289f14` (feat)
4. **Task 3: Enforce reduced-motion overrides discovered during smoke verification** - `3601ce6` (fix)

**Plan metadata:** committed separately after summary and state updates.

## Files Created/Modified

- `scripts/generate-static-metadata.ts` - Bun script that writes `public/sitemap.xml` and `public/robots.txt` from `sitemapXml()` and `robotsTxt()`.
- `package.json` - Adds `generate:static-metadata`.
- `public/favicon.svg` - Dark Bright Builds SVG favicon.
- `public/icon-192.png` - 192x192 app icon.
- `public/apple-touch-icon.png` - 180x180 Apple touch icon.
- `public/social/bright-builds-og.png` - 1200x630 social preview PNG.
- `public/sitemap.xml` - Generated static sitemap for `/`, `/about`, `/projects`, and `/contact`.
- `public/robots.txt` - Generated static robots file with sitemap pointer.
- `scripts/verify-static.ts` - Static output verifier for Phase 3 HTML, metadata, assets, structured data, anchors, and residue boundaries.
- `src/styles/app.css` - Reduced-motion override strengthened so utility transition classes cannot preserve nonessential transitions.

## Verification

- `bun run generate:static-metadata && bun run typecheck && bun run build` passed for Task 1.
- `bun run build && bun run verify:static && bun run verify:no-github-runtime` passed for Task 2.
- `bun run verify` passed after Task 3, including format check, Biome check, typecheck, Vitest, curation verifier, no-runtime-GitHub verifier, production build, and static output verifier.
- PNG dimensions verified: `bright-builds-og.png 1200x630`, `icon-192.png 192x192`, `apple-touch-icon.png 180x180`.

## Visual Smoke Evidence

- Dev server URL: `http://127.0.0.1:3000/` from `bun run dev -- --host 127.0.0.1`.
- Desktop dark check at `1440x900`: `/`, `/projects`, `/about`, and `/contact` all had `.dark` on the root, dark body background `rgb(5, 6, 8)`, no horizontal overflow, no detected text/control overlap, no detected text overflow, and visible keyboard focus targets.
- Mobile dark check at `390x844`: all four routes passed the same dark rendering, overflow, overlap, text fit, and focus checks. The hidden skip link was excluded from visible touch-target sizing because it is keyboard-only; focus visibility was checked separately.
- Minimum-width mobile dark check at `320x844`: all four routes passed dark rendering, overflow, overlap, text fit, and visible touch-target checks.
- Reduced-motion browser check: headless Chrome with `--force-prefers-reduced-motion=reduce` reported `matchMedia("(prefers-reduced-motion: reduce)").matches === true`, `scroll-behavior: auto`, no horizontal overflow on all four routes, and sampled route transitions/animations reduced to `0.01ms`.
- Console check: Chrome DevTools reported no browser console errors, warnings, or issues attributable to Phase 3 code during the route smoke pass.
- Dev server was stopped after verification.

## Decisions Made

- Checked-in sitemap and robots files stay generated artifacts rather than hand-authored documents, and `verify-static` compares generated output exactly to helper output.
- Icon and social preview files stay static and local for v1; no dynamic OG endpoint, image service, or remote image dependency was introduced.
- The static verifier reads PNG dimensions directly from PNG IHDR bytes instead of adding an image parsing dependency.
- Reduced-motion declarations use `!important` within the media query only because the CSS cascade otherwise allowed utility transition classes to keep 150ms transitions active.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Applied Biome formatting to the extended verifier**
- **Found during:** Task 2 (Extend static verification for Phase 3 HTML, metadata, assets, and anchors)
- **Issue:** The initial verifier implementation satisfied behavior but failed the repo format/check gate.
- **Fix:** Ran the configured formatter and kept the verifier behavior unchanged.
- **Files modified:** `scripts/verify-static.ts`
- **Verification:** Re-ran acceptance checks plus `bun run build`, `bun run verify:static`, `bun run verify:no-github-runtime`, `bun run format:check`, `bun run check`, `bun run typecheck`, and `bun run test`.
- **Committed in:** `a289f14`

**2. [Rule 1 - Bug] Enforced reduced-motion overrides over utility transition classes**
- **Found during:** Task 3 (Run aggregate verification and dark responsive visual smoke checks)
- **Issue:** Real-browser reduced-motion emulation showed `.nav-link`, `.primary-action`, `.footer-link`, and related controls still had 150ms transitions because utility classes outranked the global media query.
- **Fix:** Added `!important` to the reduced-motion scroll, transition, animation-duration, and animation-iteration declarations.
- **Files modified:** `src/styles/app.css`
- **Verification:** Re-ran headless Chrome reduced-motion checks and confirmed all sampled transition/animation durations were `0.01ms`; re-ran `bun run verify`.
- **Committed in:** `3601ce6`

---

**Total deviations:** 2 auto-fixed (1 blocking, 1 bug)
**Impact on plan:** Both fixes were required to satisfy the planned verification contract. No architecture changes or scope expansion were introduced.

## Known Stubs

None. Stub scan across the files created or modified by this plan found no `TODO`, `FIXME`, placeholder copy, coming-soon copy, hardcoded empty UI data, or unavailable-data placeholders.

## Issues Encountered

- Biome reports warnings for `!important` declarations in `src/styles/app.css`; they are intentionally limited to the reduced-motion media query and are required by the verified cascade behavior. `bun run verify` exits 0.
- Running the production build while the dev server was still watching files triggered transient Vite reload noise in the dev server log. The production build, static verifier, browser console check, and route smoke checks passed; the dev server was stopped before completion.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness

- Phase 3 surfaces now have generated public SEO assets and static verification coverage suitable for release checks.
- Future visual/motion work should preserve the reduced-motion browser proof and keep transitions disabled when `prefers-reduced-motion: reduce` is active.
- Release verification can rely on `bun run verify` to catch metadata drift, missing public assets, stale template residue, project anchor regressions, and runtime GitHub API/token leakage.

## Self-Check: PASSED

- Verified created and modified files exist: summary, generator, verifier, public favicon, app icons, social image, sitemap, robots, and reduced-motion stylesheet.
- Verified task commits exist in git history: `26c7b7f`, `4763a8a`, `a289f14`, and `3601ce6`.

---
*Phase: 03-portfolio-surfaces-seo*
*Completed: 2026-05-26*
