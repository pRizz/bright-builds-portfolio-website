---
phase: 03-portfolio-surfaces-seo
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/components/SiteLayout.tsx
  - src/routes/index.tsx
  - src/routes/projects.tsx
  - src/routes/about.tsx
  - src/routes/contact.tsx
  - src/styles/app.css
autonomous: true
requirements:
  - PROF-01
  - PROF-02
  - PROF-03
  - PROF-04
  - CUR-04
  - EXP-01
  - EXP-02
  - EXP-03
  - EXP-04
  - SEO-01
  - SEO-03
  - SEO-05
user_setup: []
must_haves:
  truths:
    - "Visitor sees Peter Ryszkiewicz / pRizz, Bright Builds, and the AI/Bitcoin/open-systems/developer-tooling focus in the first home content block."
    - "Visitor can browse flagship, supporting, lab/prototype, and archive groupings with stable `/projects#slug` anchors."
    - "Visitor can find GitHub and OpenLinks contact paths without OpenLinks displacing Bright Builds as the host brand."
  artifacts:
    - path: "src/routes/index.tsx"
      provides: "Identity-first home and current-focus surface"
    - path: "src/routes/projects.tsx"
      provides: "Grouped project index with stable anchors and ItemList JSON-LD"
    - path: "src/routes/about.tsx"
      provides: "Concise themes narrative"
    - path: "src/routes/contact.tsx"
      provides: "Prioritized collaboration cards"
    - path: "src/styles/app.css"
      provides: "Dark-first Phase 3 route primitives and reduced-motion guard"
  key_links:
    - from: "src/routes/index.tsx"
      to: "src/domain/projects.ts"
      via: "homeProjects and currentFocusProjects"
      pattern: "homeProjects|currentFocusProjects"
    - from: "src/routes/projects.tsx"
      to: "src/domain/seo.ts"
      via: "projectItemListJsonLd script"
      pattern: "projectItemListJsonLd"
    - from: "src/routes/contact.tsx"
      to: "src/domain/profile.ts"
      via: "peterProfile.links"
      pattern: "peterProfile\\.links"
generated_by: gsd-plan-phase
lifecycle_mode: yolo
phase_lifecycle_id: 3-2026-05-26T10-37-25
generated_at: 2026-05-26T11:10:47Z
---

<objective>
Render the Phase 3 visitor-facing portfolio surfaces from the pure contracts created in Plan 01.

Purpose: turn the curated registry and profile data into semantic dark-primary home, project, about, contact, footer, and metadata surfaces per D-01 through D-13.
Output: route components and shared CSS that expose identity, project stories, current focus, collaboration paths, stable anchors, OpenLinks identity presence, and route head metadata.
</objective>

<execution_context>
@/Users/peterryszkiewicz/.codex/get-shit-done/workflows/execute-plan.md
@/Users/peterryszkiewicz/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-portfolio-surfaces-seo/03-CONTEXT.md
@.planning/phases/03-portfolio-surfaces-seo/03-RESEARCH.md
@.planning/phases/03-portfolio-surfaces-seo/03-UI-SPEC.md
@.planning/phases/03-portfolio-surfaces-seo/03-01-SUMMARY.md
@AGENTS.md
@AGENTS.bright-builds.md
@standards-overrides.md
@src/components/SiteLayout.tsx
@src/routes/index.tsx
@src/routes/projects.tsx
@src/routes/about.tsx
@src/routes/contact.tsx
@src/styles/app.css

<interfaces>
From Plan 01 `src/domain/projects.ts`:
```ts
export function currentFocusProjects(projects?: readonly ProjectStory[]): readonly ProjectStory[];
export function projectAnchorHref(project: ProjectStory): string;
export function projectLinkDisplayLabel(link: ProjectLink): string;
```

From Plan 01 `src/domain/seo.ts`:
```ts
export const siteAssetLinks: readonly SiteAssetLink[];
export function metadataForRoute(route: SiteRoute, profile?: Profile): PageMetadata;
export function personJsonLd(profile?: Profile): PersonJsonLd;
export function projectItemListJsonLd(projects?: readonly ProjectStory[], profile?: Profile): ProjectItemListJsonLd;
export function jsonLdScriptContent(value: unknown): string;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Render identity-first home, current focus, and low-intrusion footer</name>
  <read_first>
    - `.planning/phases/03-portfolio-surfaces-seo/03-CONTEXT.md` decisions D-01, D-02, D-04, D-05, D-06, D-08, D-09, D-10, and D-11.
    - `.planning/phases/03-portfolio-surfaces-seo/03-UI-SPEC.md` Global Shell and Home contracts.
    - `/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/references/surface-patterns.md`
    - `src/domain/profile.ts`
    - `src/domain/projects.ts`
    - `src/domain/seo.ts`
    - `src/routes/index.tsx`
    - `src/components/SiteLayout.tsx`
    - `src/styles/app.css`
  </read_first>
  <files>src/routes/index.tsx, src/components/SiteLayout.tsx, src/styles/app.css</files>
  <action>
    In `src/routes/index.tsx`, replace the current foundation home with an identity-first home using `peterProfile`, `homeProjects()`, `currentFocusProjects()`, `projectAnchorHref()`, `projectLinkDisplayLabel()`, `metadataForRoute(route)`, `personJsonLd()`, `siteAssetLinks`, and `jsonLdScriptContent()`.

    The first content block must visibly include this exact body copy per D-01 and D-02: `Peter Ryszkiewicz / pRizz builds practical software through Bright Builds across AI, Bitcoin, open systems, developer tooling, and practical web experiments.` Use `Peter Ryszkiewicz` as the route `h1`, keep `pRizz / Bright Builds` as visible meta text, and keep the primary CTA label exactly `Browse projects` linking to `/projects`.

    Render a right-side current-focus panel on desktop and below the intro on mobile. The panel title must be `Now building`, and each row must link to `/projects#${project.slug}` for the five projects returned by `currentFocusProjects()`. Show project name, status/maturity, and `oneLine`; do not show raw GitHub data or live API-derived fields.

    Render 4-6 flagship cards from `homeProjects()`; with the current registry this is six cards. Each card must include name, role, status/maturity, `story.problem`, `story.approach`, `story.whyItMatters`, themes/tags, and all useful links using `projectLinkDisplayLabel(link)`. Add a project-anchor link labeled `Project details` to `/projects#${project.slug}`. Use semantic `section`, `article`, `h2`, and `h3` structure.

    In `src/components/SiteLayout.tsx`, keep the skip link, sticky header, brand, and nav. Update the footer so Bright Builds remains the host brand with visible text `Bright Builds is Peter Ryszkiewicz's portfolio for practical open-source systems.` Include one footer OpenLinks identity link labeled `OpenLinks profile` with `href="https://openlinks.us/"`, `rel="me noopener noreferrer"`, and `target="_blank"` per D-10 and D-11. Do not add OpenLinks to the header or primary nav.

    In `src/styles/app.css`, add or update dark-first classes used by the new home/footer: `.hero-grid`, `.focus-panel`, `.focus-row`, `.story-grid`, `.story-card`, `.story-label`, `.story-links`, and `.footer-link`. Keep cards at 8px radius, minimum interactive target height 44px, focus rings `#67e8f9`, dominant background `#050608`, card background `#18181b` or existing `bg-zinc-900`, and accent `#6ee7b7`. Remove or replace the current uppercase `tracking-[0.12em]` eyebrow style because the UI-SPEC requires letter spacing `0`.
  </action>
  <acceptance_criteria>
    - `rg "Peter Ryszkiewicz / pRizz builds practical software through Bright Builds" src/routes/index.tsx` finds the first-block identity copy.
    - `rg "Browse projects|Now building|Project details|currentFocusProjects|homeProjects|projectAnchorHref" src/routes/index.tsx` finds the required home wiring.
    - `rg "OpenLinks profile|rel=\"me noopener noreferrer\"" src/components/SiteLayout.tsx` confirms low-intrusion footer identity placement.
    - `rg "tracking-\\[|letter-spacing: -|bg-white|bg-stone-50|text-zinc-950" src/routes/index.tsx src/components/SiteLayout.tsx src/styles/app.css` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck && bun run build && bun run verify:static</automated>
  </verify>
  <done>Home and footer render the identity-first narrative, current-focus links, flagship story cards, and low-intrusion OpenLinks footer placement from curated/profile data.</done>
</task>

<task type="auto">
  <name>Task 2: Render grouped project index with stable anchors and ItemList JSON-LD</name>
  <read_first>
    - `.planning/phases/03-portfolio-surfaces-seo/03-CONTEXT.md` decisions D-05, D-06, D-07, D-08, D-12, and D-13.
    - `.planning/phases/03-portfolio-surfaces-seo/03-UI-SPEC.md` Projects contract.
    - `src/domain/projects.ts`
    - `src/domain/seo.ts`
    - `src/routes/projects.tsx`
    - `src/styles/app.css`
  </read_first>
  <files>src/routes/projects.tsx, src/styles/app.css</files>
  <action>
    In `src/routes/projects.tsx`, replace the flat `visibleProjects()` rendering with grouped sections in this exact order and labels: `Flagship`, `Supporting`, `Lab / Prototype`, `Archive`. Use `projectsByPlacement("home")`, `projectsByPlacement("supporting")`, `projectsByPlacement("lab")`, and `projectsByPlacement("archive")`. Hidden/excluded records must not render as cards; if referenced, show only the note `Some reviewed records stay hidden until they have enough public context.`

    Each project card must be an `article` with `id={project.slug}` and a CSS class that applies `scroll-margin-top: 64px`. Each card heading must be a meaningful `h3` and include an anchor link to `#${project.slug}` or `/projects#${project.slug}`. For flagship cards, render `story.problem`, `story.approach`, `story.whyItMatters`, role, status/maturity, themes/tags, and all useful links. For supporting/lab/archive cards, render role, status/maturity, `oneLine`, tier/placement labels, themes/tags, and at least one useful link.

    Use the empty state copy exactly from the UI-SPEC for any group with no projects:
    - heading `No reviewed projects in this group yet`
    - body `This section only shows entries from the curated registry after they have enough authored context.`

    Emit full route metadata using `metadataForRoute(route)`, `siteAssetLinks`, and OG/Twitter fields from Plan 01. Emit both `personJsonLd()` and `projectItemListJsonLd(visibleProjects())` as `application/ld+json` scripts using `jsonLdScriptContent()`.

    In `src/styles/app.css`, add grouped project index classes: `.project-section`, `.project-section-header`, `.project-anchor-card`, `.project-anchor-link`, `.empty-state`, `.label-row`, and `.link-list`. Keep 8px card radius, stable vertical rhythm, no nested cards, no hover-only content, and no layout resizing on hover.
  </action>
  <acceptance_criteria>
    - `rg "Flagship|Supporting|Lab / Prototype|Archive|No reviewed projects in this group yet" src/routes/projects.tsx` finds all group labels and empty-state copy.
    - `rg "id=\\{project\\.slug\\}|scroll-margin-top|projectItemListJsonLd|jsonLdScriptContent" src/routes/projects.tsx src/styles/app.css` confirms stable anchors, anchor offset, and structured-data scripts.
    - `rg "projectsByPlacement\\(\"home\"\\)|projectsByPlacement\\(\"supporting\"\\)|projectsByPlacement\\(\"lab\"\\)|projectsByPlacement\\(\"archive\"\\)" src/routes/projects.tsx` confirms data-driven grouping.
    - `rg "api.github.com|@octokit|GITHUB_TOKEN|VITE_.*GITHUB.*TOKEN" src/routes/projects.tsx` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck && bun run build && bun run verify:static</automated>
  </verify>
  <done>`/projects` distinguishes flagship/supporting/lab/archive work, exposes stable anchors for visible projects, and emits project ItemList structured data.</done>
</task>

<task type="auto">
  <name>Task 3: Render about themes and contact collaboration surfaces</name>
  <read_first>
    - `.planning/phases/03-portfolio-surfaces-seo/03-CONTEXT.md` decisions D-02, D-03, D-09, D-10, D-11, and D-12.
    - `.planning/phases/03-portfolio-surfaces-seo/03-UI-SPEC.md` About and Contact contracts.
    - `/Users/peterryszkiewicz/.codex/skills/openlinks-identity-presence/references/snippets.md`
    - `src/domain/profile.ts`
    - `src/domain/seo.ts`
    - `src/routes/about.tsx`
    - `src/routes/contact.tsx`
    - `src/styles/app.css`
  </read_first>
  <files>src/routes/about.tsx, src/routes/contact.tsx, src/styles/app.css</files>
  <action>
    In `src/routes/about.tsx`, replace the current focus-area list with a concise themes narrative. The route must have one `h1`, an intro paragraph from `peterProfile.summary`, and five theme cards with these exact headings: `Agentic engineering`, `Open source`, `Bitcoin and decentralized systems`, `Web tooling`, and `Creative experiments`. Each theme card must include 1-2 sentences connecting Peter's work to the theme using only Phase 3 context and existing registry/profile facts. Do not add resume timelines, skill bars, fake clients, awards, metrics, or copied Bright Builds template copy.

    Add one low-intrusion inline OpenLinks identity link in the about profile area only if it is not adjacent to another OpenLinks link. Use label `OpenLinks identity hub`, `href="https://openlinks.us/"`, `rel="me noopener noreferrer"`, and `target="_blank"`.

    In `src/routes/contact.tsx`, keep contact cards rendered from `peterProfile.links`. Preserve priority order GitHub, OpenLinks, Bright Builds. Each card must be a full-card link with visible label, visible URL, `target="_blank"`, and the link's `maybeRel` value. Add route copy that says `GitHub is the best place to start for code and collaboration. OpenLinks is Peter's identity hub for current links.` Do not make OpenLinks the primary CTA.

    Both routes must emit full metadata using `metadataForRoute(route)`, `siteAssetLinks`, OG/Twitter fields, canonical link, and `personJsonLd()` through `jsonLdScriptContent()`.

    In `src/styles/app.css`, add `.theme-card`, `.theme-title`, `.profile-link-row`, `.contact-copy`, and ensure `.contact-url` uses `overflow-wrap: anywhere`. Keep contact cards at least 44px tall on mobile and preserve visible focus rings.
  </action>
  <acceptance_criteria>
    - `rg "Agentic engineering|Open source|Bitcoin and decentralized systems|Web tooling|Creative experiments|OpenLinks identity hub" src/routes/about.tsx` finds the about narrative headings and identity link.
    - `rg "GitHub is the best place to start for code and collaboration|OpenLinks is Peter's identity hub" src/routes/contact.tsx` finds the contact copy.
    - `rg "metadataForRoute|siteAssetLinks|personJsonLd|jsonLdScriptContent|og:image|twitter:image" src/routes/about.tsx src/routes/contact.tsx` confirms full metadata wiring.
    - `rg "skill bar|Selected works|Awwwards|Creative Director|Lorem ipsum|bg-white|bg-stone-50|text-zinc-950" src/routes/about.tsx src/routes/contact.tsx src/styles/app.css` returns no matches.
  </acceptance_criteria>
  <verify>
    <automated>bun run typecheck && bun run build && bun run verify:static</automated>
  </verify>
  <done>About explains the required themes editorially, contact prioritizes GitHub/OpenLinks/Bright Builds from profile data, and both routes emit the shared metadata contract.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| static route content -> visitor | Curated content becomes public visitor-facing HTML before hydration. |
| external links -> new browser tabs | GitHub, OpenLinks, Bright Builds, and project links leave the site. |
| domain JSON-LD helpers -> route scripts | Pure structured data becomes route script content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02-01 | Spoofing | external route and project links | mitigate | Every `target="_blank"` external link must include `noopener noreferrer`; self-owned identity links include `me noopener noreferrer`. |
| T-03-02-02 | Tampering | JSON-LD route scripts | mitigate | Route components must render scripts through `jsonLdScriptContent()` from Plan 01, not raw `JSON.stringify` calls. |
| T-03-02-03 | Information Disclosure | route components | mitigate | Do not add GitHub API calls, Octokit imports, `process.env`, or browser token names; acceptance criteria grep route files. |
| T-03-02-04 | Spoofing | OpenLinks placement | mitigate | Keep OpenLinks in footer/about/contact identity surfaces only; do not add it to header, primary CTA, or repeated nearby placements. |
| T-03-02-05 | Repudiation | project claims | mitigate | Render story fields from the checked-in curated registry and avoid invented metrics, clients, awards, or unverified external facts. |
</threat_model>

<verification>
Run `bun run format:check`, `bun run check`, `bun run typecheck`, `bun run test`, `bun run build`, and `bun run verify:static`. After Plan 03 assets and verification land, run `bun run verify`.
</verification>

<success_criteria>
Home, projects, about, contact, and footer surfaces satisfy the approved UI-SPEC in dark-first styling, consume only Phase 3 domain helpers/profile/registry data, and avoid deferred per-project routes, search/filtering, runtime GitHub enrichment, and motion-heavy work.
</success_criteria>

<output>
After completion, create `.planning/phases/03-portfolio-surfaces-seo/03-02-SUMMARY.md`.
</output>
